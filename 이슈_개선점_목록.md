# 이슈 / 개선점 추적 목록

> 오류가 나거나 개선 아이디어가 생기면 여기에 기록하고, 해결되면 맨 아래 **해결됨** 섹션으로 옮깁니다.
> 오류 분석 자료: `Log\error_시각.png`(화면) + `error_시각.log`(로그 전문) 세트
>
> **표기 규칙** (2026-08-03 정리): 항목 형식은 `- **제목**: 내용`, 배경 설명은 인용문(>),
> 부가 정보는 들여쓴 하위 항목. 체크박스는 쓰지 않는다 — 완료는 체크가 아니라 '5. 해결됨'
> 으로의 이동으로 표현된다. 이력 제목은 `### 날짜 — 제목` 형식.

---

## 1. 확인 필요 (아직 확인 못 한 동작)

> **여기 두는 것**: 동작 자체가 아직 확인되지 않은 것만. 구현이 끝난 항목은 '5. 해결됨'으로
> 보내고(이력에 "실기는 자연 발생 시 로그로 확인" 식으로 남김) 여기 중복해서 두지 않는다.
> 확인이 끝나면 '5. 해결됨'으로 옮긴다 — 항목이 이동으로만 사라지므로 체크박스는 쓰지 않는다.

### 2026-08-11 전체 점검 — 실측 대기 후보 5건 (Codex 교차 + 직접 검증, **아직 미수정**)

> v2.0.2 배포 직후 어비스→던전→심층→사냥터→생활 순으로 **한 영역씩 각자 보고 대조**했습니다.
> **다섯 건 모두 코드로는 확정했고, 실기 영향 확인만 남았습니다.** 규칙 8·9에 따라
> 실측 없이 고치지 않습니다 — 각 항목의 '실측 방법'대로 확인한 뒤 착수할 것.
> 코드만 읽어 나온 나머지 지적은 전부 기각했습니다(이미 방어 있음 / 관측 근거 없음).
>
> **교차 점검이 실제로 값을 했습니다.** 어비스에서 제가 낸 의심 3건은 전부 빗나갔고(기각),
> 그중 두 건은 **근거 서술까지 틀렸는데 결론만 우연히 맞았습니다** — Codex 가 잡아냈습니다:
> ①`dungeons.profiles` 를 최상위 `dungeonProfiles` 로 잘못 찾아 "config 에 없음"이라고 단정
> (실제로는 있고, 값이 같아서 결론만 유지) ②`Test-AbyssSelectionScreen` 이 `Test-MenuExitLabel`
> 2차 신호를 쓴다고 서술(실제로는 `Test-AbyssMenu` 의 특성). 반대로 ⑤는 Codex 가 찾았고
> 제가 대조군(`field-evidence`)까지 확인해 확정했습니다.
>
> **영역별 새 결함**: 어비스 0건 · 던전/심층 0건(⑤ 제외) · 사냥터 0건(⑤ 제외) · 생활 0건.
> 즉 ①~⑤는 특정 콘텐츠 결함이 아니라 **공용 경로·설계 불일치**입니다.
> 2026-08-11 에 고친 생활 수정(마지막 매치·로그 게이트) 주변 회귀도 **없음**으로 교차 확인.

**① [생활 + 전투 전체] 시간 지정 모드가 목표 시각을 크게 넘길 수 있음** (우선순위 1)

> ★ **생활은 2026-08-11 수정·재실측 완료** (초과 2분 24초 → **0초**. 워커가
> `HONEYNOGI_UNTIL_TIME` 으로 목표 시각을 받아 사이클 중에도 도달 즉시 exit 4 -
> 이슈_해결_이력.md 참고). **남은 것은 전투 초과분**: 아래 '전투' 항목과 실측(전투)만
> 유효하고, 전투는 판 중간 정지가 불가라 같은 방식은 못 씁니다 - 실측 후 별도 판단.

`Get-CycleWaitSecondsForEstimate`(mabinogi_gui.ps1)는 한 사이클 상한을 **설정값 하나**로 봅니다 —
생활은 `gatherWaitSeconds`, 전투는 `dungeonClear`. 그런데 워커의 실제 상한은 둘 다 더 큽니다.

- **생활**: `gatherWaitSeconds` 는 '총 시간'이 아니라 **'진행이 멈춘 시간'** 이고 수량이 늘 때마다
  되감깁니다(2026-08-08 의미 변경). 절대 상한은 `lifeGatherHardCapSeconds`(3600초).
  → 권장값 1200초면 GUI 예측 20분 vs 실제 최대 60분.
- **전투(어비스·던전·심층·사냥터)**: `dungeonClear` 하나로 예측하지만 실제 사이클에는 이동,
  파티 매칭(`partyMatching` 기본 300초), 부활 때마다 되감기는 클리어 대기, 결과·복귀가 더 붙습니다.
  (Codex 교차 지적 - 어비스 기준. 실측 미확인이라 '얼마나' 는 미상)

→ **무인 종료 약속이 깨집니다.** 2026-08-08 에 `gatherWaitSeconds` 의미를 바꿀 때 GUI 예측이
안 따라온 것으로 보이고, 전투 쪽은 애초에 클리어 대기만 셈에 넣고 있었습니다.
- **실측(전투)**: `클리어 대기=60초`, 목표 시각 2분 뒤, 60초 넘는 판으로 같은 방식 확인. (미실측)

★ **생활 실측 완료 (2026-08-11 10:42~10:48) — 2분 24초 초과 확정. 수정 착수 가능.**

`채집 대기=180초` + 시간 지정 `10:46` + 일상 채집/거미줄 뭉치 (GUI 상태는 시작 전 캡처로 검증):

```
10:42:49 시작 (GUI 셈법: 지금+180초 = 10:45:49 ≤ 10:46 → 허용)
10:43:17 퀘스트 생성 → 자동 이동 시작
10:46:22 1/10  ← 이동만 185초 (이날 캐릭터 위치가 멀었음. 전날 실측은 95초)
         ── 목표 10:46 을 지나는 순간 진행률 1/10 ──
10:48:24 10개 완료 = [완료] 채집 1사이클 완료
10:48:24 GUI: "지정 시간(10:46)까지 남은 시간이 부족해 다음 회차를 시작하지 않습니다"
         상태줄: "중지됨 - 지정 시간(10:46) 도달 (완료: 1회)"  ← 2분 24초 뒤에야 도달 선언
```

확인된 것: ①GUI 는 `gatherWait`(180초)를 사이클 상한으로 보고 허용했지만 실제 사이클은
5분 35초(이동이 진행 없음 한도의 대부분을 먹고, 채집은 그 뒤부터) ②목표 시각이 지나도
워커는 모르고 완주 ③GUI 는 사이클이 끝나야 시간을 다시 봄 - 다음 회차 차단은 정상 동작.
초과 폭은 '이동 거리 + 채집 속도'에 따라 변동 - `gatherWait` 는 사이클 길이와 상관관계가
약함이 실측으로 확인됨 (같은 대상인데 이동이 95초 vs 185초).

부수 실측: 행 찾기 **순서 추정 폴백**이 이 회차에서 처음 실기로 밟혔고 정확했음
(`'거미줄 뭉치' 행을 목록 순서로 추정했습니다 (Y=679)` → 상세 제목 검증 통과 → 10개 완주.
Codex 가 지적했던 '폴백 미검증' 구멍 중 하나가 닫힘. 격자 추론 경로만 오늘 실기 미통과).

**② [전 영역] 거점 부활 클릭이 생략돼도 성공으로 계상** (우선순위 2)

`Click-*` 는 커서 확인 실패 시 **클릭을 건너뜁니다**(`$script:lastClickPerformed = $false`).
그런데 부활 경로는 그 결과를 안 봅니다:
- 개인 거점 부활: `$reviveCount++` 가 **클릭보다 먼저**(3769) 실행되고 로그도 먼저 찍힘
- 전멸: `Click-ScreenPoint` 직후 **무조건** `$wipeClicked = $true`(3735)
- R키 경로도 `$reviveCount++` 선행(3791)
→ 클릭이 안 나가도 횟수만 소모되고, 상한 10회에 닿으면 **정말 죽어 있어도 부활을 포기**합니다.
커서 확인 실패는 2026-08-02·08-11 실측이 있는 **관측된 조건**입니다(가정 아님).
- **실측**: 전멸 또는 남은 0회 화면에서 부활 시도 순간 마우스를 계속 움직여 생략을 유도.
  로그에 `이번 클릭은 건너뛰었습니다` 다음 줄에 `…에서 부활 클릭`이 찍히고 화면은 그대로인지 확인.

**③ [던전·사냥터] 난이도 확인 실패를 무시하고 진행** (우선순위 3)

`Confirm-DifficultySelected` 가 `$false` 를 반환해도:
- 던전 선택 화면: `$ndVeryHardTarget`(매우 어려움)일 때만 중단 — **일반/어려움은 그대로 진행**
- 사냥터: `... | Out-Null` 로 **반환값을 통째로 버림**(7301)
→ ②와 이어지면(클릭 생략 → 난이도 그대로 → 확인 실패 → 진행) 잘못된 난이도로 입장합니다.
커스텀과 '매우 어려움'은 이미 엄격 중단이라 대상이 아닙니다.
- **실측**: 목표와 다른 난이도가 선택된 화면에서 시작 → 난이도 클릭 구간에 마우스를 움직여
  생략 유도 → `현재 상태로 진행합니다` 직후 알약 캡처로 실제 난이도 확인. **입장 전 F10 정지**로
  재화 소모 없이 확인할 것.

**④ [전 영역] `Press-KeyOnce` 가 전면 확인 없이 전역 입력** (우선순위 4)

부활 R키·자동출발 Space 등은 `Focus-Game` 뒤 바로 `keybd_event` 를 쏩니다. 게임이 전면이
아니면 **다른 창에 입력이 들어갑니다.** 생활에서는 이 문제를 이미 알고 `Press-LifeMenuKey`
(전면 확인 후 입력)로 고쳤는데(주석: "전역 입력이라 게임이 전면이 아니면 엉뚱한 곳에 들어감"),
전투 쪽은 그대로입니다. ②와 겹쳐 R키 부활은 실패해도 횟수가 깎입니다.
- **실측**: 부활 대기 중 다른 창을 전면으로 올려 두고 R키가 그 창에 들어가는지 확인
  (메모장 등 안전한 창으로). `Focus-Game` 실패 빈도가 관건.
- 해당 입력: 입장 직후 키(Space 등), R 부활, 자동사냥 재개, ASSIST, 파티장 인원 부족 Space.

**⑤ [던전·사냥터·공통] 나가기 클릭을 확인하지 않고 종료** (우선순위 2 — ②와 같은 뿌리)

②와 같은 원인(클릭이 생략될 수 있음)인데 **종료 직전**이라 재시도 기회가 없습니다. 세 곳:
- `Exit-HuntingGroundExhausted`(4768): X·나가기 클릭 후 확인 없이 `exit 4`.
  로그(`[완료] … 사냥터에서 나가고 자동화를 마칩니다`)는 **클릭보다 먼저** 찍힙니다.
- `Invoke-SafeStopExitIfRequested`(5243): 신호 파일을 **먼저 삭제**하고 클릭 → 확인 없이 `exit 0`.
  **코드 0 이라 GUI 가 회차 완료로 계상합니다**(mabinogi_gui.ps1 `$completedCycles++`). 셋 중 가장 무겁습니다.
- 던전 잔량 부족 종료(7071): 클릭 → 2초 대기 → 확인 없이 `exit 4`.
→ 클릭이 씹히면 사냥터 첫 화면·결과 화면에 그대로 남은 채 자동화가 끝납니다.

★ **올바른 형태가 이미 이 파일 안에 있습니다** — 마지막 회차 종료는 `Get-DgLastRunExitStep` 의
`'field-evidence'`(게임플레이 HUD + 퀘스트 추적기에 던전 목표 '구역' 없음)로 **필드 복귀를 확인**합니다.
같은 '나가기 후 종료'인데 한쪽만 검증하는 일관성 문제입니다.
- **실측**: 사냥터 은동전을 10개 미만으로 두고 시작 → 우상단 X 클릭 순간 마우스를 계속 움직여
  생략 유도 → `이번 클릭은 건너뛰었습니다` 다음에 `[완료] … 사냥터에서 나가고` 가 찍히고
  코드 4 로 끝나는데 **화면은 사냥터 첫 화면에 남는지** 확인.
  안전 중지 쪽은 결과 화면에서 F9 예약 후 같은 방식 → 결과 화면 잔류 + GUI 완료 횟수 증가 확인.
- 실측 없이 판단 가능(제어 흐름으로 확정). 실측은 발생 빈도 확인용.

**적용 범위 (2026-08-11 어비스 교차 점검에서 확인)** — ②③④는 **어비스에도 그대로 적용**됩니다.
②는 어비스 본류·파티원 모두 공용 `Wait-ForDungeonClearScreen` 을 타므로 개인 거점·전멸·R키
세 경로가 전부 해당하고, ③은 비커스텀 어비스도 `10654` 에서 반환값을 받고도 커스텀일 때만
정지합니다. 즉 ②③④는 특정 콘텐츠 결함이 아니라 **공용 경로 결함**입니다.

**교차 점검 이력 (2026-08-11)**: 처음에는 어비스만 양쪽이 각자 보고 대조했고 던전·심층·
사냥터·생활은 Codex 단독이었다(사용자 지적으로 드러남 - "전 영역 완료"라고 잘못 보고했었다).
이후 던전·심층(`Invoke-NormalDungeonCycle` 시작 판정/탭 전환/주간 매우 어려움/복구 회차/구역
선택 루프/카드·소모량 검증/결과 분기)과 사냥터(주 흐름 전체), `Wait-ForResultScreen` 을
직접 읽고 소견을 내 Codex 와 상호 반박까지 마쳤다. **다섯 영역 전부 양쪽 합의: ①~⑤ 외
새 확정 결함 없음.**
- 직접 독해에서 확인한 것: 던전 '다시 하기'·사냥터 '새 임무 선택'은 40초 상태 기반 재클릭
  루프가 있어 클릭 생략이 **자기 회복**된다(⑤와 다른 구조 - 종료 직전이 아님).
  7177 `$ptDgRetry` 고정 좌표 폴백은 죽은 코드(`Wait-ForResultScreen` 이 못 찾으면 throw).
- 생활 검증 범위의 정확한 표기: 오늘 6사이클은 **이름 매칭 경로만** 밟았다(대상이 전부
  2글자 이상). 격자·순서 폴백은 오늘 실기로는 미통과 - 단 08-09 전수 배치(8종 전 대상,
  1글자 '물' 포함)에서 검증됐고 오늘 수정은 행 찾기를 건드리지 않았다.

**관측 대기 (실측이 있어야 후보로 승격 - 실패 화면을 아직 아무도 못 봄)**:
- **사냥터 시작 오인 가능성**: 시작 판정(7275)이 퀘스트 추적기 첫 줄의 '소탕'/'정찰' 조각으로
  '사냥 진행 중'을 판정한다. 필드의 주간/이벤트 퀘스트 제목에 같은 낱말이 오면 사냥터 밖인데
  진행 중으로 오인 → 600초 소모 후 오류. **그런 제목이 추적기 첫 줄에 실제로 노출되는지가
  미관측**이라 보류(Codex 합의). 실측: 그런 퀘스트를 추적기에 고정하고 필드에서 시작 →
  첫 상태 판정 로그가 '사냥 진행 중'인지 확인.
- 40초 전환 제한이 저사양/네트워크 지연에서 정상 전환보다 짧은지(느린 환경 실측 필요).

아래 두 가지도 같은 이유로 보류합니다 — 실패 화면을 아직 아무도 못 봤습니다:
- **파티찾기 실패·취소·해산을 별도로 감지하지 않음**: 상세 화면이 닫힌 뒤 `Test-InDungeonQuest`
  하나만 최대 `partyMatching`(300초) 기다립니다. 이미 실패한 매칭에서도 전체 타임아웃을 소비하고
  코드 1로 끝납니다(커스텀은 같은 항목 2회 자동 재시도 후 정지 - 오계상은 없음).
  실측: 파티찾기 항목 1개로 커스텀 1바퀴, `partyMatching=30초`로 낮추고 **매칭을 일부러 취소** →
  즉시 상태 로그가 없는지, 정확히 30초 뒤 코드 1인지, progress 가 그대로인지 확인.
- **파티원이 '준비 완료' 를 누른 뒤 파티가 해산되면** 해산 감지가 없어 최소 1800초 대기 후 코드 1.
  실측: 파티원 대기 중 파티장이 파티를 해산하고 로그·소요 시간 확인.

---

- **채집 GUI 경유 반복 동작**: 안전 중지(08-08) + **사이클 완료 후 다음 회차 자동 시작**
  (2026-08-10 실기: 무한 반복으로 2사이클 연속 완주 확인) + 안전 중지 예약→완주→정지 확인 완료.
  남은 것: **즉시 중지**(진행 중 강제 종료)와 **횟수 지정** 모드의 종료 계상
- **생활 커스텀 반복의 실제 반복 실행**: ~~리스트 순서·항목별 횟수대로 도는지~~
  **2026-08-11 확인 완료** (항목 2개 x 1회로 2바퀴 = 4사이클 전부 완주, 스킬·대상 전환 정상).
  이력은 '4. 최근 검증 기록' 참고. **남은 것 없음 - 다음 정리 때 이 줄 삭제**
- **어비스 '파티 찾기' 매칭 리스트 1바퀴**: 매칭 통일 규칙상 파티찾기 항목만으로 리스트를
  만들어 1바퀴 (`함께하기+우연한 만남`·`혼자하기`·`파티(파티장)`는 07-22 검증 완료)
- **던전 파티찾기 '다시 하기' 파티 유지 여부**: 파티로 클리어 후 다시 하기 시 파티가
  유지되는지 (파티찾기로 돌릴 때 로그만 확인, 2026-07-15부터 이월)
- **미해금 구역 감지 실측**: 해금 안 된 구역이 있는 계정이어야 재현 가능
  ('미해금이거나 화면 인식 실패' 문구 분리는 07-24 완료)

---

## 2. 개발 예정

> 구현이 끝난 항목은 이 목록에 남기지 않습니다. — 구현 세부는 '5. 해결됨' 이력, 남은 실기
> 검증은 '1. 확인 필요' 체크리스트에서 관리합니다.

- **생활 - 낚시**: 채집 8종과 달리 낚시는 흐름이 다릅니다(찌 던지기·입질 타이밍). 실측 자료가
  모이면 진행 (지금은 고르면 시작 전에 안내로 차단)
- **생활 - 가공**: 채집 안정화 후 2차. 현재는 화면·config 자리만 있고 시작이 차단됩니다
- **채집 대상 아이콘 카드**: 지금은 글자 카드입니다. 대상별 이미지 캡처가 모이면 스킬 아이콘과
  같은 방식으로 추가

---

## 3. 개선점 (미구현)

> 실사고 수정이 아니라 "더 좋게" 만드는 보류 항목만 둔다 (전부 우선순위 낮음 - 급한 것은
> 애초에 여기 안 온다). 구현하면 검증(문법·테스트·빌드·가능한 실측)을 거쳐 '5. 해결됨'으로
> 이동. 형식: `- **제목**: 왜 보류인지 한두 줄 (출처)`.

### OCR/캡처 구조 — 실측이 더 모이면 진행

- **큰 창 실효 배율 정규화**: Get-GameRegionCapture 확대 목표가 '기준 크기×배율' 고정이라
  큰 창일수록 실효 배율이 떨어짐(1908 = 배율/1.5) - 카드/알약/협동 창 다중 스케일 실사고들의
  공통 배경. 공용 캡처 의미 변경은 전 OCR 경로 파급이 커서 지금은 국소 사다리 폴백으로 대응
  중 - 근본 정규화는 실측 세트를 모아 별도 검토 (08-03 실사고에서 구조 확인)
- **매우 어려움 s2 폴백 확장**: s2 최종 폴백은 위치 게이트가 있는 '어려움' 키 한정 -
  1908 창 매우 어려움 실측(깨짐/정상 사례)이 모이면 '매우' 토큰 게이트 검증 후 확장
  (08-03 보수 조건의 후속)
- **재화줄 아이콘 앵커링**: 공물/은동전 잔량 위치를 고정 x좌표 대신 아이콘 이미지 대조로
  찾기 - UI 개편/재화 순서 변경에 강해짐. 원본 자산 확보됨(2026-08-04 사용자 제공:
  던전이미지\마족공물이미지.png·은동전이미지.png·골드이미지.png). 창 크기별 배율 대조가
  필요해 실사고 재발 시 착수
- **폴링당 화면 캡처 공유**: 폴링 1회에 감지들이 각자 캡처(3~6회)하는 것을 1회 캡처 공유로
  축소. 감지 사이 화면 변경 타이밍 이슈로 회귀 위험이 이득보다 큼 - **폴링 속도가 실제로
  부족하다고 계측될 때만** 진행. OCR 엔진 교체/메모리 리딩/템플릿 매칭은 검토 후 기각
  (무의존 단일 exe·다중 PC·RDP 제약에서 현 구조가 최선)

### 판정/복구 보강 — 전수 점검 보류분 (2026-08-01 설계 합의)

- **픽셀 판정 실패의 tri-state 화**: 픽셀 읽기 실패를 false(미선택)로 반환 → 캡처 불능 중
  재클릭 가능. unknown 도입은 호출부 파급이 커서 보류
- **Return-ToAbyssSelection X 후보 순환 클릭**: 상태 기반 원칙과 어긋나지만 장기 검증된
  복구 경로라 재설계는 신중히
- **Find-GameTextPoint 절대좌표**: 캡처 시점 좌표를 Focus-Game(창 이동 가능) 후 클릭 -
  재측정 계약 통일 검토
- **Get-GamePixel 캡처 실패 플래그 대칭**: 픽셀 캡처 실패/성공이 screenCaptureFailing 을
  갱신하지 않음
- **next-floor 완료 판정 층 검증**: '다음 층으로' 후 아무 옵션 화면 제목도 성공 처리 -
  후속 시작 검증(구역 확인 → 불일치 시 복구/정지)이 오입장을 차단하는 안전망 (3차 점검 보류)
- **협동 미션 완료 '확인' 탐색 실패 시 Space 폴백**: 부제 확정 상태 한정. 화면 전환 직후
  Space 오입력 잔여 위험과 트레이드오프 - 채택 시 설계 합의 (사용자 문의 - 결정 대기)

### GUI/도구

- **커스텀 리스트 비셀편집 저장 실패 완전 롤백**: 추가/삭제/이동은 저장 실패 시 경고만
  (07-31 문구 강화 완료) - UI 재동기화 방식 검토
- **GetNewClosure 스코프**: 셀 편집 콜백의 $script: 변수가 생성 시점 복사본 - 방어 로직
  실효성 재검토
- **커스텀 토큰 형식 검증**: 환경변수 토큰의 0/1 필드 손상 값이 전부 false 로 해석됨
- **빌드 대장 동시 접근 잠금(TOCTOU)**: 재조회+재검사까지 반영됨 - 파일 잠금은 로컬 1인
  빌드 도구라 실익 낮음 (3차 점검 보류)
- **창 정렬(nearest) 스냅 임계값**: 1340×754 창이 1272×717로 스냅되지 않고 1px만 조정 -
  좌표가 전부 비율 환산이라 **무해 확인 완료** (07-30 타 PC 완주 실측). 비권장 크기를 권장
  크기로 당길지 결정만 남음 - 실사용 관찰로 충분하면 유지

---

## 4. 지난 기록

**최근 검증 기록**과 **해결됨(이력)** 은 [이슈_해결_이력.md](이슈_해결_이력.md) 로 옮겼습니다
(2026-08-11 분리 - 이 파일이 4570줄까지 커져 진행 중인 항목이 묻혔습니다).

- 수정을 마치면 그 이력은 **이력 파일의 `5. 해결됨` 맨 위**에 씁니다.
- 실기로 확인한 내용은 이력 파일의 **`4. 최근 검증 기록`** 에 씁니다.
- 이 파일에는 **아직 안 끝난 것만** 둡니다 (확인 필요 / 개발 예정 / 개선점).

