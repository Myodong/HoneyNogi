# 이슈 / 개선점 추적 목록

> 오류가 나거나 개선 아이디어가 생기면 여기에 기록하고, **해결되면 그 자리에서
> `이슈_해결_이력.md` '5. 해결됨'으로 옮깁니다** — '수정 완료' 표기로 여기 남겨두지 않습니다.
> 오류 분석 자료: `Log\error_시각.png`(화면) + `error_시각.log`(로그 전문) 세트
>
> **표기 규칙** (2026-08-03 정리): 항목 형식은 `- **제목**: 내용`, 배경 설명은 인용문(>),
> 부가 정보는 들여쓴 하위 항목. 체크박스는 쓰지 않는다 — 완료는 체크가 아니라 '5. 해결됨'
> 으로의 이동으로 표현된다. 이력 제목은 `### 날짜 — 제목` 형식.

---

## 1. 확인 필요 (아직 확인 못 한 동작)

> **여기 두는 것**: 동작 자체가 아직 확인되지 않은 것만. 구현이 끝난 항목은 '5. 해결됨'으로
> 보내고(이력에 "실기는 자연 발생 시 로그로 확인" 식으로 남김) 여기 중복해서 두지 않는다.
> 확인이 끝나면 '5. 해결됨'으로 옮긴다 — 항목이 이동으로만 사라지므로 체크박스는 쓰지 않는다.

- **임베디드 호스트 잔여 실기 (v2.1.4 - 구현·계약 매트릭스·GUI 실기·기본 전환은 09-01 이력)**:
  작업 관리자 표시(사용자 확인)·워커 --run 스폰·종료 코드 처리(코드 1 재시도→정지)·
  기본 전환(인자 없음 = 임베디드, --legacy-powershell 폴백)까지 09-01 완료. 남은 것:
  ①게임 켠 상태의 실전 회차 완주(그냥 실행해 정상 회차 코드 0 회전 - 임베디드 워커 첫 실전)
  ②리사이즈 헬퍼 `--run` 실기(게임 창 필요). 문제가 나오면 `--legacy-powershell` 로 임시
  복귀 가능.

### 2026-08-11 전체 점검의 이월 실측 1건 (수정 ①~⑤ 자체는 완료 — 원문·근거는 이력 08-11 이관분 참고. ① 전투 시간 지정 초과 폭은 08-14 실측 완료 → 이력 이관)

- **② 부활 오계상 수정의 실기 확인**: 클릭 생략 기전 자체는 ③ 실측(사냥터 커서 흔들기)이
  증명 — 전멸·횟수 소진의 실제 발생이 필요해 자연 발생 시 로그로 확인.

- **제목 전 배율 사망 옵션 화면에서 일반 회차 '진행' 개방 (2026-08-13 설계 항목)**: 지금은
  복구 회차만 수용하고 일반 회차는 정확한 정지(fail-closed). 진행까지 열려면 ①현재 구역의
  독립 증거(지도 선택 강조 판별 - 미실측) ②입장 확인 등 제목 의존 4곳의 대체 신호가 함께
  필요 (이력 08-13 참고). 같은 PC 재발 빈도를 보고 착수 판단. 그 창의 제목 사망이 게임
  재실행으로 해소되는지도 미확인 - 제보자에게 확인 요청.

---

- **어비스 '파티 찾기' 매칭 리스트 1바퀴**: 매칭 통일 규칙상 파티찾기 항목만으로 리스트를
  만들어 1바퀴 (`함께하기+우연한 만남`·`혼자하기`·`파티(파티장)`는 07-22 검증 완료)
- **던전 파티찾기 '다시 하기' 파티 유지 여부**: 파티로 클리어 후 다시 하기 시 파티가
  유지되는지 (파티찾기로 돌릴 때 로그만 확인, 2026-07-15부터 이월)
- **미해금 구역 감지 실측**: 해금 안 된 구역이 있는 계정이어야 재현 가능
  ('미해금이거나 화면 인식 실패' 문구 분리는 07-24 완료)
- **게임 무한 로딩 감지 (관측 1건 - 실측 부족으로 설계 보류)**: 2026-08-11 20:51 타 PC(1273 창)
  제보(v2.0.2, 피오드 1-3 파티). 클리어 화면 위에 로딩 스피너가 무한히 남아 정위치 터치
  18회(90초)가 전부 무반응 → `결과 화면을 찾지 못했습니다` 오류로 오진. 클릭 전송·좌표는
  정상이었음(커서 확인 안내 0건 + 캡처의 커서 위치 일치). 입장 로딩도 32초(평소 3~7초)로
  그 세션 전반이 느렸음. **무한 로딩은 어느 화면에서든 걸릴 수 있어**(사용자 확인) 클리어
  한정 문구 수정은 기각 - 범용 스피너 감지가 정답인데, 스피너는 회전 애니라 픽셀 시그니처가
  프레임마다 다르고 **실측이 1프레임뿐**이라 설계 보류(규칙 8). 오류 세트 캡처에 스피너가
  찍히므로 지금도 제보 분석으로는 판별 가능(이번 건이 그 증명).
  → **무한 로딩 오류 캡처가 여러 화면·여러 프레임으로 모이면 착수.** 실측 보관:
  `던전이미지\실측기록\20260811_무한로딩_*` (스피너 1프레임 + 제보 로그)

---

## 2. 개발 예정

> 구현이 끝난 항목은 이 목록에 남기지 않습니다. — 구현 세부는 '5. 해결됨' 이력, 남은 실기
> 검증은 '1. 확인 필요' 체크리스트에서 관리합니다.

- **생활 - 낚시**: 채집 8종과 달리 낚시는 흐름이 다릅니다(찌 던지기·입질 타이밍). 실측 자료가
  모이면 진행 (지금은 고르면 시작 전에 안내로 차단)
- **생활 - 가공**: 채집 안정화 후 2차. 현재는 화면·config 자리만 있고 시작이 차단됩니다
- **채집 대상 아이콘 카드**: 지금은 글자 카드입니다. 대상별 이미지 캡처가 모이면 스킬 아이콘과
  같은 방식으로 추가

---

## 3. 개선점 (미구현)

> 실사고 수정이 아니라 "더 좋게" 만드는 보류 항목만 둔다 (전부 우선순위 낮음 - 급한 것은
> 애초에 여기 안 온다). 구현하면 검증(문법·테스트·빌드·가능한 실측)을 거쳐 '5. 해결됨'으로
> 이동. 형식: `- **제목**: 왜 보류인지 한두 줄 (출처)`.

### OCR/캡처 구조 — 실측이 더 모이면 진행

- **큰 창 실효 배율 정규화**: Get-GameRegionCapture 확대 목표가 '기준 크기×배율' 고정이라
  큰 창일수록 실효 배율이 떨어짐(1908 = 배율/1.5) - 카드/알약/협동 창 다중 스케일 실사고들의
  공통 배경. 공용 캡처 의미 변경은 전 OCR 경로 파급이 커서 지금은 국소 사다리 폴백으로 대응
  중 - 근본 정규화는 실측 세트를 모아 별도 검토 (08-03 실사고에서 구조 확인)
- **매우 어려움 s2 폴백 확장**: s2 최종 폴백은 위치 게이트가 있는 '어려움' 키 한정 -
  1908 창 매우 어려움 실측(깨짐/정상 사례)이 모이면 '매우' 토큰 게이트 검증 후 확장
  (08-03 보수 조건의 후속)
- **재화줄 아이콘 앵커링**: 공물/은동전 잔량 위치를 고정 x좌표 대신 아이콘 이미지 대조로
  찾기 - UI 개편/재화 순서 변경에 강해짐. 원본 자산 확보됨(2026-08-04 사용자 제공:
  던전이미지\마족공물이미지.png·은동전이미지.png·골드이미지.png). 창 크기별 배율 대조가
  필요해 실사고 재발 시 착수
- **폴링당 화면 캡처 공유**: 폴링 1회에 감지들이 각자 캡처(3~6회)하는 것을 1회 캡처 공유로
  축소. 감지 사이 화면 변경 타이밍 이슈로 회귀 위험이 이득보다 큼 - **폴링 속도가 실제로
  부족하다고 계측될 때만** 진행. OCR 엔진 교체/메모리 리딩/템플릿 매칭은 검토 후 기각
  (무의존 단일 exe·다중 PC·RDP 제약에서 현 구조가 최선)

### 판정/복구 보강 — 전수 점검 보류분 (2026-08-01 설계 합의)

- **픽셀 판정 실패의 tri-state 화**: 픽셀 읽기 실패를 false(미선택)로 반환 → 캡처 불능 중
  재클릭 가능. unknown 도입은 호출부 파급이 커서 보류
- **Return-ToAbyssSelection X 후보 순환 클릭**: 상태 기반 원칙과 어긋나지만 장기 검증된
  복구 경로라 재설계는 신중히
- **Find-GameTextPoint 절대좌표**: 캡처 시점 좌표를 Focus-Game(창 이동 가능) 후 클릭 -
  재측정 계약 통일 검토
- **Get-GamePixel 캡처 실패 플래그 대칭**: 픽셀 캡처 실패/성공이 screenCaptureFailing 을
  갱신하지 않음
- **next-floor 완료 판정 층 검증**: '다음 층으로' 후 아무 옵션 화면 제목도 성공 처리 -
  후속 시작 검증(구역 확인 → 불일치 시 복구/정지)이 오입장을 차단하는 안전망 (3차 점검 보류)
- **협동 미션 완료 '확인' 탐색 실패 시 Space 폴백**: 부제 확정 상태 한정. 화면 전환 직후
  Space 오입력 잔여 위험과 트레이드오프 - 채택 시 설계 합의 (사용자 문의 - 결정 대기)

### GUI/도구

- **커스텀 리스트 비셀편집 저장 실패 완전 롤백**: 추가/삭제/이동은 저장 실패 시 경고만
  (07-31 문구 강화 완료) - UI 재동기화 방식 검토
- **GetNewClosure 스코프**: 셀 편집 콜백의 $script: 변수가 생성 시점 복사본 - 방어 로직
  실효성 재검토
- **커스텀 토큰 형식 검증**: 환경변수 토큰의 0/1 필드 손상 값이 전부 false 로 해석됨
- **빌드 대장 동시 접근 잠금(TOCTOU)**: 재조회+재검사까지 반영됨 - 파일 잠금은 로컬 1인
  빌드 도구라 실익 낮음 (3차 점검 보류)
- **창 정렬(nearest) 스냅 임계값**: 1340×754 창이 1272×717로 스냅되지 않고 1px만 조정 -
  좌표가 전부 비율 환산이라 **무해 확인 완료** (07-30 타 PC 완주 실측). 비권장 크기를 권장
  크기로 당길지 결정만 남음 - 실사용 관찰로 충분하면 유지

---

## 4. 지난 기록

**최근 검증 기록**과 **해결됨(이력)** 은 [이슈_해결_이력.md](이슈_해결_이력.md) 로 옮겼습니다
(2026-08-11 분리 - 이 파일이 4570줄까지 커져 진행 중인 항목이 묻혔습니다).

- 수정을 마치면 그 이력은 **이력 파일의 `5. 해결됨` 맨 위**에 씁니다.
- 실기로 확인한 내용은 이력 파일의 **`4. 최근 검증 기록`** 에 씁니다.
- 이 파일에는 **아직 안 끝난 것만** 둡니다 (확인 필요 / 개발 예정 / 개선점).

