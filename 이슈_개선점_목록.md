# 이슈 / 개선점 추적 목록

> 오류가 나거나 개선 아이디어가 생기면 여기에 기록하고, 해결되면 맨 아래 **해결됨** 섹션으로 옮깁니다.
> 오류 분석 자료: `Log\error_시각.png`(화면) + `error_시각.log`(로그 전문) 세트
>
> **표기 규칙** (2026-08-03 정리): 항목 형식은 `- **제목**: 내용`, 배경 설명은 인용문(>),
> 부가 정보는 들여쓴 하위 항목. 체크박스는 쓰지 않는다 — 완료는 체크가 아니라 '5. 해결됨'
> 으로의 이동으로 표현된다. 이력 제목은 `### 날짜 — 제목` 형식.

---

## 1. 확인 필요 (아직 확인 못 한 동작)

> **여기 두는 것**: 동작 자체가 아직 확인되지 않은 것만. 구현이 끝난 항목은 '5. 해결됨'으로
> 보내고(이력에 "실기는 자연 발생 시 로그로 확인" 식으로 남김) 여기 중복해서 두지 않는다.
> 확인이 끝나면 '5. 해결됨'으로 옮긴다 — 항목이 이동으로만 사라지므로 체크박스는 쓰지 않는다.

- **채집 GUI 경유 반복 동작**: 안전 중지는 **채집 중 동작 확인 완료** (2026-08-08 사용자).
  남은 것: **사이클 완료 후 다음 회차가 자동으로 시작되는지**(무한/횟수 지정)와 즉시 중지
- **생활 커스텀 반복의 실제 반복 실행**: 담기·재시작 복원·셀 편집·낚시 차단은 확인 완료
  (08-08). 리스트 순서와 항목별 횟수대로 실제로 도는지가 남음 (항목 2개 x 1회면 충분)
- **어비스 '파티 찾기' 매칭 리스트 1바퀴**: 매칭 통일 규칙상 파티찾기 항목만으로 리스트를
  만들어 1바퀴 (`함께하기+우연한 만남`·`혼자하기`·`파티(파티장)`는 07-22 검증 완료)
- **던전 파티찾기 '다시 하기' 파티 유지 여부**: 파티로 클리어 후 다시 하기 시 파티가
  유지되는지 (파티찾기로 돌릴 때 로그만 확인, 2026-07-15부터 이월)
- **미해금 구역 감지 실측**: 해금 안 된 구역이 있는 계정이어야 재현 가능
  ('미해금이거나 화면 인식 실패' 문구 분리는 07-24 완료)

---

## 2. 개발 예정

> 구현이 끝난 항목은 이 목록에 남기지 않습니다. — 구현 세부는 '5. 해결됨' 이력, 남은 실기
> 검증은 '1. 확인 필요' 체크리스트에서 관리합니다.

- **생활 - 낚시**: 채집 8종과 달리 낚시는 흐름이 다릅니다(찌 던지기·입질 타이밍). 실측 자료가
  모이면 진행 (지금은 고르면 시작 전에 안내로 차단)
- **생활 - 가공**: 채집 안정화 후 2차. 현재는 화면·config 자리만 있고 시작이 차단됩니다
- **채집 대상 아이콘 카드**: 지금은 글자 카드입니다. 대상별 이미지 캡처가 모이면 스킬 아이콘과
  같은 방식으로 추가

---

## 3. 개선점 (미구현)

> 실사고 수정이 아니라 "더 좋게" 만드는 보류 항목만 둔다 (전부 우선순위 낮음 - 급한 것은
> 애초에 여기 안 온다). 구현하면 검증(문법·테스트·빌드·가능한 실측)을 거쳐 '5. 해결됨'으로
> 이동. 형식: `- **제목**: 왜 보류인지 한두 줄 (출처)`.

### OCR/캡처 구조 — 실측이 더 모이면 진행

- **큰 창 실효 배율 정규화**: Get-GameRegionCapture 확대 목표가 '기준 크기×배율' 고정이라
  큰 창일수록 실효 배율이 떨어짐(1908 = 배율/1.5) - 카드/알약/협동 창 다중 스케일 실사고들의
  공통 배경. 공용 캡처 의미 변경은 전 OCR 경로 파급이 커서 지금은 국소 사다리 폴백으로 대응
  중 - 근본 정규화는 실측 세트를 모아 별도 검토 (08-03 실사고에서 구조 확인)
- **매우 어려움 s2 폴백 확장**: s2 최종 폴백은 위치 게이트가 있는 '어려움' 키 한정 -
  1908 창 매우 어려움 실측(깨짐/정상 사례)이 모이면 '매우' 토큰 게이트 검증 후 확장
  (08-03 보수 조건의 후속)
- **재화줄 아이콘 앵커링**: 공물/은동전 잔량 위치를 고정 x좌표 대신 아이콘 이미지 대조로
  찾기 - UI 개편/재화 순서 변경에 강해짐. 원본 자산 확보됨(2026-08-04 사용자 제공:
  던전이미지\마족공물이미지.png·은동전이미지.png·골드이미지.png). 창 크기별 배율 대조가
  필요해 실사고 재발 시 착수
- **폴링당 화면 캡처 공유**: 폴링 1회에 감지들이 각자 캡처(3~6회)하는 것을 1회 캡처 공유로
  축소. 감지 사이 화면 변경 타이밍 이슈로 회귀 위험이 이득보다 큼 - **폴링 속도가 실제로
  부족하다고 계측될 때만** 진행. OCR 엔진 교체/메모리 리딩/템플릿 매칭은 검토 후 기각
  (무의존 단일 exe·다중 PC·RDP 제약에서 현 구조가 최선)

### 판정/복구 보강 — 전수 점검 보류분 (2026-08-01 설계 합의)

- **픽셀 판정 실패의 tri-state 화**: 픽셀 읽기 실패를 false(미선택)로 반환 → 캡처 불능 중
  재클릭 가능. unknown 도입은 호출부 파급이 커서 보류
- **Return-ToAbyssSelection X 후보 순환 클릭**: 상태 기반 원칙과 어긋나지만 장기 검증된
  복구 경로라 재설계는 신중히
- **Find-GameTextPoint 절대좌표**: 캡처 시점 좌표를 Focus-Game(창 이동 가능) 후 클릭 -
  재측정 계약 통일 검토
- **Get-GamePixel 캡처 실패 플래그 대칭**: 픽셀 캡처 실패/성공이 screenCaptureFailing 을
  갱신하지 않음
- **next-floor 완료 판정 층 검증**: '다음 층으로' 후 아무 옵션 화면 제목도 성공 처리 -
  후속 시작 검증(구역 확인 → 불일치 시 복구/정지)이 오입장을 차단하는 안전망 (3차 점검 보류)
- **협동 미션 완료 '확인' 탐색 실패 시 Space 폴백**: 부제 확정 상태 한정. 화면 전환 직후
  Space 오입력 잔여 위험과 트레이드오프 - 채택 시 설계 합의 (사용자 문의 - 결정 대기)

### GUI/도구

- **커스텀 리스트 비셀편집 저장 실패 완전 롤백**: 추가/삭제/이동은 저장 실패 시 경고만
  (07-31 문구 강화 완료) - UI 재동기화 방식 검토
- **GetNewClosure 스코프**: 셀 편집 콜백의 $script: 변수가 생성 시점 복사본 - 방어 로직
  실효성 재검토
- **커스텀 토큰 형식 검증**: 환경변수 토큰의 0/1 필드 손상 값이 전부 false 로 해석됨
- **빌드 대장 동시 접근 잠금(TOCTOU)**: 재조회+재검사까지 반영됨 - 파일 잠금은 로컬 1인
  빌드 도구라 실익 낮음 (3차 점검 보류)
- **창 정렬(nearest) 스냅 임계값**: 1340×754 창이 1272×717로 스냅되지 않고 1px만 조정 -
  좌표가 전부 비율 환산이라 **무해 확인 완료** (07-30 타 PC 완주 실측). 비권장 크기를 권장
  크기로 당길지 결정만 남음 - 실사용 관찰로 충분하면 유지

---

## 4. 최근 검증 기록

### 2026-08-09 — 현재 상태 (이 기록이 최신) — **v2.0.1 빌드 (배포 대기)**

- **v2.0.1 빌드: `93e77e6a…`** (1737KB, 미배포). v2.0.0(`4ace76c5…`)은 배포 완료.
  v2.0.1 = 08-09 chyui 제보 핫픽스 (라벨 깨짐 + 파란 배경 글리프 미검출)
- **생활(채집) 대분류 신설**이 이번 버전의 본체입니다. 채집 **8종 69대상**(낚시 제외),
  전수 배치로 스킬별 전 대상 연속 2라운드 통과. 클릭 직전 대상 재확인으로 제목 인식률 96.3%
- **생활 커스텀 반복**: 채집 스킬을 섞어 리스트를 짜고 **항목마다 반복 횟수**를 지정.
  셀 편집·랜덤 진행·진행 이어가기 포함
- 회귀 **44종 전부 통과**, 문법 0건
- `configSchemaVersion` **7**, `coordsVersion` **7**
- **미개발 명시**: 낚시·가공은 시작 전 안내로 차단. 가방 가득 / 도구 내구도 소진 '중' 감지는
  없습니다 — 게임이 스스로 멈추고 자동화는 '진행 없음' 한도로 정지합니다
- **사용자 설정**: GUI '진행 없음' 기본 **600초**. 총 시간 제한이 아니라 '진행이 멈춘 채로
  견디는 시간'이라, 이동이 아주 먼 대상에서 멈추면 이 값만 늘리면 됩니다
- **실기 확인 완료 (08-08 사용자)**: 1908 창 채집 정상 동작 / 채집 중 안전 중지
- **남은 실기**: '1. 확인 필요' 참고 — 사이클 완료 후 다음 회차 자동 시작, 생활 커스텀 실제 반복

### 2026-08-06 — 직전 상태

- **v2.0.0 빌드: b38ce02e…** (미배포 - released 미표시). 포함: 채집 워커 + GUI
  차단 해제 + 로그 폴더 통일 + GUI 실기 실사고 수정(행 조합/스크롤/이형) + 생활 시작
  전투 상태 누출 4건 + 탐색·deadline 보강 (아래 '해결됨' 08-05~06 항목들)
- 실기 완주 2회: 일상 채집 - 사과 나무 (08-05 23:04) / 나무 베기 - 흰 껍질 나무
  (사냥터 이동 포함, 08-05 23:58). 회귀 42종 전부 통과, 리뷰 승인 완료

### 2026-08-05 — v2.0.0 채집 실기 통과·GUI 차단 해제

- **워커 단독 실기 3차에서 1사이클 완주 (23:01~23:04, exit 0)** — 1·2차 실패는 실기
  전용 사고 2건(OCR '채집물' 깨짐 / 반투명 배경 픽셀 오판)으로 전부 수정·이력화
  ('5. 해결됨' 같은 날 실기 항목). 설계 합의 출시 조건 충족 → **GUI 시작 차단 해제**
  (게이트는 '지원 범위'로 전환: 채집 4종 허용, 가공·미지원 스킬 5종은 안내 팝업 유지)
- 회귀 테스트: **42/42 통과** (`test_life_gather` 진리표 84케이스 + `test_life_ocr_offline`
  실측 재현 39케이스 - 실기 캡처 2장 자산 포함), GUI·워커 구문 오류 0건
- 구현 리뷰 2라운드 전부 반영 후 최종 승인 (캡처 실패 경계·스크롤 궤적 동적 재현
  검증). 실기 후 게이트 전환·해시까지 재확인 - "배포 대기 상태로 승인 가능" 회신
- 최종 빌드: `HoneyNogi.exe` v2.0.0, 해시 2a4683bd… (미배포 - released 미표시.
  이후 로그 폴더 통일까지 포함한 빌드 - 아래 '해결됨' 로그 통일 항목)
- 남은 실기: GUI 경유 반복/중지 + 나머지 지원 스킬 3종 ('1. 확인 필요')

### 2026-08-05 — 직전 상태 (v1.2.1 배포 완료)

- **v1.2.1 릴리스 (2026-08-04)**: 해시 0672033a…, released 잠금 완료, zip 생성
  (version\v1.2.1\HoneyNogi.zip), 릴리스 노트 전달 — 사용자가 GitHub 업로드(태그 v1.2.1).
  이 버전 재빌드는 차단됨 - 이후 수정은 다음 버전으로. 타 PC 사용자들은 exe 교체 시
  월요일 6시 협동 창 정지·네트워크 팝업 정지가 해소됨
- 회귀 테스트: **39/39 통과**, GUI·워커 구문 오류 0건 (배포 시점 기준)
- 08-01~08-03 실사고 7건+ 수정, 08-04 커스텀 '랜덤 진행' 신설 + GUI 개선(실행 중 리스트
  스크롤·설정/로그 탭 토글) — 상세는 '5. 해결됨' 해당 날짜 항목
- 실기 확인 이월: 커스텀 '랜덤 진행'(셔플 표기/바퀴 전환/재시작 이어가기), GUI 탭
  토글·폭 560 화면 확인, 다음 월요일 6시 협동 창 자동 닫기, 네트워크 팝업 자연 발생
- **다음 개발: v2.0.0 '생활' 카테고리** (GUI 시안 확정 08-05) — 스펙은 요청사항.md

### 2026-07-30 — 릴리스 최종 상태 (v1.2.0 배포분 최종)

- 회귀 테스트: **37/37 통과**, GUI·워커 구문 오류 0건
- 07-29 이후 추가 반영: 심층 라벨 화이트리스트 신뢰(픽셀 미확인 채택) / 마스 '파스' 이형 /
  공물 부족 코드 4 안내 정지 — 전부 실기 검증 완료
- **타 PC(1908·1340 창) 검증 종결**: 심층 일반 판(01:16) / 마스 1272(01:23) /
  일반 던전 색 왜곡(01:29) / 심층 공물 판(01:34) / 공물 소진 정지 구문구(01:49) — 5판 실기
- 최종 빌드(배포 원본): `HoneyNogi.exe` v1.2.0, 해시 c91cacef… (release_hashes.json 최신
  항목 — 타 PC 01:49 실기에 쓰인 빌드. released 표시는 업로드 확인 후)

### 2026-07-29 — 전수 점검 (v1.2.0 릴리스 준비 과정 기록)

- Windows PowerShell 5.1 파서: GUI·워커 구문 오류 0건
- 회귀 테스트: **37/37 통과** (07-28 이후 신설: `test_after_entry_popup`(구매 팝업)
  `test_assist_toggle`(어시스트) `test_ime_popup`(IME 팝업·소모량·역방향 계약) + 기존
  확장: 심층 탭·수동 확인 계약·다중 스케일·이형 진리표 다수)
- 실기 검증: 심층 9던전 스윕 완주 / 커스텀 완주 다수 / 완료 마커 복구 재현 / 승인제·구버전
  정리 종결 — 이 과정의 실기 사고 20건± 전부 수정·이력화 ('해결됨' 07-28~29 참고)
- 당시 빌드: `HoneyNogi.exe` v1.2.0 '파스' 이형 포함분 (이후 07-30 추가 수정으로 대체 —
  최종 배포 원본은 위 07-30 기록 참고)

### 2026-07-28 — 전수 점검 (v1.2.0 릴리스 준비 기준)

- Windows PowerShell 5.1 파서: GUI·워커 구문 오류 0건
- 회귀 테스트: **34/34 통과** (심층던전 67케이스 `test_deep_dungeon` + 전멸 재도전 22케이스
  `test_wipe_revive` 신설 포함. 오프라인 레이아웃 40장 212지점은 저장소 `던전이미지\던전`
  경로로 이전해 재활성)
- `coordsVersion` 6 / `configSchemaVersion` 3 / 앱 버전 1.2.0 (미배포 — released 미표시)
- 빌드 정상: `HoneyNogi.exe` v1.2.0 (`release_hashes.json` 갱신 + `version\v1.2.0\` 자동 보관)

### 2026-07-22 — 재검토 검증 (v1.1.1 기준)

- Windows PowerShell 5.1 파서: 현재 소스 PS1 32개 구문 오류 0건
- 회귀 테스트: 운영 함수 AST 직접 검증을 포함한 26/26 통과
- 전체 `.ps1` UTF-8 BOM 확인, `config.json` JSON 파싱 정상
- `coordsVersion`: 워커·config 모두 6, `configSchemaVersion` 2, 앱 버전 1.1.1
- `git diff --check` 오류 없음, `app.ico` Git 추적 확인
- PSScriptAnalyzer는 현재 환경에 없어 미실행
- Git 제외 빌드 도구 `build\build_exe.ps1` 실제 실행 성공: `HoneyNogi.exe` v1.1.1, 1124.5KB

---

## 5. 해결됨 (이력)

### 2026-08-07 — 채집 지원 4종→8종 + 퀘스트 판독 신뢰성 (전수 배치 연속 2라운드 통과)

**지원 스킬 확장**: 일상 채집·나무 베기·광석 캐기·약초 채집에 **양털 깎기·추수·호미질·
곤충 채집**을 더해 8종. 스킬 창은 8열 그리드이고 셀 x 좌표를 실측으로 확정
(`755 / 884 / 1014 / 1143`). 각 스킬의 대상 목록 순서(`Order`)와 판별 조각(`Sig`)도
probe 로 직접 읽어 등록 — 낚시만 남았고 GUI 게이트(`$lifeSupportedSkillIds`)도 8종으로 확대.

**퀘스트 판독 신뢰성 (사용자 실기 지적 연속 5건 - 이번 작업의 핵심)**:
> 증상은 전부 하나로 모입니다. "채집하러 이동 중인데 중간에 다른 대상을 누른다."
> 원인은 퀘스트 트래커가 잠깐 가려지거나 로딩으로 사라진 것을 '퀘스트 없음 = 사이클 완료'
> 로 오판한 것이었습니다. 없는데 있다고 보면 잠깐 기다릴 뿐이지만, 있는데 없다고 보면
> 남의 채집을 망칩니다 — 이 **비대칭 위험** 때문에 판정을 전부 present 우선으로 바꿨습니다.

- `Get-LifeQuestState` 3단 판독: 좁은 ROI → 넓은 ROI(`$rgLifeQuestWide`) → 그래도 안 읽히면
  게임이 전면인지 확인하고 아닐 때만 전면화 후 재판독. 전면화도 실패하면 `unknown`
- **맵 이동 로딩 화면에서는 퀘스트도 HUD 도 없음** → `unknown` 은 부재로 세지 않음
  (부재로 세면 이동 중에 메뉴를 열어 버림)
- 소멸 판정은 `absent` **3연속** (1회 확정은 트래커가 가려진 사이 '끝났다'고 판단)
- 다른 대상의 채집이 진행 중이면 **최대 3분 대기**(3초 간격, 남은 수량 로그) 후 시작.
  즉시 정지로 만들었더니 대상을 바꿀 때마다 멈춰 5연속 실패
- `absent` 는 'HUD 는 보이는데 퀘스트가 없다' = 게임 화면이 앞에 있다는 증거이므로
  **전면화하지 않음** (사용자: "가려지지도 않았는데 게임이 자꾸 앞으로 튀어나온다")
- 퀘스트 문구 조각은 '채집/장소/**탐색**' 3개 중 2개 매칭 (사용자가 '탐색'이 공통임을 지적)
- 개발 창이 게임을 덮은 채 자동화가 돌아 에디터 글자를 판독하던 사고 → 판독·클릭 전
  `Confirm-LifeGameFront`

**스크롤 최적화 (사용자 지적)**: 대상이 이미 화면에 보이면 정렬 없이 바로 클릭하고,
목록이 이미 최상단이면 정렬 드래그를 생략 (오른쪽 목록은 순서가 고정이라 매번 올릴 이유가 없음).

**이름 깨짐은 개별 이형 대신 공통 치환 규칙으로**: `나부→나무`, `부리→무리`, `곤춤→곤충`.
곤충 채집은 9종 중 7종이 '~빛 무리/곤충 무리'라 개별 등록이면 누락이 생깁니다.
한 행에 깨짐이 **둘 겹치는** 실측(`황폐한곤춤부리` = 곤춤 + 부리)이 있어 개별 치환 사본뿐
아니라 **전부 적용한 사본**까지 비교합니다 (리뷰 지적 — '부리'만 고치면 '곤춤'이 남음).
그 외 이형은 파스님(파스닙), 국빛무리·빛부리, 들곤충무리 추가.

**전수 배치 결과**:
- 지원 4종 41대상(계정 제약 2 제외) **연속 2라운드 41/41** (라운드 21·22)
- 양털 깎기·추수 10대상 **연속 2라운드 × 2회**
- 호미질 7 + 곤충 채집 8대상 연속 2라운드 (아래 2건 수정 후 재검증)

**배치에서 드러난 마지막 사고들 (교차 리뷰 2라운드 포함)**:
- **[근본 원인] 생활 분기에 캡처 복구 탐침이 없었음**: 호미질 '개암 버섯' R2 가 채집
  3/10 진행 중 RDP 창 최소화로 캡처 실패 → 16분 뒤 한도 초과 정지. 처음엔 환경 문제로
  봤지만 교차 리뷰에서 진짜 원인이 나왔습니다 — 생활 분기의 **모든 대기 지점이 '캡처
  실패 중이면 판독을 건너뛰고 continue'** 하는 바람에 아무도 캡처를 시도하지 않고,
  `$script:screenCaptureFailing` 은 **캡처가 성공해야만** 풀리므로 화면이 돌아와도 영영
  감지하지 못합니다 (던전 분기에는 같은 자리에 OCR 복구 탐침이 있었는데 생활 분기에만
  빠짐). → `Test-CaptureRecovered` 신설, 4곳(초기 확인/이전 퀘스트 대기/메뉴 캡처 대기/
  채집 대기) 전부에 배치. OCR 없이 작은 영역만 떠서 플래그를 갱신합니다.
  - **반환형 함정**: `Get-GameRegionCapture` 는 Bitmap 이 아니라 `Bitmap` 속성을 가진
    래퍼를 돌려줍니다. 래퍼에 `Dispose()` 를 부르면 **복구되는 순간에만** 예외가 터져
    실기에서 늦게 드러났을 사고 (2차 리뷰 지적) → `.Bitmap.Dispose()`
- **레벨 미달이 로그에 안 남음**: 곤충 '일렁이는 빛 무리'가 R1·R2 모두 메뉴 사이클 3회
  소진. 상세 판독을 보고서야 `곤충채집레벨27이실h1`(= 레벨 27 이상)을 확인 — 캐릭터는
  Lv.25 라 게임이 퀘스트를 만들어 주지 않는 **계정 제약**이었습니다. 원인이 화면에만
  남아 로그만으로는 자동화 결함과 구분할 수 없었음 → `Get-LifeRequiredLevel` 신설,
  실패 안내에 요구치를 함께 표기. 캐릭터의 현재 레벨은 머리글 판독이 불안정해
  (`LⅥ2512,396`) **비교하지 않고 요구치만** 알립니다 (단정 금지).
  - 방어: 라벨 '집물' **뒤에서만** 탐색(라벨 없으면 안내 생략), 서로 다른 값이 둘 이상이면
    생략, `(?!\d)` 로 긴 숫자의 접두부 절단 금지(`레벨1000`→100 방지), 상한 100
  - 저장 시점은 **상세가 이 대상의 팝업으로 확정된 뒤**로 제한하고 `@{ Target; Level }`
    로 묶어 안내 시점에 대상을 재대조 (확정 전에 남기면 오클릭한 다른 대상의 요구치가
    실려 나감 - 리뷰 지적)
- **한도 초과 원인 오안내**: 위 캡처 실패 건이 로그에는 "채집 대기를 늘려 주세요"로만
  남아 원인을 오인하게 됨 → 캡처 실패 중 초과는 **화면 미표시**로 구분 안내. 한도 자체는
  무인 운용에서 영원히 매달리지 않기 위한 하드 상한이라 그대로 둠 (리뷰 계약 유지 —
  이번 실기에서도 캡처가 끝내 복구되지 않아, 연장했어도 무한 대기였을 뿐)
- **Sig 조각 충돌**: 호미질에 '개암 버섯'이 생기면서 약초 `Sig` 의 '버섯'이 더는 고유
  조각이 아님 → 제거. 재발 방지로 **모든 스킬의 Sig × 다른 스킬의 Order 전 조합 대조**
  테스트를 넣어 향후 대상 추가 시 자동으로 잡히게 했습니다
- **부수 발견 — 테스트가 이름과 다른 것을 검증하고 있었음**: 시뮬레이터 `deadline`
  시나리오가 한도를 0초로 둬서 **초기 확인 단계에서 이미 지나** 메뉴 경로로 끝났습니다.
  즉 '대기 루프의 한도'는 한 번도 검증된 적이 없었음. 마찬가지로 `menu-fail-level` 초안은
  메뉴를 3회 실패시켜 **실제 사고 경로(링크까지 성공 → 생성 확인 8회 실패)** 를 안 탔습니다
  → 둘 다 실제 경로로 교정. 시간 의존 시나리오는 실제 대기 대신 **가상 시계**(Get-Date
  스텁 + Start-Sleep 이 가상 시각만 전진)로 바꿔 느린 머신 플래키를 없앴습니다

**사용자 확정 설정**: GUI '채집 대기' **1200초** (젖소·광맥·뾰족 나무가 900초 초과 실측).

**계정 제약 (자동화 결함 아님)**: 얽힌 거미줄(일상 Lv.30 필요/현재 28), 산뜻 버섯(레벨 미달),
일렁이는 빛 무리(곤충 Lv.27 필요/현재 25). 빈 병 필요 대상(물·우물·젖소)은 사용자가 300개
구매로 해소.

### 2026-08-08 — 생활(채집) 커스텀 반복 신설 (시안 확정 → 구현)

> "생활 커스텀도 만들자 기존 전투 커스텀반복처럼" — 실물 패널 대조 + WinForms 목업으로
> 시안을 확정한 뒤 구현. 조건표는 `요청사항.md` 참고.

**던전/어비스/심층과 결정적으로 다른 점은 하나 — 항목마다 반복 횟수(count)** 입니다.
기존 셋은 '항목 1개 = 1판'이라 진행 기록(lap/index)이 등록 항목 위에서 그대로 셌지만,
생활은 한 항목을 count 사이클 돌아야 다음 항목으로 넘어갑니다.

**설계: 등록 목록은 펼치지 않고, 실행 시점에만 펼친다.**

| 축 | 기준 | 이유 |
|---|---|---|
| `items`(config) | 등록 그대로 (count 포함) | 사용자가 짠 리스트가 화면·로그에 그대로 보여야 함 |
| 지문 / 랜덤 순열 | **등록 단위** | 순열은 '항목'을 섞는 것 - 펼친 칸을 섞으면 3회 연속이 흩어짐 |
| 진행 `index` / 전진 칸 수 | **펼친 단위** (`Expand-LifeCustomItems`) | 한 칸 = 한 사이클이어야 count 가 정확히 지켜짐 |

이 두 축이 어긋나면 나는 사고가 정확히 둘이라 진리표로 못 박았습니다:
- 전진 칸 수를 등록 수로 세면 → **count 2 이상인 항목이 1회만 돌고 넘어감**
- index 상한을 등록 수로 검사하면 → **정상 진행이 1번으로 되감김** (기존 코드의
  `if ($index -ge $items.Count) { $index = 0 }` 를 펼친 개수 확정 뒤로 옮김)

**셔플 → 펼침 순서도 계약입니다.** 반대로 하면 '사과나무 3회'가 리스트 전체에 흩어집니다.

**공용 인프라 재사용 / 생활만 다르게 한 것**

- 재사용: `Get-CustomCurrentContext` / `Step-CustomProgress` / `Reset-CustomProgress` /
  `Confirm-CustomShuffleReady` / 시작 게이트(빈 리스트·지문·완주 취급) — 섹션명
  `lifeCustomRepeat` 분기만 추가
- 제외: **층 전환 규칙**(생활 항목엔 stage 가 없어 검사를 돌리면 빈 값끼리 비교한 헛 위반),
  **혼합 리스트 1바퀴 고정**(사이클마다 C키부터 새로 시작해 스킬을 섞어도 제약 없음),
  **완료 마커**(채집은 '퀘스트 소멸 = 완료'라 던전의 '클리어는 됐는데 마무리 중 종료'라는
  중간 상태가 없음 — 경로만 전용으로 두어 마커 검사가 항상 '없음'으로 통과)
- 전달: `HONEYNOGI_CUSTOM_ITEM` 에 `L|<스킬Id>|<대상>|<횟수>` 4조각. 워커는 이 값으로
  `$lifeSkillId`/`$lifeTargetName` 을 덮어쓰고 **사이클 1회만** 수행합니다(횟수는 GUI 가 셈).
  토큰이 깨지면 config 값으로 대신 돌지 않고 오류로 끝냅니다 — 리스트에 없는 대상을 캐면
  **남의 채집을 밀어낼 수 있기** 때문입니다

**미지원 스킬(낚시) 2중 차단**: ①`[추가]` 시점 즉답 팝업 ②시작 게이트(config 직접 편집 대비,
팝업 없이 로그만). 두 곳이 `$script:lifeSupportedSkillIds` 하나를 봅니다.

**화면**: 커스텀에서는 슬라이더가 '담기용'이라 카드를 아이콘 없는 글자 카드 26px 로 줄여
두 줄에서 60px 를 벌고 그 자리에 리스트를 넣습니다(아이콘 9종은 일반 생활 모드 그대로).
리스트 392x174 / 버튼 94x30 은 전투 커스텀과 동일 치수.

**검증**: 진리표·시뮬레이션 `tests\test_life_custom_repeat.ps1` 신설(회귀 43종),
실기 — 항목 2개 담기·재시작 후 복원·낚시 차단 팝업 확인. **남은 실기: 실제 반복 실행.**

### 2026-08-08 — 생활에서 안전 중지 안내가 '던전에서 나오면'으로 표시 (문구)

> 사용자 지적: 채집 중 안전 중지를 눌렀더니
> `안전 중지 예약: 던전에서 나와 밖이 확인되면 멈춥니다.` — 생활에는 던전이 없습니다.

문구를 고치기 전에 **생활에서 안전 중지가 실제로 어떻게 동작하는지** 확인했습니다:

- 전투: 워커가 결과 화면에서 `Invoke-SafeStopExitIfRequested` 로 '나가기'를 눌러
  **회차 도중에** 마칩니다. 그래서 '던전에서 나오면'이 정확한 설명입니다.
- 생활: 신호 파일을 소비하는 지점이 `Test-SafeStopDuringCaptureFail`(캡처 실패 2분 이상)
  **하나뿐**입니다. 정상 동작 중에는 워커가 사이클을 끝까지 마치고(코드 0), GUI 의
  `$script:stopRequested` 가 **다음 사이클을 시작하지 않는** 방식으로 멈춥니다.

이건 결함이 아니라 올바른 동작입니다 — 채집을 중간에 버리면 퀘스트만 날아가고 얻는 게
없습니다. 다만 안내가 그 차이를 말하지 않아 사용자가 '던전?' 하고 혼란스러웠습니다.

→ 안내를 대분류별로 갈랐습니다. 생활은 **`이번 채집을 마치면 멈춥니다`** 이고, 로그에는
소요 시간까지 밝힙니다(`진행 중인 채집을 끝까지 마친 뒤 다음 사이클을 시작하지 않습니다 -
채집이 끝날 때까지 몇 분 걸릴 수 있습니다`). 전투 문구는 그대로입니다.
신호 파일 정리(`Stop-AllRun` / 시작 시 잔존 제거)는 이미 정상임을 확인했습니다.

회귀 가드 3건 추가(생활 문구 / 전투 문구 유지 / 분기 구조). 44종 통과, 빌드 `4ace76c5…`.
**실기 확인은 하지 않았습니다** — 문구를 보려면 실제 채집을 시작해야 해서 사용자 게임을
건드리게 됩니다. 다음에 채집 돌리실 때 눈으로 확인해 주세요.

### 2026-08-09 — [타 PC 제보] 상세 팝업이 떠 있는데 '확인 못 함' → exit 4 (v2.0.1)

> chyui 님 제보. 광석 캐기 - 운철 광맥, 게임 창 **1273x718(= 기준 크기)** — 스케일 문제가
> 아닙니다. 오류 캡처에 **팝업이 정상적으로 떠 있고** 대상도 맞는데 워커가 못 알아봤습니다.
> 자산 이관: `던전이미지\생활\1273캡처\` (팝업 2장 + 스킬 그리드 1장 + 필드 1장 + 로그)

#### ① 직접 원인 — 라벨 `채집물` 이 `채집묻` 으로 깨짐

팝업 판독: `문철광맥채집묻광석개기레豊30이상…`

제목부 절단이 `IndexOf('집물')` 한 줄에 걸려 있었습니다. 라벨을 못 찾으면 제목이 빈 값이 되고
판정이 `unknown` → 팝업 확인 실패 → 3회 소진. **이름(`문철광맥`)은 이미 이형 표에 있어
제목만 잘렸으면 통과했을 건**이라, 라벨 한 글자 때문에 전부 무너진 셈입니다.

`'집물'` 리터럴이 워커 **7곳**에 흩어져 있어 단일 진입점으로 모았습니다:

| 신설 | 역할 |
|---|---|
| `$lifeDetailLabelFragments = @('집물', '집묻')` | 실측된 깨짐만 등록 (느슨하면 본문에서 오절단) |
| `Get-LifeDetailLabelIndex` | 가장 앞 조각 채택 + **위치 상한 16자** |
| `Test-LifeDetailHasLabel` | 팝업 인식 |

요구 레벨 정규식도 `레벨` → `레[벨豊]` (`레豊30이상` 관측).

#### ② 배포 차단 결함 — 파란 배경에서 창 글리프 미검출 (교차 리뷰가 발견)

제가 ①만 고치고 배포하려던 것을 리뷰가 막았습니다. **02:32:49 캡처(생활 스킬 그리드)에서
`창픽셀=False`** — 어제 넣은 글리프 판정이 여기서 깨집니다.

원인은 축의 '어두움'을 **절대 임계(110)** 로 본 것입니다. 그리드의 파란 배경은 밝기 약 **136**
이라 X 가 또렷한데도 "축이 어둡지 않다"로 거부됩니다. 어제 제보와 **같은 고착 경로**
(창 열림 미감지 → C 무시 → 재시도 소진)라 배포 차단급입니다.

→ **상대 대비**로 교체했습니다. 축은 절대값이 아니라 `중심 - 90` 보다 어두우면 됩니다.
형제 함수 `Test-LifeWindowClosePixels` 가 이미 쓰던 방식과 같은 결이고, 배경 밝기에
영향받지 않습니다. 임계 쓸기 실측(자산 23장 = 열림 11 / 닫힘 12):

| 방식 | 오판 0인 구간 |
|---|---|
| 절대 임계 `DarkMax` | **140 하나뿐** (110~130 은 그리드 놓침) |
| 상대 대비 `MinContrast` | **40~120 전 구간** ← 채택(기본 90) |

#### ③ 교차 리뷰가 잡은 반례 3건 (전부 '조용히 틀린 값을 확정')

로그만 보고는 못 잡는 종류라 전부 진리표로 고정했습니다.

- **라벨 위치 상한 없음**: 진짜 라벨을 놓친 뒤 설명 본문의 같은 조각을 라벨로 인정하면
  제목이 통째로 잘못 잘립니다 (`운철광맥채집들…설명집묻뒤` → 제목을 `…설명` 까지).
  오클릭 확정으로 이어질 수 있어 **16자 상한**을 뒀습니다.
- **제목 띠가 최소 Y 가 아님**: 단어 배열의 열거 순서가 보장되지 않아 설명 쪽 단어가 먼저
  걸리면 띠가 아래로 밀립니다 (440,174 대신 440,286). **후보 중 최소 Y** 채택으로 수정.
- **요구 레벨이 라벨 뒤 전체를 검색**: 진짜 요구치가 깨져 사라지면 설명 속 숫자를 요구치로
  안내합니다. **라벨 뒤 24자**로 제한.

#### 검증

고치기 전 재현 → 고친 후 통과. 회귀 **44종**, 빌드 `93e77e6a…`.

| 항목 | 전 | 후 |
|---|---|---|
| 제목부 절단 | 빈 값 | `문철광맥채` |
| 팝업 판정 | unknown | **mine** |
| 요구 레벨 | 0 | **30** |
| 스킬 그리드 창 감지 | **False** | **True** |
| 팝업 2장 + 필드 1장 | — | 열림 2/2, 필드 미검출 |
| 기존 자산 (1272 11장 + 1908 3장 + 전투) | 유지 | **오판 0** |

**교훈**: 판정 임계는 절대값보다 **상대 대비**가 안전합니다. 절대값은 '지금 이 화면'에서만
맞고 배경이 바뀌면 조용히 깨집니다 — 이번엔 같은 결함이 하루 만에 다른 화면에서 재발했습니다.

### 2026-08-08 — [타 PC 제보] 1908 창에서 채집이 exit 4 로 정지 (고장 2개, 원인 다름)

> hyodong 님 제보. 곤충 채집 - 일렁이는 빛 무리, 게임 창 **1908x1076**.
> `[완료] 채집 시작(가까운 위치 찾기)을 확정하지 못했습니다 - 조건부 정지`
> 오류 캡처 3장 모두 **생활 스킬 창이 정상적으로 열려 있는데** 로그는 `창픽셀=False`.
> 자산 이관: `던전이미지\생활\1908캡처\` (Log 폴더는 10장 회전이라 곧 사라짐)

**고장은 두 개였고 원인이 다릅니다.** 하나만 고치면 재발합니다.

#### ① 첫 방아쇠 — OCR 이형 (좌표와 무관)

상세 제목 판독 `일럼OI는빛무리…` 에서 목표를 못 찾아 사이클 1이 무너졌습니다.
`일렁`→`일럼` 은 08-08 오전에 넣었지만 **`이`→`OI` 가 빠져** 있었습니다
(개발 PC 배율표 s6 행에도 이미 관측돼 있었는데 규칙에 반영하지 않았습니다).
목록 행도 `빛`→`및`, `빛` 통째 소실로 3건이 깨져 있었습니다.

| 추가/변경 | 값 | 비고 |
|---|---|---|
| `$lifeNameRepairPairs` | `('OI','이')`, `('및','빛')` | **넓은 쌍이어야 함** |
| `$lifeTargetVariants` | `'고요한빛무리'=@('고요한무리')` | 글자 소실은 치환으로 못 잡음 |
| `$lifeTitleVariants` | `'헤이즐넛'=@('헤OI즐넛')` **제거** | `OI`→`이` 로 중복이 됨 |

`('및무리','빛무리')` 같은 **좁은 쌍은 안 됩니다.** `Get-LifeRepairedTexts` 가 치환 여부를
**원문 기준**으로 판정하므로, `일럼이는및부리` 처럼 깨짐이 겹친 문자열에서는 `및무리` 조각이
원문에 없어 규칙이 아예 발동하지 않습니다.

**덤으로 잡힌 잠재 버그**: 곤충 채집에는 `빛 무리` 라는 짧은 대상이 실제로 있어서,
깨진 트래커에서 `Get-LifeQuestOwner` 가 그것을 집어 **'다른 채집 진행 중'으로 오판하고
무인 반복 전체를 exit 4 로 정지**시킬 수 있었습니다. 같은 치환쌍으로 함께 닫혔습니다.

#### ② 2차 고장 — 기준 좌표계의 제목줄 비비례 (구조적)

기준 좌표계는 '제목줄 포함 1272x717' 인데 **제목줄 높이는 창 크기에 비례하지 않습니다.**
`Get-ScaledScreenPoint` 는 `GetWindowRect`(창 전체)를 단순 비례 환산하므로, 창이 크면
게임 콘텐츠의 상대 Y 가 어긋나고 **화면 위쪽일수록 오차가 커집니다.**

| 기준 Y | 1908 창에서의 오차(ref) |
|---|---|
| 65 (닫기 X) | **-10** |
| 205 (스킬 셀) | -8 |
| 394 (첫 대상 행) | -5 |
| 639 | -1 |

그래서 대상 목록·스킬 셀은 오차를 흡수했지만 **최상단 1점 판정만 즉사**했습니다.
실측: X 글리프는 img y 68~103(교차점 86)인데 코드는 img y 98·101 을 읽어 **두 획 사이의
검은 틈**을 찍고 RGB(1,5,5) 를 얻었습니다(글리프 '밖'이 아니라 '틈').

**제목줄 높이는 PC마다 다릅니다** — 개발/제보 PC 31px, 다른 제보 PC(KJM) **38px**.
따라서 상수 보정은 어떤 형태든 불가능합니다.

**수정: 판정을 '어디를 보느냐'에서 '무엇이 보이느냐'로 바꿨습니다.**
`Find-LifeCloseGlyph` 가 우상단 ROI 를 **`-Scale 1`(기준 단위)** 로 캡처해 'X 두 대각선의
교차' 서명을 찾습니다 — 중심이 밝고 / 네 대각선이 밝고 / 네 축이 어두운 형태.
Scale 1 이라 판정식에서 창 크기가 사라지고, 제목줄 높이·저장 좌표의 ±2px 오차에 전부
면역입니다. 기존 4점 판정은 **폴백으로 유지**해 1272 무회귀를 보장합니다.

**클릭도 같이 고쳤습니다.** 판정만 고치면 사고가 '이번엔 X 클릭이 빗나감'으로 옮겨갈 뿐이라,
탐지된 글리프 중심을 `Invoke-LifeWindowCloseClick` 이 그대로 씁니다(탐지와 클릭이 같은
환산을 왕복). 캡처도 `Get-GamePixel` 원시 경로 대신 빈 프레임 판정·`Register-CaptureFailure`
가 있는 `Get-GameRegionCapture` 로 바꿔, 캡처 끊김을 '창 닫힘'으로 오인하지 않게 했습니다.

#### 연쇄 — 그리고 내가 처음에 틀렸던 것

`Test-LifeWindowOpen`=False 가 **두 곳을 동시에** 무력화했습니다:
`Close-LifeOpenWindows` 의 닫기 루프(즉시 break)와 `Invoke-LifeMenuSequence` 의
'잔존 창이면 X 로 먼저 닫기' 분기. **제보 로그 33줄에 `창 닫기(X)` 가 0건**인데 캡처에는
창이 열려 있는 것이 그 증거입니다.

처음엔 'C 가 창을 닫는 토글로 상쇄됐다'고 봤는데 **반증됐습니다.** 세 캡처가 픽셀 0.03%만
다른 정지 화면이고, 토글이라면 다음 사이클에 자기 복구돼야 하는데 3회 모두 동일 실패였습니다.
실제로는 **창이 열려 있는 동안 C 가 무시**됩니다(같은 파일에 이미 `팝업이 남으면 C 키가
먹지 않아 연쇄 실패 - mining 5연속 실측` 이라는 정반대 실측이 있었습니다).
이 구분이 대응을 가릅니다 — 토글이면 최악 1회 손실이지만 **무시면 X 로 닫기 전까지 영구
고착**이라 재시도 3회 소진이 필연입니다. 그래서 C 를 **닫힘 확인 후에만** 누르도록
fail-closed 로 바꾸고, 확인 실패는 사이클 실패로 돌립니다.

주석 2건도 정정했습니다: `생활 스킬 창은 좌측 메뉴가 사라진 레이아웃` (캡처에 또렷이 있음),
`C 가 그 창을 닫는 토글` (원 출처가 실측이 아닌 추론이었음).

#### 개발 PC 에서는 영원히 재현할 수 없습니다

작업 영역이 **1920x1032** 라 높이 1076 창을 물리적으로 만들 수 없고, `recommended` 조건
(workW≥2100 & workH≥1150)도 미달이라 항상 1272x717 입니다. 실기 재테스트로는 못 잡습니다
— **제보 캡처를 저장소 자산으로 고정하는 것 외에 방법이 없습니다.**

#### 하지 않은 것 (다음 티켓)

**좌표 환산식 자체의 근본 수정은 보류**했습니다. `GetClientRect` 기반으로 바꾸면 1272 는
대수적으로 무변화지만 1908/1810 사용자의 **모든** 좌표가 최대 16px 이동하는데,
**저장소에 비-1272 전투 캡처가 0장**이라 전투 안전성을 오프라인으로 증명할 수 없습니다.
착수 조건: ①`[설정]` 스냅샷에 inset 로깅 ②비-1272 전투 캡처 확보 또는 합성 하니스
③환산 3함수 + `Test-DifficultySelectedAt` 인라인 사본을 한 커밋에.
같은 원인의 미해결 건: `$rgDgCoinBalance` 1908 판독 전멸(은동전 사전 소진 감지가 죽어 있음).

**`coordsVersion` 은 7 유지가 맞습니다.** 8로 올리면 config 를 갱신하지 않은 사용자의
`ocrRegions`/`clickPoints` 가 **통째로 제거**됩니다. 이번 수정은 config 좌표 키를 하나도
바꾸지 않으므로 규칙 5의 대상이 아닙니다.

#### 검증

고치기 전 재현 → 고친 후 통과 (`tests\test_life_window_glyph.ps1` 신설, 회귀 **44종**):

| 항목 | 전 | 후 |
|---|---|---|
| 제목 판정 | unknown | **mine** |
| 목록 행 6개 | 3/6 | **6/6** |
| 트래커 소유자 | 빈 값(오판) | **일렁이는 빛 무리** |
| 제보 1908 창 감지 3장 | 0/3 | **3/3** |
| 개발 1272 창 열림 5장 | 5/5 | **5/5** (무회귀) |
| 1272 창 없음 6장 + 전투 5장 | 오탐 0 | **오탐 0** |

글리프 중심은 1272 에서 `ref(1227,65)`(원래 하드코딩 좌표와 일치), 1908 에서 `ref(1229,56)`
으로 자동 추종합니다. 형태 진리표(X/십자/가로막대/세로막대/균일면/대비경계/빈 배열)도 추가.
빌드 `d870622a…`.

**실기 확인 완료 (2026-08-08 사용자)** — 개발 PC 는 작업 영역 1920x1032 라 1908x1076 창을
물리적으로 만들 수 없어(recommended 조건 workW≥2100 미달, nearest 최대 1831x1032) 실기
재현이 불가능한 건이었는데, 실사용 환경에서 정상 동작을 확인했습니다.
그래서 **제보 캡처 3장을 저장소 자산으로 고정**해 뒀습니다 (`던전이미지\생활\1908캡처\`).
`던전이미지\` 는 .gitignore 대상이라 저장소에는 안 올라가므로, 이 폴더가 없는 PC 에서는
`tests\test_life_window_glyph.ps1` 의 재현 절이 **스킵**됩니다(형태 진리표·배선 가드는 항상 동작).

### 2026-08-08 — 생활 커스텀 리스트 셀 편집 추가 + 그 과정에서 잡은 기존 결함 2건

> "전투 커스텀처럼 리스트 누르면 변경이 안돼는데" — 생활 리스트만 셀 편집이 없었습니다.

**생활만의 난제: 스킬과 대상이 종속 관계.** 스킬을 '일상 채집 → 광석 캐기'로 바꾸면 그 행의
대상 '둥지'는 광석 캐기에 없습니다. 그대로 두면 워커가 스킬 창에서 대상 행을 못 찾아 그
항목 차례에 **반드시 멈춥니다**. 슬라이더가 이미 쓰는 규칙(스킬 변경 시 대상은 첫 항목으로
폴백)을 그대로 따르되, 판정 기준을 '스킬이 바뀌었나'가 아니라 **'지금 대상이 새 스킬에
있나'** 로 뒀습니다. 이래야 같은 스킬을 다시 골랐을 때 대상이 보존돼 3필드가 전부 같아지고
'변경 없음' 조기 반환이 성립합니다(실수로 누른 재선택이 조용히 저장·로그를 유발하지 않음).
부수 효과로 손편집된 행(`{daily, '광맥'}`)은 **어느 열을 고쳐도 유효 조합으로 수리**됩니다.

컬럼별: 스킬 = **지원 8종만**(낚시 제외 — [추가]·시작 게이트의 차단을 셀 편집이 우회하는
뒷문 방지) / 대상 = 그 행 스킬의 목록만(행 상태의 함수) / 횟수 = 1~99.
현재값이 옵션에 없으면 **목록 끝에** 덧붙입니다 — 콤보가 DropDownList 라 없으면 선택이 비고,
끝에 붙여야 낚시 같은 미지원 값이 첫 줄에 와서 '고르라는 것'처럼 보이지 않습니다.

#### 같이 잡은 기존 결함 ① — 디스패처의 조건 없는 '어비스 폴백' (잠재 파괴 사고)

`$cellEditMouseUp` 과 `Invoke-CellEditApply` 의 마지막 분기가 `} else {` = 어비스였습니다.
**MouseUp 만 연결했다면** 생활 셀 클릭이 어비스 계획을 타 엉뚱한 옵션이 뜨고, 적용은
`Invoke-AcrCellEdit`(대상 리스트를 인자로 받지 않고 `$lvAcrList` 를 직접 씀)가 **화면에
보이지도 않는 어비스 리스트를 통째로 바꿔** abyssCustomRepeat 에 저장했을 것입니다.
증상이 어비스 리스트 상태에 따라 '무반응' ↔ '파괴'로 갈려 제보로는 잡기 극히 어려운 형태.
→ 네 리스트를 전부 명시 분기로 좁히고 최종 `else` 를 `return` 으로 닫았습니다. 테스트가
`else` 폴백의 부재를 직접 검사합니다(5번째 리스트가 생겨도 같은 사고 재발 차단).

#### 같이 잡은 기존 결함 ② — 드롭다운 자동 펼침이 처음부터 죽어 있었음

`Show-CellEditCombo` 의 자동 펼침 클로저가 **한 번도 실행된 적이 없었습니다.**
원인은 PS 5.1 함정: **함수 안에서 만든 `GetNewClosure()` 클로저에서 `$script:` 변수는
$null 로 읽힙니다.** `GetNewClosure` 는 새 동적 모듈을 만들고 **지역 변수만** 복사하므로,
스크립트 최상위에서는 우연히 동작하지만 함수 안에서는 빈 모듈의 script 스코프를 가리킵니다.
PS 5.1 재현:

```
[함수 안 클로저] script:probeSession=[] asInt=[0] captured=[1] 같은가=[False]
[바깥]           script:probeSession=[1]
```

그래서 `if ([int]$script:cellEditSession -ne $openSession) { return }` 이 항상
`0 -ne N` = 참 → 즉시 return. 셀을 눌러도 콤보만 뜨고 목록이 안 펼쳐졌습니다(전투 리스트도
마찬가지 — 사용자가 한 번 더 클릭해 쓰고 있었음). 실패 방향이 안전 쪽이라 사고는 없었습니다.
→ 검사를 `Open-CellEditDropDown` **함수**로 빼냈습니다. 함수 호출은 진짜 스크립트 스코프에서
실행되므로 안전합니다 — 바로 위 `Add_SelectionChangeCommitted` 가 `Invoke-CellEditApply` 를
부르는, 이미 검증된 패턴과 같습니다. **이 함정은 CLAUDE.md 'PS 5.1 함정' 절에 추가했습니다.**

덤으로 드롭다운 기본 8줄 제한을 옵션 수에 맞춰 최대 16줄까지 늘렸습니다(생활 횟수 99개·
약초 대상 16개·어비스 함께 난이도 14개가 전부 스크롤에 갇혀 있었음. 20줄로 실기해 보니
패널을 덮어 16으로 낮춤).

#### 롤백에서 부르면 죽는 함수 (주석·테스트로 못 박음)

`Update-CustomRepeatMixLock -StateKey 'lcr'` → `$script:crMixLockState` 에 cr/dcr 두 키뿐이라
`$null` 참조 예외. `Update-CustomRandomMixGate` → 생활에 없는 stage 의존 + 내부에서 진행
기록·마커를 **비가역으로** 지울 수 있음. 생활이 되돌릴 것은 **행 텍스트와 합계 라벨 둘뿐**
이며 순서는 반드시 행 복원 → 라벨(라벨이 리스트뷰를 다시 읽음).

**검증**: 진리표 40여 케이스 추가(회귀 43종 통과), 실기 — 스킬 변경 시 대상 폴백
(`일상 채집/둥지 → 광석 캐기/광맥`), 낚시 없는 8종 옵션, 횟수 99개 드롭다운, 로그 문구
`[안내] 생활 항목 1 수정: 일상 채집 - 둥지 → 광석 캐기 - 광맥`, **어비스 셀 편집 회귀**
(분기를 좁혔으므로 필수 — 혼자하기 난이도 4개 정상). 빌드 `faf4bb78…`.

### 2026-08-08 — 생활 커스텀 하단 합계 라벨이 두 줄로 깨짐 (실기 제보)

> "횟수 1바퀴 하면 옆에 텍스트 길이때문에 2줄된다" — 리스트 반복을 '횟수'로 바꾸면
> 오른쪽 합계 라벨이 두 줄이 되면서 글자가 겹쳐 보였습니다.

**원인은 폭 초과 + WinForms Label 의 기본 줄바꿈.** 라벨 폭은 119px 인데 문구
`바퀴당 6 · 총 12사이클` 이 **127px**(Segoe UI 9pt 실측). Label 은 AutoSize=false 일 때
넘치면 **잘리는 게 아니라 줄바꿈**을 하는데, 높이가 20 이라 둘째 줄이 잘려 겹쳐 보입니다.

| 문구 | 폭 | 판정 |
|---|---|---|
| `바퀴당 2 · 총 2사이클` | 121px | 넘침 |
| `바퀴당 6 · 총 12사이클` | 127px | 넘침 |
| `총 12사이클` | 71px | OK |
| `총 1,978,020사이클` (극단값) | 109px | OK |

**수정 — 사용자 지적대로 '바퀴당'을 뺐습니다.** 바퀴 수는 바로 왼쪽 입력칸에 보이므로
`바퀴당 N · 총 M` 은 중복이었습니다. 횟수 모드는 **`총 M사이클`** 만 표시하고, 무한 모드는
총합 개념이 없어 `바퀴당 N사이클` 을 유지합니다(111px 까지 여유).

추가로 **합계 라벨 3종(생활/던전 은동전/심층 공물)에 `AutoEllipsis = $true`** 를 켰습니다.
항목을 많이 담아 숫자가 커져도 두 줄이 아니라 한 줄 + `…` 로 끝납니다 — 렌더링 실측으로
말줄임이 줄바꿈을 실제로 막는 것을 확인한 뒤 적용했습니다(문서만 믿지 않음).

**실기 확인**: 2항목 1회씩 → 횟수 1바퀴 `총 2사이클` / 999바퀴 `총 1,998사이클` /
무한 `바퀴당 2사이클` 전부 한 줄. 회귀 43종 통과, 빌드 `035fa3ef…`.

**교훈**: 라벨 문구를 바꿀 때는 **가장 긴 값으로 폭을 재 보고 넣는다.** 이번 건은 좌표
계약 테스트를 통과했는데도(폭·좌표는 그대로였으니) 실행 화면에서만 드러났습니다 —
문자열 길이는 좌표 가드가 못 잡는 축이라 진리표에 문구 자체를 못 박았습니다.

### 2026-08-08 — 설정 그룹 버튼 재배치는 '생활만' (전투 클리어 대기 줄 가림 사고)

'권장 창 모드 / 적용된 설정 / 설정 저장'을 아래 가로 한 줄로 옮겨 달라는 요청에
**선언부 좌표를 바꿨더니 전투의 '클리어 대기' 줄(y108~132)이 버튼(y112)에 가려졌습니다**
— 세 버튼은 전투·생활이 공유하는 컨트롤인데 전투 쪽은 왼쪽이 체크 4개 + 클리어 대기 줄로
꽉 차 있었기 때문입니다. 실행 화면을 캡처해서 발견했습니다(테스트는 좌표만 보고 통과).

→ **대분류 전환 시점에 배치를 갈라 놓았습니다.** 전투는 오른쪽 세로 3단(390, 25/70/108,
108x30) 그대로, 생활만 아래 가로(15/183/351, y56, 158x28). 설정 그룹 높이도 전투 150 /
생활 94. 저장 안내 라벨도 함께 이동해야 버튼 줄과 겹치지 않습니다.

같은 날 '적용된 설정' 팝업의 `[생활 설정] (준비 중)` 문구도 정정했습니다. 채집 8종은 실기
검증이 끝났는데 계속 '준비 중'으로 보였습니다 — 원인은 지원 목록
`$lifeSupportedSkillIds` 가 `Test-LifeStartBlocked` **함수 지역 변수**라 팝업에서 읽지
못한 것. script 스코프로 올려 시작 게이트와 팝업이 같은 목록을 보게 했습니다.

### 2026-08-08 — 레벨 해금 3대상 검증: '일렁' → '일럼' 깨짐 발견·수정 (연속 2라운드 통과)

> 사용자가 생활 레벨을 올린 뒤 "못한 거 테스트해줘". 대기 중이던 3대상:
> 얽힌 거미줄(일상 Lv.30 필요) / 일렁이는 빛 무리(곤충 Lv.27) / 산뜻 버섯(요구치 미확인).
> 현재 레벨: 일상 **30** / 곤충 **28** / 약초 32.

**1차: 2/3 통과, 일렁이는 빛 무리만 29초 만에 '대상 미발견'.**
어제는 레벨 부족(퀘스트 생성 실패)이었는데 증상이 바뀌었습니다 - **레벨이 막고 있어서
가려져 있던 별개 버그**가 해금과 함께 드러난 것입니다.

**원인**: 진단 로그의 목록행에 답 - `황폐한곤충무리@552 | (뿐)일럼이는빛무리@642`.
행은 화면에 있는데 이름이 깨졌습니다. 전 배율로 재보니 **s3~s6 모두 일관되게
'일렁' → '일럼'**(우연한 노이즈가 아니라 이 글꼴의 고정 깨짐):

| 배율 | 판독 |
|---|---|
| s3 / s5 | `일럼이는빛무리` |
| s4 | `(뿐)일럼이는빛무리` (앞 잡음 - s4→s5 사다리가 커버) |
| s6 | `일럼OI는빛무리` |

**수정**: 개별 이형이 아니라 **음절 공통 치환 `일럼`→`일렁`** 추가 (`나부→나무`,
`부리→무리`, `곤춤→곤충` 과 같은 방식). 목록의 다른 대상에 '일럼' 이 정당하게 들어간
이름이 없어 오탐 없음 - 진리표로 고정(복합 깨짐 `일럼+부리`, 오탐 방지 포함).

**2차: 3대상 연속 2라운드 6/6 전부 통과.** 모든 완료 로그가 `10개` 로 정확히 표기되어
같은 날 바꾼 '완료 수량 = 목표 개수' 도 실기로 확인됐습니다. '진행 없음' 한도(600초)가
정상 사이클(179~203초)을 자르지 않는 것도 함께 확인.

**이로써 채집 8종 전 대상이 모두 실기 검증을 마쳤습니다** (레벨 미달로 남아 있던 3대상 포함).
### 2026-08-08 — '가방 가득 시' / '도구 내구도 소진 시' 옵션 제거 (사용자 판단)

> 사용자: "가방 가득 시, 도구 내구도 소진 시 **게임 자체가 어차피 자동으로 멈추니까**
> 저 옵션은 필요 없을 것 같아."

**맞는 판단이었습니다.** 두 상황 모두 게임이 스스로 채집을 멈추므로 '계속 진행'이라는
선택지가 성립하지 않습니다. 게다가 **워커는 `bagFull`/`toolWorn` 을 한 번도 읽은 적이
없었습니다** - GUI 가 config 에 저장만 하고 끝이라, 고를 수 있게 두면 동작하는 것처럼
오해만 됩니다. 그 상태로는 실제로 가방이 차도 '진행 없음' 한도(600초)에 걸려서 멈춥니다.

- GUI: 라디오 4개 + 라벨/패널 제거, 로드·저장·표시 경로에서 전부 제거
- config: `life.bagFull` / `life.toolWorn` 키 삭제, **`configSchemaVersion` 6 → 7**
  (이전 루프가 '최신 구조에 있는 키만' 옮기므로 기존 사용자 config 의 옛 키는 자연히 사라짐)
- 워커: 시작 안내에서 '가방/도구 소진은 아직 감지하지 않습니다' 문구 제거

**남긴 것 — 준비물 부족 감지**: 이건 옵션이 아니라 안내입니다. 도구가 0개면 게임이
'퀘스트를 위해 필요한 아이템이 없습니다' 팝업을 띄우는데, 이걸 잡아 **44초 만에 필요한
아이템 이름과 함께 정지**합니다 (`필요: 입문용 곤충 채집망`). 없애면 600초 기다렸다가
이유 없이 멈춥니다.

**검증**: 가드 5건(GUI 컨트롤/저장 경로/워커 참조/안내 문구 제거 + 준비물 감지 유지),
회귀 42종 전부 통과.

### 2026-08-08 — 어비스 메뉴 광고 배너로 '아르바이트' 오클릭 → 글자 탐색 + 클릭 검증 (실사고)

> 사용자 제보: "전투 어비스일 때 메뉴 상단에 광고가 생기면서 어비스를 못 누르고
> 아르바이트를 누르고 있어. 메뉴 누르고 벨리데이션 있어야 할 것 같아."

**실측 확인** (ESC 메뉴를 열어 타일 좌표를 직접 판독):
- 상단에 **`NEXON ESSENTIAL` 광고 배너(y33~175)** 가 생기면서 아이콘 타일 그리드가 통째로
  아래로 밀렸습니다
- 워커가 누르던 `ptAbyssMenu (971, 387)` = **'아르바이트' 아이콘** (라벨 실측 971,439)
- 실제 '어비스' = **(971, 531)**
- 판독 영역 `rgAbyssMenu (850,330,350,85)` 도 `캐릭터/가방/크래프팅/연금술` 줄을 읽어
  어비스 줄을 통째로 놓쳤습니다. 그런데도 `Test-AbyssMenu` 는 2차 신호('게임 종료')로
  true 를 돌려줘서 **메뉴가 열린 줄 알고 옛 좌표를 눌렀습니다**

**수정** — 새 고정 좌표로 바꾸면 다음 배너에서 또 깨지므로 구조를 바꿨습니다:
- **글자를 찾아 클릭**: `Find-GameTextPoint ... -SearchText '어비스' -ExactText '어비스'`.
  생활 분기의 '가까운 위치 찾기'와 같은 계약입니다. 못 찾으면 **클릭하지 않고 재시도**
  (엉뚱한 항목을 여는 것보다 낫습니다). 3회째에 진단 캡처
- **판독 영역을 타일 그리드 전체로**: `(850,330,350,85)` → `(850,180,350,480)`.
  배너 높이는 광고마다 다를 수 있어 '한 줄'이 아니라 그리드 전체를 봅니다.
  **기존 좌표 키 값 변경이라 `coordsVersion` 6 → 7**
- **클릭 후 검증** (사용자 요청): 누른 뒤 최대 6초 동안 어비스 선택 화면/메뉴를 확인하고,
  둘 다 아니고 HUD 도 아니면 '어비스가 아닌 화면' 으로 보고 진단 캡처 + ESC 로 되돌립니다.
  검증이 없으면 엉뚱한 창이 열린 채 '알 수 없는 화면'으로만 돌다 시간 초과로 끝나고,
  무엇이 잘못됐는지 로그에 남지도 않습니다

**광고가 없어지면? — 그때도 동작합니다** (사용자 질문 2026-08-08):
좌표를 새로 고정한 게 아니라 **글자를 찾도록** 바꿨으므로 어비스가 어디 있든 찾습니다.
유일한 조건은 판독 영역 안에 들어오는 것인데, 두 상태 실측이 다 들어옵니다 —
**광고 없음 y387**(2026-07-16 옛 `ptAbyssMenu` 값, 몇 주간 정상 작동한 실측) /
**광고 있음 y531**(2026-08-08). 더 큰 광고까지 대비해 영역을 `480` → `520` 으로 넓혔습니다
(y180~700). 회귀에 '두 위치를 모두 덮는지' 를 계산으로 검사하는 가드를 넣었습니다.
**위쪽(y180)은 일부러 배너 아래에서 시작합니다** - 배너까지 포함하면 '어비스 신규 던전
오픈' 같은 광고 문구를 어비스 항목으로 잡아 광고를 클릭하게 됩니다. 이것도 가드로 고정.

**실측 검증**: 새 영역에서 `'어비스' @ (971,531)` 정확 일치, `'아르바이트' @ (971,439)` 와
분리 확인. 배선 가드 7건 추가(글자 탐색/미발견 시 클릭 금지/클릭 후 ESC 복귀/영역/버전/
config 일치). 회귀 42종 전부 통과.

### 2026-08-08 — '채집 대기'를 총 시간 → **진행이 멈춘 시간**으로 (사용자 지적)

> 사용자 실기 로그: 채집 `6/10` 까지 잘 진행 중인데 `한도(120초)를 넘겼습니다` 로 정지.
> 자동화 결함이 아니라 **설정값이 멀쩡한 사이클을 죽인 것**. 사용자 질문 —
> "이거 채집 대기 초 꼭 있어야 하나?"

**진단**: 한도 자체는 필요합니다(무인 운용에서 캐릭터가 끼면 영원히 매달림). 문제는 **재는
방식**이었습니다. '총 시간'으로 재면 사용자가 대상별 소요를 미리 알아야 숫자를 정할 수 있는데
그건 알 수 없습니다 — 실측 소요가 **100~520초로 대상마다 5배 차이**입니다. 너무 작으면 멀쩡한
채집이 잘리고, 너무 크면 진짜 멈췄을 때 그만큼 낭비합니다.

**변경**: 워커는 이미 수량(`6/10`)을 읽고 있으므로 **모은 개수가 늘면 한도를 다시 잽니다**.
- 오래 걸리는 건 문제가 아니고 '아무 일도 안 일어나는' 게 문제 → `gatherWaitSeconds` 의
  의미를 '진행이 멈춘 채로 견디는 시간'으로 재정의
- **본 적 있는 최댓값**만 기준으로 씁니다. 수량 판독이 `6/10 → 0/10 → 6/10` 처럼 튀는 것이
  실측돼 있어(위 로그), 노이즈로 한도가 되살아나면 정지 자체가 무의미해집니다
- 분모는 `0/0`·`2/1` 로도 깨져(실측) 신뢰하지 않고 **분자만** 씁니다 (`Get-LifeProgressValue`)
- 최댓값은 목표 개수를 넘지 못하니 무한 연장이 불가능하지만, 그 위에 **절대 상한 1시간**을
  백스톱으로 둡니다 (실측 최장 사이클 521초라 정상 채집은 닿지 않음)
- 정지 안내에 마지막 진행 수량을 적습니다 (`마지막 진행 6개` / `수량 판독 없음(이동 중 추정)`)
- GUI 라벨 `채집 대기(초)` → `진행 없음(초)`, 툴팁·config 설명·적용된 설정 표시 모두 갱신

**남는 성질**: 이동 중에는 수량이 `0/10` 로 고정이라 진행 신호가 없습니다. 따라서 이 한도는
여전히 '가장 먼 대상의 이동 시간'보다 커야 합니다(기본 600초 권장). 달라진 건 **채집이
시작된 뒤로는 총 시간 제한이 없다**는 점입니다.

**옛 값 자동 복귀 (configSchemaVersion 5 → 6)**: 이 설정은 **의미가 바뀌었으므로** 사용자
config 의 옛 값을 그대로 옮기면 안 됩니다 — 실제로 구 기본값 120 이 남아 있어 `6/10` 까지
캐던 사이클을 잘랐습니다. 이전(Update-ConfigToLatest)에 특례를 두어 **기본값 600 으로
되돌리고**, 되돌리기 전 값을 시작 로그로 안내합니다. `life` 섹션의 나머지 키는 그대로 보존.

**완료 수량은 '목표 개수'** (2026-08-08 사용자 제보 - `헤이즐넛 9개` 로 찍힘):
마지막 개를 채우는 순간 트래커가 사라지는데 폴링이 3초 간격이라 `10/10` 프레임을 놓칩니다.
그래서 '마지막으로 본 수량'을 적으면 **거의 항상 1개 모자라게** 나옵니다. 퀘스트가 사라진
것 자체가 '목표를 다 모았다'는 뜻이므로 **분모(목표 개수)** 를 적습니다.
분모도 `0/0`·`2/1` 로 깨지므로 ①분모가 분자보다 작으면 버리고 ②**가장 많이 본 분모**를
채택합니다(`Get-LifeQuestGoalConsensus`, 동률이면 큰 값 - 깨짐은 작게 나옴).
한 번도 못 읽으면 단정하지 않고 `(마지막 판독 N개)` 로 밝힙니다.

**완료 로그에 대상 표기** (사용자 요청): 여러 대상을 번갈아 돌리면 로그만 봐서는 무엇이
끝났는지 알 수 없었습니다 →
`[완료] 채집 1사이클 완료 - 나무 베기 / 갑옷 나무 10개`. 수량은 '본 적 있는 최댓값' 이라
마지막 판독 노이즈에 흔들리지 않습니다.

**검증**: 시나리오 3종 신설 — `progress-extends`(수량이 늘면 한도를 다시 재고 완료),
`progress-noise-not-extends`(6→0→6 노이즈는 진행 아님), `hard-cap`(절대 상한).
진행값 진리표 6건, 배선 가드 7건, config 이전 특례 2건. 회귀 42종 전부 통과.

### 2026-08-08 — 클릭 직전 대상 재확인 신설 + 제목 인식률 72%→96.3% (사용자 제안)

> 사용자 제안: "'가까운 위치 찾기'를 누르기 전에 그 자리에 캐려는 대상 이름이 나오니
> 그걸로 검증하면 어떤가". 지금까지 **제목 검증(5단계)과 링크 클릭이 서로 다른 캡처**라,
> 그 사이에 팝업이 바뀌면 검증하지 않은 화면을 눌렀습니다. 링크를 찾는 판독에 제목도
> 들어 있으므로 같은 프레임에서 확인하고 누르도록 했습니다.

**전수 실측 방법** (사용자 제안 "채집물 전체 하나씩 눌러보며 확인해봐"): 8종의 모든 대상
행을 하나씩 눌러 팝업을 열고 제목 판독만 측정하는 probe 를 만들었습니다. **채집을 하지
않으므로 대상당 3초** — 채집 배치(1시간 40분)로는 불가능한 전수 측정을 5~8분에 끝냅니다.
이 측정이 아래 모든 판단의 근거가 됐습니다.

**인식률 72% → 90.5% → 96.3% (52/54). 전 구간 오차단 0건.**

- **1차 72%**: 링크 영역(420x470) s3 한 배율. 광석 캐기가 특히 나빠 '은 광맥'이 '으과DH'
- **가설이 틀렸던 기록**: "영역이 넓고 배율이 하나라 못 읽는다"고 보고 상세 영역 s3→s4
  재판독을 넣었더니 **44/62 로 그대로**였습니다. 실패 항목의 단계 표시가 여전히 `[link]`
  인 게 단서 — 재판독이 돌았는데도 못 읽은 것. **OCR 이 그 글자꼴 자체를 못 읽습니다**
- **2차 90.5%**: 실측 깨짐 문자열을 이형으로 등록 (읽히게 만들 수 없으니 읽힌 대로 등록)
- **3차 96.3%**: 배율/영역 실험에서 **좁은 띠 고배율**이 결정적임을 확인 —
  '숨숨꽃'은 s6 에서만, '옥수수'는 s4·s6 에서, '흰 껍질 나무'는 s6 에서 **깨짐 없이**
  판독됩니다(넓은 영역에서는 '혼!껍질나무'). 제목 Y 가 팝업마다 다르므로(세로 중앙 정렬 —
  사과 나무 191 / 나무 213) **라벨 '채집물' 행에서 역산**해 띠를 잡습니다
- **남은 2건은 한계**: '얼음'과 추수 1글자 대상. 전 배율·전 영역에서 판독 불가라 등록할
  문자열조차 없습니다 → 격자 추론·행 근거라는 다른 경로로 검증

**안전 설계** (인식률을 올리려고 깨진 문자열을 등록하는 건 그 자체로 위험):
- **제목 전용 이형 표 분리** (`$lifeTitleVariants`). 공용 표는 목록 행 선택과 퀘스트 소유
  판정에도 쓰이고 퀘스트 쪽은 부분 일치 비교라, 깨짐 문자열이 섞이면 엉뚱한 행을 클릭하거나
  남의 퀘스트를 자기 것으로 봅니다 (리뷰 지적). 제목 표는 클릭 직전 검증에서만 쓰이고
  그 판정은 틀려도 '차단하지 않음'으로만 흘러 **위험이 한 방향**입니다
- 제목 이형 13건 각각을 **전 대상 69개와 전 조합 대조**하는 가드 (공용 매칭 누출 0건,
  제목 매칭에서 자기 대상만 걸림)
- **판정 규칙은 fail-open**: 확신할 때만 막습니다. 깎지 않은 정확 일치만 보고(상세 판정식의
  꼬리 2자 trim 을 그대로 쓰면 '거미줄XX'가 '거미줄'로 깎여 목표 '거미줄 뭉치'를 오차단 —
  리뷰 재현), 읽힌 다른 이름이 **목표 이름의 일부면 확정하지 않습니다**('나무' ⊂ '뾰족 나무')
- **'mine' 은 단독 인정, 'other'(차단) 는 두 배율 합의** — 한 배율의 깨짐이 곧 오차단이
  되지 않도록. 5단계의 '두 스케일 모두 wrong 일 때만 오클릭 확정' 과 같은 계약
- **프레임 일관성**: 깊은 재확인은 캡처를 여러 장 씁니다. 마지막에 링크를 다시 찾고
  **첫 프레임과 같은 팝업인지 확인**(제목 일치 + 링크 Y 6px 이내)한 뒤에만 앞의 판정을
  적용합니다 - 좌표만 갱신하면 'A 팝업 판정으로 B 팝업 클릭' 이 됩니다 (리뷰 지적)
- 재확인 루프 각 회차에 사이클 한도 검사 (판독이 최악 7회로 늘어남)

**실기**: 호미질 7 + 곤충 8 대상 **연속 2라운드 30/30 완전 성공** (02:54 완료).
**검증**: 문법 0건, 회귀 42종 전부 통과. 교차 리뷰 3라운드(마지막 라운드는 자체 검증). 신규 진리표: 제목 판정 14건, 제목 띠 3건, 합의 6건, 제목부 절단 5건, 이형 14건.

### 2026-08-07 — 배포 전 다각도 감사: 확인 14건 중 high 5건 수정 (교차 리뷰 4라운드)

**방식**: 배포 직전이라, 생활 분기 전체를 **6개 렌즈로 병렬 감사**하고 각 지적을 독립
검증자가 반증 시도했습니다 (클릭 정책 / 퀘스트 판독 비대칭 / 캡처 실패 경로 / PS 5.1 함정 /
OCR 오탐 / GUI-워커 계약). 지적 26건 중 **14건 확인, 12건 기각**. 이어 교차 리뷰를 4라운드
돌려 그중 3건의 추가 high 와 경계 결함 2건을 더 잡았습니다.

**[high] 캡처가 끊기면 '목록이 사라졌다'로 읽고 자동화가 영구 정지**
> 대상 행 탐색 구간만 `$script:screenCaptureFailing` 을 보지 않았습니다. 캡처가 끊기면
> 판독이 `@()` 를 돌려주는데 이걸 목록 소멸로 해석해 `exit 4`(= GUI 반복 전체 정지) 로
> 갔습니다. 화면이 몇 초 뒤 돌아와도 이미 끝난 뒤이고, 로그에는 '미해금이거나 화면 인식
> 실패' 라는 엉뚱한 진단만 남았습니다. 검은 화면 진단 캡처가 보관 10개에서 실제 오류
> 캡처를 밀어내는 부작용까지 있었습니다.

- `Wait-LifeCaptureAlive` 신설 - 판독·입력 앞 3곳(빠른 확인/목록 정렬/대상 탐색)에 배치.
  프리즈된 화면에 드래그가 나가는 것도 함께 막습니다. 복구 후 반환 직전 deadline 재검사
- 미발견 확정 전 캡처 실패 여부 확인 → 실패 중이면 `exit 4` 대신 재시도로 넘김
- **캡처 실패 중 순서 폴백 금지**: `Find-LifeTargetScan` 의 FreshList 인덱스 계산은 판독이
  전멸해도 행을 만들어 내 '안 보이는데 클릭은 나가는' 경로였습니다 (리뷰 재현)
- **클릭 직전은 플래그를 믿지 않고 새로 캡처**: 플래그는 '마지막 캡처 결과'라 판독 직후
  화면이 멈추면 정상으로 남아 있습니다. 멈춰 있으면 기다리지 않고 되돌아가 목록을 다시 읽습니다
- 캡처가 `$null` 이면(창 좌표 실패 경로) 플래그와 무관하게 실패로 처리 - 이 경로는 플래그를
  세우지 않아 게이트가 뚫렸습니다 (리뷰 재현)
- 예산 계산을 `while` + '성공한 판독에서만 증가' 로 바꿔, 캡처 플래핑이 탐색 범위를 갉아먹지
  않게 했습니다 (초기 확인 20회 / 이전 채집 대기 60회 / 정렬 12회 / 탐색 12회)

**[high] 상세 '본문 구제'가 같은 스킬의 모든 팝업을 통과**
> 본문 구조가 '이름 → 채집물 → **스킬명** 레벨 N 이상 → 설명' 이라 본문에는 항상 스킬
> 이름이 있습니다. 목표 '나무'(나무 베기 Order[0])는 '나무베기'의 부분 문자열이라
> **나무 베기의 모든 대상 팝업에서 참**이었습니다. 같은 위험: 광맥/허브/거미줄/빛 무리/
> 곤충 무리 — 전부 형제 대상이 목표 이름을 통째로 품습니다. 2026-08-06 에 잡았던
> '라벨 집물 때문에 1글자 물이 모든 본문과 일치' 블로커가 2글자 기저 명사로 살아남은 것.
> 게다가 match 가 조기 반환되면서 격자 추론 경로의 '약한 추론 통과 금지' 블로커까지 우회.

→ `Test-LifeBodyNameAmbiguous` 신설(1글자 / 스킬명이 품음 / 다른 대상이 품음 → 모호),
전체 이름·수식어 양쪽 경로에 적용. `Get-LifeDetailVerdict` 에 `-SkillName` 추가.

**[high] 퀘스트 소유 판정이 형제 대상을 인수 (양방향 피해)**
> 부분 문자열 비교라 '우물' 퀘스트를 목표 '물' 이, '얽힌 거미줄'을 '거미줄'이,
> '뾰족 나무'를 '나무'가 가로챘습니다. 반대로 이형·공통 치환을 안 써서 자기 퀘스트를
> 못 알아보면 3분 대기 후 `exit 4` 로 무인 반복 전체가 멈췄습니다. head 폴백은 2자 이상
> 조건이라 1글자 head 대상('빛 무리'·'흰 껍질 나무'·'은 광맥')은 폴백조차 없었습니다.

- `Get-LifeQuestOwner` 신설 - **긴 이름 우선**으로 소유 대상을 먼저 확정, 이형 표 + 공통
  치환 사본(`Get-LifeRepairedTexts`)을 모두 후보로. 동률 대비 2차 정렬 키 추가
- 후보는 **전 스킬 69개 대상**(`Get-LifeAllTargetNames`). 현재 스킬만 대조하면 다른 스킬
  잔여 퀘스트가 현재 스킬의 짧은 대상으로 오인됩니다 ('사과 나무'→'나무', '양파'→'양')
- **미확정이면 '내 것'** - 비대칭 비용: 남의 것을 내 것으로 보면 한 회차 헛돌 뿐이지만,
  내 것을 남의 것으로 보면 전체 정지
- 소유 판정은 **`Test-LifeQuestFragments` 를 통과한 판독에만** 겁니다. 좁은 영역에
  '주간 목표 뾰족 나무' 같은 다른 줄이 들어오면 그 이름을 소유자로 집었습니다 (리뷰 재현)
- 공통 치환 쌍을 `$lifeNameRepairPairs` 로 추출해 이름 매칭과 소유 판정이 같은 규칙 사용

**[medium] 그 밖**
- 자기 팝업이 2자 깎여 다른 대상으로 확정되던 반례('채'가 사라져 제목이 정확히
  '거미줄뭉치') → **깎지 않은 제목 일치를 최우선**으로 배치 (더 강한 증거)
- 공지 게시판 팝업은 가장자리 HUD 가 보여 '게임 화면인데 퀘스트 없음'으로 확정됩니다.
  대기 루프에는 있던 팝업 방어가 **초기 확인에만 없어** 진행 중인 채집을 끊었습니다 → 추가
- 초기 확인 20회가 전부 `unknown` 이어도 그냥 메뉴로 진행했습니다 (주석의 계약과 코드
  불일치) → 진단 캡처 후 조건부 정지

**신규 회귀**: 소유 판정 진리표 11건, 본문 모호성 13건, 캡처 탐침 5건, 치환 사본 1건,
사이클 시나리오 2종(`resume-sibling`·`start-unknown`), 배선 가드 8건.

### 2026-08-07 — 채집 지원 4종→8종 (계속)

**검증**: 문법 0건, 회귀 **42종 전부 통과**. 신규 케이스 — 요구 레벨 진리표 14건,
캡처 탐침 단위 4건(운영 함수와 같은 반환형 스텁으로 Dispose 대상까지 확인), 사이클
시나리오 3종(`menu-fail-level`·`deadline-capture-fail`·`capture-recover`), 배선 가드
3건(탐침 4곳 고정, 요구 레벨 저장 시점·대상 재대조), 이름 복합 치환 3건, Sig 전 조합
충돌 대조. **교차 리뷰 2라운드**에서 블로커 2건(복구 탐침 부재, Dispose 반환형)을 잡아
반영 후 빌드 GO 승인.

### 2026-08-06 — 채집 전수 배치(43대상) 4라운드 - 실기 사고 12건 수정

**방식**: 지원 4종의 전체 대상 43개를 1사이클씩 자동 실행하는 배치를 만들어(스크래치패드
`batch_life_targets.ps1` - config 정확 치환+readback, 대상별 로그/진단 보관, 단계별 실패
분류, config 바이트 복원) 라운드마다 실패를 분석·수정하며 반복. **라운드 3에서 33/43 PASS**
(나무 베기 8/8, 광석 캐기 9/9 전종 통과), 라운드 4에서 둥지·거미줄까지 PASS 전환.

**실기에서만 드러난 사고와 수정**:
- **스크롤 방식**: 휠(0x0800)은 이 목록에서 이동량이 0.17행/클릭 수준이라 양 끝 대상에
  도달 불가 → **드래그로 전환**(16단계 이동 + 250ms 정지 후 up = 플링 방지). 시작점이
  설명 영역(y230)이면 목록에 안 먹는 것도 실측 → 항목 카드 영역만 사용
- **1글자 대상 판독 불가**: WinRT OCR 이 '물'을 s3~s6 전 스케일에서 놓침 → 스킬별 목록
  순서(Order)+균일 격자(90px)로 빈 행 Y 추론 `Get-LifeTargetRowByOrder` (앵커 2개+간격
  ±12px 일관 조건). 실기 Y=194 추정 성공
- **오클릭 오판**: 제목 깨짐('호b살꽃')이 한글 비율 0.8 경계에 걸려 자기 팝업을 오클릭으로
  확정 → **목록 대조 판정**으로 전환 (같은 Order 의 다른 대상과 일치할 때만 wrong-target)
- **차단 모달 3종**: 준비물 부족('빈 병 0/1' - 물/우물/젖소), '사냥터 퇴장 실패'(남으면
  C 키 무효로 5연속 실패), **서버 연결 끊김(ERROR 83)**(끊긴 채 3회차가 각 600초 소진 →
  '가방 가득'으로 오인) → `Close-LifeBlockingDialog` 신설, 각각 정지/닫기 처리
- 이형 8종 추가(등지/거미좋/거미줄풍치/호b살꽃 등), 스크롤 실패 원인 로그

**교차 리뷰 블로커 수정 (4라운드 대기 중 소스 리뷰)**:
- **본문 구제가 다른 대상을 통과시키던 블로커**: 라벨이 '집물'이라 1글자 '물'은 모든 팝업
  본문과 일치 → 판정 순서를 '다른 대상 대조 → 목표 대조 → 본문(라벨 2자 제외, 2자 이상
  이름만)'으로 재구성. 다른 대상 대조는 긴 이름 우선(거미줄 뭉치 > 거미줄)
- **격자 추론 행의 unreadable 통과 금지**: 이름 근거 없이 찍은 행은 상세 제목이 확인될
  때만 진행 (추론이 빗나가면 다른 대상을 채집하게 됨)
- deadline 을 최상단 정렬·탐색 스크롤 직전까지 확대 / 드래그 mouse-up 을 finally + 이동
  검증 / 모달 판정을 중앙 단일 ROI 강한 조합으로 제한 + 닫기 버튼은 하단 밴드 단일 후보만
  (고정 좌표 폴백 제거) + 닫힘 확인 / disconnected 우회 경로 3곳 차단(초기 판정 전, 생성
  실패 직후, 대기 루프 5회차 주기)

**검증**: 문법 0건, 회귀 42종 전부 통과 (진리표/가드 신규 35건+). 실기 재개는 게임 서버
재접속 후 (라운드 4 중단 시점 - '확인'이 게임을 종료시켜 사용자 재로그인 필요).

### 2026-08-06 — 생활 시작 시 전투 상태 누출 4건 + 채집 탐색 보강 (교차 리뷰 3라운드 승인)

**발단**: 사용자 제보 (00:06) - 생활 시작인데 "[안내] 층이 섞인 혼합 리스트라 반복을
'횟수 1바퀴'로 고정합니다" 표시. 전투 하위 라디오/커스텀 체크는 대분류 복귀 대비로
Checked 를 보존하는 설계인데, 시작 경로 곳곳에 생활 게이트가 빠져 있었음.

**전투 상태 누출 수정 4건**:
- **커스텀 시작 경로**: `$isCustomStart` 에 생활 게이트 - 없으면 생활 시작이
  customActive=true 로 커스텀 컨텍스트/마커/게이트 경로를 타는 실동작 버그
- **시작 안내**: 던전/심층 안내 블록 생활 게이트 + 생활 전용 안내 신설 + 혼합 잠금
  안내 2곳 생활 억제 (잠금 '상태' 관리는 전투 복귀 대비 유지)
- **[높음] 시간 지정 게이트**: `Test-TimeAllowsNextCycle` 이 항상 전투 클리어 대기값
  참조 → `Get-CycleWaitSecondsForEstimate`(생활=채집 대기/전투=클리어 대기) 신설
  (리뷰 - 무인 시간 지정 계약 위반)
- **코드 4 사유 폴백**: 생활 분기 선행 (숨겨진 rbCatDeep 로 '마족공물 소진' 오표시)

**채집 탐색/판정 보강 (2·3라운드 리뷰)**:
- 상세 verdict 스케일별 누적 - match 우선, **두 스케일 모두 wrong 일 때만** 오클릭 확정
  (마지막 스케일만 남기면 순서 비대칭 - 리뷰)
- 휠 전송 성공 여부 반환 + 끝 판정은 '전송했는데도 행 동일'일 때만 (일시 포커스 실패를
  목록 끝으로 오인 방지) + 마지막 회차 불필요 휠 제거
- '나무→나부' 깨짐을 개별 이형에서 **공통 정확 치환 규칙**으로 (Test-LifeNameMatches -
  뾰족/굵은/갑옷 나무 등 전 대상 커버, 현 목록에 정당한 '나부' 없음 확인)
- **[높음] 메뉴 시퀀스 내부 deadline**: Invoke-LifeMenuSequence 에 Deadline 전달 +
  검사 5곳 (서두/생활 스킬 클릭 전/탐색 매 회차/행 클릭 전/**찾기 클릭 직전 - 한도
  초과 후 채집 시작 입력 절대 금지**). 함수 한 번에 탐색 12회·OCR 수십 회라 내부 검사
  없이는 상한 계약이 깨짐 (리뷰)
- 오프라인 테스트 Get-RegionRows 를 워커와 같은 X 재정렬로 동기화

**검증**: 문법 0건 → 회귀 42종 전부 통과 (진리표/가드 +17건) → 교차 리뷰 3라운드 승인
("최종 빌드 승인") → v2.0.0 빌드 b38ce02e….

### 2026-08-05~06 — 채집 GUI 실기 실사고 3건 (나무 베기 - 흰 껍질 나무로 재현·완주)

**흐름**: 사용자가 GUI(차단 해제 후)로 나무 베기 - 흰 껍질 나무 실행 → 23:24 '대상
미발견' 조건부 정지 제보 ("스크롤이 밑에 더 있는데 내려가다 멈췄어") → 진단 세트로 원인
확정·수정 2라운드 → 23:54 재실기 **1사이클 완주 exit 0** (스크롤 탐색→상세 이형 매칭→
사냥터(장백한 산) 이동→채집 0→9/10→소멸 확인, 4분 2초).

**실사고 ① - 행 조합 순서 뒤집힘 (23:24 진단 실측)**: Get-LifeTargetRows 가 전역 Y,X
정렬 후 순차 결합이라 '뾰족 나무' 두 단어가 OCR Y 중심 1~2px 차이로 **'나무뾰족'**으로
조합 → 정확 일치 전부 실패 → 대상이 화면에 들어와도 미발견. **수정**: 행 그룹핑 후 행 내
X 재정렬로 결합 (진리표: y 304/306 어긋난 '나무','뾰족' → '뾰족나무').

**실사고 ② - 탐색 상한 부족**: 나무 베기 목록이 고정 4회 스크롤 범위보다 김. **수정**:
'목록 끝 도달' 판정(스크롤 전후 행 구성 rowsKey 동일 = 끝)까지 탐색, 무한 방지 상한
12회, 행 0 = 중단 유지. 스크롤 후 대기 400→600ms (애니메이션 중간 판독 방지).

**실사고 ③ - 상세 제목 '흰'→'혼!' 깨짐 (23:51 실측)**: 자기 팝업(판독
'혼!껍질나무자|집물...')을 다른 대상으로 확정해 재시도 3회 소진. **수정**: ①이형 맵
'흰껍질나무' = 흰껍질나부/혼!껍질나무/혼!껍질나부 + '나무→나부' 공통 깨짐(사과나부/
쓸만한나부 - 실측 2회) ②상세 판독 s3→s4 사다리 (한 스케일 깨짐만으로 wrong-target 확정
금지 - match 우선, 두 스케일 모두 wrong 일 때만 오클릭 확정).

**참고**: 사용자 GUI 저장값 '채집 대기' 120초(구 임시 기본)는 사냥터 이동 대상에 부족 -
600 권장 안내 (신규 기본값 600, 기존 저장값은 사용자가 GUI에서 변경 필요).

### 2026-08-05 — 로그 폴더 %LOCALAPPDATA%\HoneyNogi\Log 통일 (사용자 요청)

**배경**: 워커/GUI 가 로그 경로를 '스크립트 옆 Log'로 잡아 exe 실행은 %LOCALAPPDATA%\
HoneyNogi\Log, 저장소 직접 실행(개발/실기)은 저장소\Log 로 갈렸음 — 채집 실기 안내 중
사용자가 "하나로 통일" 요청.

**변경**: 워커 `$logDir`·부트스트랩 trap·GUI `$honeyLogDir`(workerLog/recovery/
safe_stop.flag/커스텀 마커 3종/보관·정리·폴더 열기까지 전부)를
`[Environment]::GetFolderPath('LocalApplicationData')` 기반 고정 경로로 전환. LOCALAPPDATA
빈 값이면 기존 스크립트 옆 폴백 (런처와 같은 가드). **exe 는 스크립트가 원래 그 폴더에
풀리므로 경로 불변 = 기존 사용자 영향 0**, 저장소 직접 실행만 통일 효과. 의도적 제외 2곳:
rdp_redirect_console.ps1(예약 작업이 SYSTEM 계정이라 LocalApplicationData 가 SYSTEM
프로필로 감 - 스크립트 옆 유지, 주석 명시) / mabinogi_controller.ps1(레거시 - 미적용 명시).

**교차 리뷰 1라운드 수정 4건**: ①GUI 회차 보관(run_*.log)이 scriptRoot 잔존 → 통일
폴더로 (저장소 실행에서 보관 실패하던 경로) ②워커 trap 이 옛 경로에 초기화 오류를 남겨
GUI 폴링이 못 보던 문제 → 같은 규칙 독립 적용 ③RDP/레거시 범위 결정(위) ④감사 가드
4중 보강(scriptRoot 'Log' 폴백 1곳 고정/("Log 형태 0곳/trap 규칙/보관 경로) + README
로그 경로 안내 갱신. 저장소의 옛 Log 폴더 삭제 (실기 진단 2장은 흐름캡처 자산으로 보관).

**검증**: 문법 0건 → 회귀 42종 전부 통과 (audit +5 가드, run_log_resilience 계약 갱신)
→ v2.0.0 빌드 2a4683bd… → 최종 리뷰 승인 ("잔존 상대 Log 참조는 의도된 폴백 3곳과
명시적 제외 2곳뿐").

### 2026-08-05 — 채집 실기 검증 (3차 만에 1사이클 완주 - 실기 전용 사고 2건 수정)

**흐름**: 워커 단독 실행 3회 (config 임시 mainCategory=life, 일상 채집 - 사과 나무).
1차(22:45) 코드 4 → 진단 캡처 신설 후 2차(22:50) 코드 4 → 원인 2건 확정·수정 후
3차(23:01) **1사이클 완주 exit 0** (메뉴 40초 + 이동/채집 2분 50초. 시작 정리→C→스킬
검증→상세 확인→가까운 위치 찾기→생성 확인→진행 0/10→9/10 추적→소멸 확인 전 구간 정상).

**실기 사고 ① - OCR '채집물' 깨짐 (2차 22:51 확정)**: 상세 팝업이 정상 표시돼도 실기
화면(무손실 캡처)에서는 '채집물'이 s3/s4/s5 전부 **'자|집물'**로 깨짐 - 시연 JPG 만 압축
스무딩 덕에 s3 정상이라 오프라인 재현이 못 잡았음. 검증 실패 → 팝업을 못 알아보고 X 클릭
→ 모달에 막혀 3회 소진. **수정**: 판정을 순수 함수 `Get-LifeDetailVerdict`로 분리 -
라벨은 안정 조각 '집물'로 찾고 제목 대조 유지 (깨진 '채' 잔여 최대 2자만 끝에서 흡수.
무제한 축소는 '거미줄 뭉치' 팝업을 '거미줄' 설정과 오인 - 2자 제한이 오탐 방어).
시작 정리의 팝업 검출도 '집물'로 통일. 실기 PNG 를 검증 자산으로 보관
(흐름캡처\3b_대상상세_실기PNG_채깨짐.png) - 진리표 8케이스 + 오프라인 재현 추가.
**교훈: 시연 JPG 와 실기 무손실 캡처는 OCR 결과가 다르다 - 실기 PNG 를 자산화할 것.**

**실기 사고 ② - 반투명 배경 픽셀 오판 (2차 22:51:46 확정)**: 미선택 생활 스킬 창은
배경이 반투명이라 뒤 필드가 비쳐 X 여백이 G103 까지 올라감 → 절대 임계(max 80) 실패 →
창 잔존 미감지 → 다음 C 가 창을 닫는 토글로 상쇄. **수정**: 판별식을 상대 대비로 전환
(교차점 min 200+ AND 여백 max ≤ 교차점-100) - 실기 캡처 포함 8장 재검증 전부 정확.
실기 캡처 보관(흐름캡처\2b_생활스킬창_미선택_전체폭.png) + 진리표/오프라인 갱신.

**1차 실패 대응 보강 (재발 방지 겸용)**: ①`Write-LifeDiagnostics` 신설 - 실패 지점
5곳에서 화면 PNG(error_* 정책 공유)+판정 재료(창픽셀/내정보/목록 행/상세 판독) 기록
(코드 4는 catch 를 안 타 캡처가 없던 2026-07-22 교훈의 생활판) ②C 입력 전 잔존 창 X 정리
③X 닫기 후 닫힘 재확인+재클릭 1회 ④전환 확인 후 0.8초 안정 대기 + 검증 3회x3스케일.

**레이아웃 발견 (실기 부산물)**: 생활 스킬 창은 **스킬 미선택 시 8열 전체 폭 그리드로
9종 채집 스킬이 스크롤 없이 전부 노출** (양털 755/추수 884/호미질 1013/곤충 1143, y210 -
2b 캡처). 스킬 선택 시 우측 대상 목록이 열리며 4열로 좁아짐 (시연 캡처 2 = 이 상태.
1행 앞 4종은 두 레이아웃에서 셀 좌표 동일 - 그래서 셀 클릭이 그대로 통과). 미지원 스킬
확장(백로그)이 예상보다 쉬움 - 셀 좌표만 추가하면 됨 (검증 시그니처는 대상 목록 기준).

**마무리**: GUI `Test-LifeStartBlocked`를 '지원 범위 게이트'로 전환 (채집 4종 시작 허용 /
가공·미지원 스킬 5종 안내 팝업). config 기본값 원복(battle/둥지) + _설명 현행화.
회귀 42종 전부 통과, v2.0.0 최종 빌드 b9021995….

### 2026-08-05 — v2.0.0 생활(채집) 워커 분기 구현 (교차 리뷰 2라운드 승인 - 실기 검증 대기)

**범위**: mabinogi_run_once.ps1 에 '생활' 대분류 채집 사이클 전체 구현. 판정은 사용자
합의대로 **퀘스트 존재 기반** (트래커 '채집'+'장소' 2조각 = present, 카운트 N/10 은 로그
보조). 실측 근거: 흐름캡처 9장(1272x717 기준 1:1) 오프라인 OCR 재현 - 내정보 '생활력'
오탐 0/8, 퀘스트 present 5장/소멸 1장 정확, 대상 행 '사과나무' Y=280.

**구조** (`# '생활(채집)' 대분류` 블록 + 메인 try 의 life 분기):
- **config**: `mainCategory`/`life.content·skill·target·gatherWaitSeconds`(600 기본,
  60~3600 - GUI 기본값도 120→600 정렬). [설정] 스냅샷에 대분류 + life 섹션 추가
- **스킬 선택**: 셀 라벨 OCR 판독 불가(실측) → 고정 좌표 클릭 + 우측 대상 목록 시그니처
  조각으로 사후 검증. 1차 지원 4종(daily/wood/mining/herb 첫 화면 셀), 스크롤 스킬
  4종+낚시는 exit 4 안내 (그리드 스크롤 위치 미실측)
- **대상 행 탐색**: 단어 OCR 행 조합(Y ±14 그룹, Lv 열 x≥1100 제외) + 공백 제거 정확
  일치 + 실측 이형 맵('읽힌거미줄'→얽힌 거미줄 등), 다중 스케일 s4→s5→s3, 휠 스크롤
  (최상단 +6 후 -3 씩 최대 4회)
- **사이클 상태 기계**: 메뉴 3회 재시도 → 생성 확인(약 12초 내 present 2회) → 존재 대기
  (absent 는 HUD 확인 상태에서만, 3연속 = exit 0. unknown 은 연속 카운트 리셋) →
  questSeen 없이 exit 0 불가. deadline 은 '준비 정리 완료 시점'부터 모든 내부 대기에 우선
- **strict C 키**: 전면화 → Test-GameForeground 확인 후에만 keybd_event (전역 입력 사고
  방지 - 사용자 합의 방식). 내 정보 열림 확인 후 진행, 이미 열려 있으면 C 생략(토글 방지)
- **팝업 방어**: 구매 스윕/주간 리셋/공지 게시판(메뉴 루프+대기 루프, 닫은 회차 continue)
  + 시작 정리 순서 '생활 창 닫기 → Clear-EventOverlay' (생활 창을 알 수 없는 화면으로
  오판하는 경로 차단 - 메인 분기도 이벤트 정리보다 앞)
- **시작 복구 멱등**: 상세 팝업(제목 검증) 확인 클릭 + 내정보/스킬창 공통 우상단
  X(1228,67) **픽셀 4점 판별**(교차점 2점 흰색 min≥200 + 여백 2점 어두움 max≤80 - 캡처
  6장 실측 대조, 필드 이중 탈락 마진 R35/R135)

**교차 리뷰 실결함 수정 (1차 7건 + 2차 3건)**:
- ①**휠 음수 uint 변환**: PS 5.1 은 `0xFFFFFFFF`=[int]-1, `[uint32](음수)`=예외 (리뷰
  실측 재현) → `[BitConverter]` 바이트 재해석(`Get-LifeWheelData` 순수 함수)으로 교체.
  **첫 화면 아래 대상 전부가 exit 1 나던 실버그**
- ②상세 팝업 검증이 '채집물'(전 대상 공통)뿐 → 제목부(`IndexOf('채집물')` 앞)를 설정
  대상과 정확 대조, 불일치 = 오클릭 확정 즉시 재시도
- ③공지 게시판 미방어(HUD 잔존형이라 absent 오판 위험) → 2곳 배선
- ④캡처 실패 무한 대기 → 메뉴 루프 캡처 대기에 deadline exit 4, 생성 확인 루프에도
  deadline 우선, deadline 생성은 준비 정리 뒤로 (계약 명확화)
- ⑤판독 '도중' 캡처 실패가 '화면 전환/안 열림'으로 오인 → C 입력 전·전환 확인 모두 판독
  후 플래그 재확인 (리뷰 가 스텁 동적 재현으로 사고 궤적 확인 후 수정 검증)
- ⑥'생활 스킬' 클릭 후 무검증 셀 클릭 → '생활력' 소멸(=전환) 확인 게이트 (스킬창은 좌측
  메뉴 없는 레이아웃 - 캡처 2 판독 공백 실측)
- ⑦휠 남발 → 전면 확인 + 휠 전 목록 행 존재 증거 + 마지막 탐색 회차 휠 생략
  (동적 재현: 궤적 +6,-3,-3,-3,-3 확인)

**검증**: 문법 0건 → 신설 `test_life_gather`(이름 매칭/퀘스트 상태/행 조합/휠 변환/픽셀
판별 진리표 + `sim_life_cycle.ps1` 자식 프로세스 시뮬레이션 8시나리오 exit 코드x호출 궤적
매트릭스 + 배선 가드 15건) + `test_life_ocr_offline`(캡처 9장 실측 재현 - run_all 자동
집계, 3초) → **회귀 42종 전부 통과** → v2.0.0 빌드(6e395bce…). GUI 시작 차단은 실기
1사이클 통과까지 유지 (설계 합의 출시 조건).

### 2026-08-05 — v2.0.0 생활 카테고리 GUI 단계 구현 (워커는 다음 단계 - 시작 차단)

**범위**: 확정 시안(던전이미지\어시스트\생활GUI_확정시안_v1.png)의 GUI 전체를 운영
mabinogi_gui.ps1 에 반영. 워커 미구현이라 생활에서 시작(F9)은 안내 팝업으로 차단
(팝업 금지 규칙의 허용 예외 ② - 버튼 클릭 즉답).

**구현 (리뷰 조건 A~G 합의 → 구현 → 리뷰 3건 수정 → 최종 승인)**:
- **대분류 [전투|생활] 버튼 줄** 상태줄 아래 신설 (활성 = 꿀색 강조), 아래 컨트롤 6개
  +40px 시프트 (grpRepeat 84/버튼 줄 144/콘텐츠 190/상세 250. 탭 이하 배치는 상세 Bottom
  기준 자동 - DetailTop 상수 없음 확인). 승인 오버레이 514×158 로 확장 + 미승인 시 대분류 잠금
- **Set-MainCategory 단일 진입점** (실행 중/같은 값 조기 반환) + Update-MainCategoryVisual.
  updateCategoryPanels 서두 $isLife 게이트 - 전투 플래그 전부 차단하되 전투 라디오 Checked 는
  보존 (복귀 시 상태 유지). supportsCustom 에 생활 제외 (커스텀 1차 미지원)
- **생활 패널**: 콘텐츠 선택 채집/가공 라디오, 채집 = 스킬 9종/대상 슬라이더 2줄
  (3개/페이지, 화살표 = 페이지 이동·끝 비활성, 점 = ceil(n/3), 카드 클릭 = 선택, 스킬 변경
  시 대상 첫 항목 폴백, 빈 칸 비활성) + 가방/도구 소진 라디오 2줄. 가공 = 안내 라벨.
  상세 높이 268 고정 (채집↔가공 전환 흔들림 방지). 스킬/대상 데이터는 나무위키 실측 상수
  (아이콘 이미지는 캡처 수급 후 - 지금은 글자 카드)
- **설정 그룹 교대**: 생활 = 채집 대기(초, 임시 120)만 + 공용 버튼 3개. 적용된 설정/설명서
  팝업도 mainCategory 분기 (가공 중엔 '가공(추후 지원)'만)
- **시작 이중 차단**: btnStart 핸들러 서두 + Invoke-StartAutomation 서두 (승인 비동기
  콜백 우회 경로까지 - 리뷰 조건 D). Set-UiRunning 에 대분류 실행 중 잠금
- **config 스키마 5**: mainCategory('battle' 기본)/life 섹션 신설, 마이그레이션 allowlist
  +life, 저장은 life 값 6개만 upsert (_설명 키 보존 - 리뷰 지적), 복원은 enum 폴백/clamp +
  대분류 복원 분리. 앱 버전 2.0.0
- **교차 리뷰 실결함 수정 3건**: ①F9 핫키 에지 래치를 PerformClick 앞으로 (모달 팝업의
  메시지 루프에서 타이머 재진입 시 팝업 중첩 - 리뷰 실험 확인) ②슬라이더 위치점 색을
  테마 적용 후 themeMuted 복원 ③life 섹션 객체 교체 → 값 upsert

**검증**: 문법 0건 → 신규 가드: audit 9건 + tab_toggle DetailBottom 3케이스(372/518/546)·
대분류 배선 + config 4→5 마이그레이션 동작 2벌(기본 보충/생활 사용자 보존) / 기존 계약
갱신 4건(allowlist·supportsCustom·schema 5·탐색 창) → **회귀 40종 전부 통과** → v2.0.0
빌드. **화면 검수 보류**: 사용자 v1.2.1 GUI+게임 운용 중이라
유휴 시점에 전투/생활채집/가공/승인 오버레이 캡처 예정 (리뷰 소스 단계는 승인 완료).

**같은 날 후속 - 채집 스킬 아이콘 내장**: 사용자 제공 게임 아이콘 9종(던전이미지\생활,
104px)을 30px 축소 → base64 로 GUI 에 내장, 스킬 카드에 아이콘+이름 표시 (대상 아이콘은
캡처 수급 후). **손 전사 사고**: base64 를 손으로 옮기다 5종 손상(herb 디코드 불가 -
빈 catch 가 숨겨 교차 리뷰가 적발) → 원본에서 스크립트로 일괄 재생성·패치로 교체.
리소스 계약 보강(실패 ID 수집+finally 정리+경고 로그, 종료 시 Bitmap Dispose - Button.Dispose
는 Image 를 해제하지 않음 실험 확인). audit 에 9종 실디코드 가드(사본 분리) 추가.
교훈: **바이너리/장문 리터럴은 손으로 옮기지 말고 스크립트로 생성·패치할 것.**

**아이콘·글자 겹침 수정 (같은 날 사용자 실기 제보)**: 30px + TopCenter/BottomCenter 분리
정렬이 카드 높이 56 에서 글자가 아이콘을 침범 → 4개 변형 렌더링 비교 후 **24px +
ImageAlign/TextAlign 모두 MiddleCenter(한 덩어리 중앙 쌓기)** 채택. base64 9종을 24px 로
일괄 재생성(9/9 실디코드), 3배 확대 캡처로 해소 확인. audit 가드: 실디코드 24×24 갱신 +
정렬 3속성·카드 크기 회귀 가드 2건 추가 (리뷰 조건). 리뷰 승인.

**슬라이더 페이지 전환 애니메이션 (같은 날 사용자 요청 - 데모 3속도 비교로 320ms 확정)**:
비트맵 슬라이드 방식 - 현재/다음 페이지 카드를 합성해 임시 클리핑 Panel 의 PictureBox
스트립을 320ms ease-out 으로 밀고 실제 버튼 노출. 상태 전부 $script: (데모 1차에서
GetNewClosure 함정 실사고 - 클로저 안 $script: 쓰기 미반영으로 화살표 영구 잠김 → 명명
함수 틱 + 클로저 금지 계약). 리뷰 조건 반영: canNavigate 게이트/카드·Start 잠금 가드/
Stop-LifeSlideNow 완전 정리 계약(Set-MainCategory 서두·패널 갱신·Set-UiRunning(일반 Stop -
SkipUiRefresh 면 화살표 false 가 실행 스냅샷에 저장돼 영구 비활성)·폼 종료 배선)/존 크기
런타임 계산/스냅샷 실패 시 Bitmap 자체 정리/예외 → Stop 수렴. audit 가드 10건.

**대분류 버튼 아이콘 (같은 날 사용자 요청 - 공격력/생활력 게임 아이콘 제공)**: 20px base64
내장(catBattle/catLife - 스크립트 패치), 글자 왼쪽 ImageBeforeText 중앙 묶음. **원본이
흰색(투명 배경)이라 크림 비활성 버튼에서 안 보임**(스모크 실측) → 시작 시 themeText 틴트
사본(alpha 유지 재채색)을 만들어 활성=흰 원본/비활성=틴트로 상태별 교체
(Update-MainCategoryVisual). 4상태 스모크 전부 가시 확인, 리뷰 픽셀 검증(가시 픽셀
142/199개 alpha·틴트 일치) 포함 최종 승인.
- **정렬 후속 수정 2회 (사용자 제보 수렴)**: ①"기존과 어긋남" - ImageBeforeText 묶음 중앙은
  글자가 아이콘 폭만큼 밀림 → Overlay 독립 정렬 + 대칭 패딩(비대칭이 글자 4px 밀던 것 리뷰
  실측)으로 글자 정중앙 복원 ②"아이콘이 글자에서 너무 멂" - MiddleLeft 는 아이콘이 버튼
  구석 → 원하는 조합(글자 정중앙+아이콘 인접)은 Button.Image 정렬로 불가라 **Paint 직접
  그리기**로 전환: TextRenderer.MeasureText 로 글자 시작점 계산 → 아이콘을 글자 왼쪽 6px 에
  DrawImage, 상태 전환 시 Invalidate. 리뷰 실측: 일반/Bold 동일 좌표(점프 없음), DPI 비인식
  모드라 배율 불일치 없음. 가드 3건 교체. 회귀 40종 통과, 최종 빌드는 대장 참조, 리뷰
  최종 승인.

### 2026-08-04 — 커스텀 반복 '랜덤 진행' 신설 (v1.2.1, 사용자 확정 스펙 - 구현 완료·실기 검증 대기)

**확정 스펙 (사용자 결정 5건)**: ①켜면 **매 바퀴(첫 바퀴 포함)** 시작 때 리스트 순서를 무작위
셔플 - 항목 구성/개수는 불변(정확히 1회씩) ②1층·2층 혼합 리스트는 사용 불가(토글 비활성,
던전/심층만 - 어비스는 층 제약 없음) ③재시작 시 그 바퀴의 섞인 순서를 이어감 + **리스트
화면도 섞인 순서로 표기** ④적용: 던전/심층/어비스 커스텀 전부 ⑤배치: 리스트 버튼 열 맨
아래 [랜덤] 토글(누르면 크림색 유지 = 활성, 재클릭 해제 - 설정/로그 탭 토글과 동일 방식).

**구현 (설계 합의 - 조건 전부 반영)**:
- **셔플 주체 = GUI, 워커 무변경**: 순열은 progress.shuffleOrder(등록 인덱스 배열)로 저장.
  생성 지점은 딱 2곳 - 시작 게이트(Confirm-CustomShuffleReady, 순열 선확보·저장 실패 시
  시작 금지)와 Step 의 바퀴 전환(index=0 복귀). getter/재시도/복구 경로는 읽기 전용이라
  호출 횟수에 따라 순서가 바뀌는 사고 없음. 컨텍스트는 Items(등록 순서 - 지문/마커 계약
  불변)와 ExecutionItems(실행 순서)를 분리, 워커 env(ITEM/NEXT/LIST/POSITION)는 실행
  순서 기준. 랜덤은 같은 층 전용이라 next-floor 갈림길 자체가 없음(불변식 주석+가드 고정)
- **순열 수명**: 검증(길이/정수/범위/중복) 실패 시 - index=0 이면 재생성, 바퀴 중간이면
  전체 초기화(중복/누락 방지). 같은 바퀴 전진은 순열 그대로 복사. 랜덤 켬/끔 자체가 순서
  의미를 바꾸므로 토글 변경 = 진행 초기화 + 마커 무효화(원자적 저장)
- **완료 마커 v2**: orderKey(순열 식별자) 추가 - 진행 초기화 후 우연히 같은 lap/index 가
  재현돼도 낡은 마커가 일치하지 않음. v1 마커는 순차 모드와만 일치(하위 호환). 진행
  초기화가 마커도 함께 삭제
- **층 혼합 처리(자동 해제 정책)**: 편집으로 혼합이 되면 랜덤 자동 해제+안내(차단 아님),
  혼합 해소 시 토글만 재활성(자동 재켜짐 없음). config 직접 편집으로 혼합+랜덤이면 시작
  게이트가 해제 정규화. 게이트 호출 6곳(저장2+셀 편집 롤백2+로드2)
- **실행 중 표기**: 회차 시작마다 리스트 행을 순열 순서로 재배열(# 열 '진행순서 (등록번호)',
  현재 항목 크림 하이라이트+EnsureVisible, # 열 폭 52 확대. 리스트 높이 150→174 - 아래끝을
  [랜덤] 버튼과 일치, 같은 날 추가 요청). 행 객체 재사용(Tag=등록
  인덱스)이라 체크/셀 손실 없음. **저장 함수 3곳은 Tag 정렬로 등록 순서 보존**(실행 중
  [설정 저장]이 화면 순서를 저장하는 사고 방어 - 리뷰). 정지 시 Set-UiRunning 경유 전
  경로에서 등록 순서 표기 복원. 상태줄/워커 POSITION 에 '(랜덤)' 접미
- **config**: 3섹션 randomOrder(기본 false) + configSchemaVersion 3→4 (자동 이전이 기본
  보충 - 기존 사용자 기본 꺼짐)

**검증**: 문법 0건 → 신규 test_custom_random.ps1 **43케이스**(순열 검증/생성/중복 항목
매핑/orderKey/마커 v2·v1 하위 호환 - 실물 함수 AST 추출 + 배선 가드 19건) +
test_config_persistence 스키마 4·randomOrder 보충 갱신 → **전체 40종 통과** → v1.2.1
재빌드 (해시 33c62b7b…). **실기 검증 대기**: 토글 표시/색, 랜덤 1바퀴 셔플 표기, 재시작
이어가기(같은 순열), 바퀴 전환 재셔플, 층 혼합 자동 해제, 순차 모드 회귀 없음
(격리 캡처 검증은 라이브 GUI 중복 인스턴스로 보류 - 다음 재시작 때 확인).

### 2026-08-04 — GUI 폼 폭 616→560 축소 (v1.2.1, 사용자 확정 시안 - 문구·배치 불변)

**요청/시안**: 폼이 넓다는 피드백 → 실제 캡처 기반 "문구·배치 그대로, 폭만" 시안 560px 확정.
(문구 무변경 조건의 하한 = 던전 커스텀 리스트 열들이 실폭을 사용하기 때문)

**구현 (리뷰 좌표 전수 검수 반영 - 계획의 잘림/겹침 8곳을 사전 교정)**: 폼 (600,872)→(560,872),
최소 폭 616→560(기본+접힘/열림 잠금 3곳), 최상위 그룹 7개 554→514, 승인 안내 2개 530→490,
커스텀 리스트 3개 420→392 + 열 폭 재배분(던전 합 418→368, 어비스 395→367, 심층 유지 - 헤더
말줄임 여유 확보), 리스트 버튼 12개 x445→413(w94 유지), 진행 초기화 3개 x414·w80(합계 라벨
119와 겹침 방지), 반복 커스텀 라디오 390/110, 심층 설명서 413, 흩어진 물길 155, 어비스 매칭
줄 재압축(찾기 85/파티장 280·100/파티원 385), 어비스 콤보 115, 설정 우측 버튼 x390 +
chkAssist 175 + 저장 안내 180, 푸터(지우기 280·80, 버전/링크 369, Log 폴더 열기 100=[설정] 토글과 동일 폭), 미리보기 라벨 299,
리스트 반복 패널 524→494.

**검증**: 구 계약 테스트 2건 갱신(test_normal_dungeon_options 초기화 폭) + 폭 가드 3건 신설 →
**전체 39종 통과**, 문법 0건 → **격리 실행 캡처 실측**: 접힘 560×446(잘림 없음, 로그 접힘 중
경고 발생 → [로그 ● 경고 1] 배지+미리보기 라이브 확인), 둘 다 열림 560×762(설정·로그·하단 줄
겹침 없음) → v1.2.1 재빌드 (해시 0265507f…). 남은 실기: 사용자 재시작 시 심층 커스텀 리스트
열 표시 확인.

### 2026-08-04 — GUI 설정/로그 탭 토글 신설: 접으면 폼 축소 (v1.2.1, 사용자 확정 시안)

**요청/시안**: 설정·로그 영역을 버튼으로 표시하고 싶다는 요청 → 실제 GUI 캡처 기반 시안
3종(접이식/탭/별도 창) 중 "탭 모양 토글 + 둘 다 독립 열림"을 확정. 콘텐츠 상세 아래
[설정][로그] 토글(탭 모양, CheckBox Appearance='Button') — 각각 독립적으로 열고 닫히며
접힌 만큼 폼 높이가 줄어듦 (기본 어비스 기준 둘 다 접힘 ≈ 세로 450px, 기존 872px).

**구현 (리뷰 조건 반영)**:
- **순수 계산/적용 분리**: `Get-TabToggleLayout`(진리표 테스트 대상)이 토글 줄→설정→로그→
  하단 줄 배치와 ClientHeight·높이 잠금·동적 최소를 계산, updateCategoryPanels 끝의
  적용부가 반영. 로그는 총 폼 높이가 아니라 **뷰포트 높이를 기억** — 설정을 접으면 폼도
  함께 줄어듦 (리뷰 지적). 흡수는 '직전 레이아웃이 열림'일 때만 (3상태 전이 토큰)
- **폼 크기 계약**: 제약은 Max→Min 순서로 해제 후 새 크기 적용, 접힘 = Min/Max 동일
  높이로 잠금(가로는 자유)+MaximizeBox 끔+최대화 상태면 Normal 복귀, 열림 = 동적
  최소(로그 100+하단 줄 보장). 하단 줄은 footerGap 실측값으로 직접 계산(Bottom 앵커
  지연 무관), 로그 앵커는 프로그램적 리사이즈 동안 Top 계열로 전환 후 복원, 화면 아래
  초과 시 현재 모니터 작업 영역 기준 Top 보정 + 순수 함수의 뷰포트 축소(최소 100)
- **배지/미리보기**: 로그 접힘 중 오류/경고 발생 시 토글이 '로그 ● 오류 N'(빨강)/'●
  경고 N'(Gold)으로 변하고 토글 줄 우측 라벨에 마지막 해당 줄 미리보기. 판정은 색
  입히기와 같은 심각도 공유 + **논리 상태($chkTabLog.Checked) 기반**(Visible 은 폼 표시
  전 false - 리뷰). 리셋은 로그를 열 때/로그 지우기 때만 ('마지막으로 본 뒤'의 의미 -
  새 회차 시작에 지우지 않음). 자동 펼침 없음. 접힘 중 ScrollToCaret 생략(열 때 1회)
- **상태 저장**: config ui.settingsOpen/logOpen (기본 둘 다 접힘, JSON 불리언만 인정) —
  즉시 저장 헬퍼 + 시작 시 Save-SettingsFromUi 병합 2경로. config.json 기본 키 추가
  (자동 이전 보존 대상, coordsVersion 무관)
- **테마 팔레트를 폼 생성 앞으로 이동** (토글 생성/초기 배지가 색 변수를 먼저 사용 - 리뷰)
- 실행 중에도 토글 사용 가능 (Set-UiRunning 잠금 그룹 밖 - 커스텀 리스트 스크롤 허용과 합)

**배포 전 실사고 정정 (같은 날 사용자 실기)**: 접힘 높이 잠금의 `MaximumSize=(0,높이)`가
창을 **가로 136px(윈도우 최소 크롬 폭)로 짜부라뜨림** — '0 = 무제한' 규칙은 일반 컨트롤
전용이고 **최상위 Form 은 문자 그대로 가로 최대 0으로 해석** (사용자 제보 → 라이브 캡처
136×568 실측). 가로 최대를 65535로 수정 + 재발 가드 2건('가로 0 사용 없음' 포함).

**추가 요청 (같은 날)**: ①로그 접힘 시 로그 전용 하단 컨트롤(글자 크기 라벨·숫자, 로그
지우기)도 함께 숨김 — 'Log 폴더 열기'/버전 표시는 항상 유지. 숨김 중에도 numFontSize.Value
는 배율 재적용/저장 로직이 정상 참조 ②로그 기본 뷰포트 300→150 (열었을 때 너무 높다는
실기 피드백 - 사용자가 창을 늘리면 그 높이가 세션 동안 유지됨). 가드 2건.

**검증**: 문법 0건 → 신규 test_gui_tab_toggle.ps1 = 실물 함수 추출 진리표 24케이스(상태
조합 4종/설정 접기 158px 축소 계약/뷰포트 보존·최소/작업영역 축소/멱등) + 배선 가드 17건,
test_config_persistence 에 두 키 보존 3건 추가 → **전체 39종 통과** → v1.2.1 재빌드
(최종 해시 ce087923…). 실기: 격리 2상태 자동 검증은 기존 창의 중복 인스턴스 안내에 막혀
보류 - 사용자 재시작 시 캡처 검증 예정 (토글 동작·접힘 높이·배지·상태 복원·하단 숨김).

### 2026-08-04 — 실행 중 커스텀 리스트 스크롤 잠김 해제 (v1.2.1, GUI 개선 - 사용자 요청)

**요청**: 커스텀 반복 시작 후 리스트 항목이 많으면 스크롤이 생기는데, 시작하면
콘텐츠 상세 그룹이 통째로 잠겨 스크롤도 안 됨 — 실행 중 진행 항목을 확인할 수 없음.
원인: Set-UiRunning 이 `$grpContentDetail.Enabled = $false`로 그룹을 꺼서(WinForms 는
부모 비활성 = 자식 스크롤까지 사망) 직속 자식인 커스텀 ListView 3개(던전/어비스/심층)가
같이 죽는 구조.

**수정 (리뷰 조건 반영)**: 그룹 통째 잠금 → **자식 개별 잠금**으로 변경 —
- 실행 전환 시 grpContentDetail 직속 자식들의 Enabled 를 **스냅샷(해시) 후 개별 비활성**,
  커스텀 리스트 3개만 제외(스크롤/행 선택 허용). 종료 전환 시 스냅샷 복원(+IsDisposed
  방어) 후 폐기 — 조건부 Enabled 자식(주간 카드 연동 구역 콤보 등) 상태 보존.
  **스냅샷 존재 여부가 전이 토큰**이라 중복 호출(true→true)에도 원상태 유실 없음.
  캡처는 전부 뜬 뒤 별도 루프로 비활성 (Enabled 게터가 조상 반영 유효값이라 - 리뷰 지적)
- 실행 중 리스트 편집 전 경로 차단: 셀 편집/콤보 지연 드롭다운(기존 running 가드 확인) +
  **머리글 전체 토글(ColumnClick) 3곳 running 가드 추가** + **체크 토글(ItemCheck) 취소
  가드 3곳 신설**(마우스·Space 키 전 경로. crLoading 은 조건에 안 넣음 - 우회 방지 리뷰
  계약). 스크롤·행 선택·열 폭 조절만 허용 (실행 항목 판독은 Checked/Selected 를 읽지 않아
  상태 오염 없음 - 리뷰 확인)

**검증**: 문법 0건 → 기존 잠금 계약 단언 교체 + 신규 가드 6건(test_custom_cell_edit) →
전체 38종 통과 → v1.2.1 재빌드 (해시 ae8562e0…). 실기: 커스텀 시작 → 리스트 스크롤 가능·
체크/편집 불가 확인, 중지 → 버튼/입력 복원 확인 (사용자 확인 대기).

### 2026-08-03 — 네트워크 불안정 팝업이 클리어 대기를 600초 삼킴 → '다시 시도하기' 자동 클릭 (v1.2.1, KJM PC 실사고)

**사고 (08-01 06:40~06:53, KJM PC 1810×1020, v1.2.0, 심층 무한 반복 - 로그 폴더 제보로 발견)**:
심층 클리어 대기 중 **'네트워크 연결이 불안정합니다' 팝업**(던전 안, 좌 '시작 화면으로' /
우 녹색 '다시 시도하기'+Space 배지) 발생 → 전용 처리가 없어 클리어 대기 600초 통째 소진 →
재시작 워커의 알 수 없는 화면 중앙 5회+X 후보 15회 전부 빗나감 → 3연속 정지.

**실측 (오류 캡처 3장, 워커 동일 파이프라인 - 3장 s3/s4 전부 동일)**: 제목 '네트워크'(532,389)
'불안정합니다'(718,389)(s4 '불안정합LI다' - 조각 생존) / 버튼 '다시 시도하기'는 s3
'다셔'+'kl도하기'(764,619), s4 '다시'+'人I도하기'(764,619) — '시도' 조각은 불안정,
**'도하기' 조각은 양 배율 생존**. 좌측 '시작 화면으로'(488~550,619)는 타이틀 이탈 위험 버튼.

**수정 (리뷰 조건 3 반영)**: 신설 `Close-NetworkUnstablePopup` + 순수 선택 소함수
`Select-NetworkRetryWord` —
- 제목 ROI(420,355,440,70) s3 **'네트워크'+'불안정' 엄격** ('연결' 완화는 다른 연결 오류
  팝업 오포섭 위험으로 기각 - 리뷰 ①)
- 버튼: rgClearExit s3→s4 사다리에서 '도하기' 조각 + **위치 게이트(X≥640, Y 585~655)**로
  우측 버튼만 선택 (좌측 버튼 원천 차단), 전 배율 실패 시 실측 예비 좌표(743,620)
- **Focus 후 제목 재확인 후에만 클릭** (전면화 사이 팝업 소멸 시 예비 좌표가 밑 화면을
  누르는 사고 방지 - 리뷰 ②). 단어 좌표는 기준 좌표라 Click-GamePoint 사용 (리뷰 지적)
- Space 배지가 있지만 위험 화면 오입력 금지 정책상 키 대신 상태 확정 클릭
- **배선 순서 조건 (리뷰 ③)**: '다시 시도하기'가 결과 화면 '다시 하기' 탐색 어휘
  (다시/다셔/하기)와 겹치므로 **클리어 대기는 결과 화면 감지보다, 결과 대기는 반복 버튼
  탐색보다 먼저** 배선. + 입장/매칭 스윕 끝, 이벤트 스킵 선두(알 수 없는 화면 루프의
  정식 출구) 총 4곳.

**검증**: 제목 진리표 5 + 버튼 선택 진리표 7 케이스(test_weekly_coop_popup) + 배선 가드
7건(test_audit_fixes) → 전체 38종 통과, 문법 0건 → 오프라인: 캡처 3장 전부 제목 확정 +
s3 'kl도하기'(764,619) 채택 / **기존 Find-DgRetryButtonPoint ROI(s5)는 3장 모두 빈 판독 -
어휘 충돌 실증 음성** / 음성 96장 제목 판정 오탐 0건 → v1.2.1 재빌드 (해시 42332800…).
캡처 보관: `던전이미지\어시스트\네트워크불안정_다시시도_1810_KJM.png`.
실기: 네트워크 순단 자연 발생 시 로그 확인 ("네트워크 불안정 팝업 감지 - '다시 시도하기'
클릭"). 네트워크가 계속 죽어 있으면 기존 대기 한도가 오류로 정지 (최후 방어 현행 유지).

### 2026-08-03 — 1908 창 난이도 알약 판독 전멸 + 주간 리셋 '협동 미션' 전체 창 (v1.2.1, 실사고 2건 - 2차 대응)

**사고 A (08:50, 사용자 아침 재시작 후 1908×1076 창)**: 커스텀 시작이 페카 1-3 옵션 화면에서
난이도 '어려움' 확정 실패 3연속 → fail-closed 정지. 진단 캡처 3장 모두 옵션 화면 정상 + 알약이
검색 영역 안 — 판독만 전멸.
- **원인 (오프라인 완전 재현)**: Get-GameRegionCapture 의 확대 목표가 '기준 크기×배율' 고정이라
  1908 창(비율 1.5)에서는 **실효 배율 = 배율/1.5**. Find-DgDifficultyPoint 의 s4/s3/s5 가 실효
  2.67/2.0/3.33배가 되고, 금색 선택 강조 알약은 이 구간 전멸('1권하움'/빈 판독 - 캡처 3장 모두
  라이브와 동일 재현). s6~s8(실효 4.0~5.3)도 전부 깨짐 — 고배율 확장은 답이 아님.
  **s2(실효 1.33배, 거의 원본)만 3장 모두 '어려움'(660,121) 정상 판독 + Select 채택.**
  ※ 전날 밤 "오프라인 성공 vs 라이브 실패 모순"의 정체: 당시 검사 스크립트가 네이티브 크롭에
  배율을 곱해(실효 4.5/6/7.5배) 워커와 다른 조건을 검사했던 것. 워커 동일 수식(크롭·리사이즈·
  단어 좌표·Select·HardX)으로 다시 만들자 라이브 실패가 그대로 재현됐다.
- **수정**: Find-DgDifficultyPoint 배율 사다리 @(4,3,5) → **'어려움' 키 한정 s2 최종 폴백 추가**
  (`$pillScales = @(4,3,5); if ($key -eq '어려움') { $pillScales += 2 }`). 기존 성공 경로 비용·
  동작 불변(마지막 폴백). '어려움' 한정 이유: 앵커/HardX 위치 게이트가 어려움에만 있어 s2
  오독이 있어도 오채택 불가 — 일반/매우어려움은 텍스트 게이트뿐이라 판정면을 늘리지 않음
  (보수 조건). 매우 어려움의 1908 실측이 모이면 확장 검토(개선점 백로그).

**사고 B (06:02 재분석 + 같은 날 KJM PC 제보로 2건 확정)**: 위 정정처럼 월요일 6시 리셋이
**친구 창 '협동 미션' 전체 창**을 자동으로 띄움 — 기존 팝업 핸들러('협동'+'참여' 버튼 줄
매칭)는 이 창을 못 잡음 (두 PC 로그 모두 미발동).
- 개발 PC (1272, v1.2.1): 3연속 오류 뒤 '알 수 없는 화면' X 후보가 우연히 닫아 자가 복구
  (약 90초 손실 + 오류 세트 3개)
- **KJM PC (1810×1020, v1.2.0, 심층 무한 반복)**: 06:02:27 같은 사고 — 재시도 로그 없이
  **그대로 정지** (5시대까지 2분 간격 정상 회차). 캡처: `던전이미지\어시스트\협동미션_전체창_
  주간리셋_1810_KJM.png` 보관. 매주 월요일 전 사용자 재발 예정이라 전용 방어 추가.
- **수정**: 신설 `Close-CoopMissionBoardScreen` — 좌상단 제목 ROI(120,45,220,50) 판독.
  1차 구현은 s3 단일 배율 + '협동'+'미션' 엄격 매칭이었는데, **KJM 캡처 실측으로 1810 창
  s3 에서 '미션'→'미선' 깨짐 발견** (또 단일 배율 함정) → 재합의로 보강:
  ①순수 판정 소함수 `Test-CoopMissionBoardTitle` = '협동' AND ('미션' OR '미선' - 실측
  이형만, 광범위 완화 금지) ②판독 `Test-CoopMissionBoardVisible` = **s3→s4 사다리, '판정
  참'만 조기 종료** (빈/깨진 판독은 다음 배율) ③Focus 전후 이중 확인 후 X(1228,67) 클릭.
  실측: 1272 = s3 '협동'+'미션' / 1810 = s3 '미선' 이형 채택·s4 정상, X 좌표 두 캡처 일치.
  완료 팝업(Close-CoopMissionScreen, 제목 중앙)과 ROI 가 달라 충돌 없음.
- **배선 (리뷰 권고 반영)**: ①입장/매칭 스윕(Invoke-PurchasePopupSweep) 끝에 추가 —
  스윕을 쓰는 입장 대기·복귀 대기·시작 판정 일괄 커버 ②이벤트 스킵 경로 선두(범용 '건너'/
  '지원'/'확인' 탐색보다 먼저 - 전용 제목 판정 우선) ③클리어 대기 ④결과 화면 대기(클리어
  직후~결과 최대 90초 사각지대) — ③④에는 주간 리셋 팝업 핸들러도 함께 배선 ⑤커스텀 '다음
  층으로' 전환 대기에 스윕+주간 리셋 배선(복귀 대기와 같은 계약).

**검증**: 문법 0건(워커/GUI) → 기존 배율 단언 갱신(test_dg_layout_system 사다리 3건) + 신규
배선 가드 9건(test_audit_fixes) + 보드 제목 진리표 8케이스(test_weekly_coop_popup) →
**전체 38종 통과** → 오프라인 재현 4종: ①사고 A "수정 전 실패(3장 s4/s3/s5 전멸) → 수정 후
통과(3장 모두 s2 채택 (660,121))" ②사고 B 양성: KJM 캡처 s3 '미선' 이형 채택 ③사다리 분기
실픽셀 증명(미선 불허 조건에서 s3 불일치 → s4 '협동미션' 채택) ④음성 97장×2배율 194회 판독
'협동' 오탐 0건, 사다리 2회 판독 23ms(성능 무시 가능) → v1.2.1 재빌드 (해시 03fe0409…).
실기: 사고 A 는 1908 창 커스텀 재시작으로 즉시 확인 가능, 사고 B 는 다음 월요일 6시 자연 확인.
KJM PC 는 v1.2.0 이라 v1.2.1 릴리스 전까지 매주 월요일 수동 재시작 필요.

### 2026-08-03 — 월요일 6시 주간 리셋 팝업이 '다시 하기' 복귀를 막아 무인 정지 → 복귀 대기 블로커 처리 (v1.2.1, 실사고)

**사고 (06:02, 심층 커스텀 무인 - 밤새 13바퀴+ 정상 후)**: '다시 하기' 클릭 직후 **주간 협동
미션 리셋**(월요일 6시)이 화면을 덮음 → '다시 하기 → 옵션 복귀 대기' 루프에 팝업 처리가 없어
40초 초과 → 재시도의 '알 수 없는 화면' X 후보(1229,67) 클릭이 우연히 닫음 → 화면이 던전 안
잔류라 복구 가드 안전 중단(설계 정상, 마커 보존) → 3연속 정지. 주간 리셋 핸들러는 07-20부터
있었지만 이벤트 스킵 경로에만 배선.
> 정정 (08-03 오전 캡처 재실측): 복귀를 막은 화면은 '새로운 한 주' 팝업이 아니라 **친구 창의
> '협동 미션' 전체 창**이었다 — 06:02:21 캡처(재시도의 어떤 클릭보다 이전)에 이미 전체 창이
> 떠 있고, 그 사이 자동화 클릭 로그가 없으므로 **리셋이 이 창을 자동으로 띄운 것**. 아래
> 배선(팝업용 '협동'+'참여' 매칭)은 이 창을 잡지 못한다 → 같은 날 2차 대응 항목에서 전용
> 핸들러 추가.

**수정 (리뷰 조건 3 반영)**: ①핸들러를 Close-WeeklyCoopResetPopup 소함수로 추출(동작 불변,
이벤트 스킵은 호출로 치환) ②**'다시 하기 → 옵션 복귀 대기'(던전/심층)와 '새 임무 선택 →
첫 화면 복귀 대기'(사냥터)에 6시 블로커 3종 배선** - '계속하시겠습니까' 처리 뒤 순서로
구매/보상/협동완료 스윕 + 주간 리셋 닫기 + 공지 게시판 X. 닫은 경우 continue 재캡처(같은
프레임 재판정 금지), 캡처 실패 중 전부 건너뜀.

**검증**: 배선 가드 4건, 전체 38종 통과, 문법 0건, v1.2.1 재빌드 (해시 983e4f37…).
실기: 다음 월요일 6시 자연 확인 (또는 6시 공지/리셋 체인 발생 시).

### 2026-08-02 — 커서 간섭 오클릭이 '보유한 재화' 화면을 열어 대기 가림 → 클릭 게이트 + 자동 닫기 (v1.2.1, 실사고)

**사고 (22:02, 개발 PC 라이브 - 사용자 실관찰 제보)**: 컷신 '장면 넘기기' 클릭 직전 커서
이동이 실패(마우스 간섭)했는데 **기존 정책이 "확인 실패해도 클릭 강행"** → 커서가 있던 임의
지점(재화줄)이 눌려 **'보유한 재화' 전체 화면이 열림** → 클리어 대기가 가려져 타임아웃 직전
(수동 닫기로 구조). 참고: 컷신 클릭 직전 라벨 재확인은 이미 있었음 - 뚫린 건 커서 강행 쪽.
전날(08-01 12:59) '다시 하기' 40초 미클릭 사고도 같은 원리(커서 간섭 강행 클릭)로 재해석됨.

**수정 (리뷰 승인 - 커서 미확인 클릭 금지·좌표 폴백 금지 조건)**:
1. **Click-ScreenPoint 커서 게이트**: 2회 이동 후에도 커서가 목표 ±3px 확인이 안 되면 클릭을
   **건너뜀** (경고 문구도 "클릭을 건너뜁니다 - 다음 감지에서 재시도"로 교체). 전 클릭 공통 -
   상태 기반 재확인 구조라 건너뛴 클릭은 다음 폴링에서 자연 재시도됨
2. **'보유한 재화' 화면 자동 닫기**(Close-CurrencyOverviewScreen): 좌상단 제목 ROI(25,45,260,50)
   엄격 확인 후 우상단 X(1228,67) 클릭. 클리어 대기·결과 대기 루프에 배선. '보유한'이 s3에서
   '모유한'으로 깨져(실측) '유한/보유'+'재화' 조각 판정. 미니맵 확대 화면은 캡처 미확보로 제외
   (뜨면 캡처 요청)

**검증**: 라이브 캡처(보유한재화_전체화면.png 보관) 감지 재현 + 보관 93장 오탐 0, 배선 가드
3건, 전체 38종 통과, 문법 0건, v1.2.1 재빌드 (해시 10134cd7… - 최신).

### 2026-08-02 — 타 PC 게임 프리즈 제보: 이벤트 오인 헛클릭 + 무응답 안내 (v1.2.1)

**제보 (10:00, 타 PC v1.2.0, 심층 커스텀 25바퀴째)**: 게임이 클리어 화면에서 **로딩 멈춤
(프리즈)** — 제보자 확인. 꿀비가 2.5분간 50+회 클릭했지만 전부 무시됨 → "결과 화면(다시
하기 버튼)을 찾지 못했습니다" 오류. 게임 재시작으로만 해결 가능한 환경 요인이었으나 꿀비
동작에서 개선점 2개 발견:
1. **Clear-EventOverlay 가 클리어 화면을 '출석/이벤트 화면 추정'으로 오인** → 중앙 5회 +
   우상단 X 후보 20회 헛클릭(72초). 원인: Test-KnownScreen 에 클리어/던전 결과 화면이 없었음
2. 타임아웃 문구가 원인을 알 수 없는 범용 문구

**수정 (리뷰 조건 반영)**: ①Test-KnownScreen 에 클리어 화면(Test-DungeonClearPrompt)과
던전 결과 화면(Find-DgRetryButtonPoint) 추가 - 이벤트 스킵이 두 화면을 건드리지 않음 (던전
결과 화면 중앙 클릭 → 전리품 상세 오픈 위험도 함께 제거) ②Wait-ForResultScreen 타임아웃
시점에 클리어 문구를 **재판정해 여전히 보이는 경우에만** "클리어 화면이 터치에 반응하지
않습니다 - 게임이 응답 없음(로딩 멈춤) 상태로 보입니다. 게임을 재시작한 뒤 다시 시작해
주세요."로 교체 (아니면 기존 문구 폴백, throw·코드 1 유지).

**검증**: 배선 가드 2건, 전체 38종 통과, 문법 0건, v1.2.1 재빌드 (해시 378ec057… - 최신).

### 2026-08-02 — 카드 확인이 RDP 전환에 시도를 다 뺏겨 게이트 정지 → 캡처 대기 + 재확인 연장 (v1.2.1, 실사고)

**사고 (06:03, 아침 6시 리셋 + RDP 본체 자동 전환이 겹침)**: 리셋이 소탕 카드를 켜둬 미사용
항목이 해제 필요 → Set-DgToggleCard 6회 중 **5회가 RDP 전환(캡처 불가 5초) 구간에 소모** →
복구 직후 6회째에 '선택됨'(OCR 'A•에태됨')으로 읽혀 해제 클릭 → **마지막 회전이라 재확인
없이 종료**($false) → 커스텀 게이트(카드 미확인+소모량 null)가 코드 4 정지. 게이트 동작은
설계대로였으나 확인 기회가 불공정 (3차 점검 때 보류한 '마지막 회전 재확인' 항목의 실사고화).

**수정 (리뷰 조건 3 반영)**: ①토글 루프 회전 서두에 캡처 실패 대기(While + SafeStop 검사 +
카드 영역 복구 탐침, **회전 미소모** - 입장 재시도 루프와 동일 계약) ②마지막 회전에서
클릭했으면 **1회전 연장**해 재확인 보장 (연장 회전에서도 반대 상태면 성공 처리 없이 실패 -
fail-closed 게이트 유지, 연장은 1회뿐).

**검증**: 토글 루프 상태 기계 시뮬레이션 4케이스(사고 재현: 캡처 실패 5프레임 미소모 →
클릭+재확인 성공 / 6회째 첫 클릭 → 연장 재확인 / 연장에도 반대 상태 → 실패 / 무한 연장
금지) + 배선 가드 2건, 전체 38종 통과, 문법 0건, v1.2.1 재빌드(미배포 - 재빌드 허용)·보관.

**같은 날 후속 (GUI 문구 오해)**: 위 정지가 GUI에는 범용 문구("조건 충족으로 정지 - 마족공물
소진 등")로 떠서 사용자가 "공물 미사용인데 왜 공물?"으로 오해 (실사례). → 코드 4 상태줄이
로그의 **실제 [완료] 사유를 표시**하게 변경 (기본/복구 로그 tail + 종료 직전 최종 폴링으로
수집, 개행 제거·80자 제한, 회차 시작 시 초기화, 미수집 시 기존 범용 문구 폴백 - 리뷰 조건
3 반영). 배선 가드 3건, 38종 통과, v1.2.1 재빌드 (해시 1b0733b9…, 이 빌드가 최신).

### 2026-08-01 — 협동 미션 완료 라이브 미감지(두 밤 연속) + 시작 팝업 즉사 → v1.2.1 (실사고)

**사고 (08-01 07:41~42, 밤새 무한 24바퀴 후)**: 물약 소진 상태 입장 → 물약 부족 팝업(스윕이
1회 닫음) → 밑에 겹쳐 있던 **협동 미션 완료 화면이 43초간 미감지** → 입장 대기 45초 초과 →
재시도 워커 2개가 시작 화면 판정(제목/HUD)이 팝업에 가려 "던전 화면이 아닙니다" 즉사 →
3연속 오류 정지. 07-31 22:08에도 같은 협동 화면 미감지 1회 (당시엔 '로딩 지연 끝자락 팝업'
으로 추정했으나 오판 - 재발로 재조사).

**원인 (오류 캡처 2장 + 원본 실측 캡처 오프라인 재현으로 확정)**:
- Close-CoopMissionScreen 은 ①부제 감지(정상) → ②'확인' 버튼 단어 탐색(Find-GameTextPoint
  **기본 s3**) 2단계인데, ②가 s3 에서 '>poce'/'할인'으로 깨져 실패 (원본 캡처 재현. s4 정상).
  실패 시 **로그 없이 $false** → 매 폴링 조용히 포기. 카드 버튼 '서대되'와 같은 단일 배율 결함.
- 워커 시작 단계(제목 판독 → 화면 분기)에 팝업 처리가 전혀 없어 전체 화면 팝업 = 무조건 오류.
  (기존 Clear-EventOverlay 는 출석/이벤트 전용이라 구매 팝업을 모름)

**수정 (v1.2.1, 리뷰 승인 - 조건 3 반영)**:
- 확인 버튼 탐색을 **다중 스케일 3→4→5**로, 전 배율 실패 시 **부제 재확인 후 실측 좌표
  (636,654) 클릭 폴백** + 안내 로그 (조용한 포기 제거)
- **시작 화면 판정 전 팝업 스윕**: 던전/심층 사이클·사냥터 사이클·메인 공통 3곳 - 닫은
  경우에만 1.2초 대기 후 재확인, 최대 2회. 이번 3연속 즉사가 자가 복구로 전환됨
- 버전 1.2.1 (v1.2.0 은 released 잠금 - 무변경)

**검증**: 오프라인 - 협동 화면 3장 전부 닫기 성공(07:42:31=s3, 원본=**s4 구제** - 다중 스케일
실효 증명), 물약 팝업 프레임은 1분기('닫기') 담당 확인, 오탐 92장 0. 배선 가드 4건 추가,
전체 38종 통과, 문법 0건, v1.2.1 빌드·보관. **실기 확인 완료 (08-02 20:26, 최신 빌드)**:
클리어 대기 중(= 기존 사각지대) 협동 미션 완료 화면 발생 → 4초 만에 "협동 미션 완료 화면
감지 - 확인 클릭"(버튼 다중 스케일 경로) → 클리어 감지·완료 마커·다시 하기까지 회차 무결
계속. 이틀 밤 정지를 만들던 시나리오가 완전 해소됨 - **v1.2.1 릴리스 조건(협동 실기 확인)
충족, 배포는 사용자 결정 대기.**

### 2026-08-01 — 3차 전수 검사(수정 후 재검증, 교차 리뷰 4세션): 회귀 2 + 보강 12 반영 (v1.2.0)

일괄 수정 직후 같은 방식(구간 분담 4세션)으로 재검사 → **일괄 수정이 만든 회귀 2건을 포함**
14건을 다시 수정. 리뷰 계획 승인(조건 6) → 구현 → 리뷰 잔여 2건 반영 → **최종 승인**.
검증: 문법 0건, 회귀 38종 전부 통과(3차 가드 16건 추가), 빌드 OK.

**실기 스모크 (2026-07-31 저녁, 사용자 실행)**: ✅A1 심층 커스텀 D1-1(공물 사용)→D1-2(미사용)
1바퀴 완주 - 미사용 카드 해제·잔상 가드·새 게이트 헛 정지 없음 ✅A2 같은 층(1-2 옵션 시작
→구역 전환)·다른 층(2-2 옵션 시작→'<' 복귀→재선택) 모두 오류 0회 한 번에 진행 - **복귀 플래그
회귀 수정 실기 증명** ✅A3 파티찾기 전 단계 정상(탭 자동 전환/토글 끔 fail-closed 정상 케이스
통과/소모량 검증) - 매칭 자체는 인원 없어 시간 초과(코드 무관), 완주는 사람 많은 시간대에
C2(파티 유지 관찰)와 겸사 확인 예정. ✅A4 어비스 무한 3판+F9 - 준비 실행 2회 미계상 정상,
완주 2판 코드 0, 마지막 판은 나가기→HUD 시점 안전 중지 소비(회차 계상) - 클리어 대기 재구조·
안전 중지 종료 코드 수정 실기 확인 (중간 1회 무로그 종료는 사용자 즉시 중지로 확인 - 정상).
**스모크 A1~A4 전부 통과 - 배포 가능 상태.**

**추가 실기 (같은 날 밤)**: ✅B1 혼합 잠금 - 잠금('횟수 1바퀴' 고정)과 해제 시 '이전 반복
설정 복원' 로그·라디오 모두 확인 ✅B2 = A4 준비 실행 미계상으로 커버 ✅C6 완료 마커 콜드
재시작 - 마커 기록 순간 GUI+워커 강제 종료(진행도 미전진 확인) → exe 재시작 → "1/2번 완료
항목 마무리 복구" → 재입장 없이 2번 항목 전진 → 2바퀴 진행 (복구 중 협동 팝업+로딩 지연
오류 1회는 커스텀 자동 재시도가 이벤트 스킵으로 자가 복구 - 사각지대 아님 확인) ✅C3 공물
잔량 판독 강화 - **잔량 0 사전 정지 실기 발동** ("[완료] 마족공물 소진(잔량 0개, 필요 1개)"
입장 클릭 없이 코드 4, 캡처 실측 잔량 0과 일치 = '뗳' 앵커 판독이 기존 실패 케이스(한 자리
고립 0)를 정확 판독). 잔량 있는 판 회귀는 A1 D1-1(공물 1개 정상 사용 입장)로 커버.
✅B3 장시간 운용 - 타 PC 심층 장시간 실사용 문제없음(07-31 전언) + **개발 PC 본인 실기
(07-31 밤~08-01 종일, 심층 커스텀 무한 27바퀴+)**로 종결. 이 장시간 운용이 협동 미션
라이브 미감지 결함을 표면화시켜 v1.2.1 수정으로 이어짐 (그 외 이슈는 마우스 간섭 1회 -
환경 요인, 복구 가드 정상 동작). 남은 실기: 파티찾기 완주(C1·C2 - 매칭 인원이 없어 보류),
협동 미션 완료 자동 닫기 v1.2.1 판(자연 발생 대기), 미해금(계정 조건 - 타 PC 제보 대기).

**일괄 수정이 만든 회귀 (재검사가 잡음)**
- 복구 전용 회차의 선택 화면 판정이 새 플래그를 안 봐서, 탭 복귀+제목 일시 공백 조합에서
  완료 항목을 재입장할 수 있었음 → `$recoveryOnSelection` 이 `$onSelectionScreen` 신뢰
- 클리어 대기 마감 판정을 최상단으로 옮기며, 마감 직전 성공 화면 전환(클리어/결과/보상/선택)
  을 확인 없이 throw 할 수 있게 됐음 → throw 직전 4상태 최종 탐침(+탐침 중 캡처 실패 동결)
- 카드 다중 스케일 1회전 한정은 첫 회전이 화면 전환 중이면 구제 상실 → 1~2회전으로 확대
  (최악 비용 24회 - 재산정)

**과소/보강**
- 카드 게이트: 사용 경로 재판독 null·미사용 경로 유효 밖 잡음 값도 커스텀 정지로 확장
- 정정 클릭 잔상 가드: 방금 카드 클릭+확인이면 소모량 불일치를 표시 지연으로 판단(클릭 생략),
  클릭+미확인이면 무클릭 재판독→커스텀 정지/비커스텀 경고 (07-29 잔상 사고와 같은 클래스)
- Return-ToAbyssSelection: 선택 화면 도달 직전의 안전 중지 예약도 소비 (다음 실행 누수 방지)
- 필드 준비 복귀의 SafeStopExitCode 를 recoveryOnly 포함 무조건 10으로 (복구 전진 오계상 방지)
- GUI 혼합 잠금: 잠금 진입 시 이전 반복 설정(무한/바퀴 수) 저장, 해제 시 복원(범위 보정)
- GUI Kill: 무기한 WaitForExit() 제거 → 10초 타임아웃+false 재시도 연결 (GUI 멈춤 방지, 2곳)
- 빌드: 대장 releases null fail-closed(초기+재조회) / coordsVersion 범위(0~100000)도 검사
- 런처: 임시 파일명 PID 포함(동시 실행 충돌) / powershell 이름 검색 fallback 제거(fail-closed)
- Read-Config 실패 조기 반환에도 화면-디스크 어긋남 안내 / 파티장 토글 끄기 후 'off' 확인만
  성공 로그(unknown 을 '끔'으로 보고하지 않음 - 정지는 하지 않는 경고 유지)

**v1.2.1 보류 추가 (설계 합의)**: 빌드 대장 TOCTOU 파일 잠금(로컬 1인 도구라 재조회+재검사로
실용 충분) / next-floor 완료 판정의 층 검증(후속 시작 검증이 오입장 차단 - 안전망 문서화) /
카드 6회전 마지막 클릭 재확인(소모량 교차 검증+잔상 가드가 백업)

### 2026-08-01 — 전수 점검(교차 리뷰 5세션) 결함 일괄 수정: 워커 12 + GUI 9 + 빌드/런처 8 (v1.2.0)

2회차 전수 점검(워커 전 구간·GUI·빌드·런처를 5세션으로 분담)에서 나온 문제를 사용자 지시
("전부 수정")로 일괄 반영. 모든 항목 설계 합의(조건 5) → 구현 → 리뷰 지적 9건 재반영 →
잔여 3건 재반영 → **최종 승인**. 검증: 문법 0건, 회귀 38종 전부 통과(신규 가드 파일
tests/test_audit_fixes.ps1 31건+), 빌드 OK(새 fail-closed·문법 검사 활성 상태).

**워커 (mabinogi_run_once.ps1)**
1. 선택 화면 복귀(탭 전환/다른층 2종) 성공 후 화면 플래그 미갱신 → "던전 화면이 아닙니다"
   헛 오류(GUI 재시도가 가려줌) — 3경로에 `$onSelectionScreen=$true` 확정
2. 클리어 대기 do-while 바닥의 전투 연장 판정을 continue 가 건너뜀 — `while($true)` + 판정을
   본문 최상단으로 (캡처 실패 중/연장 판독 중 실패는 throw 제외 - 리뷰 조건)
3. 준비/정리 실행 중 안전 중지가 무조건 exit 0 → 회차 오계상 — Return-ToAbyssSelection 에
   `-SafeStopExitCode` 파라미터, 호출부 5곳이 문맥별 코드 전달 (recoveryOnly 복귀 중도 10)
4. 파티찾기 매칭 토글 unknown 을 '꺼짐'으로 가정 → 혼자 오입장 위험 — 재판독 3회 후에도
   unknown 이면 코드 4 정지, 끄기 후에도 'off' 확정 요구(unknown=코드 4, on=throw)
   (던전+어비스. 우연한 만남/파티장은 경고 진행 유지 - 비대칭 리뷰 승인)
5. 커스텀에서 카드 설정 미확인+소모량 null 인데 입장 — 사용/미사용 경로 모두 코드 4 정지
   (null 은 미사용 증거 불가 - IME/일반 실패 공통. 일반 모드는 경고 진행 유지)
6. IME 팝업 감지 후에도 다음 재시도가 확인 없이 재클릭 — 던전·사냥터 입장 루프에 클릭 전
   사전 게이트 (40초 한도·시도 미계상 공유)
7. 사냥터 재시도 실패 경로가 continueSweepOnly 무시 — 잔량 10~19 실측 시 더블 루팅 끄고
   재입장 (확인된 경우만 상태 변경, 폴백 바퀴 미계상으로 마지막 회전에서도 재입장 보장)
8. 여신상/전멸 부활 직후의 임의 구매 팝업을 '가루 부족'으로 오인 → 여신상 전환 영구 고정 —
   해석을 `revivePendingKind -eq '가루 부활'` 한정 (완료 로그용 pending 은 전 종류 유지)
9. Wait-ForScreen 이 캡처 실패 중의 빈 판독을 성공으로 인정 가능 — Condition 실행은 유지하되
   `-and -not screenCaptureFailing` 게이트 (Invoke-ClickUntil 과 동일 계약)
10. 어비스 커스텀 명시 난이도 확인 실패 시 경고 진행 → 던전 -Strict 와 통일해 코드 4
11. 파티원 '준비 완료' 버튼 조건에 '준비' 조각 요구 (완료성 문구 오클릭 방지)
12. 좌표 [int] 오버플로 가드 + referenceResolution 1272×717 강제(하드코딩 좌표 정합)

**GUI (mabinogi_gui.ps1)**
Start-Process 실패 시 실행 상태 복원 / Kill 실패 재시도+잔존 경고(Stop-AllRun·폼 종료 공통,
폼 종료 모달은 UserClosing 한정) / preparedStreak 코드 10 외 전부 초기화(10↔1 교차 오판) /
.bak 청소 실패를 저장 실패로 안 침(역방향 불일치 방지) / 일반 던전 커스텀 로드 stage 형식
검증(심층과 통일) / 새 버전 팝업 승인 조회 중 생략 / 종료 확인 UserClosing 한정 / 프로세스
열거 실패를 실패로 보고 / 저장 실패 경고에 화면-디스크 어긋남 안내

**빌드/런처 (build_exe.ps1 / launcher.cs / .gitignore)**
해시 대장 없으면 빌드 차단(fail-closed) / released 잠금 중복 항목 전수 검사 / PS 문법 검사
내장 / coordsVersion 판정을 런타임 검증기와 통일 / 대장 쓰기 직전 재조회+재검사(fail-closed)
/ LOCALAPPDATA 빈 값 가드 / 구 config 이전 임시 파일 경유(부분 파일 잔존 방지) / 시스템
PowerShell 절대 경로 우선 / .gitignore 를 생성 산출물 2개만 무시로 축소(빌드 스크립트·런처·
해시 대장 버전 관리 편입 - 깨끗한 checkout 빌드 가능)

**v1.2.1 보류 (설계 합의)**: 픽셀 판정 실패의 tri-state 화 / 커스텀 리스트 비셀편집 저장
실패 완전 롤백 / GetNewClosure 스코프 / Return-ToAbyssSelection X 후보 순환 클릭 /
Find-GameTextPoint 절대좌표 재측정 / Get-GamePixel 캡처 플래그 대칭 / 커스텀 토큰 0/1 검증

### 2026-07-31 — 릴리스 전 전체 소스 점검(교차 리뷰) 지적 4건 반영 (v1.2.0)

배포 전 워커·GUI 전수 점검에서 나온 지적을 전부 수정했습니다. **치명 0건**, 아래 4건.

**① [중요] 협동 미션 완료 화면이 클리어 대기 중에는 안 닫혔음 (사각지대)**
- 팝업 스윕(Invoke-PurchasePopupSweep)은 입장/매칭 대기 6곳에서만 호출되고, **클리어 대기
  루프(Wait-ForDungeonClearScreen)는 자체 팝업 처리**(구매 팝업 '닫기'만)를 씀. 협동 미션은
  몬스터 처치 누적으로 완료되므로 **정작 전투/클리어 대기 중에 뜰 확률이 가장 높은데** 그
  구간이 비어 있었음 → 전체 화면이 덮여 클리어 감지가 가려지고 대기 타임아웃(600초) 위험.
- **수정** (리뷰 조건: 공용 소함수): `Close-CoopMissionScreen` 을 새로 만들어 스윕과 클리어
  대기 루프가 공용. 클리어 루프에서는 기존 2회 폴링·캡처 실패 가드 안, **구매 팝업 '닫기'를
  못 찾았을 때만** 확인하고 실제로 닫았을 때만 `continue`. 부활 대기 상태는 건드리지 않음.

**② [주의] 카드 판별 완전 실패 시 OCR 최대 36회**
- 07-31 다중 스케일 추가로 6회전 × 3배율 × 2영역 = 36회까지 늘어남(기존 12회).
- **수정** (리뷰 조건 반영): 다중 스케일은 **첫 회전과 클릭 직후 첫 재확인에서만**, 나머지
  회전은 s5만. 최악 비용 36회 → **16회**. 판독 구제 효과는 두 지점에서 그대로 유지.

**③ [사소] 경고 로그가 최신 판독을 안 보여줌**: 빈 판독은 갱신을 건너뛰어 이전 오독값이
남았음 → 매 판독 그대로 반영하고, 빈 값이면 `'(판독 없음)'` 표기.

**④ [주의, GUI] 저장 실패 롤백 후 잠금 상태 불일치**: 저장 함수가 먼저 혼합 잠금을 바꾼 뒤
행만 되돌려 잠금이 실제 리스트와 어긋날 수 있었음 → 던전·심층 롤백 경로에서 합계 갱신 뒤
`Update-CustomRepeatMixLock` 재호출(호출 4곳 → 6곳).

**검증**: 카드 판독 재현 — 제보 캡처 첫 회전 3회째(s3)에서 '선택됨' 구제 유지, 보관 21장
전부 1회째 성공(회귀 0), 완전 실패 비용 36→16 확인. 협동 감지 — 대상 캡처 감지 + 보관 92장
오탐 0 (공용 함수로 옮긴 뒤 재확인). 배선 가드 8건 추가(협동 공용 함수·클리어 배선·호출
2곳 / 다중 스케일 조건·빈 판독 표기 / 혼합 잠금 6곳·롤백 재계산 2건), 전체 37종 통과,
문법 0건, v1.2.0 재빌드 (이 빌드가 배포 원본).

**점검에서 이상 없음으로 확인된 것**: PS 5.1 함정(배열 풀림 오용 0·변수 뒤 한글 보간 0·
스윕 반환값 소비 계약 6곳 준수), 무한 루프 없음(1곳뿐이며 deadline throw), 잔량 판독 심층
분기의 스코프·폴백·일반 경로 무영향, GUI 재귀 방지·해제 복원·상태 분리·팝업 금지 규칙.

### 2026-07-31 — 타 PC: 카드 버튼 '선택됨'이 스케일 5에서 깨져 소탕 해제 실패 → 다중 스케일 (v1.2.0, 제보 04:52)

**현상** (타 PC, 창 1273×718, 심층 페카 1-2 공물 미사용): "[경고] 마족공물(소탕) 상태를
설정값에 맞추지 못했습니다 (버튼 OCR: '서대되')" ×2 → 카드를 못 끄고 소모량 1 잔존 →
"[오류] 소탕을 해제하지 못했습니다" 안전 정지. 캡처상 카드는 실제 '선택됨'이라 오작동이
아니라 판별 실패에 의한 정지.

**원인 (제보 캡처 오프라인 재현)**: Set-DgToggleCard 가 주/보조 영역을 **스케일 5 고정**으로
읽는데 이 창에서는 보조 s5='서대되'(로그 일치)·주 s5=',k에태돈!'로 깨지고, **같은 화면을
스케일 3으로 읽으면 보조='선택됨' 정확 판독**. 07-29 난이도 알약(단일 스케일 고정 → 특정
창 전멸)과 동일 패턴 — 이형 문자열 추가는 미봉책이라 기각.

**수정** (리뷰 조건부 승인 → 조건 반영): 버튼 글자 판독을 **다중 스케일 재시도**로 —
**스케일 우선 순회(5→3→4, 각 배율에서 주 영역 → 보조 영역)**, 판별되면 즉시 채택·조기 종료.
리뷰 조건: 스케일 우선이어야 기존 '보조 s5 성공' 경로의 비용도 유지됨. 판별 실패 시 기존
흐름(픽셀 회색 판별 → 6회 루프 → 경고 진행) 무변경. 던전·심층·사냥터 공용 함수라 은동전·
더블 루팅 카드에도 함께 적용.

**검증**: ①제보 캡처 — 현행 s5 단독은 판별 실패(사고 재현), 새 계약은 3회째(s3)에서
'선택됨' 채택 ②회귀 보관 21장 — **전부 1회째(s5 주 영역)에서 그대로 성공**(추가 비용 0).
배선 가드 추가, 전체 37종 통과, 문법 0건, v1.2.0 재빌드 (이 빌드가 배포 원본).

### 2026-07-30 — 협동 미션 완료 전체 화면 자동 닫기 (v1.2.0, 사용자 요청)

**문제**: 협동 미션 완료 화면이 자동화 중 뜨면 전체 화면을 덮어 입장·클리어 감지를 가림.
기존 팝업 스윕은 구매 제안 팝업('닫기')과 퀘스트 클리어 보상('확인')만 처리.

**실측** (캡처: `던전이미지\어시스트\협동미션완료_확인버튼.png`, 1272×717): 제목 '협동 미션
완료'는 OCR이 '협동1/四완로'로 심하게 깨져 감지에 쓸 수 없음. 부제 '아이템은 대표 캐릭터가
위치한 서버 우편으로 전송됩니다.'는 단어별 정확 판독. 하단 초록 '확인' 버튼 중심 (636,654)
— **퀘스트 보상 화면과 같은 자리**라 기존 영역(400,628,470,55) 재사용 가능.

**구현** (리뷰 승인): 부제 영역(360,285,560,35) 판독에서 ('우편으로'+'전송') 또는
'캐릭터가위치한'이면 이 화면 확정 → 같은 확인 영역에서 '확인' 글자 찾아 클릭 →
"협동 미션 완료 화면 감지 - 확인 클릭" 로그 → $true 반환. Space 배지가 있지만 위험 화면
오입력 금지 정책상 키 미사용(상태 기반 클릭). 입장·매칭 대기 6지점에 적용.
**(07-31 점검 후속: 클리어 대기 루프는 스윕을 쓰지 않아 빠져 있었음 → 공용 소함수
`Close-CoopMissionScreen` 으로 분리해 두 경로가 함께 사용. 위 07-31 항목 참고)**

**검증**: 오프라인 — 대상 캡처 감지 성공(부제·확인 글자 모두) + **보관 캡처 92장 오탐 0건**.
배선 가드 5건(부제 영역/조각 조건/확인 재사용/깨진 제목 미사용), 전체 37종 통과, 문법 0건,
v1.2.0 재빌드 (이 빌드가 배포 원본). 실기는 자연 발생 시 로그로 확인.

### 2026-07-30 — 공물 잔량 '0'/'1' 판독 강화: 공물 자리 단어 판독 (v1.2.0, 사용자 요청)

**문제**: 심층 잔량 영역(1056,45,64,44)이 숫자 하나를 고립시켜 잘라 '0'/'1' 한 자리가 OCR
미검출 → 사전 소진 감지 생략, 예비 경로([완료] 부족으로 보입니다)로만 정지 (두 환경 실측).

**스윕 (캡처 6장: 정답 0/1/3 + 회귀, 스케일 3/4/5 × ko/en)**: 현행식은 잔량 1에서 전 조합
실패. 재화줄을 넓게(960,45,175,44) 읽으면 공물 아이콘이 '뗳'으로 오독되며 값과 한 단어로
붙어 나옴('뗳0'@1087·'뗳1'@1086·'뗳3'@1088 - 전수 일관) → **단어 중심 x가 공물 자리
(1080~1110)인 단어의 끝 숫자** 채택 시 정답률 100%, 이웃 잡음(은동전@1038·하트토큰
오독@1122)은 x 범위 밖이라 자연 배제.

**수정** (리뷰 승인): Get-DgCoinBalance 에 deepMode 분기 — 넓은 영역 단어 판독(ko 4→5→3
재시도) + 공물 자리 끝 숫자(상한 15 유지). 전 스케일 실패 시 기존 좁은 영역 방식 폴백,
일반 던전(은동전) 경로 무접촉. 이제 잔량 0이 읽히면 사전 소진 감지("[완료] 공물 소진(잔량
0개...)")가 이상 경로대로 발동.

**검증**: 오프라인 스윕 100%(위), 배선 가드(공물 자리 판독+폴백 유지), 전체 37종 통과,
문법 0건, v1.2.0 재빌드. **실기 확인 완료 (2026-07-31 22:27)**: 잔량 0 사전 정지 발동 -
"[완료] 마족공물 소진(잔량 0개, 필요 1개)" 입장 클릭 없이 코드 4, 화면 캡처 실측 잔량 0과
일치 (기존 실패 케이스인 한 자리 고립 '0'을 정확 판독). 잔량 있는 판 회귀는 같은 날 A1
스모크의 D1-1(공물 1개 정상 사용 입장)로 커버.

### 2026-07-30 — 혼합 리스트 자동 잠금: 반복을 '횟수 1바퀴'로 자동 고정 (v1.2.0, 사용자 요청)

**기존**: 던전/심층 커스텀 리스트가 혼합(마지막→첫 항목의 바퀴 순환이 게임에서 불가능한
층 조합 - Wrap 이슈)인데 무한/여러 바퀴면 시작 게이트가 거부하고 "횟수 1바퀴로 바꿔
주세요" 팝업으로 수동 변경 안내.

**개선** (리뷰 승인): 혼합 리스트가 되는 순간 GUI가 자동으로 ①'무한' 라디오 비활성
②'횟수'로 전환 ③바퀴 수 1 고정(입력 비활성). 혼합 해소 시 자동 해제 + 안내 로그(상태
전이 시 1회). 구현: 공통 함수 Update-CustomRepeatMixLock — 던전/심층 저장 함수 서두(모든
리스트 변경이 저장을 경유해 단일 지점 커버, 강제값이 그대로 저장) + config 로드 직후
2곳에서 갱신. 어비스는 층 개념이 없어 미적용, 기존 시작 게이트는 config 직접 편집
방어선으로 유지. crLoading 가드로 저장 이벤트 재귀 없음.

**검증**: GUI 배선 가드 3건(Wrap 필터/잠금 동작/호출 4곳), 전체 37종 통과, 문법 0건,
v1.2.0 재빌드. **GUI 실기 확인 완료** (2026-07-30 사용자 확인: "혼합 리스트 자동 고정 문제
없어") — 혼합 리스트 구성 시 무한 잠금 + '횟수 1바퀴' 자동 전환 정상 동작.

### 2026-07-30 — 공물 부족 정지가 '오류'로 표시되던 문구 개선: 코드 4 안내 정지 (v1.2.0, 실기 01:42)

**현상**: 소탕 사용 + 잔량 0에서 잔량 '0' OCR 실패 → 사전 소진 감지 생략 → 입장 거부 감지
후 "[오류] 입장하기가 진행되지 않습니다..." throw — 동작은 안전하지만 사용자에게 고장으로
보임 (01:42 타 PC 실측, 사용자 요청으로 릴리스 전 반영).

**수정** (리뷰 승인): **소탕 사용 중 + 최종 잔량 미판독 + 입장 거부** 조합만 throw 대신
코드 4 — "[완료] 입장이 진행되지 않습니다 - 마족공물 부족으로 보입니다 (잔량 판독 불가).
잔량을 확인해 충전하거나 소탕을 끄고 다시 시작해 주세요." 임의 해제 없음(부족 추정 해제
금지 계약 유지), 잔량이 읽힌 증거 분기·기타 원인 throw·사냥터 루프는 무변경. 이 지점은
클릭 5회 재시도+IME 팝업 대기+확인 팝업 처리를 지난 뒤라 일시 원인은 소진된 상태.

**검증**: 배선 가드 추가, 전체 37종 통과, 문법 0건, v1.2.0 재빌드 (이 빌드가 배포 원본).
**01:49 타 PC 실기 확인 완료** — 같은 조건(공물 0, 무한 반복)에서 재시도 5회 후 새 문구로
코드 4 정지 + 반복 중단. 릴리스 전 마지막 수정 종결.

### 2026-07-30 — 1908 타 PC: 심층 라벨 경로가 픽셀 확인에 막혀 정지 → 화이트리스트 라벨 신뢰 (v1.2.0, 제보 00:36)

**현상** (1908×1076, 남쪽폐허 심층 1-1): 제목 '남쪽 폐허'가 '크똑페어'로 오독
(1908 창은 제목 오독이 1272보다 훨씬 잦음) → ID 불명 → 라벨 경로 진입 — 진단상 라벨 6개
전부 정상 판독(DI-1 등)됐는데 카드 픽셀 확인(남색 기준)이 적갈색 심층 카드에서 전멸 →
후보 경로도 동일 → 안전 정지. 어제 마스 '파스'(21:52)와 동일 구조로, 1908에서는 상시
재발 예상이라 릴리스 차단으로 격상. 이름 이형 등록은 불가('크똑페어'는 북쪽/남쪽 구분
불능 - 오판 위험).

**색 판별식 접근 기각 (실측)**: 1908+1272 캡처 표본 — 심층 카드 몸통 R24~103 / 패널
배경·틈 R16~45로 겹침(둘 다 어두운 적색 우세) → 남색 카드 같은 깨끗한 분리 불가.

**수정** (리뷰 승인): 심층 모드의 라벨 경로에서 픽셀 확인 실패 시에도 채택 —
①선택 화면(배율 3/5) Source '라벨(픽셀 미확인)' ②옵션 화면 정확 일치 후보(D1-1/DI-1/01-1)
동일. 근거: 심층 라벨은 Get-DgMapLabelText 화이트리스트(D[12]-[123] 통과분만) 통과 필수 +
클릭 후 진입 버튼/제목 2차 검증·복구가 기존재("템플릿 픽셀 미확인 그대로 시도"와 동일
철학). 일반 던전(남색)은 기존 픽셀 확인 유지, 후보 유형(A/B/CR/CN) 경로도 유지(무차별
클릭 방지 - deep ID 불명 + 라벨 전멸이면 여전히 안전 정지).

**검증**: 타 PC 캡처 오프라인 재현 — 라벨 6개 전부 정규화 매칭, 목표 1-1 채택 확인.
배선 가드 3건(선택 2곳/일반 유지 순서/옵션), 전체 37종 통과, 문법 0건, v1.2.0 재빌드.
**01:16 타 PC 새 exe 재시도 완주 확인** — 제목 판독 실패(ID 불명) 상태에서 "구역 1-1 클릭
(라벨(픽셀 미확인))" → 선택 확인 → 공물 소모량 1개 확인 → 입장·클리어·나가기 완주.
1908에서 다중 스케일 알약 탐색·dy 그물 판정·소모량 OCR까지 동시 실증.
**01:23 마스 심층 1272 실기 완주** — 마스도 선택 화면 제목이 빈 판독(ID 불명)이었는데 같은
라벨 경로로 구역 1-2 클릭·확인·완주. 마스 선택 화면 카드 클릭 잔여 항목도 이것으로 종결
(1908·1272 양쪽 실증).
**01:29 타 PC 일반 던전 색 왜곡 내성 완료** — 마스 일반 1-2를 1340×755(비권장 크기)에서
완주: "구역 1-2 클릭 (템플릿:B)" 픽셀 확인 통과(남색 판별이 타 PC 색감에서도 정상) +
탭 자동 전환(심층→던전)·dy 그물 알약 판정·은동전 역방향 생략 계약 실동작.
**01:34 타 PC 마스 심층 공물 판 완주** — 탭 전환 → 라벨 경로 → 공물 소탕 사용 판 입장·
클리어·전리품 공개·나가기 (소모량 판독 실패 1회는 검증 생략 안전 경로로 흡수 - 비권장 창
크기 관찰로 기록).
**01:42 타 PC 공물 소진 시 멈춤 검증 완료 (예비 경로)** — 잔량 0 상태(캡처 재화줄 실측)
에서 소탕 사용으로 시작 → 잔량 '0' OCR 실패로 사전 감지는 생략됐지만, 입장 거부를 정확히
감지해 오입장·임의 해제 없이 "[오류] 입장하기가 진행되지 않습니다" 안전 정지. "공물 없으면
멈춘다"는 결과 동작은 타 PC에서 확인 — 잔량 0 판독 강화·문구 친절화는 '개선점' v1.2.1
후보로 분리. **이것으로 타 PC(1908/1340) 검증 전체 종결.**

### 2026-07-29 — 던전 4유형 잔여 검증 2건 실기 완료 (v1.2.0)

- **2단계 던전 시작 차단** (22:54 실기): 매우 어려움 + 룬다 선택 화면으로 시작 → 클릭 없이
  즉시 차단 "던전 '룬다'에는 '매우 어려움' 난이도가 없습니다" (제목이 '눛나'로 깨져도 이형
  매칭으로 던전 확정 후 가드 발동 — 설계대로)
- **비포커스 층 구역 클릭** (23:04 실기): 룬다 선택 화면을 1층 포커스(1-1 선택) 상태로 두고
  2-1 시작 → 비포커스 2층 패널의 2-1 카드 정확 클릭(템플릿:CR, 픽셀 확인 통과) → "2층
  1구역 진입" 확인 → 입장·클리어·안전 중지 완주. 덤: 은동전 역방향 생략 계약·dy 그물
  판정이 일반 던전 경로에서도 실동작 확인 (회귀 없음)

### 2026-07-29 — 마스 심층: '마스'→'파스' 오독으로 ID 불명 → 안전 정지 (v1.2.0, 실기 21:52)

**현상** (9던전 스윕 중): 제목 '파스던전' 오독 → ddNamePatterns 미등록으로 ID 불명 →
템플릿 배치 경로 대신 라벨·후보 경로로 진행했는데, 심층 카드가 적갈색이라 남색 기준 카드
픽셀 판별이 전부 실패 → "구역 1-2 카드를 찾지 못했습니다" 안전 정지 (방어는 정상 동작.
진단 지도 OCR에는 라벨 정상 존재 - 'DI-2' 정규화도 기구현).

**수정** (리뷰 승인): ddNamePatterns 마스 항목에 실측 이형 '파스' 추가 — ID 확정 시
알비·라비와 동일한 템플릿 경로("픽셀 미확인이어도 실측 매핑 시도" 예비 포함)로 정상 진행.
심층 ID 진리표 2케이스 추가, 전체 37종 통과, v1.2.0 재빌드. 심층 카드 적갈색 판별식은
'확인 필요'에 별도 항목으로 분리 (설계 합의).

### 2026-07-29 — 난이도 확정 픽셀 판정 간헐 실패(6회 재발)의 원인 확정: OCR 박스 Y 출렁임 → dy 그물 확대 (v1.2.0, 계측 21:28)

**이력**: 옵션/선택 화면 난이도 '어려움' 확정의 픽셀 판정이 6회 간헐 실패
(07-28 20:59/21:06/23:56, 07-29 02:00/20:32/21:28). 매번 정지 직후 캡처는 정상 + 오프라인
재현 통과 + GUI 자동 재시도가 흡수 → 원인 미상으로 계측(실패 시 표본 요약 로그)만 추가해
두었는데, **21:28 남쪽폐허 실기에서 계측이 첫 재발을 잡아 원인 확정**.

**원인 (계측 실측)**: `기준(660,114) 2/18` — 알약 중심 y≈103, 금 테두리 y92~95/111~120인데
기준점이 y114(하단 모서리)로 잡혀 dy ±14~18 표본이 테두리를 전부 비껴감. 선택 화면도
`기준(77,192) 0/18`로 동일 패턴(중심 186 대비 6px 아래). 즉 **OCR 단어 박스의 Y 중심이
판독(스케일/깨짐)마다 최대 ~11px 아래로 출렁**(글자 아래 글로우 포함 특성)하는데 기존 dy
창(±14~18)이 좁아 놓친 것. 오프라인 재현이 항상 통과했던 이유 = 정중앙 Y 가정.

**수정** (리뷰 승인): dy 그물을 @(-18,-16,-14,14,16,18) → **@(-18,-14,-10,-6,0,6,10,14,18)**
(9행×dx 3열=27표본)로 확대. 색 조건·문턱(3히트)·dx(±12) 무변경 — 선택 알약은 글자도
금색이라 dy 0 근처가 유효 신호이고, 비선택은 어두운 배경+흰 글자라 색 조건에 걸러져 오탐
없음. 실패 계측 로그는 유지.

**검증 (빌드 조건 - 리뷰)**: 보관+오류 캡처 오프라인 스윕 — 선택 알약 7케이스 × Y 지터
5단계(-4~+12, 실측 어긋남 0~+11 + 여유) 전부 4~14히트 통과 / 비선택 5케이스(매우 어려움
알약·빈 자리·일반 던전 '일반') 전부 0히트(오탐 0). 지터 상한을 실측 기반으로 조정한 근거
포함 최종 리뷰 승인. 전체 37종 통과, 문법 0건, v1.2.0 재빌드.

### 2026-07-29 — 승인제 + 구버전 자동 삭제: 실기 검증 전체 완료 (v1.2.0)

- **승인 온보딩 전 과정(타 PC)** 통과 (07-27): 기기 코드 수신 → 시트 명단 추가(☑) →
  [승인 다시 확인] → 승인·정상 작동. 과정에서 발견 1건 즉시 수정(같은 결과 재확인 시 로그
  침묵 → 매번 결과 로그 + denied 시 기기 코드 끝자리 표시).
- **denied 문구 간소화** (07-28 사용자 확정·실기 테스트): '사용 승인이 확인되지 않았습니다 -
  개발자에게 문의해 주세요 (기기 코드 끝자리 XXXXXX)' — 끝자리는 제보 스크린샷 대조용.
- 첫 실행 정리·시작 재검사·구버전 삭제 통과 (07-27). 시트 체크 시 날짜 기록 3~7초는 구글
  바닥값으로 수용 (승인 자체는 체크 즉시 반영).
- **배포 후 관찰 항목(지인 온보딩·타 PC 동작)까지 포함해 2026-07-29 사용자 최종 확인으로
  검증 종결.**
- 참고: v1.1.1 이하 exe 만 해시 미보유로 자동 삭제 안 됨 — 구본 확보 시
  build/release_hashes.json 에 수동 추가 (다음 릴리스부터 그 버전도 삭제 대상).

### 2026-07-29 — 심층던전 실기 검증 종합 완료 (v1.2.0)

체크리스트 전 항목 통과 — 개별 사고 이력은 아래 2026-07-28~29 항목들 참고:

- **일반 반복(어려움)** (07-28 23:27): 탭 자동 전환 → 구역 카드 → 공물 소탕 1개 교차 검증 →
  입장 → 클리어 → 전리품 공개 → 결과 화면 완주
- **마족공물 옵션 전 조합**: ①사용+소진 시 멈춤 ②사용+소진 시 미사용으로 진행(자동 해제 후
  무료 입장 완주) ③미사용. '은동전 부족 추정' 표기는 통화명 변수화로 정정
- **주간 매우 어려움** (07-28 21:13~22): 8차 중 2회차 연속 완주 — 구역 동적 채택(선택=진입
  버튼/옵션=제목) → 확정 → 입장 → 클리어 → 다시 하기 회차 완료. F9 안전 중지 정상
- **심층 커스텀 반복** (07-29 새벽): 다수 완주 — 순서 실행·옵션 구역 전환(예비 좌표)·
  이어가기·오류 자동 재시도·마지막 항목 '나가기' 필드 복귀·완주 정지·진행도 초기화
- **커스텀 1-3→2층 전환** (07-29 20:47 키아 실기): D1-3 클리어 → "다음 항목이 2층 -
  '다음 층으로'로 이동" → 클릭 → 다음 층 화면 전환 확인 → 회차 완료 → 이어진 회차가
  D2-1 완주. 층 상향 유일 경로 실기 검증 완료
- **탭 자동 전환 양방향** (07-28 22:18/23:34): 던전↔심층 각 방향 완주
- **9던전 배치 전체 완료** (07-29 저녁 21:28~22:15 스윕으로 종결): 키아 — 선택 화면 난이도
  클릭(다중 스케일 검증) + L1 유형 구역 1-3 카드 클릭·확인, 클리어 3판(D1-1/D1-3/D2-1) 완주.
  페카 — D2-1 공물 사용 완주(소모량 1 교차 검증). 남쪽 폐허(CR)·알비(A)·라비(A)·바리
  1광구(A)·바리 2광구(B) — 각 1-2 카드 클릭 명중 + 클리어 완주. 마스 — '파스' 오독 수정 후
  클리어 완주(옵션 시작이라 카드 클릭만 배포 후 관찰). 북쪽 폐허(A) — 커스텀·주간 다수.
  스윕 중 실기 사고 2건(마스 이름 오독 / 난이도 판정 dy 그물) 발견·수정
- **완료 마커 복구** (07-29 22:41 실기 - 마커 기록 직후 워커 강제 종료로 재현): GUI가
  오류+마커를 감지해 "완료 항목 마무리 복구" 전용 회차 실행 — 재입장 없이 결과 화면
  마무리(나가기)만 처리 → 이중 계상 없이 완료 계상 → 완주 정지 + 진행도 초기화 + 마커 소비.
  (꿀비 전체 재시작 변형도 같은 마커 소유자-일치 검사를 사용 - 배포 후 자연 관찰)
- 검증 과정에서 실기 사고 15건+ 발견·수정 (전부 리뷰 승인 + 회귀 테스트 + 이력 기록)

### 2026-07-29 — 금색 강조 알약의 스케일별 깨짐으로 난이도 탐색 전멸 → 다중 스케일 재시도 (v1.2.0, 실기 20:02/20:17)

**현상 2건**: ①키아 심층 옵션(20:02/20:06) — 난이도 '어려움' 탐색 3회 실패, 경고 후 진행
②페카 심층 커스텀 선택 화면(20:17, '이려움' 이형 수정 배포 후) — 같은 실패로 커스텀
strict 오류 정지. 두 던전 모두 '매우 어려움' 미해금이라 알약 1개 + 앵커 없음.

**원인 (오류 캡처 3장 + 보관 캡처, 스케일별 오프라인 전수 재현)**: 탐색이 스케일 4 단일
판독인데, **금색 선택 강조가 걸린 알약은 깨짐이 스케일마다 다름** — 선택 화면(페카·키아·
알비 3장 일치): s3 '어려움' 정상 / s4 '이컪움' / s5 '이컪울'. 키아 옵션: s3 '이려움' /
s4 빈 판독 / s5 '어려움' 정상. 첫 수정('이려움')은 s3 산물이라 실제 s4 실패를 못 막았음.
모든 실측 케이스에서 최소 한 스케일은 정상 판독. HardX(심층 선택 78/옵션 660, 단어 중심)는
전 케이스 실측 일치 확인 — 텍스트만 통과하면 정상 채택.

**수정** (리뷰 승인): ①Find-DgDifficultyPoint 를 다중 스케일 재시도(4→3→5, 첫 성공 채택,
성공 시 조기 종료라 기존 성공 경로 비용 불변 — Get-DgTributeCost 전례) ②'어려움' 실측
이형 3종('이려움'/'이컪움'/'이컪울') 허용 — 기존 가드(매우 짝 제외/앵커·HardX ±35) 전부
유지라 오채택 불가.

**검증**: 진리표 7케이스 + 다중 스케일 배선 가드, 전체 37종 통과, 문법 0건, v1.2.0 재빌드.
키아·페카 실기 재시도로 확인.

### 2026-07-29 — (통합됨: 위 항목 참고) 키아 심층 '이려움' 1차 수정 (v1.2.0, 실기 20:02)

**현상** (키아 심층 검증 중, 20:02/20:06 두 회차 재현): 옵션 화면 난이도 '어려움' 글자
탐색 3회 전부 실패 → 경고 후 현재 난이도로 진행 (비커스텀이라 무해했지만 커스텀이면
정지 사유). 키아는 '매우 어려움' 미해금이라 알약이 '어려움' 1개뿐 = 앵커('일반'/'매우')가
전혀 없는 화면.

**원인 (오류 캡처 오프라인 재현)**: 스케일 3 OCR이 '어려움'→'이려움'으로 깨짐 (스케일 5는
정상, 같은 위치의 북쪽폐허는 안 깨짐). Select-DgDifficultyWord 가 정확 일치만 허용해 텍스트
단계에서 탈락. HardX(심층 옵션 660, 단어 중심 기준)는 실측(662)과 일치해 텍스트만 통과하면
정상 채택됨을 확인. 선택 화면도 같은 구조인데 심층 전용 HardX(78)가 실측(78)과 일치해
알약 1개 던전(키아·알비 캡처 실측)도 텍스트만 멀쩡하면 통과.

**수정** (리뷰 승인 — 새 스레드): Key '어려움' 텍스트 매칭에 실측 이형 '이려움' 1개만 허용.
기존 가드(매우 짝 제외 / 앵커·HardX ±35 검증) 전부 유지 — 이형 단어도 같은 가드를 통과해야
채택되므로 매우 어려움 뒷단어가 '이려움'으로 깨져도 오채택 불가.

**검증**: 진리표 4케이스 추가(이려움+HardX 채택 / 매우 짝 제외 / 자리 밖 거부 / 어려움 Key
전용), 전체 37종 통과, 문법 0건, v1.2.0 재빌드. 키아 실기 재시도로 확인.

### 2026-07-29 — 소모량 '1' 지속의 최종 확정: 게임의 표시 지연 → 전환 직후 검증 생략 (v1.2.0, 실기 01:45)

**커서 파킹 반증**: 아래 항목의 커서 파킹을 넣은 새 빌드(01:42 추출 확인)에서도 01:45 회차에
'1' 재현 → 호버 이론 반증, 파킹 제거 (리뷰 지시). 사용자 화면 실측(소탕 '선택됨' 상태의
D2-3)으로 **'1'은 소탕이 켜져 있을 때 입장 버튼에 표시되는 정상 소모량 표기**임을 확인 —
게임이 구역을 열면 소탕이 기본 '선택됨'이고, 카드를 '도전'으로 꺼도 **버튼의 소모량 표기가
13초 이상 남는 게임 자체 표시 지연**이 전 사고의 공통 원인 (카드 글자는 즉시 갱신되는데
버튼 표기만 늦음. 1분+ 뒤 캡처가 항상 깨끗했던 이유).

**수정** (리뷰 승인 — 플래그 매 호출 초기화 조건): Set-DgToggleCard 에
`$script:dgToggleClicked`(이번 호출에서 실제 클릭 여부, 매 호출 $false 초기화) 추가.
카드 설정 단계의 반환값+클릭 플래그를 받아, **방금 우리가 클릭해 '도전' 전환을 카드 글자로
확인한 경우 역방향 소모량 검증 자체를 생략** (안내 1줄 - 전환 확인은 카드 자체 재판독으로
완료돼 클릭 대상 오인 불가, 이 경우 소모량 표기는 지연이 정상이라 교차 검증 정보가치 없음).
클릭이 없었는데 소모량이 보이는 경우(진짜 걸린 카드/잔존 화면)는 기존 검증+카드 우선 계약
유지. 던전+사냥터 동일 적용. 효과: 경고 소음 제거 + 회차당 ~13초 단축.

**검증**: 배선 가드 갱신(파킹 부재/클릭 플래그/생략 배선 2곳), 전체 37종 통과, 문법 0건,
v1.2.0 재빌드. **02:01 실기 확인 완료** — "방금 마족공물(소탕) 카드를 도전(미사용)으로 전환
확인 - 소모량 표시 검증 생략" 안내 후 경고·대기 없이 2초 만에 입장 (기존 13초+경고 → 0초).
사용자 최종 확인("드디어 해결"). 판독 영역 위치도 단어 좌표 실측으로 재확인 (두 버튼 배치
모두 아이콘 959~977/숫자 980~999 - 영역 978~1130이 아이콘 제외+숫자 포함으로 정확).

### 2026-07-29 — 소모량 '1' 지속 판독의 원인 후보였던 커서 호버 (v1.2.0, 실기 01:33 → 01:45 반증됨)

**신규 실측**: 01:29/01:33 회차에서 '카드 우선' 계약이 정상 동작(경고 후 진행 → 입장 →
완주)했으나 '1' 판독 자체는 계속 재현. 특히 **01:33 회차는 카드 클릭이 전혀 없었는데도**
트리거+루프 내내 '1' → "클릭 직후 표시 잔상" 단독 이론 반증. 반면 정지 직후 수동 캡처
2회는 항상 버튼 깨끗 + 그 캡처의 4조합 오프라인 판독 전부 빈 값 = 화면 렌더링 자체는
깨끗할 수 있고, 차이는 **커서 위치**뿐 (워커 흐름에선 커서가 마지막 클릭 지점이나 GUI
시작 버튼 자리에 머묾. Click-ScreenPoint 는 클릭 후 커서를 치우지 않음). 캡처에 커서
아이콘은 안 찍히지만 **게임이 그리는 호버 UI(툴팁·상세)는 찍힘** — v1.0.7 전리품 커서
가림, 사냥터 라벨 커서 주차(OCR 가림) 전례와 동일 유형.

**수정** (리뷰 승인 — 기준 좌표를 창 위치·배율 반영해 변환 조건): Get-DgTributeCost
서두(IME 검사 전)에 **클릭 없는 커서 파킹** — Get-ScaledScreenPoint(640,8)=게임 창
제목줄 중앙(호버 UI가 존재할 수 없는 안전지대)으로 SetCursorPos + 300ms 후 판독.
best-effort(try/catch, 확인·재시도 없음) - 이동 실패 시에도 기존 '카드 우선' 계약이
안전망. 모든 소모량 판독(사전 교차 검증/역방향/입장 재시도)이 한 지점에서 보호됨.

**검증**: 배선 가드 추가(파킹 → 팝업 검사 순서), 전체 37종 통과, 문법 0건, v1.2.0 재빌드.
실기에서 '1' 경고 소멸 여부로 확인 (남더라도 카드 우선 계약이 진행 보장 - 소음만 남음).

### 2026-07-29 — 카드 끈 직후 소모량 표시 잔상(스테일 UI)으로 미사용 항목 반복 오정지 (v1.2.0, 실기 00:58 확정)

**현상**: 00:07/00:48/00:58 세 번 모두 같은 지점(커스텀 D1-2 미사용, 소탕 카드를 끈 직후)에서
"미사용인데 소모량 1개" → 코드 4 정지. 00:58 은 새 빌드(IME 선차단 포함) 확정 상태였는데
팝업 안내 로그가 전혀 없었음 = 팝업 아님.

**원인 확정 (정지 직후 게임 화면 직접 캡처)**: 캡처에는 소탕 카드 '도전' 정상 + **입장하기
버튼 깨끗(숫자 없음)** + 팝업 없음 + 공물 잔량 0. 그런데 정지 전 6회 연속 '1' 판독(트리거+
루프 5회, null이면 조기 종료되므로 전부 유효값) → **카드를 끈 직후 입장 버튼의 소모량
표시가 갱신되지 않고 남는 잔상**이며, 클릭 없는 수동 대기(10초)로는 사라지지 않고 이후
리렌더 시점에야 사라짐. 카드 클릭이 없던 D1-1 항목(카드가 이미 도전)은 잔상 구간이 없어
무사 통과 — 가설과 정합. 사용자도 같은 진단("입장은 공물 없이 되는데 수량 검증이 세움").

**수정** (리뷰 승인 — 증거 우선순위 재정렬): 역방향 검증을 "카드 상태(1차 증거) > 소모량
표시(교차 검증)"로. Set-DgToggleCard 반환값($true=도전 확정 판독)을 $offCardConfirmed 로
받아, 재판독 루프 소진 후 ①카드 확인됨 → 소모량이 남아 있거나 불명확해도 "표시 잔상으로
판단, 미사용으로 진행" 경고 후 진행 ②카드 미확인(글자 깨짐/설정 실패) → 기존 정지/throw
유지. 07-19 원사고(카드 판별 불가 + 표시 10 = 진짜 켜짐 → 정지)와 잔상 사고를 모두 커버.
던전+사냥터 사본 동일 적용, 5×2초 대기 루프는 유지(표시가 제때 갱신되면 기존 깨끗한 경로).

**검증**: 배선 가드 추가(카드 확인 우선 2곳), 전체 37종 통과, 문법 0건, v1.2.0 재빌드.
실기 재시도로 확인. 잔여 위험(카드 OCR 오판 시 소탕 판 1회 오계상)은 무인 운용 지속 우선으로
수용 (설계 합의).

### 2026-07-29 — IME 팝업 글자가 소모량 '1'로 오독되어 미사용 항목 오정지 (v1.2.0, 실기 00:48 발견)

**현상** (심층 커스텀 D1-2, 아래 00:20 수정 배포 직후 재시도): 소탕 카드 '도전' 확인까지
정상(00:07 수정한 상태 기반 해제가 재클릭 없이 동작)인데, 입장 버튼 소모량이 **10초 내내
'1'로 판독** → "미사용 항목인데 소탕 해제가 안 됩니다 (소모량 1개)" 코드 4 오정지.

**종합 판정**: 소모량 판독 영역(978,636,152,44)은 IME 팝업 범위(x≈908~) 안. 팝업 문구의
OCR 깨짐 '전환하려면'→'전환하**己1**면'(103장 스윕 실측 원문)에서 숫자 '1'이 나와 유효
소모량(심층 1)과 일치. 미사용 상태의 실제 버튼은 4조합(스케일3/5×ko/en) 전부 빈 판독임을
오프라인 재현으로 확인했으므로, 지속되는 '1'은 팝업 오독으로 판정.

**수정** (최종 리뷰 승인 — 플래그 매 호출 초기화 조건 포함):
1. **Get-DgTributeCost 팝업 선차단**: 서두에서 매 호출 `$script:dgCostImeBlocked=$false`
   초기화 → 전용 ROI($rgImePopup)로 팝업 감지 시 `$true` + $null 반환. 판독 영역이 팝업
   안쪽 조각이라 조각 복합 판별이 슬라이스에서 안 통해 전용 ROI 판별 사용. 팝업 오독
   숫자가 모든 소비처(사전 교차 검증/정정 재확인/역방향)에 도달하지 못하고 기존 null
   안전 경로(검증 생략/경고 진행)로 흡수됨.
2. **null-as-확인 지점 보강**: 역방향 해제 루프(던전+사냥터)의 'null=해제 확인' 분기는
   팝업 중이면 판정하지 않고 시도 미계상 대기(2초 간격, 총 40초 한도, 초과 시 기존 불명확
   분기). 역방향 트리거의 팝업-null은 안내 로그 후 카드 증거 기준 진행 (오판 시 최대
   손실 = 공물 1개, 무인 운용 정지 회피 우선 — 리뷰 위험 허용 합의).

**검증**: 배선 가드 3건 추가(선차단/루프 대기 2곳/트리거 안내 2곳), 전체 37종 통과,
문법 0건, v1.2.0 재빌드. 실기 재시도로 확인.

### 2026-07-29 — Windows 입력기(IME) 팝업이 우하단 버튼을 가려 클릭·확인 동시 무력화 (v1.2.0, 실기 00:20 발견)

**현상** (심층 커스텀 D1-2 재시도): 선택 화면에서 구역 선택 확인까지 정상 → 'N층 M구역
진입' 클릭 단계에서 20초 대기 초과 정지. 오류 캡처에 **Windows IME 알림 팝업**("한국어
Microsoft 입력기 / 입력 방법을 전환하려면 Windows 키+스페이스")이 우하단(x≈908~1265,
y≈612~700)을 덮고 있음 — 정확히 진입 버튼 오른쪽 절반 + 클릭 지점(918,655) 자리.

**메커니즘**: ①재클릭 게이트(진입 버튼 OCR)가 팝업 문구로 대체돼 매칭 실패 → 첫 클릭조차
차단 ②클릭이 나갔어도 팝업(OS 최상위 창)에 먹힘. 팝업은 21초+ 지속 실측. 보관 캡처 65장
중 2장(남쪽폐허/페카 심층 옵션)에도 같은 팝업이 찍혀 있어 재발성 실증 — **07-28 23:49
"공물 2개 보유인데 입장 안 됨(부족 추정) → 소탕 해제 후 진행" 사고도 같은 팝업이 입장하기
클릭을 먹은 정황**.

**수정** (설계 합의 — 복합 마커/전용 ROI/증거 기반 계약 3조건 반영 후 최종 승인):
1. **판별식** Test-ImeOverlayText: 단독 조각 불인정, ('Microsoft'+'입력')/('입력방법'+'전환')/
   ('Windows'+'스페이스') 복합 조건만. 전용 판독 영역 $rgImePopup=(905,610,360,95) 신설.
2. **선택→옵션 전환 3곳**: Invoke-ClickUntil에 -FallbackPoint/-FallbackCondition 추가
   (사이클당 1클릭 유지). 팝업 감지 + 좌상단 제목이 선택 화면일 때만 버튼 왼쪽 예비 지점
   $ptDgStageEnterLeft=(770,655) 클릭. 43장 픽셀 실측: 전 선택 화면에서 버튼 위(심층 분홍/
   일반 보라). **옵션 화면의 같은 자리는 '파티 찾기' 초록 버튼이라 제목 게이트 필수**.
3. **입장 재시도 루프(던전+사냥터)**: 팝업 감지 시 시도 미계상 + 2초 간격 소멸 대기(총
   40초 한도, 초과 시 안전 정지). 팝업 중엔 재화 폴백 평가 금지.
4. **'부족 추정' 해제 전면 제거**: 잔량 미판독 상태에서 소탕/더블 루팅을 추정으로 풀던
   분기 3개(던전 2 + 사냥터 1) 삭제 — 해제는 잔량이 실제 읽힌 증거 기반 분기만. 미판독은
   재시도 후 안전 정지 (클릭이 팝업에 먹힌 것을 부족으로 오판하는 사고 원천 차단).

**검증**: 오프라인 OCR 스윕 103장(보관 전체+오류 캡처) — IME 판정 = 팝업 실존 3장만, 오탐
0, 깨짐 이형('전환하己1면','Windows기') 통과. test_ime_popup.ps1 신설(진리표 13 + 배선 9),
전체 37종 통과, 문법 0건, v1.2.0 재빌드. 실기 재시도로 확인.

### 2026-07-29 — 미사용 소모량 역방향 검증: 해제된 카드를 도로 켜는 토글 자기 방해 (v1.2.0, 실기 00:07 발견)

**현상** (심층 커스텀 2번 항목 D1-2): 옵션 구역 전환(1-1→1-2)·난이도 확인까지 정상 →
소탕 카드 클릭 → "미사용(도전) 확인" 통과 → 그런데 "미사용인데 입장 버튼에 소모량 1개"
경고 → 해제 루프가 5초 만에 포기 → 코드 4 정지 ("소탕 해제가 안 됩니다 (소모량 1개)").

**종합 판정** (캡처 재현: 미사용 상태 입장 버튼은 4조합 판독 전부 빈 문자열 = '1'이 남을 수
없음): 카드는 이미 '도전'인데 **입장 버튼 소모량 표시가 카드보다 늦게 갱신**되는 잔상 구간에
역방향 루프가 걸림. 기존 루프는 offCost 만 보고 카드 상태 확인 없이 raw 클릭(최대 2회) →
이미 해제된 카드를 도로 켜는 토글 자기 방해 (23:56 난이도 확정 자기 방해와 동일 패턴 3번째).

**수정** (리뷰 승인 — 조건: Set-DgToggleCard 내부에 자체 재클릭이 있어 루프 안 반복 호출
금지): 역방향 해제 루프를 **Set-DgToggleCard 1회(카드 상태 기반, 이미 도전이면 클릭 안 함)
+ 클릭 없는 소모량 재판독 5회×2초(버튼 갱신 지연 흡수)** 로 교체. 소모량 사라짐 = 해제 확인,
유효값 잔존 = 기존 코드 4 정지, 불명확 = 기존 경고 진행 분기 유지. **사냥터 은동전 역방향
사본(동일 구조 결함)도 같은 계약으로 함께 교체** (사냥터도 5765에서 같은 헬퍼 사용 중).

**검증**: 배선 가드 추가(상태 기반 1회 + 수동 재판독 + offClicks 잔재 없음), 전체 36종 통과,
문법 0건, v1.2.0 재빌드. 실기 재시도로 확인 (같은 D1-2 소탕 해제 구간 통과 여부).

### 2026-07-28 — 옵션 난이도 확정: 진입 직후 연출 구간에 확인이 전멸 (v1.2.0, 실기 23:56 발견)

**현상** (심층 커스텀 1번 항목, 커스텀 strict 라 오류): 진입 직후 옵션 화면에서 어려움
확정 — 선확인 3회(클릭 전, 도달 직후 0~3초)부터 전부 미선택 판정 → 확정 클릭+확인
3회(1.2초 간격)도 실패 → 오류. **오류+3초 시점 캡처는 판정 6/18 완벽 통과** (금색 테두리
정상) — 매우 어려움 20:59/21:06과 동일한 '런타임만 실패' 패턴.

**종합 판정**: 진입 직후 옵션 화면은 ①초기 페이드인으로 알약 강조가 수 초 늦게 그려지고
(글자는 먼저 그려져 단어 탐색은 성공) ②클릭 후 눌림/전환 연출이 확인 간격(1.2초)보다 김.
확인 실패마다 재클릭하면 연출이 다시 시작돼 확인이 영원히 연출 구간에 걸리는 자기 방해
루프 (옵션 '재시작' 회차 4회는 안정 화면이라 선확인 즉시 통과 — 진입 직후만 실패로 정합).

**수정** (리뷰 설계 — 대기 연장만으론 재클릭 자기 방해가 재발한다는 지적 반영):
Set-DgOptionDifficulty 를 수동 확인 계약으로 교체 — ①클릭 전 선확인 5회×1초(페이드 흡수)
②클릭 1회 후 **재클릭 없이** 2초 간격 3회 수동 확인 ③전부 실패 시에만 최종 1회 재클릭 +
수동 확인 3회 ④그래도 실패면 기존 경고/Strict 계약. 선택 화면용 Confirm-DifficultySelected
는 무변경(통과 이력 일관).

**검증**: 배선 가드 추가, 전체 36종 통과, 문법 0건, v1.2.0 재빌드. 실기 재시도로 확인.

### 2026-07-28 — 잔량 판독 상한 검증 + 재화줄 순서 실측 확정 (v1.2.0, 사용자 제보)

- **재화줄 순서 확정** (사용자 제보 + 캡처 2장 단어 좌표 실측): 옵션 화면 우상단 =
  골드(950)·은동전(1039)·마족공물(1087)·하트토큰(1146) 순서 고정. 현재 공물 잔량 영역
  (1056~1120)은 공물만 정확히 잡음 — 07-27 실측 유효 확인, 수정 불필요 (주석에 근거 명시).
- **보유 상한 반영** (사용자 제보: 은동전 최대 150 / 마족공물 최대 15): Get-DgCoinBalance
  잔량 판독에 모드별 상한 검증($dgBalanceMax 150/15) — 상한 초과 판독은 오독 확정
  (하트토큰/골드 조각 오인 등)으로 null 처리 = 검사 생략 진행(안전측, 과잉 정지 없음).
  기존 '6자리 초과 실패 처리'(07-17) 전례와 같은 원리의 조임. 리뷰 승인, 배선 가드 추가,
  전체 36종 통과, v1.2.0 재빌드.
- **소모량 '7X' 이형 보정** (23:40 실기 '71' 1회 — 아이콘 제외 영역에서도 렌더링 순간에
  아이콘 가장자리가 '7'로 걸림. 창 1272 정확·직전 직후 회차 정상 = 일시 이형): 유효값
  불일치 그룹이 '^7(숫자)'면 앞 7을 떼서 유효값일 때 채택 ('71'→1, '72'→2). 경고 후
  진행하는 기존 안전 분기는 그대로 (리뷰 승인).

### 2026-07-28 — 퀘스트 클리어 보상 화면이 입장 감지를 가림 (v1.2.0, 실기 23:12 발견)

**현상** (심층 어려움+공물 4차 — **공물 소모량 1개 확인 통과** = 아이콘 제외 수정 검증 후):
입장 로딩 중 '퀘스트 클리어!!!' 보상 전체 화면('법황청의 특별 의뢰' 완료 - 보상 아이콘 +
하단 'Space 확인' 버튼)이 덮여 입장 완료 감지 45초 초과. 진단 하단 OCR '아이템을 누르면
상세 정보矗 볼 수 있습니다 확인' 실측.

**수정** (리뷰 승인): Invoke-PurchasePopupSweep 본문 확장(호출부 6지점 무변경) — 구매
팝업 '닫기'에 이어, 하단 공용 영역에 이 화면 전용 문구('아이템을누르'/'상세정보')가 보일
때만 확인 버튼 영역(400,628,470,55 - 캡처 실측)에서 '확인' 글자를 찾아 클릭. Space 배지가
있지만 위험 화면 오입력 금지 정책상 키 입력 대신 상태 기반 클릭만.

**검증**: 배선 가드 2건 추가(퀘스트 확인 처리 + 직전 수정 '이미 선택' 확인), 전체 36종
통과, 문법 0건, v1.2.0 재빌드. 실기 재시도로 확인.

### 2026-07-28 — 구역 좌표 소진 시 '이미 선택' 미확인 정지 + '록쪽' 오독 (v1.2.0, 실기 23:04 발견)

**현상** (심층 어려움+공물 3차 — 2차 인식·탭 자동 전환은 실전 통과 후): 구역 1-1 카드
선택 정지. 진단: 제목 '록쪽폐하'('북'→'록' 오독, 비권장 창 1279x721) → 던전 ID 불명 →
템플릿 좌표 불가 + 라벨 탐색 실패 → stagePlan null → 정지. **그런데 진입 버튼은 이미
'Space심층1층1구역진입' = 목표 1-1이 선택된 상태**였음 (탭 전환 직후 심층 기본 선택) —
좌표 후보가 소진되면 '이미 선택돼 있는지' 확인 없이 정지하는 구조적 빈틈.

**수정** (리뷰 승인, 2건): ① 구역 선택 루프에서 stagePlan null 시 정지 전에 진입 버튼
텍스트로 '이미 목표 구역 선택' 확인 — selected 면 클릭 없이 진행('카드 클릭 불필요' 로그),
아니면 기존 안전 정지 (검증 후 조작 정책 - 상태 확인만 추가). ② $ddNamePatterns 북쪽폐허에
'록쪽' 오독 조각 추가 (ID 판별 복원 → 템플릿 경로 재확보).

**검증**: 전체 36종 통과, 문법 0건, v1.2.0 재빌드. 참고: 비권장 창 크기(1279x721)가 판독
품질 저하의 공통 배경 — 사용자에게 [권장 창 모드] 사용 안내.

### 2026-07-28 — 시작 검증 선택 화면 2차 신호: 제목 공백 시 게이트 미도달 (v1.2.0, 실기 22:48 발견)

**현상**: 비권장 창 크기(1279x721 — 사용자가 창을 조정)에서 선택 화면 제목('북쪽 폐허'가
화면엔 선명)이 OCR 빈 값, 진입 버튼('Space2츰3구역진입')은 정상 판독 → 선택 화면 미인식
→ 탭 게이트 스킵 + '던전 화면이 아닙니다' 정지. 오늘 세 번째 '제목 공백' 계열 (20:36/
20:53/22:48 — 원인은 제각각이지만 증상 동일).

**수정** (리뷰 승인): 시작 검증에 선택 화면 2차 신호 — $onSelectionScreen 변수로 확정
(Test-DgSelectionTitle 실패 시 진입 버튼 '진입' 조각 프로브로 인정 + 로그 명시), 탭 게이트
조건과 던전 안/결과/화면 아님 분기 두 곳을 이 변수로 통일 (기존 각자 재호출).
Test-KnownScreen 의 이중 신호와 동일 원리를 시작 검증에 확장. 제목 공백 시에도 게이트
도달 → 탭 자동 전환 정상 발동.

**검증**: 배선 가드 추가, 전체 36종 통과, 문법 0건, v1.2.0 재빌드. 실기 재시도로 확인.

### 2026-07-28 — 심층 공물 소모량 판독: 뿔 아이콘 오독 (v1.2.0, 실기 22:40 발견)

**현상** (심층 어려움+마족공물 사용 첫 실기): 탭 자동 전환·구역 1-1 카드 클릭·옵션 확정·
공물 카드 '사용' 확인까지 통과 후, 입장 버튼 소모량 교차 검증이 '예상 1, 실제 7→7'로
안전 정지 (공물 잔량 충분 — 판독 문제).

**원인** (오프라인 재현): 입장 버튼의 공물 뿔 아이콘이 숫자 판독을 오염 — 기존 영역
(840,636,290,44)에서 'VI입장하기'(아이콘+1→VI), 매우 어려움 진단 'Space72입장하기'
(아이콘=7+2). 실기 '7'도 아이콘 오독 이형. 캡처 기준 ko배율5에서야 1이 잡히는 불안정 판독.

**수정** (리뷰 승인): 아이콘/숫자 픽셀 경계 실측(아이콘 x953~977, 숫자 x980~) →
**deep 모드에서 $rgDgTributeCost 를 (978,636,152,44)로 오버라이드** (아이콘 제외,
숫자+'입장하기'만). 던전 10/20은 기존 영역 무변경, config 키 값 불변 = coordsVersion 6
유지, 파서 무변경.

**검증**: 심층 소탕선택 캡처 21장 전수 — IME 팝업 오염 2장 제외 **19장 전부 첫 시도(ko3)에
'1입장하기'/'2입장하기' 즉시 정확 판독** (기존 영역은 ko3 전멸). 배선 가드 추가, 전체
36종 통과, 문법 0건, v1.2.0 재빌드. 실기 재시도로 확인. (부수 확인: 심층 어려움 카드는
빨간 톤이라 남색 픽셀 검증이 '픽셀 미확인' 폴백으로 진행됨 — 동작엔 문제없으나 심층
카드 색 판별식 실측 보강은 추후 과제)

### 2026-07-28 — 신규 기능: 던전|심층 던전 탭 자동 전환 (v1.2.0, 사용자 요청·리뷰 승인)

**스펙** (사용자 확정): 꿀비 모드와 게임 탭이 어긋나면(심층 모드+던전 탭 / 던전 모드+심층
탭) 기존 코드 4 정지 대신 선택 화면 상단 '던전|심층 던전' 탭을 자동 클릭해 전환. 전환
확인 실패 시에만 기존 안내로 정지 (fail-closed 유지).

**실측** (1272x717): 탭 단어 중심 = 던전(66,128) / 심층(135,128) 던전(172,128), 탭 OCR
영역 (20,100,280,50). 예비 좌표 심층 던전 탭 (150,128) / 던전 탭 (66,128).

**구현** (탭 게이트 교체): 순수 함수 2종 — Select-DgTabWord(심층 탭='심층' 단어·'심충'
오독 관용 / 던전 탭=왼쪽 70px 안에 심층 짝이 없는 '던전' - 매우 어려움 뒷단어 제외와 같은
패턴), Test-DgTabProbeMatchesMode(전환 확인 - 빈 판독이 '심층 없음=던전 탭 성공'으로
오인되지 않게 '진입' 존재 필수, 리뷰 조건). 옵션 화면에서 어긋난 경우 '<' 복귀 후 전환
('다시 하기' 복귀 옵션 화면은 '<'가 없어 복귀 실패 시 기존 안내). 클릭 최대 3회·1.2초
확인. 전환 후 제목 재판독 → 던전 ID 재산출 + 매우 어려움 2단계 던전 가드 재평가 (리뷰
조건 - 전환 전 판별의 좌표·배치 판정 오염 방지). 게이트가 주간 채택·난이도·구역 선택보다
앞이라 전환 후 흐름 무변경.

**검증**: 진리표 13케이스(실측 단어 세트 양방향·오독·누락·빈 판독·진입 소실) + 배선 가드,
전체 36종 통과, 문법 0건, v1.2.0 재빌드. 실기 양방향 확인 대기 ('확인 필요' 체크리스트).

**보강** (역방향 실기 21:58): 던전 모드 + 심층 전용 던전(북쪽/남쪽 폐허) 화면에서는
Test-DgSelectionTitle 이 선택 화면을 미인식(던전 모드 이름 테이블에 없음)해 게이트 도달
전에 '던전 화면이 아닙니다'로 정지 → 심층 이름 조각($ddNamePatterns)을 모드 무관으로
선택 화면 제목에 추가 인정 (옵션 제외 규칙은 유지 - 진리표 4케이스 추가, 리뷰 승인).
공용 던전(페카·알비 등)은 기존 조각으로 이미 인식돼 영향 없음.

### 2026-07-28 — 옵션 매우 어려움 확정: 이미 선택된 알약 재클릭의 자기 방해 (v1.2.0, 실기 20:59·21:06 발견)

**현상** (매우 어려움 실기 6·7차 — 이중 판독·주간 재채택·옵션 도달 전부 통과 후): 옵션
화면 '매우 어려움' 확정의 강조 확인이 실패해 오류 종료. 재시도 2회→3회·대기 800ms→1200ms
연장(1차 수정)으로도 동일 실패 → 타이밍 가설 기각.

**원인** (결정적 재현): 오류 '시점' 캡처에서 정확한 샘플 지점((752,120)/(799,120),
dy±14~18/dx±12) 판정 = **6/18 히트 완벽 통과** (테두리 dy±16 = 255,39,0 선명한 빨강).
화면·판정식·좌표 전부 정상 — **이미 선택된 알약을 확정 클릭/재클릭할 때마다 전환·눌림
연출로 강조가 잠깐 꺼지고, 확인이 매번 그 연출 중에 샘플링**되는 자기 방해 루프. 심층
매우 어려움 진입 경로는 옵션 도달 시 항상 이미 선택 상태라 확정 클릭 자체가 불필요한
동작이었음 (4차 통과는 연출 타이밍 간헐. 마지막 실패 직후 진단 캡처에는 강조 복귀).

**수정** (리뷰 승인, 2건): ① Set-DgOptionDifficulty 에 **클릭 전 선확인** — 알약 단어를
찾은 뒤 Test-DifficultySelectedAt 이 이미 선택이면 클릭 생략('이미 선택 확인 - 클릭 생략'
로그) 후 성공 반환. 미선택일 때만 기존 클릭+확인. '검증 후 조작' 클릭 정책 정합 + 일반
던전도 불필요한 재클릭 감소. ② (1차 수정분 유지) Confirm-DifficultySelected 3회·1200ms —
실제 전환이 필요한 경우의 확인 여유.

**보강** (22:14 실기): 옵션 화면에서 '시작'한 경우(안정 상태)는 선확인이 통과했지만,
선택 화면 → 진입 직후 옵션 도달 경로에서는 알약 강조 연출이 끝나기 전에 선확인 1회가
미선택으로 오판 → 클릭 → 자기 방해 재발. **선확인을 최대 3회·1초 간격**으로 확장
(리뷰 승인) — 3회 모두 미선택일 때만 진짜 미선택으로 보고 클릭+확인 진행.

**검증**: 전체 36종 통과, 문법 0건, v1.2.0 재빌드. 실기 확인: 21:13·21:17·21:51 매우
어려움 3회차 완주(옵션 시작 경로 '클릭 생략' 실동작 포함).

### 2026-07-28 — 밝은 배경에서 확장 제목 판독 전멸 → 이중 판독으로 정정 (v1.2.0, 실기 20:53 발견)

**현상** (매우 어려움 실기 5차): 밝은 배경(노을 항구)의 심층 선택 화면에서 제목 OCR 빈 값
→ '던전 화면이 아닙니다' 오류. (신규 진입 버튼 2차 신호 덕에 시작 정리 오폭주는 없었음 —
20:36 수정 실전 검증)

**원인** (결정적 오프라인 재현): 같은 오류 캡처가 **폭 250 = '북쪽폐하' / 폭 420 = ''(전멸)**.
밝은 영역이 넓게 들어오면 WinRT OCR 텍스트 검출 자체가 실패 — 20:20 사고를 고친 '폭 420
전역 확장'이 밝은 배경 선택 화면을 역으로 깨뜨림 (앞선 420 전수 검증 캡처들은 어두운
시간대라 통과했던 것. 옵션 화면은 배경이 항상 어두운 오버레이라 420이 안정 — 21장 유효).

**수정** (리뷰 승인 — 폭 420 전역 오버라이드 철회): **Read-DgTitleText 이중 판독** 신설 —
좁은 기본 영역(config 250) 우선, '구역'이 없을 때 **심층에서만** 폭 420 재판독해 '구역'이
보일 때만 채택. 일반 던전은 기존과 동일한 좁은 1회(무접촉). 제목 소비 지점 6곳 교체
(Test-KnownScreen / $readDgTitle / 옵션 도달 ClickUntil / 입장 시작 판정 2곳 / 다시 하기
복귀 판정 — 좁은 250만 쓰면 심층 옵션 제목에 '구역'이 없어 "옵션 벗어남" 오판 위험이
있던 곳까지 함께 방어). 캡처 실패 복구 탐침 1곳은 판독 내용 무관이라 유지.

**검증**: 양방향 오프라인 재현(20:53 캡처 좁은 판독 성공 + 20:20 캡처·옵션 21장 확장 판독
성공 — 이중 판독이 두 사고 모두 통과), 배선 가드 3건 교체, 전체 36종 통과, 문법 0건,
v1.2.0 재빌드. 실기 6차로 확인.

### 2026-07-28 — 입장 직후 물약 팝업이 입장 완료 감지를 가림 (v1.2.0, 실기 20:45 발견)

**현상** (매우 어려움 실기 4차 — 직전 수정 전부 통과 후): 주간 구역 2-3 채택·옵션 화면
확인·공물 설정·입장 클릭까지 정상 → 심층 입장 직후 '회복 물약이 부족합니다' 대형 구매
팝업이 화면을 덮어 HUD/퀘스트 기반 **입장 완료 감지가 45초 내내 가려짐** → 오류 종료.
진단 하단 OCR '닫기 6,000 구매하기' 실측 (error_20260728_h20m45s05). 클리어 대기 루프와
입장 직후 키 입력에는 팝업 닫기가 있었지만 **입장/매칭 대기 루프들에는 없던 구멍**.
20:36 사고의 제목 공백도 이 팝업/협동미션 완료 전체 화면이 화면을 덮은 것이 원인으로 추정.

**수정** (리뷰 승인): Invoke-PurchasePopupSweep 헬퍼 신설(rgPopupClose '닫기' 탐색·클릭,
캡처 실패 중 무동작 — 클리어 대기 루프의 검증된 프로브와 동일) 후 입장/매칭 대기 6지점의
Condition 머리에 추가: 던전 우연한 만남/파티찾기, 사냥터, 어비스 매칭/혼자하기, 파티원
대기 루프. **Condition 안에서는 `if (스윕) { return $false }` 계약으로 반환값을 소비** —
PS 5.1 스크립트블록 출력 오염으로 대기가 성공 오판되는 것 방지 (리뷰 지적).

**검증**: 배선 가드(호출 5 + [void] 1 정확 계수 — 주석 예시 오계수 1차 수정), 전체 36종
통과, 문법 0건, v1.2.0 재빌드. 실기 5차로 확인.

### 2026-07-28 — 시작 정리가 심층 선택 화면을 몰라 X 클릭으로 필드 이탈 (v1.2.0, 실기 20:36 발견)

**현상** (매우 어려움 실기 3차): 시작 정리 단계가 심층 선택 화면을 '출석/이벤트 화면
추정'으로 오인 → 중앙 클릭 5회 + X 후보(1229,67) 클릭 → **X가 던전 우상단 X(1228,67)와
같은 자리라 던전 UI를 닫고 필드 이탈** → 필드 HUD가 인식돼 '복귀' 판정 → 주간 구역 판독
1회 시도 ''(필드라 당연) → 코드 4 정지. 사용자가 화면 가운데 연속 클릭을 목격.

**원인**: 그 순간 제목 OCR이 빈 값(일시적 - 직후 OCR은 정상. Test-DgSelectionTitle 자체는
deep 이름 패턴으로 '북쪽폐허' 인식 가능 확인). Test-KnownScreen 이 던전 선택 화면을
제목 단일 신호로만 인식해 일시 공백에 무방비 + 주간 판독도 1회 실패 즉시 정지.

**수정** (리뷰 승인):
1. Test-KnownScreen 에 던전 선택 화면 2차 신호: 하단 진입 버튼 영역($rgDgEnterBtn)의
   '진입' 조각 — 제목(좌상단)과 독립된 우하단 영역이라 이중 신호. 폴백 루프가 매 회
   재확인하므로 OCR 복구 즉시 클릭 중지. ('진입'은 선택 화면 전용 문구 - 옵션/어비스
   상세 '입장하기', 결과 '다시 하기'라 오탐 없음)
2. 주간 구역 판독 1회 → 최대 3회 재시도(1.2초 간격, 매회 재판독 - 옵션 경로 제목/선택
   경로 진입 버튼). 3회 실패 시 기존 코드 4 안내 유지.

**검증**: 배선 가드 2건 추가, 전체 36종 통과, 문법 0건, v1.2.0 재빌드. 실기 4차로 확인.

### 2026-07-28 — 심층 주간 구역 오채택: 난이도 전환 전 버튼을 읽음 (v1.2.0, 실기 20:27 발견)

**현상** (매우 어려움 실기 2차): 시작 시 주간 구역 채택이 '1층3구역'을 판독·채택 → 실제
주간 구역은 2-3. 목표 1-3 vs 실제 2-3 = 다른 층 복구 경로 → 매우 어려움 지도는 단일
카드(D2-3)라 1-3 탐색 실패 → 안전 정지 (error_20260728_h20m27s56).

**원인**: 채택(20:27:48)이 난이도 '매우 어려움' 클릭(20:27:49)보다 먼저 실행 — 화면에
남아 있던 '어려움' 난이도 상태의 진입 버튼(어려움에서 1-3 선택돼 있었음)을 읽음. 매우
어려움 전환 후 버튼은 '2층3구역'으로 정상 판독됨(진단 OCR 확인). 채택 로직은 정상,
읽는 시점이 틀린 타이밍 버그.

**수정** (시작 시 채택은 기준선으로 유지 + 난이도 확정 '후' 재채택 2지점 — weekly 전용
분기라 일반 던전·심층 어려움 무접촉):
1. 선택 화면 경로: 구역 확인 루프에서 진입 버튼 텍스트를 읽은 직후(매우 어려움 클릭 완료
   시점) 그 텍스트로 $ndStage 재채택 — 같은 텍스트로 목표를 만들어 판정이 즉시 selected,
   4회 재시도 루프가 매 시도 갱신하므로 일시 오독도 자기 교정.
2. 옵션 화면 수렴점: Set-DgOptionDifficulty 로 매우 어려움 확정 직후 제목 기준 재채택 —
   어려움 옵션 화면을 열어 두고 시작하면 난이도 전환과 함께 구역도 바뀌는 것을 흡수.

**검증**: 배선 가드 2건 추가, 전체 36종 통과, 문법 0건, v1.2.0 재빌드, 리뷰 승인.
실기 3차로 최종 확인.

### 2026-07-28 — 심층 옵션 화면 확인 실패: 긴 이름 던전의 제목 '구역' 잘림 (v1.2.0, 실기 20:20 발견)

**현상** (심층 매우 어려움 첫 실기): 주간 구역 채택(2-3)·매우 어려움 클릭·진입 클릭까지
정상 → '던전 진입 옵션 화면' 확인(제목 OCR '구역' 포함 조건) 20초 실패 → 오류 종료.
오류 캡처는 정확히 옵션 화면('북쪽 폐허 심층 2층 3구역') 위였음 (error_20260728_h20m20s09).

**원인** (OCR 재현 확정): 제목 영역 $rgDgTitle 폭 250이 일반 던전 제목 기준이라, 심층
제목('던전명+심층+N층 M구역')이 길면 끝 '구역'이 영역 밖으로 잘림. 폭 250 판독
'〈북쪽폐61심층2층3국'(구역 소실) / 폭 420 '…2층3구역'(포함). 이름 짧은 던전(라비·마스
등)은 250도 통과 — 긴 이름(북쪽/남쪽 폐허·페카 고분·바리 광구)만 걸리는 이름 의존 사고.

**수정**: 워커 심층 모드 블록에서 제목 영역 폭만 420으로 오버라이드
(`$rgDgTitle = @($rgDgTitle[0], $rgDgTitle[1], 420, $rgDgTitle[3])`). 일반 던전 무변경
(60판 무실패 검증분 보존), config 키 값 불변 = coordsVersion 6 유지. 부수 효과: 심층
제목에서 층·구역 숫자도 읽혀 제목 기반 판정이 좋아짐.

**검증**: 심층 옵션 캡처 **21장(9던전 전체+매우 어려움) 폭 420 전수 판독 '구역' 포함**,
심층 선택 화면 24장은 여전히 미포함(옵션 판정 오통과 없음). 배선 가드 테스트 추가,
전체 36종 통과, 문법 0건, v1.2.0 재빌드, 리뷰 승인. 실기 재시도로 최종 확인.

### 2026-07-28 — 신규 기능: 어시스트(ASSIST) 자동 켜기 (v1.2.0, 설계·리뷰 승인)

**스펙** (사용자 확정): 전투 HUD 우측 ASSIST 토글이 꺼져 있으면 자동으로 H키를 눌러 켬.
모든 전투 콘텐츠(어비스/던전/심층/사냥터) 적용, GUI 설정 체크박스(기본 켬)로 끔 가능.

**실측** (1272x717, 던전이미지\어시스트\ 4장 — 토글 필 y=513 의 L1209/M1216/R1228 3점):
꺼짐 = 흰 점 왼쪽(255,255,255)+회색 필(78~100) / 켜짐 분홍(클래스 특화)=(206,64,96) R우세 /
켜짐 초록(일반)=(13,179,118) G우세 (켜지면 흰 점이 오른쪽 이동 — R점은 켜짐 판정 제외).

**구현**:
- 워커: 순수 분류기 Get-AssistStateFromColors(off=3점 전부 일치 보수 판정 / on=L·M이 같은
  색 계열일 때만(혼합색 오탐 방지 — 리뷰) / 그 외 unknown 무동작) + Get-GamePixel 3점
  프로브 Get-AssistState. Wait-ForDungeonClearScreen 루프(4개 흐름 공용)에 자동사냥 감시와
  나란히 독립 감시 블록: off일 때만 H 입력+2초 대기, 1회째 로그+15회마다 경고, 켜짐 전환
  확인 로그. config assist 섹션(autoEnable 기본 true/key 72) + clickPoints.assistToggle
  (1216,513) 신설 — 새 키 추가만이라 coordsVersion 6 유지.
- GUI: 설정 그룹 2열 상단(210,25) 체크박스 '어시스트 자동 켜기 (H)' (1열 y106은 클리어
  대기 줄과 겹침 — 교차 리뷰 지적으로 이동), 툴팁·'적용된 설정' 팝업 반영, 저장/로드,
  Update-ConfigToLatest 섹션 목록에 assist 추가.

**검증**: tests\test_assist_toggle.ps1 신설 23케이스 — 실측 3상태 진리표 + unknown 8종
(혼합색/스킬 버튼/로딩 등) + 실측 캡처 4장 정확 판정 + **비전투 캡처 85장(던전 40·심층 45
선택/옵션 화면) 전수 스윕 'off' 오탐 0건**(리뷰 오탐 검증 요구) + 배선 가드 5종. 전체
36종 통과 / 문법 0건 / v1.2.0 재빌드. **실기 검증 통과** — 2026-07-28 사용자 확인
("어시 잘켜진다": 꺼짐 감지 → H 입력 → 켜짐 전환 정상). (assist 추가로 심층 테스트의
섹션 목록 끝 고정 정규식 1건을 포함 검사로 완화 — 의도된 목록 확장)

### 2026-07-28 — 부활 로그 종류 표기 (v1.2.0, 사용자 요청)

- **요청**: 로그만 보고 "이때 죽어서 어떻게 부활했구나"를 알 수 있게 부활 종류(가루/여신상/
  전멸 재도전)를 표기. (부활 로그 자체는 GUI 필터에 안 걸려 이미 표시되고 있었음 —
  Convert-WorkerLogLineForGui 확인. 문구가 종류를 안 알려주는 게 문제)
- **수정** (mabinogi_run_once.ps1 클리어 대기 부활 블록): ① R키 제자리 부활 문구에 재화
  명시 — '행동불능(남은 부활 횟수 N회) - 여기서 부활 (R키, 불사의 가루 소모)' ② 부활 완료
  로그에 종류 표기 — '부활 완료(가루 부활|여신상 부활|전멸 재도전) - 전투 계속'
  ($revivePendingKind 상태 변수, 시작 시 지정·완료 시 초기화). 여신상/전멸 시작 문구는
  기존대로 사유 포함(재료 부족/횟수 없음/전멸 감지).
- **검증**: 문법 0건 / 전체 35종 통과 / v1.2.0 재빌드.

### 2026-07-28 — 물약 구매 팝업에 음식(B) 키가 먹히는 문제 (v1.2.0, 설계·리뷰 승인)

**현상** (사용자 제보 + 02:56 로그 실측): 회복 물약 부족 상태로 입장하면 게임이 구매 제안
팝업을 즉시 띄우는데, 입장 직후 자동 키 입력(Space 자동출발 → B 음식 먹기)이 팝업 위에서
눌려 B가 먹힘 → 그 판 내내 음식 미섭취. 02:56:16 입장 → 02:56:17 Space·B 입력 →
02:56:20에야 클리어 대기 루프의 팝업 감시가 닫기 클릭 (키 입력이 팝업 감시보다 먼저라
구조적으로 못 막았음).

**수정** (mabinogi_run_once.ps1 Invoke-AfterEntryKeys — 어비스 본류/파티원/던전/사냥터
4개 흐름 공용): 키 입력 전에 rgPopupClose 영역 '닫기' 사전 탐색 → 발견 시 닫기 클릭 +
1초 대기 후 재확인. 확인 최대 4회 = 닫기 최대 3회 + 마지막 재확인 (무팝업이면 OCR 1회만
추가 — 설계 합의 계약). 3회 닫고도 남으면 경고만 남기고 키 입력 진행(입장 키를 막는 게
더 나쁨 — 이후 클리어 대기 팝업 감시가 계속 닫기 시도). 캡처 실패 중엔 사전 처리를
건너뛰고 바로 키 입력(실패를 '팝업 잔존'으로 오인 금지 — 리뷰 지적). 키 입력 후 재입력
없음(팝업이 키보다 늦게 떴다면 키는 전달된 것 — B 이중 입력은 음식 중복 소모 위험).

**검증**: tests\test_after_entry_popup.ps1 신설(소스 계약 8건: 닫기 탐색 < 키 입력 순서,
확인 상한 4회, 4회째 클릭 없음, 캡처 실패 가드, Press-KeyOnce 1곳 유지, 잔존 경고) 통과 /
전체 35종 통과 / PS 5.1 문법 0건 / v1.2.0 재빌드. **실기 검증 통과** (2026-07-28 20:16
물약 부족 재현): '구매 팝업 감지 - 닫은 뒤 키 입력 진행'(20:16:28) → Space·B 입력(20:16:29)
순서 확인 — 사고 때와 반대로 닫기가 키보다 먼저. 12초 뒤 재표시된 팝업은 기존 클리어 대기
감시가 정상 처리(20:16:41).

### 2026-07-28 — 파티 전멸 화면 미인식 → 클리어 대기 시간 초과 오류 (v1.2.0, 설계·리뷰 승인)

**현상** (03:27 실기, 어비스 허상의 정박지 · 함께하기 매우 어려움): 03:15 개인 행동불능
R키 부활까지는 정상 → 이후 파티 전원 전멸로 '전멸하였습니다 / 마지막 캠프파이어에서부터
재도전하시겠습니까?' 화면 표시 → 자동화에 이 화면 처리가 없어 클리어 대기 연장만 하다
절대 한도 초과 오류(코드 1) 종료, 화면 방치 (error_20260728_h03m27s19 세트).

**원인**: 개인 행동불능(중앙 '남은 부활 횟수' 안내) 감지만 있고 파티 전멸 화면은 별개
문구라 기존 조각(남은부활/부활횟수/행동불능)에 걸리지 않음 — 실측으로 '부활 가능 횟수'는
Contains('부활횟수') 불일치까지 확인. 미지원 화면(회귀 아님).

**실측** (오류 캡처를 워커 동일 영역·배율로 OCR 재현): 중앙 rgDeathStatus 판독
'전별하였습LI다 … 재도전하시겠습니)가?' ('전멸'→'전별' 깨짐, '캠프파이어' 전파괴,
'재도전' 조각 온전). 우하단 rgReviveButtons 에서 '여신상에서' 중심 (986,670) 안정 판독.
사용자 확정: 여신상 부활 = 세이브 지점(캠프파이어) 재도전 → "전멸 뜨면 여신상에서 부활".

**수정** (mabinogi_run_once.ps1 — 클리어 대기 루프는 어비스/던전/심층/사냥터 공용이라 전
콘텐츠 적용):
- 판정 순수부 Get-DeathInfoFromText 분리(+Wiped). Wiped 조각 = '전멸하/전별하/재도전하'만 —
  '재도전' 단독은 클리어 점수표 '재도전 보너스' 실측 판독과 겹쳐 제외 (리뷰 지적).
- 클리어 대기 부활 블록에서 Wiped 를 Dead 보다 먼저 분기: 우하단에서 '여신' 글자를 실제로
  찾은 뒤에만 클릭(이중 확인 = 오탐 방어 + 상태 기반 클릭 정책). 연속 3회 미발견 시에만
  예비 좌표(신규 config 키 clickPoints.wipeStatueRevive=(986,670), coordsVersion 6 유지) —
  단 클릭 직전 전멸 재확인 통과 시에만, Wiped 해제 폴링에서 미발견 누적 즉시 초기화
  (리뷰 조건). 클릭 후 기존 계약 재사용($reviveConfirmPending → '부활 완료' 로그, 클리어
  deadline 리셋). 재도전 횟수는 기존 revive.maxPerCycle(기본 10) 공유 — 전멸 무한 재도전
  루프 방지, 실제 클릭한 경우에만 계상.

**검증**: tests\test_wipe_revive.ps1 신설 22케이스(실측 전문 양성 6 + 음성 6(점수표
'재도전 보너스' 2종 포함) + 행동불능 회귀 4 + 배선 가드 6) 통과 / 전체 34종 통과 /
PS 5.1 문법 0건 / v1.2.0 재빌드. 실기: 다음 전멸 발생 시 재도전 클릭 로그
('전멸 감지 - 여신상에서 부활 클릭') 확인.

### 2026-07-28 — 심층던전 카테고리 (v1.2.0, 리뷰 설계·A-lite 합의·리뷰 승인)

**확정 스펙** (2026-07-27 실측 45장 기반 — 세부 좌표·오독 표는 이 항목으로 통합):
- 구조 = 던전과 동일 흐름. 차이: ①재화 = **마족공물**(입장 무료 + 소탕 카드 소모 — 어려움
  1개/매우 어려움 2개, 해제 시 공물 없이 입장 — 사용자 확정. 은동전 10/20 로직을 수량
  파라미터화로 재사용) ②난이도 = 어려움 고정 + **주간 매우 어려움**(주마다 9곳 중 1곳,
  단일 구역 — 층·구역 전환 없는 한 곳 반복 전용) ③구역 라벨 D1-1~D2-3 ④제목 '~심층 N층 M구역'.
- 심층 9종(룬다·피오드·페론 없음): 페카, 북쪽 폐허, 남쪽 폐허, 알비, 키아, 라비, 마스,
  바리 1광구, 바리 2광구. 배치 유형(1층/2층): 페카 B/A(재실측: D1-1(206,338) D1-2(293,338)
  D1-3(397,394)), 북쪽폐허 A/A, 남쪽폐허 CR/B, 알비 A/B, **키아 L1/L2(신규 유형)** —
  L1: D-1(891,326) D-2(891,239) D-3(979,238) / L2: D-1(891,326) D-2(979,326) D-3(979,239),
  라비 A/A, 마스 B/CN, 바리1 A/B, 바리2 B/A. 기존 A/B/CR/CN 그리드와 픽셀 일치, 포커스
  이동 29px 동일. OCR 오독 실측: D1→'DI', 'D2-3'→'02-3', 페카→메카·키아→기아·폐허→폐하.
  알약: 선택 (78,186)/(215,186), 옵션 (660,120)/(798,120). 공물 잔량 영역 (1056,45,64,44).
  캡처: 던전이미지\심층던전\ 45장.

**워커 구현** (mabinogi_run_once.ps1 — $deepMode 주입, 던전 사이클 공유):
- `contentCategory='deepdungeon'` → deepDungeon 섹션 오버라이드(difficulty/stage/useTribute/
  continueWithoutTribute/matching, 더블 루팅 강제 false). 모드 테이블 치환: $ddLayoutTable/
  $ddNamePatterns/L1·L2 좌표, $dgSweepCost=1(매우 2)/$dgFullCost 동일/$dgCurrencyName
  '마족공물'/$dgValidCosts @(1,2)/$dgExhaustLabel '공물 소진 시'/알약 X 좌표 변수화.
- D라벨 정규화 Get-DgMapLabelText: 실측 3종만 문맥 제한('DI'→D1, 전체 일치 시 '0'→D,
  최종 D[12]-[123] 화이트리스트 — 전역 치환 금지, 설계 합의). 라벨 비교·옵션 카드 후보에 적용.
- Get-CustomCoinDecision/Get-DgTributeCost 파라미터화(SweepCost/FullCost/CurrencyName/
  ExhaustLabel/ValidCosts — 기본값은 기존 던전 값이라 던전 로그·판정 계약 불변).
- **주간 매우 어려움** = single-area 전용: 제목/진입 버튼에서 구역 동적 채택($ndStage 재계산,
  실패 시 코드 4), 구역 클릭 우회(카드가 빨간색이라 남색 판별 불가 — 진입 버튼 확인만).
- **심층 탭 게이트**: 선택/옵션 화면에서 '심층' 표기 유무로 던전↔심층 교차 시작을 코드 4 정지.
- 심층 커스텀 = 던전 6조각 토큰 재사용 + 강제 규칙(어려움 고정·더블 false). 파티(파티원)
  분기 제외, 로그 태그 $script:contentTag('[심층]'/'[던전]').

**GUI 구현** (mabinogi_gui.ps1 — 경량안 합의: 기존 던전 커스텀 코드 무접촉 격리 복제):
- 카테고리 라디오 '심층던전' 활성(어비스·던전·심층·사냥터 순). 단일 패널 pnlDd* 5줄:
  난이도(어려움|매우 어려움(주간)), 구역 콤보(표시 D1-1~D2-3·저장 내부 '1-1'), 마족공물,
  공물 소진 라디오(공물 체크 시만), 매칭. 주간 선택 시 구역 콤보 비활성.
- 심층 커스텀 Dcr* 완전 분리 세트(입력 줄+소진 라디오+5열 리스트+추가/삭제/↑↓+반복 줄+공물
  합계+진행 초기화) + 전용 저장 함수 Save-DeepCustomRepeatToConfig(공용 저장 경로 무접촉).
  셀 편집은 공용 디스패처에 lvDcrList 분기만 추가. 전환 게이트/추가 차단은 순수 함수
  Get-CustomTransitionIssues 재사용(심층도 같은 1층/2층 구조).
- config: deepDungeon/deepCustomRepeat 섹션 신설, **configSchemaVersion 2→3**(교차 리뷰 —
  안 올리면 구버전 이전이 실행되지 않음), Update-ConfigToLatest 섹션 목록+진행 기록 초기화
  특례, 시작 게이트 3분기(deepCustomRepeat 섹션/Log\deep_custom_done.marker), 심층 설명서.
- 교차 리뷰 반영 4건: schema 3 / 로드 stage 정밀 검증(`^[12]-[123]$` — D 접두 검사만으로는
  D3-1 통과) / 심층 로그 재화 표기($deepMode 조건 '마족공물', GUI 코드 4 정지 문구 분기 —
  던전 리터럴 불변) / 신규 테스트 커밋 대상 확인.

**검증**: PS 5.1 문법 0건(GUI+워커) / **tests\test_deep_dungeon.ps1 신설 67케이스**(D라벨
정규화·공물 판정 파라미터화+던전 기본값 회귀 가드·D표기 변환·심층 셀 편집 계획/적용 정규화·
합계/압축/라벨·층 전환 재사용·배선 가드) / 기존 테스트 4건 갱신(어비스 섹션 선택 정규식
3분기 허용, 매칭 안내 소스 2곳, 시작 로그 태그 변수화, config 이전 schema 3+심층 섹션 2케이스,
오프라인 레이아웃 캡처 경로를 저장소 던전이미지\던전으로 이전) / **전체 33종 통과** /
v1.2.0 빌드 OK. 실기 검증은 '1. 확인 필요' 체크리스트.

### 2026-07-27 — 화이트리스트 승인제 + 구버전 exe 자동 삭제 (v1.2.0, 리뷰 설계 2R+리뷰 2R 최종 승인)

**확정 스펙 요약** (설계 논의 전문은 이 항목으로 통합):
- **승인제**: 기기 코드 = SHA256('HoneyNogi:device:v1:' + 정규화 MachineGuid) hex64.
  명단 = 비공개 구글 시트(활성☑|No|이름|코드|비고|승인일·철회일 자동, 이력 탭 자동 누적,
  onEdit 트리거) + Apps Script /exec 가 활성 코드만 `HONEYNOGI-WL-V1`/`COUNT=n`/hex 줄로 응답
  (이름 비노출, 불량 행 발견 시 전체 오류). 유예 7일, 온보딩 B안, 구글 계정 2FA 필수.
  기각: 서명 승인 파일·MariaDB 자가 호스팅·GitHub 공개 해시 리스트. **한계(명시)**: 평문 ps1
  구조라 보안 경계가 아닌 지인 관리용 소프트 게이트. 시계/config 조작 방어 의도적 포기.
  denied 시 캐시 삭제가 실패하면 '재시작+오프라인' 조합에서 유예가 살아날 수 있는 잔여 위험
  (세션 내에는 tombstone 으로 차단, config 원자 저장이라 실패 자체가 극히 드묾).
- **구버전 삭제**: 신규 버전 최초 실행 1회(자동 닫힘 팝업, config `oldExeCleanup` 마커),
  탐색 = exe 폴더(직속만)+Downloads+바탕화면+문서(하위 5단계), 삭제 = SHA-256이 내장 대장의
  '이전 릴리스'와 정확 일치할 때만 완전 삭제(휴지통 X — 사용자 확정). 실행 중 구버전은
  종료 후 삭제(GUI 뮤텍스 검사보다 먼저), 실패분은 무팝업 재시도.
  **HoneyNogi*.zip 배포 압축본도 정리**(2026-07-27 사용자 추가 요청): zip 자체 해시는
  압축마다 달라 못 쓰므로 **내용물 판정** — 모든 엔트리의 잎 이름이 HoneyNogi*.exe 이고
  해시가 전부 대장의 이전 릴리스일 때만 삭제 (Get-ZipEntryInfos 가 디스크 추출 없이 스트림
  해싱, 실제 누적 읽기량 10MB 상한 = 압축폭탄 방어(리뷰), 엔트리 >5·손상·혼합·빈 zip·
  현재 버전 내용물은 보존). exe+zip 합산 후보 20개 상한.

**구현** (mabinogi_gui.ps1 v1.2.0, 워커 무변경, coordsVersion 6 유지):
- 순수 판정부 8종(진리표 대상): Get-DeviceCode / Test-WhitelistResponse(-cnotmatch, [0-9] —
  둘 다 진리표·리뷰가 잡은 결함 수정) / Get-ApprovalDecision(엄격 UTC, 0≤Δ≤7일, 미래 시각
  무효, 기기 일치) / Get-CleanupPlan(다운그레이드=skip) / Test-OldGuiTitle([Version] 이중
  검증) / Select-OldGuiProcesses(제목+사용자별 추출 경로 이중 확인, 빈 경로=전부 보존) /
  Test-ReleaseManifest(중복·형식 결함 시 전체 무효) / Select-OldExeTargets(미지 해시·현재
  이상 버전 보존).
- 승인 런타임: 시작 시 캐시 잠정 판정 → 러닝스페이스 비동기 조회(TLS12, UseBasicParsing,
  10초, 캐시버스터) → Complete-ApprovalCheck 가 판정·캐시 갱신/삭제·UI·예약 시작 처리.
  시작 버튼 = 게이트(미승인 거부 / 승인 시 재조회 후 Invoke-StartAutomation — 기존 본문을
  함수로 분리). single-flight + 세션 denied tombstone. 미승인 UI = grpApproval(코드 표시+
  복사+재확인, 팝업 없음). Set-UiRunning 활성 조건 합성. Update-ConfigToLatest 가
  approval/oldExeCleanup 키 보존.
- 정리 런타임: 'Global\HoneyNogiCleanup' 뮤텍스(40초, 타임아웃 시 인스턴스 양보 종료) →
  fresh 마커 판정(full/files-only/skip) → 팝업(DoEvents, 2.5초 자동 닫힘) → 프로세스 정리
  (워커 스냅샷 선수집, Process.Handle 바인딩으로 PID 재사용 방어, 현재 버전 GUI 존재 시
  워커 킬 전면 생략, 워커 있으면 구 GUI 즉시 Kill/없으면 Close→3초→Kill, 강제 종료 후
  Release-StuckInput) → 파일 정리(2단계 평가: 후보 전수집→21개↑ 0건 중단, 매니페스트 사전
  검증 실패=Pending, 깊이5/폴더5000/20초/10MB 상한, reparse·OFFLINE·RECALL 스킵, 자기
  경로+자기 해시 보존) → 마커(프로세스 단계 완료 후에만 기록 — 미완이면 다음 실행 full 재시도).
- launcher.cs: release_hashes.json 리소스 추출 + exe_path.txt 기록(승격 환경변수 유실 대비).
- build_exe.ps1: 내장 매니페스트 = '현재 버전 제외'(자기 해시 순환 방지 — v1.2.0 exe 에는
  v1.1.3 만 내장), 빌드 후 자기 해시를 build/release_hashes.json 에 upsert(다음 릴리스용).
  **릴리스 업로드 후 재빌드 금지**(해시 어긋남). 해시 확보 현황(2026-07-27):
  **v1.0.7·v1.1.0·v1.1.1·v1.1.2·v1.1.3 전부 등록 완료** (version\ 보관 폴더 + 바탕화면 원본,
  v1.0.7·v1.1.2 는 두 사본 해시 일치로 교차 확인). v1.0.6 이전 구본이 나오면 수동 추가.
  주의: v1.1.0·v1.1.1 보관본의 수정일이 07-27 이라 배포 당시 파일과의 바이트 일치는 미확증 —
  일치하지 않아도 삭제만 안 될 뿐 부작용 없음(실기에서 지인 PC 삭제 여부로 자연 검증됨).
- **버전 보관 자동화 + 릴리스 잠금** (2026-07-27 추가, 설계 합의): 빌드 후
  `version\vX.Y.Z\HoneyNogi.exe` 사본 자동 보관(개발 PC 보관 - 정리 탐색 범위 밖 / 대장
  해시의 실물 / 복구본). 대장 항목에 `released: true` 표시(릴리스 절차 필수 단계)된 버전은
  **빌드 자체가 실패** — 재빌드로 대장·보관본이 배포 원본과 어긋나는 사고 원천 차단.
  미배포 버전은 자유 재빌드(보관본·대장이 최신 빌드로 동기). 잠금 실동작 검증 완료
  (released 상태 빌드 → 실패 코드 1 + 안내, 해제 → 정상, 타 버전 플래그 보존).
  v1.0.7~v1.1.3 은 released=true 로 표시됨.
  **exe 폴더 탐색은 직속만(깊이 0)**: 개발 PC 의 version\ 보관 폴더가 exe 폴더 하위라
  하위 포함이면 보관본이 삭제되는 사고 발견 → 원 스펙('같은 폴더')대로 축소. Downloads·
  바탕화면·문서만 하위 5단계 포함(사용자 확정 범위 그대로).
- 교차 리뷰 반영 9건: 워커 과잉 종료(사용자별 경로+현행 GUI 보호), 정리 직렬화(40초+양보
  종료), PID 재사용(핸들 바인딩), denied tombstone, 마커 단계 플래그, 매니페스트 실패=재시도,
  COUNT [0-9], 팝업 Pending 문구, 조회 기동 실패 복구. FormClosing 건은 오탐 판정(ShowDialog
  반환 후 finally).

**시트 인프라 실기 안정화 (2026-07-27 밤, 협의)**: 실기 중 3중 사고 — ①스크립트 부분
교체 안내 실수로 상수·doGet 소실(onEdit 전멸) ②코드 없는 행을 ☑해 두면 엄격 검증이 API 전체
다운 ③**웹앱 배포가 '버전 1'에 고정돼 있어 그간의 doGet 수정이 /exec에 반영된 적 없음**(가장
숨은 원인). 해결: Apps Script 최종본 v6 — doGet 은 '코드가 빈' 활성 행만 무해 스킵(빈 코드는
매칭 불가라 조용한 철회 불가능, 값이 있는데 형식 오류면 전체 실패 유지 - 설계 합의) /
onEdit 은 tryLock(0) 스킵 + '변경 없어질 때까지' 반복 패스(잠금 줄서기 제거) + 시트 호출
3회 일괄(읽기1·명단1·이력1) + 날짜 형식은 열 전체 사전 적용 / **timedSync 1분 시간 트리거**가
놓친 동기화 최종 보정(리뷰: 병행 권고). 배포 버전 2로 갱신(URL 동일). Claude in Chrome 으로
원격 진단·수정·배포·검증 수행. **속도 최적화 v7.1**(같은 날 밤, 리뷰 검증): onEdit 30초
타임아웃·timedSync 매분 9~27초의 원인 = 전체 읽기+F:G 전면 재쓰기+패스 반복의 수정 폭주 →
①onEdit 은 fast path 전용(편집된 행만 소범위 읽기→변경 행만 쓰기→**flush 즉시 반영**→이력,
이력을 함께 계산해 누락 없음) ②전체 보정은 1분 트리거가 단일 패스로 전담 ③sleep 제거.
**실측: onEdit 0.6~3.4초(대부분 1~2초), 실패 0건** — 체감 토글→날짜 약 2~4초.
교훈: ①Apps Script 웹앱은 배포가 버전 고정 - doGet 수정 시 '배포 관리→새 버전' 필수
②시트 스크립트 안내는 항상 전체본으로 ③setValues 후 flush 없으면 지연 커밋으로 체감 손해.
**최종 v8 - 즉시 날짜(사용자 요청)**: 승인일/철회일을 **반복 계산 순환 참조 수식**이 소유 —
체크 순간 **0.5초 내 표시**(실측, 트리거 무관). 스크립트는 이력 기록만 담당(H열 숨김 섀도로
전이 감지, onEdit 편집 행 + 1분 timedSync 전체 보정). setupInstantDates 가 수식 설치·반복
계산 활성·섀도 초기화 일괄 처리(100행까지 사전 설치, 초과 시 재실행). 리뷰 사전 검증:
수식 패턴 안정성 확인(대가: 새 행 수식 관리·정렬/복사 취약·F:G 수동 편집 금지 — 사용자가
즉시성 우선으로 선택). 주의: 승인 효력은 이전부터 체크 즉시(doGet 이 A열 실시간 판독) —
수식은 '표시'만 즉시화한 것.

**검증**: 문법 0건(gui/build), 신규 `tests\test_approval_and_cleanup.ps1` 92 assert(응답
검증 14케이스·승인 판정 11·정리 계획 6·제목 7·프로세스 선정 6·대장 7·대상 선정 4·소스 계약
12 — 실결함 2건 검출: -notmatch 대소문자, 이후 리뷰 결함 다수 선반영), 전체 회귀 **32종
통과**, 빌드 OK(v1.2.0, 1238.5KB). 구글 시트+Apps Script 인프라는 사용자가 직접 구축·실검증
(COUNT=1 응답 확인). /exec URL 내장:
https://script.google.com/macros/s/AKfycbwHLZtR0EPApTIkU-IQUc2Jc7mMPCySPWs1caqKqLPm1tN9Q6IhC_JmAeb_OygNNZZkkQ/exec

### 2026-07-26 — 커스텀 시작 구역 전환이 클릭 없이 정지 (v1.1.3, 실기 제보 16:08)

- **사고**: 피오드 옵션 화면(1-1)에서 항목 어1-2 시작. 제목이 계속 `[피오듸층1구역`(층 숫자
  유실)이었는데, **시작 분기 보조 판정은 성공**(현재 1-1 추정 → stay-select)했으나
  `Set-DgOptionStage` 안에서 **카드 클릭 로그가 한 줄도 없이** 10초 만에 "전환을 확인하지
  못해"로 [완료] 정지. 진단 덤프(배율 3)는 지도 라벨 `1-2(918,238)`·`1-3(1022,295)`을 멀쩡히
  읽었음.
- **원인 2중**: ① 내부 보조 판정 `Get-DgOptObservedStage`가 지도를 배율 4·6으로만 읽는데
  피오드 옵션 1층은 S4=1표·S6=0표(40장 실측과 일치)라 '표 2개' 요건 미달 → null.
  ② null(불명)인데도 화면 상태별 1회 잠금(`$observedTried`)이 걸려 남은 회차 내내 재시도
  불가 → 제목 불명 + 보조 불명 = 클릭 경로 자체가 없어 8회 대기 후 not-confirmed.
- **수정 3건**:
  1. 보조 판정 지도 판독 배율을 **3·4·6**으로 확장 (S3가 소카드까지 읽힌 진단 실측 반영 —
     이번 사고 화면이면 S3 2표로 판정 성립).
  2. 1회 잠금을 **판정이 '나온' 경우에만** 적용 — null이면 다음 회차에 재시도 (전환 중 화면
     오확정 방지라는 원래 취지는 비-null에만 해당).
  3. `-AssumeMismatchFirst` 신설: 커스텀 시작 stay-select 호출(이미 '같은 층 다른 구역'이
     확정된 유일한 호출부)은 제목이 불명확해도 **첫 클릭 1회는 진행**. 같은 층 목표 카드
     클릭은 멱등(이미 목표여도 재선택일 뿐)이라 무해하고, 전환 확인은 기존대로 제목/보조
     판정으로 검증. 이후 불명 지속 시 재클릭 없이 안전 정지(맹목 재클릭 금지 유지).
- **검증**: 신규 `tests\test_dg_opt_stage_switch.ps1` — 실함수(Set-DgOptionStage)를 AST로
  추출해 모의 시퀀스로 돌리는 시뮬레이션 진리표 9케이스+소스 계약 2건(29 assert): 사고 재현
  (보조 재시도 성공), 첫 클릭 허용, 멱등 재선택, 재클릭 금지, wrong-floor/not-found/
  not-confirmed 계약 유지. 전체 테스트 **31종 통과**, 문법 0건, 빌드 OK(v1.1.3).
  교차 리뷰 미가용 — 자체 검증으로 대체, 07-25 두 건과 함께 일괄 재검토 예정.

### 2026-07-25 — 4유형 재설계 실기 검증 (10던전 60판 무실패) + 실기 발견 5건 즉시 수정

**실기 결과** (00:15~03:51, 커스텀 6항목(1-1~2-3) 랩): 피오드·룬다(일반) / 페카·페론(매우
어려움, 페론은 은동전 포함)·바리1광구·바리2광구·마스·라비·알비·키아(매우 어려움) =
**10던전 60판 전판 클리어, 오클릭·오입장 0건**. 실전 통과 경로: 템플릿 클릭(A/B/CR/CN), 미등록후보
명중(A·B·CR), 옵션 행 픽셀 판별(B), stay-select/stay-adjust/go-back('<' 복귀), 1-3→2층
'다음 층으로', 보조 판정 2단 콤보('〈라HII층3구역' → 현재 구역 추정 → 같은 층 카드 클릭 승격),
매우 어려움 클릭·확정(앵커 규칙), 페론 세로형 안전 정지 + 진단 세트.

**실기 중 발견·수정 5건** (전부 교차 리뷰·승인, 각각 테스트 28종+빌드 통과):
1. **커스텀 시작 분기 보조 판정 사각지대** — '피오듸층1구역'(층 숫자 유실)이 unclear →
   go-back → '<' 없는 다시 하기 화면에서 정지. 시작 분기에도 Get-DgOptObservedStage 연결
   (같은 층=stay, 다른 층/불명=기존 go-back).
2. **고분·광구 던전 선택 화면 인식 불가(기존 버전부터의 미발견 버그)** — 제목에 '던전'이 없어
   시작 전 정리(Test-KnownScreen)가 알 수 없는 화면으로 오인, 중앙 클릭+X로 선택 화면을 닫고
   필드 이탈. `Test-DgSelectionTitle` 신설('고분'/'광구' 조각 + 던전 ID 매칭, '구역'/'층' 선제외로
   옵션 오판 차단 - 리뷰 지적 반영) 후 5개 지점 교체. 진리표 13케이스.
3. **미등록 C형 과잉 정지** — 페론 오독('페붇고분')으로 ID 불명 + 지도 라벨 미판독 시 세로형
   정지 규칙 발동. 대카드(구역 3)는 CR/CN 공통 위치(979,297)라 예외 허용(픽셀 확인 필수,
   소카드 1/2는 정지 유지).
4. **오독 이형 12종 실측 추가** — 룬다(른다/분다/닡다/눛나), 피오드(깨오/오드), 페카(패가),
   페론(페혼/페붇), 마스(마싀='마스1층' 합침), 라비(라바), 키아(기아). 다중 매칭/바리 숫자 가드 유지.
5. **GUI 리스트 압축 표기** — '매우 어려움'이 '일'로 표기되던 것을 '매'로 (표시 전용).

**정지 2건의 성격**: ①필드에서 시작(사용자 실수) → "던전 화면이 아닙니다"+진단 저장 = 정상
방어 ②페론 세로형 정지 = 설계된 안전 정지(→ 3번으로 개선). 둘 다 잘못된 클릭 0건.

### 2026-07-25 — 난이도 '어려움' 과잉 거부 + 난이도 실패의 오류 격상 (v1.1.2, 실기 제보)

- **사고**: 피오드 옵션 화면(어려움이 이미 선택된 상태)에서 `난이도 '어려움' 글자를 찾지
  못했습니다` 3회 → [완료]로 정지. 원인: 앵커 규칙('어려움'은 '일반' 또는 '매우'가 같이
  보여야 채택 - 리뷰 2차 리뷰 조건)이 **선택된 난이도의 옆 알약('일반')이 흐릿해 OCR이 못
  읽는 상태**를 고려하지 못함. 2단계 던전이라 '매우'도 없어 앵커 전멸 → 멀쩡히 읽힌
  '어려움'을 버림.
- **수정 1**: `Select-DgDifficultyWord`에 표준 위치 검증(HardX) 추가 - 앵커가 없어도 단어
  위치가 그 영역의 표준 '어려움' 알약 자리(선택 140 / 옵션 724, ±35px)면 채택. '매우 어려움'
  뒷단어는 표준 자리에서 110px 이상 떨어져 있어 위치로는 오채택 불가. 매우 짝 제외 규칙이
  HardX보다 우선. 진리표 5케이스 추가.
- **수정 2** (사용자 지적): 난이도 탐색·확정 실패의 커스텀 정지를 [완료](코드 4) →
  **오류(코드 1)** 로 격상 - 화면 인식 실패는 조건부 정상 정지가 아니라 오류이며, 오류로
  던져야 자동 재시작 1회와 오류 스크린샷 세트가 남는다 (3곳: 선택 화면 확정 실패 /
  stay-adjust / 공통 옵션 확정). 내부 함수의 '[완료]' 로그도 '[경고] (호출부가 오류 처리)'로
  정리.
- **검증**: 테스트 30종 통과, 빌드 OK. 교차 리뷰 미가용으로 이번 건은 자체 검증 + 진리표
  (셀 편집 건과 함께 일괄 재검토 예정).

### 2026-07-25 — 신규 기능: 커스텀 리스트 셀 편집 (v1.1.2)

- **스펙** (사용자 요청): 리스트 항목의 셀을 클릭하면 그 자리 드롭다운으로 수정 — 삭제 후
  재추가 없이 값 변경. 던전(난이도/구역/은동전/소진 시/더블 불가 시)과 어비스(난이도/어비스
  던전)는 행 단위, **어비스 방식/매칭은 통일 규칙에 따라 리스트 전체 일괄 변경** 진입점.
  방식 셀은 '혼자하기 / 함께하기 · 매칭 3종'의 원자 옵션 4개(혼자→함께 전환 시 매칭 명시
  선택 보장), 함께→혼자 전환으로 지옥 난이도가 강등될 때는 사전 확인창(YesNo).
- **구현**: ListView 유지 + 오버레이 ComboBox 1개 (DataGridView 전환은 기존 기능 37곳 결합
  때문에 기각 - 설계 합의). 에디터는 드롭다운 수명 동안만 존재(닫히면 즉시 숨김 - 스크롤
  동기화 문제 원천 제거). SelectionChangeCommitted 가 세션을 클로저로 캡처해 적용 예약,
  적용부는 세션·행 존재 검증 후에만 실행. 표시 규칙은 Set-CustomListRowTexts /
  Set-AbyssListRowTexts 로 단일 소스화([추가]와 공용). 저장 실패 시 스냅샷 원복 + 은동전
  합계 재계산($script:lastCustomSaveOk 부채널). 실행 시작 시 커밋된 편집은 완료·미커밋은
  폐기. 던전 구역 편집은 이동(↑↓)과 동일하게 경고+시작 게이트(차단 없음). 잠금 툴팁·시작
  게이트 문구를 새 UX("방식/매칭 칸 클릭으로 전체 변경")로 갱신.
- **검증**: 신규 `test_custom_cell_edit.ps1`(계획/적용 정규화/옵션 매핑/일괄 변환 진리표
  33케이스 + 소스 계약 11검사), 전체 30종 통과, 빌드 OK. **교차 리뷰 2라운드에서 지적 8건
  전부 반영** (치명 1건 포함: 어비스 리스트 생성 전 이벤트 연결 = 시작 크래시 — 순서 검사
  테스트로 고정). 최종 재리뷰는 교차 리뷰 미가용으로 미완 - 반영 지점마다 소스 계약 테스트
  추가로 대체, 이후 재확인 예정. **GUI 실기 확인 필요** (셀 클릭 → 드롭다운 →
  값 변경·저장·진행도 리셋).

### 2026-07-25 — 신규 기능: 마지막 판 완료 시 '나가기'로 필드 복귀 (v1.1.2)

- **스펙** (사용자 확정): 커스텀 반복(N바퀴)의 마지막 바퀴 마지막 항목 / 횟수 지정의 마지막
  회차를 마치면, 결과 화면에서 '다시 하기'(옵션 화면 잔류) 대신 **'나가기'로 필드에 나가며**
  자동화를 마친다. 시간 지정/무한/안전 중지는 마지막 판을 사전에 알 수 없어 기존 유지.
- **구현**: GUI `Test-CustomLastRun`(순수 판정) + 비커스텀 횟수 조건 → `HONEYNOGI_LAST_RUN`
  env 신호(명시 초기화·정리 목록 포함). 워커 14-0 단계(은동전 잔량 검사보다 앞): '나가기'
  글자 탐색+예비 좌표(515,654) → `Get-DgLastRunExitStep` 순수 판정 루프 - 팝업('탐험'+'계속하'
  이중 신호)이면 Space(=나가기), 결과 화면 잔존이면 상태 기반 재클릭, **필드 증거(HUD+퀘스트
  추적기 '구역' 부재)가 연속 2회**일 때만 확정. 판독별 캡처 실패는 래치로 누적해 그 판독분 폐기.
  타임아웃 시 진단 캡처+경고 후 정상 종료(완료된 판의 오류 재시작 방지). 수동 정리 모드 제외.
  마지막 판 마무리 복구 회차는 필드 상태(연속 2회)를 복구 완료로 인정하고, 팝업에서 끊긴 경우
  Space를 이어서 처리(전환 중 'wait'는 관찰 계속).
- **검증**: 신규 `test_last_run_exit.ps1`(마지막 판 진리표 9 + 나가기 판정 진리표 7 + 소스 계약
  12), 전체 29종 통과, 빌드 OK. **교차 리뷰 3라운드 - 지적 5건(복구 필드 인정, 연속 확인,
  Space 이중 신호, 캡처 실패 누적, 팝업 후 관찰 유지) 전부 반영 후 최종 승인**. 실기 미검증
  (다음 커스텀/횟수 지정 마지막 판에서 자연 확인 - 실패해도 판 계상은 이미 확정 상태라 무해).

### 2026-07-24 — 던전 구역 좌표 4유형 재설계 + 오진단·매우 어려움·제목 유실 해결

- **구현 (A)**: 룬다 단일표(`$ndStagePoints`/`$ndMapAnchorY`)와 라벨 평균 오프셋 보정을 폐기하고
  상태·기하 중심으로 재설계 — 시작 시 제목 OCR 1회로 **던전 ID 확정**(`Get-DgDungeonIdFromTitle`,
  오독 이형 포함, 바리 광구는 숫자 필수, 다중 매칭은 불명 처리) → **10던전 매핑표 + 4유형(A/B/CR/CN)
  좌표 템플릿**(선택/옵션) + **포커스 2상태 focusDy(0/-29)** + **카드 픽셀 검증**(`Test-DgCardColor`
  B-R≥14·B≥50·G≥R-10 — 비포커스 딤 카드 포함 600표본/오탐 0 재보정) + 라벨 OCR은 교차확인으로
  강등(다중 배율 4→6→8). 포커스는 진입 버튼 층 2회 연속 일치로 판별. 미등록 던전은 라벨→배치
  후보(A→B→CR→CN, 오선택 후보는 제외 목록 순환)→안전 정지. 확정 좌표가 없으면 클릭하지 않는다.
- **구현 (B)**: '미해금 추정' 단정 문구를 원인별로 분리('미해금이거나 화면 인식 실패' /
  '화면 인식 문제 가능')하고, 코드 4 정지 전 `Write-DgStageDiagnostics`로 진단 세트
  (스크린샷 error_* + 지도 OCR 원문 + 진입 버튼 OCR + 던전 ID)를 남긴다.
- **구현 (C)**: GUI 일반 던전 라디오·커스텀 콤보에 '매우 어려움' 추가(저장/복원/열 폭 포함).
  워커는 `Select-DgDifficultyWord` 단어 판정 — '매우어려움'→'매우' 단어, '어려움'→'매우' 짝 제외
  + **앵커 규칙**('일반' 40~110px 오른쪽 또는 '매우' 존재 시에만 채택 — 뒷단어 오채택 차단).
  2단계 던전(룬다·피오드)에 '매우 어려움' 요청 시 시작 차단, 비커스텀도 탐색·확정 실패는 치명 처리.
  `$rgDgOptDifficulty` 폭 190→320(하드코딩 — coordsVersion 무관).
- **구현 (D)**: 제목 숫자 유실('피오듸층3구역') 시 `Resolve-DgObservedStage` 보조 판정 —
  지도 라벨 층(단일 층 + 표 2개 이상) + 제목 꼬리 구역. 목표 일치→성공, 같은 층 다른 구역→
  카드 클릭 승격, 다른 층→wrong-floor 안전 실패. 화면 상태별 1회만 시도.
- **검증**: 문법 0건 / 전체 테스트 28종 통과 — 신규 `test_dg_layout_system.ps1`(ID·매핑·템플릿·
  색·난이도·보조 판정 진리표, 데이터 표는 AST로 본체에서 직접 추출)과
  `test_dg_layout_offline.ps1`(**40장 캡처 전수 대조 212지점** — 템플릿 전부 카드 위, 반대 배치
  오탐 0) / 빌드 OK / **리뷰(gpt-5.6-sol, ultra) 코드 리뷰 3라운드 — 지적 8건 전부 반영 후 최종 승인**
  (매우 어려움 비커스텀 오입장, 어려움 뒷단어 오채택, 후보 순환 미작동, 보조 판정 남용,
  후보 소진 시 이전 판정 누수 등).
- **coordsVersion 6 유지**: config의 좌표 키 값은 바꾸지 않았다 (스크립트 하드코딩 표만 변경).
- **남은 실기 검증** (배포 전 체크리스트 — 1장 확인 필요에도 등재):
  ① 던전별 1판 실기(특히 마스 CN, 페론/키아 CR, 라비 A/A) ② '매우 어려움' 실선택 1판
  ③ 비포커스 층 구역 클릭(1층 포커스 상태에서 2-n 시작) ④ 미등록 던전(심층 던전 등) 안전 정지 확인
  ⑤ 다른 PC(1908×1076·RDP) 재보 - 픽셀 판별식 색 왜곡 내성
- 아래는 구현 전 원인 분석·확정 실측 기록 원문 (2026-07-22 제보 사고의 판독 근거 보존):

#### (기록) 원인 분석 원문 — 다른 PC 실사고 (2026-07-22 20:55 제보, run_20260722_h20m55s16.log)

#### (A) 구역 좌표가 던전 공용이라 다른 던전에서 오선택
- **사고**: 피오드 커스텀 반복 24항목 1번(어려움 1-1, 은동전 10)에서
  `구역 1-1 클릭 대신 같은 층의 1-3이 선택` → 옵션 화면 복구도 실패 → 정지.
  사용자 확인 결과 **미해금 아님**(해당 구역 해금 상태, 은동전도 보유).
- **원인 특정**: 로그 `스테이지 지도 스크롤 보정: 세로 -10px (라벨 5개 기준)` 이 찍혔다는 것은
  `Get-NdStageClickPoint`의 **2순위 경로**를 탔다는 뜻이다. 1순위(목표 라벨 정확 일치)가 성공하면
  즉시 반환하므로 이 줄이 나올 수 없다. 따라서 확인된 사실은 **`1-1` 정확 일치 실패**이며,
  원시 OCR 단어 전체가 보존되지 않아 미검출과 `1~1`/`1_1` 같은 오독은 아직 구분할 수 없다.
  폴백인 `$ndStagePoints`는 **던전 하나 기준 단일 표**라 배치가 다른 피오드에서 카드 밖에 떨어졌고,
  보정도 **세로만 있고 가로가 없다.**
  옵션 화면 복구에 쓰는 층별 예비 좌표(`Get-DgOptStageFallbackPoint`)도 같은 한계라 연쇄 실패.
- **필수 안전 규칙** (2026-07-24 40장 확정 실측·설계 합의로 우선순위 개정):
  1. **틀린 좌표로 클릭하지 않기** — 목표 카드 위치를 상태·기하·OCR 중 2개 이상으로 확인한
     경우에만 클릭한다. 아니면 안전 정지해 은동전 낭비·엉뚱한 판 진입을 막는다.
  2. **던전·층→유형 매핑표 + 4유형 좌표 템플릿을 1순위로** — 같은 유형 안 던전 간 편차 ~1px라
     유형 템플릿이면 던전별 좌표가 필요 없다. 프로파일 선택 키는 자동화가 이미 아는 상태 ID
     (진입 시점의 던전·층·포커스). 단 현재 config/커스텀 항목에 던전 이름이 없으므로
     `GUI에 던전 선택 추가` 또는 `시작 시 제목 OCR 1회 확정` 중 상태 ID 확보 방법이 선결 과제.
     **세로형 카드 순서(C역/C정)는 외곽으로 구분 불가라 매핑표가 필수**.
  3. **카드 외곽 픽셀 검출을 2순위 검증으로** — 보라 테두리 색 거리·카드 배경(34,39,50) 검출로
     가로상/가로하/세로 3종 판별 + 위치 확인 (난이도 알약 픽셀 검증과 같은 기법).
  4. **라벨 OCR은 교차확인 전용으로 강등** — 실전 엔진 판독률이 낮아(아래 (3)) 라벨 우선 설계는
     사실상 매판 폴백이 된다. OCR은 좌표를 결정하지 않고 "예상과 맞는가"만 확인. 미식별 라벨을
     단일 앵커 표(`$ndMapAnchorY`)로 평균하는 현행 방식은 제거.
  5. 매핑표에 없는 새 던전: 외곽 검출로 3종 분류하되 세로형이면 라벨 정확 일치 시에만 진행,
     아니면 안전 정지.

##### 확정 실측 (2026-07-24, 던전 10종 × 4화면 = 40장 — 아래 07-22 12장 실측을 대체)

> 캡처: 룬다·피오드·페카고분·페론고분·바리1광구·바리2광구·마스던전·라비던전·알비던전·키아던전
> × {선택_1층포커스, 선택_2층포커스, 옵션1층, 옵션2층}. **전부 1272×717 정규 창**(환산 불필요),
> `C:\Users\User\Desktop\새 폴더` 보관. 옵션 20장은 소카드(n-1) 선택 상태로 통일 촬영.
> 검증 3중: ① WinRT OCR(ko) Scale 3/4/6/8 실측 ② 독립 시각 판독 80건(이미지당 2명,
> 분류 불일치 0건) ③ 리뷰(gpt-5.6-sol, ultra) 독립 분석 + 의견 교환 — 3자 완전 일치.

**(1) 배치는 4유형** — 외곽 모양은 3종(가로 상/하, 세로)이지만 세로형은 위/아래 카드 순서가
던전마다 달라(룬다 2-2위 vs 마스 2-1위) 의미까지 포함하면 4유형이다.

| 유형 | 옵션 화면 라벨 중심(기준좌표) | 해당 던전·층 |
|---|---|---|
| **A. 가로·하단** | `n-1=(830,326)` `n-2=(918,326)` `n-3=(1022,298)` | 룬다1, 피오드2, 페카2, 페론2, 바리1광구1, 바리2광구2, 라비1, 라비2, 알비1, 키아2 (10) |
| **B. 가로·상단** | `n-1=(830,238)` `n-2=(918,238)` `n-3=(1022,295)` | 피오드1, 페카1, 바리1광구2, 바리2광구1, 마스1, 알비2 (6) |
| **C역. 세로(위=n-2)** | 위 `n-2=(874,238)` 아래 `n-1=(874,326)` `n-3=(979,295)` | 룬다2, 페론1, 키아1 (3) |
| **C정. 세로(위=n-1)** | 위 `n-1=(874,238)` 아래 `n-2=(874,326)` `n-3=(979,298)` | 마스2 (1) |

- 같은 유형 안에서 던전 간 편차 **최대 ~1px** → 유형 템플릿 4개면 던전별 보정 불필요.
- 선택↔옵션 지도 모양·카드 순서 **20/20쌍 일치**.
- 이번 옵션 20장(소카드 선택)과 07-22 옵션 8장(대카드 선택)의 좌표가 같은 유형에서 일치
  → **카드 선택 상태가 바뀌어도 카드 위치는 고정** 확정.
- A/B의 `n-3`는 y 3px 차이뿐 → n-3으로 A/B 구분 불가. **C역/C정은 외곽 전체로도 구분 불가**
  (대카드 y 3~4px 차이뿐) → 세로형 카드 의미는 매핑표가 담당.
- 07-22 기록 정정: 마스던전 1층은 A가 아니라 **B** (실시간 육안 메모 오류를 80건 판독이 정정).
- 난이도 2단계 던전은 룬다·피오드뿐, 나머지 8개는 3단계(매우 어려움). 심층 던전 탭은
  페카·바리1·바리2·마스·라비·알비·키아 7곳. 룬다만 어비스 배지. 키아 옵션1층에서 `동행` 토글
  실측(우연한 만남과 배타) — 토글·버튼은 지도 판별 앵커로 부적합.

**(2) 선택 화면 포커스 밀림 = 정확히 2상태 강체 이동.** 10던전 전부
`1층포커스 → 2층포커스 = 세로 -29px, 가로 0px` (패치 상관검증, 시각 판독 평균 +28.5±2
일치, WinRT OCR 대카드 좌표도 동일). 연속 스크롤이 아니므로 라벨 평균 오프셋 보정 대신
**`기준좌표[유형,층,카드] + focusDy(0 | -29)`** 모델이 성립한다. 포커스 판별 순서:
마지막 클릭 상태 → 패널 보라 하이라이트 픽셀 → 지도 절대 y 2후보 대조 → 헤더 OCR(보조).

**(3) 실전 엔진(WinRT OCR ko) 판독률 — 라벨 우선 설계를 지탱할 수 없는 수준**

- 옵션 지도(750,170,360,210): Scale 4/6/8 **세 배율 합집합**으로도 라벨 3개 전부 판독 = **7/20**.
  배율별 전부 성공 화면 수 S4=1, S6=4, S8=2 — 최적 배율이 화면마다 다르고 역전도 있음
  (키아 옵션2층: S4는 3개 전부, S8은 0개).
- 선택 지도(40,230,520,470) Scale 3(워커 실설정): 대부분 대카드(1-3/2-3)+`2-2`만 읽히고
  소카드 `1-1`/`1-2`/`2-1`은 거의 미검출. **라벨 0개 화면 3장**(페론 1층포커스,
  피오드 2층포커스, 바리2광구 2층포커스).
- 앵커 오염 실측: `$ndMapAnchorY`는 룬다·1층포커스 기준 단일 표. A형 2층 던전의 `2-2`는
  1층포커스에서 y=668이라 앵커(580)와 **+88px** 어긋남 — 07-22 사고의 보정 오염과 동일 구조.

**(4) 설계 결론 (교차 리뷰 2라운드 합의)** — OCR 중심 → **상태·기하 중심** 전환. 우선순위는 위
필수 안전 규칙 1~5로 확정. OCR 원문 단어·좌표는 실패 시 진단으로 저장.

##### (구) 실측 검증 (2026-07-22, 던전 4종 × 12장 — 위 07-24 확정 실측으로 대체, 기록 보존)

캡처: `룬다`, `피오드`, `페카 고분`, `페론 고분`. 원본은 현재
`C:\Users\User\Desktop\새 폴더`에 임시 보관. 창은 1256~1269×705~713이며 기준 1272×717로 환산.

- 옵션 화면 8장은 모두 `n-3` 선택 상태라 던전·층별 배치 비교 조건이 같다.
- 선택 화면 4장은 선택 구역이 서로 다르다(룬다 2-1 / 피오드 1-3 / 페카 2-3 / 페론 2-1).
  선택 카드가 펼쳐지면 다른 카드 위치가 움직이므로 **던전별 고정 좌표 산출 자료로 사용하지 않고**
  사고 화면 재구성 자료로만 사용한다. 선택 화면 좌표표가 필요하면 같은 구역·같은 스크롤 상태로
  다시 캡처해야 한다.
- **층 패널 선택에 따른 자동 밀림 실측(2026-07-24)**: 배치 조합이 같은 피오드(1층 선택)와
  페카(2층 선택)를 비교하면 모든 라벨이 일제히 **약 28~29px** 이동한다
  (`2층` 헤더 507→479, `1-3` 397→368, `2-3` 646→618). 밀림이 균일하므로 기존
  "라벨 평균 오프셋 스크롤 보정" 방식 자체는 유효하다. 단 페론(2층 선택)의 `2층` 헤더는
  y=471로 페카보다 8px 더 위 — 1층이 세로형(C)이라 **패널 높이가 배치 유형에 따라 달라**
  아래 패널의 기준 y도 달라진다. 즉 앵커 표는 선택 상태와 무관한 단일 값일 수 없고,
  **배치별 앵커 + 이상치 제거**가 함께 필요하다(사고의 `2-2` +93px 오염 참고).

**1) 옵션 화면 구역 지도는 기본 형태 2종(가로/세로), 실제 폴백 좌표군 3종 확인**

> **2026-07-24 3자 교차 검증으로 배치 할당 정정.** ① WinRT OCR 다중 배율 실측
> ② 독립 시각 판독 24회(이미지당 2명 × 12장, 분류 불일치 0건) ③ 리뷰(gpt-5.6-sol, ultra)
> 독립 분석 — 세 경로 모두 아래 표로 일치. 최초 기록(07-22)은 피오드·페카·페론 **2층**을
> '위 정렬'로 잘못 분류했었다(실제는 전부 '아래 정렬'). 좌표는 **라벨 글자 중심**의
> 기준좌표 환산 실측값(카드 안이므로 클릭 유효점) — 원본픽셀 환산치와 ±2px 내 일치.

| 레이아웃 | 라벨 중심 실측(기준) | 해당 화면 |
|---|---|---|
| **A. 가로형(아래 정렬)** | `n-1=(835,331)` `n-2=(922,330)` `n-3=(1028,303)` | **룬다 1층**(현재 코드 1층 값), **피오드·페카·페론 2층** |
| **B. 가로형(위 정렬)** | `n-1=(833,241)` `n-2=(921,241)` `n-3=(1027,298)` | 피오드 1층, 페카 1층 |
| **C. 세로형** | 위 `n-2=(879,242)` 아래 `n-1=(877,328)` `n-3=(983,300)` | **룬다 2층**(현재 코드 2층 값), 페론 1층 |

- **선택 화면 층 패널과 옵션 화면 지도는 같은 던전·같은 층이면 8쌍 전부 모양이 일치**한다
  (3자 공통 확인). "화면이 2종류"가 아니라 "지도 배치가 3종이고 두 화면이 배치를 공유"가 정확하다.
- 현재 `Get-DgOptStageFallbackPoint`의 1층 값 `1-1=(830,308) 1-2=(918,310) 1-3=(1022,266)`은
  **A형(룬다 1층) 전용**이다. B형인 피오드·페카 1층은 소카드 라벨이 y≈241이라 y 308~310 클릭이
  카드 아래 빈 공간에 떨어져 **아무 것도 선택되지 않는다**(= 이전 선택 유지). C형인 페론 1층은
  `1-2` 폴백 `(918,310)`이 아래 카드(`1-1`) 우측에 걸릴 수 있어 **다른 구역 오선택 위험**까지 있다.
- 2층 값 `2-1=(875,307) 2-2=(875,225) 2-3=(980,266)`은 **C형(룬다 2층) 전용**. A형인 피오드·
  페카·페론 2층에서 `2-2` 클릭 `(875,225)`는 빈 공간이고, `2-1` 클릭 `(875,307)`은 카드
  가장자리에 걸리는 불안정 히트다.
- **배치 판별 주의(리뷰 교차 의견)**: C형은 `n-3` x≈983으로 구분되지만, **A/B의 `n-3`는
  (1028,303) vs (1027,298)로 사실상 같은 자리**라 `n-3`만으로는 A/B를 구분할 수 없다.
  A/B 구분은 소카드 라벨 y(331 vs 241) 또는 던전·층→배치 매핑표를 써야 한다.
  현재 8장 관측 범위의 규칙이며 다른 던전까지의 불변 규칙으로 단정하지 않는다.
- 배치는 던전별로도, **같은 던전의 층별로도** 다르다
  (룬다 1층=A·2층=C / 피오드·페카 1층=B·2층=A / 페론 1층=C·2층=A).

**2) 라벨 정확 일치 실패가 재현된다 — 작은 `n-1`/`n-2`를 자주 놓친다**

옵션 화면 지도(`$rgDgOptStageMap` 750,170,360,210, Scale 4) 판독 결과:

| 화면 | 읽힌 라벨 |
|---|---|
| 룬다 옵션1층 | `1-1`(834,331) `1-2`(923,331) `1-3`(1028,303) — 전부 성공 |
| 피오드 옵션1층 | **`1-3`만** (1029,298) |
| 페카 옵션1층 | **`1-3`만** (1028,293) |
| 페론 옵션1층 | **없음** (`7` 오독 1건) |
| 룬다 옵션2층 | **없음** |
| 피오드 옵션2층 | `2-3` `2-2` (2-1 누락) |
| 페카 옵션2층 | `2-3` `2-1` (2-2 누락) |
| 페론 옵션2층 | `2-1` `2-2` `2-3` — 전부 성공 |

→ 8화면 중 라벨 전부 판독은 2화면뿐. 대카드 `n-3`은 글자가 커서 잘 읽히고, 소카드는 자주 놓친다.
크롭 이미지를 눈으로 보면 사람은 다 읽을 수 있으므로 **작은 글자 검출 자체**가 주된 문제다.
조각 매칭만으로는 부족하며 배율 상향·영역 분할/축소 등을 함께 비교해야 한다.

**배율별 재실험(2026-07-24, 같은 영역)** — 배율은 단조 개선이 아니라 화면마다 최적점이 다르다:

| 화면 | Scale 4 | Scale 5 | Scale 6 | Scale 8 |
|---|---|---|---|---|
| 피오드 옵션1층 | `1-3`만 | `1-3`만 | 없음 | 없음 |
| 페카 옵션1층 | `1-3`만 | — | **전부 성공** | — |
| 페론 옵션1층 | 없음 | `1-2` `1-3` | `1§2` 오독 | 없음 |
| 룬다 옵션2층 | 없음 | 없음 | 없음 | **전부 성공** |

→ **단일 배율로는 어떤 값을 골라도 실패 화면이 남는다.** 개선 시 다중 배율 재시도(예: 4→6→8)
또는 카드 영역별 분할 판독이 필요하다. 피오드 옵션1층 소카드는 4~8 어느 배율에서도 안 읽혔다.

> 위 표는 해당 영역·배율로 실행한 오프라인 OCR 결과다. PNG만으로 같은 결과를 재현할 수 없으므로
> 개발 전에 OCR 원문 단어·좌표를 저장하거나 동일 재현 명령을 문서에 남긴다.

**3) 사고 재구성 (피오드 1-1)**

- 선택 화면: `1-1` 정확 일치 실패 → 2순위. 기준 라벨로 채택된 5개 =
  `1층`·`1-3`·`2층`·`2-3`·`2-2`.
  이 중 `2-2`는 **피오드 2층이 가로형**이라 세로형 기준의 `$ndMapAnchorY['2-2']=580`과
  실제 위치(기준 y≈673)가 **+93px** 어긋난다. 나머지 4개는 서로 일치.
  → 평균 보정이 오염돼 `-10px`이 나왔다(정상 라벨만 쓰면 ≈`-36px`, **26px 오차**).
  **즉 2순위 보정 자체가 배치가 다른 층의 라벨을 섞어 쓰면서 틀린다.**
- 그 결과 `(196, 396)` 부근을 클릭 → 카드 밖 → 선택 변화 없음 → 기존 선택 `1-3` 유지.
  로그의 `1-1 클릭 대신 1-3이 선택`은 "오클릭"이 아니라 **"클릭이 아무 효과가 없었다"**가 정확하다.
- 옵션 화면 복구도 위 1)의 이유로 실패 → `(B)` 미해금 오진단으로 종료.
- **완료 기준**: 4개 던전의 선택/옵션 화면에서 6개 구역 목표가 `정확한 카드 안 클릭` 또는
  `입력 없는 안전 정지` 중 하나로만 끝나야 한다. 미확정 좌표 클릭·다른 구역 진입은 0건이어야 하며,
  실패 시 원시 OCR과 진단 스크린샷을 남긴다.

#### (B) '미해금 추정' 오진단
- 구역 선택·전환 실패 최종 분기가 원인과 무관하게 **미해금으로 단정**한다
  (`Get-NdStageClickPoint`/`Set-DgOptionStage` 이후의 종료 메시지).
  이번처럼 좌표 문제일 때도 "미해금"이라 안내해 사용자가 원인을 찾지 못했다.
- **개선 방향**: ①지도에서 목표 라벨이 **읽혔는데** 전환 실패 → "구역 전환 실패(화면 인식 문제
  가능)" ②라벨이 **아예 안 읽힘** → "구역을 찾지 못함(미해금이거나 인식 실패)"로 문구 분리.
  어느 쪽이든 **진단 스크린샷 저장**(현재는 정지만 하고 캡처가 없어 제보 로그만으로 배치 확인 불가).
- **완료 기준**: 미해금은 화면에서 직접 확인할 수 있는 근거가 있을 때만 단정한다. 그 외 실패는
  `라벨 정확 일치 실패 / 클릭 후 목표 미전환 / 제목·진입 버튼 불일치`를 구분해 로그와 캡처를 남긴다.

#### (C) 피오드 제목에서 던전명 끝 글자와 `1층` 숫자가 합쳐지는 OCR 오류
- `'〈피오듸층3구역'` — `피오드`+`1층`이 뭉개져 **층 숫자를 잃었다**. 같은 층/다른 층 판정이 이
  제목에 의존하므로 판독 보강 필요(영역·배율 조정 또는 층 숫자 별도 판독).
- **운영 조건**: 현재 `$rgDgTitle=(30,45,250,55)`, Scale 3. 제보 로그에서 위 문자열이 기록됐다.
- **오프라인 확대 재현(2026-07-22)**: 사용자 캡처의 넓은 제목 영역(기준 20,45,520,45 / Scale 4)
  - 룬다 → `'로다1층3구역'` (층 숫자 유지)
  - **피오드 → `'피오듸층3구역'`** ← 제보 로그와 동일 문자열 재현
  - 페카 고분 → `'페카고분1층3구역'`, 페론 고분 → `'페로고분1층3구역'` (유지)
  - 즉 **`드`+`1`이 `듸`로 합쳐지는 던전 이름 의존 사고**다. 던전 이름 끝 글자에 따라 재발한다.
  - 제목의 `N층 M구역` 시작 x는 던전 이름 길이에 따라 달라져 좁은 고정 꼬리 영역만으로는 부족할
    수 있다. 넓은 제목 줄에서 층·구역 단어 위치를 찾거나, 진입 버튼(`$rgDgEnterBtn`의
    `N층 M구역 진입`, 글자가 커서 신뢰도 높음)을 교차 검증에 쓴다.
- **완료 기준**: 4개 던전 × 1·2층 옵션 화면에서 층·구역을 확정하고, 제목이 불명확해도 진입 버튼과
  교차 검증해 같은 층/다른 층 판정을 잘못하지 않아야 한다. 두 신호가 충돌하면 클릭 없이 정지한다.

#### (기록) 스크린샷 대조 중 추가 발견 — 매우 어려움 (위 구현 (C)로 해결)

##### 던전 난이도 '매우 어려움' 미지원 (분석 원문)
- 일반 던전 상세 설정(`$rbNdNormal`/`$rbNdHard`)과 던전 커스텀 목록(`$cboCrDifficulty`)이 모두
  `일반`/`어려움` 2개뿐이다. 사냥터·어비스에는 이미 `매우 어려움`이 있다. 실제 게임에는 던전에도
  매우 어려움이 있고 **던전마다 제공 난이도가 다르다**.
- 현재 공통 `Find-GameTextPoint`는 정확 일치가 없으면 부분 일치로 내려간다. 3단계 던전에서
  `어려움`을 목표로 할 때 `매우 어려움`의 뒤쪽 단어를 후보로 잡을 수 있으므로 별도 검증이 필요하다.
- 커스텀 반복은 난이도 확정 실패 시 정지하는 엄격 경로지만, 비커스텀 던전은 현재 경고 후 진행할 수
  있어 `없는 난이도는 기존 안전장치가 모두 정지시킨다`고 볼 수 없다. 난이도 지원표를 둘지,
  비커스텀도 엄격 정지로 맞출지 개발 전에 결정한다.
- **개선 범위**: 일반 던전 설정·커스텀 항목 추가·설정 저장/복원·워커 선택/검증·표시 로그를 함께
  `매우 어려움`까지 확장한다.
- **실측(2026-07-22)**: 옵션 화면 난이도 줄(기준 560,95,420,50 / Scale 4)
  - 룬다·피오드(2단계): `일반(655,122)` `어려움(726,122)`
  - 페카 고분·페론 고분(3단계): `일반(653,122)` `어려움(724,122)` `매우(796,122)` `어려움(841,122)`
  - **`일반`·`어려움` 알약 위치는 현재 4개 던전에서 동일하게 관측**됐다.
  - `매우 어려움`은 OCR에서 `매우` + `어려움` 두 단어로 나뉜다. 선택 화면 층 패널도
    `2층 매우 어려움`으로 표시된다(페카·페론 실측).
- **완료 기준**: 일반/커스텀 양쪽에서 저장·복원이 되고, 3단계 던전에서 `어려움`과 `매우 어려움`을
  각각 정확히 선택해야 한다. 해당 난이도가 없는 던전은 현재 난이도로 진행하지 않고 명확한 안내 후
  안전 정지한다.


### 2026-07-22 — 전체 재검토 후 설정·종료 자원·문서 정합성 보강

- **설정값 형식·범위 검증 확대**: 워커의 이동/매칭 제한 시간, 부활 키·횟수, 포커스 주기,
  창 좌표·크기, 입장 후 키·지연, 진단 세트 보관 개수를 공통 정수 검증으로 통일. Int32 범위를
  넘는 값도 변환 전에 차단해 기본값으로 복구한다. 던전·사냥터 옵션, 자동부활, 창 정규화 등
  불리언은 JSON `true/false`만 인정해 문자열 `"false"`가 PowerShell에서 참으로 처리되는 문제를 차단.
  잘못된 입장 후 키 코드는 누르지 않고 경고만 기록한다. GUI 설정 복원·RDP 예약 작업 판단과
  자동 이전도 같은 엄격 불리언 규칙을 적용했다.
- **설정 이전 실패 진단**: `Update-ConfigToLatest`가 '이전 불필요'와 '이전 실패'를 구분하도록 실패
  원인을 보존하고, GUI 시작 로그에 경고를 남긴 뒤 기존 설정으로 계속하도록 보강했다.
- **GUI 종료 자원 정리**: 창 종료 시 GUI/로그/업데이트 타이머, GitHub 업데이트 PowerShell 작업,
  WinForms 폼과 전역 GUI 뮤텍스를 명시적으로 중단·해제·Dispose한다. 중복 실행 안내로 빠르게
  종료하는 경로도 생성한 뮤텍스를 Dispose한다.
- **문서 정합성 보강**: README에 커스텀 반복 동작·제약을 추가하고 진단 세트 기본 보관 수를
  실제 값 10개로 수정. CLAUDE 테스트 수와
  이 문서의 최근 검증 기록을 현재 값으로 동기화했다.
- **검증**: 전체 PS1 32개 PowerShell 5.1 파서 오류 0건, UTF-8 BOM 누락 0건,
  config JSON 파싱 정상, 회귀 테스트 **26/26 통과**, `git diff --check` 오류 없음,
  빌드 성공(`HoneyNogi.exe` v1.1.1, 1124.5KB).

### 2026-07-22 — 어비스 커스텀 반복: 리스트 방식·매칭 통일 강제

- **원인/배경**: 어비스 커스텀 리스트는 항목마다 방식(혼자하기/함께하기)과 매칭(우연한 만남/
  파티 찾기/파티(파티장))을 다르게 담을 수 있어, 항목이 섞이면 파티 상태가 항목 사이에서 꼬인다.
- **사용자 확정 규칙**: 리스트에 항목이 하나라도 있으면 **방식과 매칭 모두** 그 리스트(첫 항목)의
  값으로 고정. 리스트를 비우면 다시 자유롭게 고를 수 있다.
- **수정 (`mabinogi_gui.ps1`)**
  - `Get-AbyssListLock` 신설: 리스트가 비면 `$null`, 아니면 `@{ Mode; Matching }` **단일 해시테이블**
    (PS 5.1 배열 풀림 무관). config 직접 편집 표기 흔들림('파티찾기', '함께하기')을 정규화.
  - `Get-AbyssMatchingIssues` 신설: 첫 항목과 방식/매칭이 다른 항목 목록 `[{Index; Mode; Matching;
    Reason}]` 반환 (`return $issues` + 호출부 `@()` 규약).
  - `Update-AbyssInputLock` 신설: 잠금 시 라디오 5종(`$rbAcrSolo/$rbAcrParty/$rbAcrChance/
    $rbAcrFindParty/$rbAcrPartyLead`) `Enabled=$false` + 리스트 값으로 `Checked` 보정
    (`$script:crLoading` 가드로 저장 이벤트 억제, `$script:acrLockUpdating` 로 재진입 차단).
  - **잠긴 이유 안내(툴팁) — 실기 2회 조정**: WinForms 는 비활성 컨트롤에 마우스 이벤트를 주지
    않아 `SetToolTip` 이 통하지 않는다. ①처음엔 라디오+패널에 겹쳐 걸었더니 커서가 둘 사이를
    오갈 때 **깜박였고**, ②패널에만 걸었더니 잠긴 라디오 위에서는 **아예 안 떴다**.
    → 최종: 패널 `MouseMove` 에서 커서가 어느 비활성 라디오 `Bounds` 안인지 직접 판정해
    `$toolTip.Show(...)` 로 띄우고, **같은 컨트롤에서는 재호출하지 않아** 깜박임이 없다
    (`$script:acrTipShownFor` 로 추적, `MouseLeave`·잠금 해제 시 `Hide`). 자동부활 체크박스와
    같은 사용감 확보. 항상 보이는 안내 라벨도 시도했으나 툴팁으로 충분해 사용자 요청으로 제거.
  - 갱신 시점: 항목 추가/삭제/↑↓ 이동/`Load-SettingsToUi` 복원/`$updateCategoryPanels` 끝.
  - 시작 게이트: 던전 전환 규칙 게이트와 같은 자리에서 어비스 섹션일 때
    `Get-AbyssMatchingIssues` 검사 → 위반이면 시작 거부 + 위반 항목 번호·사유를 GUI 로그로 안내
    (무인 운용 보호를 위해 팝업 없음).
- **차단 방식이 팝업이 아니라 비활성인 이유**: 애초에 섞인 항목을 만들 수 없게 해서 사용자가
  실수로 리스트를 만든 뒤 시작에서 막히는 흐름을 없앴다. 게이트는 config 직접 편집 대비 방어.
- **검증**: `tests\test_abyss_matching_uniform.ps1` 신설(진리표 29 + GUI 배선 13 = **42 케이스** 통과) /
  두 본체 스크립트 PS 5.1 파서 오류 0건 / `tests\run_all_tests.ps1` **26개 전부 통과** / 빌드 OK (v1.1.1).
  워커 `ConvertFrom-AbyssCustomItemSpec` 스펙(`A|mode|난이도|던전|매칭`)과 어긋나지 않음을 대조 확인
  (워커 수정 없음).

### 2026-07-22 — OCR 공통 캡처 리팩터링 좌표 대조 검증

- 2026-07-21에 `Get-GameRegionOcrText`/`Find-GameTextPoint`/`Get-GameRegionOcrWords` 의 캡처·확대·
  실패기록·해제를 `Get-GameRegionCapture` 하나로 통합한 뒤, **실제 화면에서 좌표가 어긋나지
  않는지 직접 대조**(던전 진입 옵션 화면 '룬다 1층 3구역', 창 1272x717):

  | 검사 | 결과 | 기대(같은 날 픽셀 실측) | 오차 |
  |---|---|---|---|
  | `Find-GameTextPoint('어려움')` | 기준(723,120) | (723,120) | **0px** |
  | `Find-GameTextPoint('일반')` | 기준(653,120) | (652,120) | **1px** |
  | `Get-GameRegionOcrWords` 난이도 영역 | 일반=(653,120), 어려움=(723,120) | 클릭 좌표와 동일 | — |
  | `Get-GameRegionOcrText` 제목 | `〈 로다1층3구역` | 층·구역 숫자 정확 | — |
  | `Get-GameRegionCapture` 환산 | 기준(600,95,190,50) → 화면 L=1228 T=402 190x50 → 확대 570x150(x3) | 창 rect와 일치 | — |

- **핵심 확인**: 세 함수가 공통 캡처를 공유해도 ①단어 좌표와 클릭 좌표가 픽셀 단위로 일치하고
  ②독립적으로 측정한 알약 중심과 0~1px로 맞으며 ③기준좌표↔화면좌표 환산이 정확하다
- 검증 방법: 운영 소스에서 대상·보조 함수 15종과 `HoneyNogiInput`·OCR 엔진 생성부를 원문 그대로
  가져온 독립 하네스로 라이브 화면 판독(게임에는 캡처만, 클릭·키 입력 없음). 스텁은 `Write-RunLog`
  (로그 폴더 오염 방지)와 캡처 실패 상태 전역 초기화 2건뿐이라 판정에 영향 없음

### 2026-07-22 — 장시간 운용 리소스 누수 검증

- **조건**: 어비스 무한 반복 **136회차 / GUI 가동 9시간(537분)** 시점에서 측정 후,
  32분간 2분 간격 16회 추이 기록(어비스 판당 4~5분 → 그 사이 6~8회차 추가 진행).
- **결과 — 누수 없음**:

  | 지표 | 시작 | 32분 후 | 추이 |
  |---|---|---|---|
  | GUI 핸들 | 802 | **789** | 감소 후 완전 고정 (증가 0) |
  | GUI 개인 메모리 | 139.1MB | 132.7MB | 117~139MB 진동, 우상향 없음 |
  | GUI 스레드 | 15 | 13 | 13~15 진동(정상) |
  | 워커 프로세스 수 | 1 | 1 | **항상 1개 — 좀비 누적 없음** |

- **판정 근거**: 136회차·9시간을 돈 시점에도 핸들이 800개(WinForms 앱 기본 수준)이고,
  이후 32분간 **한 번도 증가하지 않고 오히려 13개 감소**했다. 회차마다 누수가 있었다면
  수천 개로 불어났을 값이다. 2026-07-21의 Process 객체 `Dispose()` 정리가 정상 동작함을 확인.
- **범위**: 무한 반복으로 측정했으므로 두 모드 공통 경로(워커 생성·종료·Dispose, 로그 증분
  읽기 핸들, GUI 로그창·상태줄 갱신)가 검증됨. 커스텀 고유 경로(진행도 저장·마커 파일)는
  회차당 파일 I/O 1~2회 추가라 위험이 낮아 실사용 관찰로 대체.

### 2026-07-22 — 어비스 방식·매칭별 실주행 (사용자 검증)

- 통일 규칙상 한 리스트에는 한 가지 방식·매칭만 들어가므로, **각 값으로 통일된 리스트를 따로
  만들어 1바퀴씩** 돌려 확인했다.
  - `함께하기 + 우연한 만남` ✅ (01:26~01:53, 상세는 아래 어비스 커스텀 검증 항목)
  - **`혼자하기`로만 된 리스트** ✅ 사용자 실기 검증 완료 (매칭 단계 없이 바로 입장하는 경로)
  - **`파티(파티장)`로만 된 리스트** ✅ 사용자 실기 검증 완료
- 남은 것: **`파티 찾기`로만 된 리스트** 1바퀴

### 2026-07-22 — 어비스 강제 종료 복구(완료 마커) 실기 검증

- **클리어 직후 강제 종료 → 재시작 시 재입장 없이 마무리** ✅ (01:49~01:53, 어비스 커스텀
  2번 항목 '어려움 광기의 동굴'):
  - 클리어 → `[커스텀] 완료 마커 기록 (결과 화면 도달)`(01:49:35) 직후 **즉시 중지(F10)**로
    워커 강제 종료 (선택 화면 복귀 도중, 로그는 `어비스 메뉴 클릭`에서 끊김)
  - 이때 상태: 마커 `abyss_custom_done.marker` 존재 + 진행도 `lap=1 index=1`(미전진)
    마커 내용에 **소유자 컨텍스트**가 모두 포함 —
    `{version:1, fingerprint:"(리스트 전체)", lap:1, index:1, item:"A|party|어려움|광기의 동굴|우연한 만남"}`
  - **재시작 결과**: `[커스텀] 어비스 선택 화면이 이미 준비돼 있습니다 - 완료 항목 재입장 없이
    복구 완료` → **입장하기 클릭 없음**(재입장 안 함) → 진행도 `index=1 → 2` **한 칸만 전진** →
    마커 소비 후 삭제 → 3번 항목 정상 시작
- **마커 없는 강제 종료는 미계상** ✅ (같은 구간, 01:42~01:45): 클리어 화면 터치 직후
  (마커 기록 전) 강제 종료 → 재시작 시 보상 화면을 마무리하지만 **진행도 전진 없음**(`index=1` 유지)
  → 해당 항목을 처음부터 다시 실행. "확인되지 않은 판은 완료로 세지 않는다"는 보수적 원칙대로 동작
- 실무 메모: 마커 유효 구간은 `결과 화면 도달 ~ 선택 화면 복귀 완료`로 **약 12초**.
  사람이 로그 알림을 보고 반응하기엔 촉박하므로, 재검증 시에는 **나가기가 자동 클릭되어 화면이
  전환되기 시작할 때** 누르는 것이 가장 확실하다

### 2026-07-22 — 난이도 선택 강조색 픽셀 실측

- **던전 진입 옵션 화면(룬다 2층 3구역)에서 `일반`/`어려움` 각 상태를 실측**해
  `Test-DifficultySelectedAt`의 판정(표본 18점 중 `max>150 && 채도>60`인 픽셀 3개 이상)이
  실제 UI에서 맞는지 확인. 결과 4상태 모두 정확:

  | 알약 | 상태 | 테두리 RGB | max / 채도 | hits |
  |---|---|---|---|---|
  | 일반 | 선택 | (129, 99, 255) 보라 | 255 / 156 | 6/18 |
  | 일반 | 비선택 | (34, 39, 50) | 50 / 16 | 0/18 |
  | 어려움 | 선택 | (239, 174, 66) 금색 | 239 / 173 | 6/18 |
  | 어려움 | 비선택 | (34, 39, 50) | 50 / 16 | 0/18 |

- **어려움 금색이 2026-07-17 어비스 실측값(239,174,66)과 정확히 일치** — 콘텐츠가 달라도
  난이도 알약은 같은 UI 자산을 쓰므로 판정식을 어비스/던전 공용으로 유지하는 것이 맞다
- 판정 여유가 큼: 선택은 채도 156~173(기준 60의 2.6~2.9배), 비선택은 16으로 중간값이 없어
  경계 오판 여지 없음. hits도 6 또는 0으로만 갈림(임계값 3)
- 표본 위치 참고: 알약 글자 중심 기준좌표 `일반=(652,120)`, `어려움=(723,120)`,
  강조 테두리는 dy=±16 줄에서 검출됨(dy=±14는 어두운 안쪽, ±18은 화면 배경)

### 2026-07-22 — 커스텀 반복 이어가기·오류 자동 재시도 실기 검증

- **이어가기(중단 후 재개)** ✅ (01:09~01:12, 던전 커스텀 3항목 리스트):
  1번 항목 완료 → `progress lap=1 index=1` 저장 확인 → **즉시 중지(F10)** → 재시작 시
  `현재 1바퀴째 2/3번`으로 **중단 지점부터 재개**. 즉시 중지가 진행도를 건드리지 않는다는
  스펙(무변경)도 함께 확인
- **수동 판 오계상 방지** ✅ (같은 구간): 던전 안에서 중지한 뒤 **사용자가 시작 버튼을 누른**
  경우(`재시작 False`) → `새 시작인데 던전 안/결과 화면 감지 - 이 판은 계상하지 않고 화면
  정리만 진행합니다` → `수동 진행분 화면 정리 완료 - 판으로 계상하지 않습니다 (준비 실행)`.
  진행도는 그대로 유지되고 해당 항목이 처음부터 다시 실행됨
- **오류 자동 재시도** ✅ (01:23~01:25, 클리어 대기를 60초로 낮춰 인위적 타임아웃 유발):
  ```
  01:24:15 [오류] 던전 클리어 화면 감지 대기 시간이 초과됐습니다.   ← 코드 1
  01:24:16 [설정] 현재 1바퀴째 2/3번 ... 재시작 True              ← 2초 만에 자동 재시작
  01:24:40 [커스텀] 완료 마커 기록 (결과 화면 도달)                ← 던전 안을 이어받아 클리어
  01:24:43 [던전] 다시 하기 → 옵션 화면 복귀 - 회차 완료           ← 정상 계상 후 3번으로 전진
  ```
  **`재시작 True`와 `새 시작`이 정확히 갈렸다**: 재시작이면 던전 안 상태를 '내가 돌던 판'으로
  보고 정상 완료 계상, 새 시작이면 '수동 판'으로 보고 미계상 정리. 오류 1회가 진행도 손실 없이
  자동 복구됨(같은 항목 유지 → 완료 → 다음 항목)
- 참고: 검증 후 `클리어 대기`를 기본값 600초로 되돌릴 것

### 2026-07-22 — 일반 던전 구역 오선택 복구 실기 검증

- **두 복구 경로 모두 실측 확인** (단일 던전 모드, 설정 `어려움 1-1`, 다른 구역의 진입 옵션
  화면을 열어 둔 채 시작해 오선택 상태를 재현):
  - **다른 층**(2층 1구역 화면, 01:00): `다른 층의 구역입니다 (제목: '〈로다2층1구역', 설정: 1-1)`
    → `좌상단 < 클릭 (1/4)` **1회 성공** → `선택 화면 복귀 확인` → 난이도 '어려움' →
    `구역 1-1 선택 확인 (진입 버튼: 1층 1구역 진입)`. 감지~목표 확정 **7초**
  - **같은 층**(1층 3구역 화면, 01:05): `같은 층의 다른 구역입니다 ... 이 화면에서 목표 구역으로
    변경합니다` → `구역 1-1 카드 클릭 (1/3)` **1회 적중** → `옵션 화면에서 구역 1-1 전환 확인` →
    난이도 확정 → 입장. **선택 화면을 경유하지 않음**(불필요한 '<' 복귀 없음), 감지~전환 **4초**
  - 두 경로 모두 목표 `N층 M구역`이 확정되기 전에는 입장하지 않음을 로그로 확인
- 부수 확인: 사용자가 **직접 연 옵션 화면에는 좌상단 '<'가 존재**한다는 전제가 실증됨
  (다시 하기로 온 화면에는 없어 커스텀 반복은 '다시 하기/다음 층으로' 경로를 쓰는 것이 맞음)

### 2026-07-22 — 어비스 커스텀 반복 실기 검증

- **어비스 커스텀 반복 1바퀴 완주 확인** (00:26~00:40, 함께하기·우연한 만남 3항목:
  어려움 허상의 정박지 → 어려움 광기의 동굴 → 입문 허상의 정박지, 횟수 1바퀴):
  - **항목별 어비스 던전 전환** 3회 모두 카드 클릭 1회에 성공(허상→광기→허상),
    **항목별 난이도 전환**(어려움→입문)도 정상. 매칭은 3회 모두 18초 내 자동 입장
  - **완료 마커** 3회 기록(어비스 경로에서도 공용 계약 동작 확인),
    **진행도 분리** 확인 — 어비스 3판을 도는 동안 던전 `customRepeat.progress`는 `null` 유지
  - **완주 판정·자동 초기화** 정상: 3번 항목 완료 → lap 2 > 1 → `progress: null`
  - **시작 화면 어긋남 자동 복구** 2회 정상: 필드 상태 감지 → ESC → 어비스 메뉴 →
    선택 화면 복귀 후 `[완료] 준비 실행 - 회차로 세지 않음`(코드 10)으로 미계상 처리
  - 로그는 회차당 3,142~3,583바이트로 전 구간 보존(07-22 로그 수정 이후 어비스에서도 확인)
- **확인 경고 반복 억제** (위 실주행에서 관측된 노이즈 정리): `Focus-Game`의 전면화 확인 실패와
  `Click-ScreenPoint`의 커서 위치 확인 실패 경고가 **클릭마다** 기록돼(사용자가 PC를 쓰는 동안
  다른 창이 전면이면 계속 발생) 로그를 도배했다. 순수 판정 함수 `Get-RepeatWarnAction`
  (WasWarned x Failed → warn/recover/none)을 신설해 **연속 실패 구간에서는 첫 1회만 경고**하고,
  회복되면 **그 사이 생략한 횟수와 함께 1회 안내**하도록 변경(진단 정보 보존). 두 확인은 각각
  별도 상태 변수를 쓴다. 실패해도 클릭을 계속하는 기존 동작은 그대로라 회귀 없음.
  진리표 16케이스 테스트 신설(test_repeat_warn.ps1 — 4조합 + 시퀀스 6종 + 호출부 계약 6종),
  전체 25종 통과, 빌드 OK
- **관측된 후속 과제 1건**
  - 첫 실행(00:21:29)에서 워커가 `허상의 정박지 카드 클릭` 직후 34초간 로그 없이 종료된 사례 1건.
    오류 세트·복구 로그 없음, 다음 워커가 필드 상태를 감지해 정상 복구. 재현되지 않아 보류하되
    재발 시 GUI 로그의 종료 코드로 원인 특정할 것

### 2026-07-22 — 로그 기록 안정화

- **실행 로그 중간 유실 방지 보강** (23:24~23:36 커스텀 6개 항목 실측): 로그 확인용으로
  남아 있던 `tail -F` 프로세스 2개가 `mabinogi_run_once.log`를 쓰기 불가 공유 모드로 붙잡았고,
  기존 `Write-RunLog`는 `Add-Content`를 50ms 간격으로 20번 재시도한 뒤 실패를 알리지 않고 해당
  줄을 버려 자동화는 다음 항목으로 진행하지만 파일은 설정 또는 중간 단계에서 끝났다. 워커가 회차
  시작부터 종료까지 `FileShare.ReadWrite|Delete`를 허용한 단일 `StreamWriter`를 유지하도록 변경해
  후발 읽기 도구가 쓰기를 차단하는 방식으로 파일을 열지 못하게 했다. 시작 시 기본 로그가 이미
  잠겼거나 열린 스트림 쓰기가 실패하면 `mabinogi_run_once.recovery.log`로 전환하고 경고와 이후
  로그를 전부 보존한다. GUI도 복구 로그를 별도 오프셋으로 실시간 표시하고 다음 회차에
  `run_시각_recovery.log`로 보관하며, 오류 세트는 고정 기본 경로가 아니라 실제 활성 로그를 복사한다.
  기본·복구 로그가 모두 기록 불가능하면 자동화를 조용히 계속하지 않고 오류 종료한다. 실제 파일
  잠금 진리표(후발 쓰기 차단 방어/시작 잠금 시 복구/양쪽 잠금 시 오류)를 회귀 테스트로 추가.
- **수정 후 실주행 검증 완료** (00:09~00:15, 던전 커스텀 3항목: 일반 1-1 → 어려움 1-1 →
  어려움 1-2, 1바퀴): 세 회차 모두 로그가 **입장 → 클리어 → 결과 화면 → 다시 하기 → 회차 완료**
  전 구간 보존(파일 1,628B → 3,153~3,457B). 함께 실동작 확인된 것: `[커스텀] 완료 마커 기록
  (결과 화면 도달)` 3회, 같은 구역 난이도 전환(stay-adjust)에서 `난이도 확정은 커스텀 시작
  단계에서 완료 - 추가 클릭 생략`, 같은 층 구역 전환(stay-select) 카드 클릭 1회 적중,
  '던전 탐험을 계속하시겠습니까?' 팝업 자동 처리, 완주 후 `progress: null` 자동 초기화.
  문법 0건 · 회귀 테스트 24종 전부 통과 · 빌드 OK(v1.1.0).

### 2026-07-21 — 전체 재점검 후 순차 개선

- **다른 PC 1908×1076 던전 `진입` 무클릭 시간 초과 수정** (21:13, 커스텀 일반 1-1):
  구역 선택과 버튼 문구 확인은 성공했지만 `Invoke-ClickUntil`의 클릭 전 `SourceCondition`이 던전
  전용 넓은 영역 `(660,620,520,70)` 대신 어비스 상세 화면용 좁은 영역 `(880,630,200,48)`을
  읽어 `1층 1구역` 숫자를 놓침. 조건이 거짓이라 실제 클릭은 한 번도 하지 않은 채 20초 후 종료됐고,
  클릭 전에 `[던전] ... 진입 클릭` 로그가 먼저 찍혀 분석도 혼동시켰다. `Get-DgStageEnterButtonText`와
  `Test-DgStageEnterButtonVisible`을 추가해 정상 선택·같은 층 오선택·다른 층 오선택의 세 진입
  경로가 모두 던전 전용 영역과 공용 구역 판정기를 사용하도록 교체. 클릭 전 오해 로그는 제거하고,
  던전 오류 진단도 동일한 전용 영역 원문을 기록한다. 목표/공물 숫자 결합/다른 구역/숫자 소실
  진리표와 세 호출부 소스 계약을 추가했으며, 좌표 변경은 없어 `coordsVersion` 6 유지.
- **오류 이미지·로그 파일명 완전 통일**: 오류 발생 시 `error_날짜_h시m분s초` 공통 기본 이름을
  한 번만 만들고 이미지 `.png`와 로그 `.log`가 함께 사용하도록 고정. 두 파일은 확장자만 다르고
  `_h21m13s00` 형태의 날짜·시각 접미사는 항상 동일하며, 운영 소스 계약 테스트를 추가함.
- **일반 던전 시작 구역 오선택 복구 보강**: 사용자 오류 로그에서 설정은 어려움 1-1이지만
  선택 결과가 1-3(`11층3구역진입`)으로 확인되어 안전 정지한 사례를 재현. 선택된 카드가 펼쳐지며
  작은 카드 위치가 움직이는 화면에서 OCR이 1-1/1-2 라벨을 놓치면 고정 예비 좌표가 빗나가는 것이
  원인이었다. 클릭 후 진입 버튼의 층·구역을 판독해, **같은 층 오선택이면 현재 구역의 옵션 화면을
  연 뒤 커스텀 반복과 같은 옵션 화면 카드 전환기로 목표 구역을 재선택**하고, **다른 층 오선택이면
  현재 옵션 화면의 좌상단 '<'로 선택 화면에 복귀한 뒤 한 번 다시 선택**하도록 변경. 옵션 화면 제목이
  목표 구역으로 확인되어야만 다음 단계로 진행한다. 선택 성공·같은 층 복구·다른 층 재선택 모두
  옵션 화면에서 목표 난이도를 다시 클릭하고 선택 강조를 확인해, 복구 중 난이도가 유지된다고
  가정하지 않는다. OCR 불명확·재시도 실패는 기존처럼 안전 정지한다.
  실제 오류 캡처 Windows OCR 재현과 운영 함수 AST 진리표(정확 선택/같은 층/다른 층/불명확),
  전체 회귀 테스트 23/23 통과. 좌표 기본값 변경은 없어 `coordsVersion` 6 유지.
  추가 실측에서 설정 2-1·열린 상세 화면 2-3인데 기존 선행 검사가 같은 층도 무조건 '<' 복귀하는
  경로를 확인해 보강: **처음부터 같은 층의 다른 구역 상세 화면을 열어 둔 경우에도** 선택 화면으로
  나가지 않고 그 화면의 구역 카드로 목표를 변경한 뒤 난이도를 재확정한다. 다른 층일 때만 '<'로
  복귀한다. GUI에 나타난 숫자 단독 `1`은 원본 로그에는 없던 표시 노이즈라 숫자 단독 줄을 숨김 처리.
  후속 실측(20:55, 룬다 2층 3구역 상세 화면→설정 2-1)에서 2층 지도가 1층과 다름을 확인:
  기존 1층 예비 좌표 `(830,308)`이 2-1 카드 왼쪽 빈 곳을 세 번 눌러 제목이 2-3에 머물렀고
  안전 중단했다. 옵션 카드 예비 좌표를 전체 구역 문자열 기준 층별 맵으로 분리해 1층 좌표는 유지하고,
  2층은 실측 `2-1=(875,307)`, `2-2=(875,225)`, `2-3=(980,266)`을 적용. 운영 함수 AST 좌표
  진리표에 6개 구역과 미지원 구역을 추가했으며 OCR 우선·제목 사후 확인·3회 상한은 그대로 유지.
- **앱 버전 v1.1.0 적용**: 기존 배포판 v1.0.7 이후 누적된 안정화와 던전·어비스 커스텀 반복이라는
  대형 신규 기능을 함께 반영하는 마이너 릴리스로 결정. `$appVersion` 단일 원본을 1.1.0으로 올려
  GUI 제목·로그·exe 파일 버전이 모두 동일하게 생성되도록 반영.
  (2026-07-22 사용자 결정으로 실제 배포 버전은 **v1.1.1** — 실기 검증 중 잡은 로그 유실·경고
  노이즈 수정까지 포함해 릴리스)
- **어비스 커스텀 반복 전체 기능 연결**: 승인된 WinForms 화면에서 혼자하기/함께하기·난이도·
  어비스 던전·매칭 항목을 목록으로 추가하고 즉시 저장. 던전 목록과 분리된
  `abyssCustomRepeat` 진행 기록, 무한/N바퀴, 진행 초기화, 이어가기와 완료 마커·오류 복구를
  기존 커스텀 실행기에 연결. 워커는 항목별 던전 프로파일·방식·난이도·매칭을 실제 어비스 흐름에
  적용하며, 새 시작 시 이미 진행 중인 수동 판은 정리만 하고 미계상. 완료 마커도 콘텐츠별 파일로
  분리해 다른 커스텀 모드를 먼저 시작해도 복구 근거를 보존. config 직접 편집으로 미지원
  던전/난이도를 넣으면 잘못된 카드 클릭 대신 형식 오류로 안전 중단. 구조 추가로
  `configSchemaVersion` 2 적용(`coordsVersion` 6 유지), 던전/어비스 진행도 분리 회귀 테스트 추가.
- **커스텀 반복 표시 문구 정리**: 상세 설정 그룹 제목은 `콘텐츠 상세 설정`으로 통일하고,
  기능 연결 후 `GUI 시안 · 실행 미지원`/`자동화 실행 미지원` 표기를 제거. 커스텀 반복 중
  비활성화되는 사냥터 문구는 `사냥터(미지원)`으로 간결하게 정리.
- **비커스텀 던전 소진 대응·버튼 폭 통일**: 단일 던전 모드도 커스텀과 동일한 `멈춤 /
  미사용으로 진행`, `멈춤 / 소탕만 진행` 라디오 2줄로 변경. 공용 판정에서 잔량 <10은 동전 소진
  설정만, 10~19는 더블 불가 설정만 적용하도록 정리. 권장 창 모드·적용된 설정·설정 저장·진행
  초기화 버튼은 리스트 옆 추가/삭제/위로/아래로 버튼과 같은 94px로 정렬하고 전용 회귀 테스트 추가.
  커스텀 반복 선택 중에는 던전 전용 모드가 유지되도록 사냥터 카테고리 선택도 비활성화.
- **커스텀 완료 상태 소유권·안전 저장 (13)(15)**: 완료 마커를 리스트 지문/바퀴/인덱스/항목
  토큰이 든 JSON으로 바꾸고 현재 컨텍스트와 일치할 때만 사용. 코드 1은 진행도를 먼저 넘기지 않고
  같은 항목의 마무리 전용 복구를 거쳐 코드 0에서 한 번만 전진. 진행/완주 초기화 저장 실패 시
  다음 워커 시작을 차단하고 마커를 보존해 중복 입장·은동전 재소모 방지.
- **난이도 확정 강화 (14)**: 옵션 화면에서도 OCR 라벨 클릭 뒤 선택 테두리를 확인하고, 실패 시 같은
  지점을 1회 재클릭. 커스텀은 끝까지 확정되지 않으면 코드 4로 안전 정지. 실제 UI 색상은 릴리스 전
  확인 필요 섹션에서 추가 검증.
- **config 검증·스키마·원자 저장 (3)(6)**: `configSchemaVersion`을 좌표 버전과 분리하고 `ui` 설정을
  이전 보존 목록에 포함. 좌표/OCR 배열·주요 정수의 형식과 범위를 검증해 잘못된 값은 경고 후 기본값
  사용. 완성 JSON 재파싱 후 같은 폴더 임시 파일 + `File.Replace`로 저장하며 실패 시 기존 파일 보존.
- **입력·재클릭 안전성 (1)(4)(9)**: `Invoke-ClickUntil` 9개 호출부 모두 `SourceCondition`을 사용해
  원본 화면이 사라진 뒤 같은 좌표를 누르지 않음. 전면 창 루트와 실제 커서 위치를 확인해 1회 재시도,
  32비트 tick rollover를 고려한 유휴 시간 계산 적용.
- **중복·장기 자원 정리 (7)(12)(17)**: OCR 텍스트/클릭 위치/단어 목록의 캡처·확대·실패 기록·해제를
  `Get-GameRegionCapture` 하나로 통합. GUI 로그는 전체 재읽기 대신 마지막 완성 LF까지의 UTF-8 바이트
  오프셋으로 증분 읽기. GUI/레거시/OCR 설치의 Process 객체를 정상·강제 종료 모두 `Dispose()`.
- **레거시·테스트·문서 정합성 (10)(16)(18)**: RDP 예약 작업명을 `HoneyNogiRDPToConsole`로 통일하고
  이전 작업 제거 및 강제 종료 후 키/마우스 해제 추가. 커스텀 테스트는 운영 함수 사본을 제거하고 AST로
  본체 구현을 직접 검사. README 로그 보관 수, 완료 마커 계약, CLAUDE 테스트 수를 동기화하고
  관련 개발 문서의 테스트 수와 검증 기록을 동기화.
- **회귀 테스트 확장**: config 잠금/원자 교체·좌표 검증·마커 복구·Process 해제·tick rollover·원본 화면
  재클릭 차단·OCR 공통 캡처·UTF-8 로그 증분 읽기·어비스 커스텀 GUI/워커 계약을 포함해 총 23종으로 확장.
  신규 핵심 판정은 운영 소스를 AST로 직접 불러 검사하며 23종 전체 통과.

### 2026-07-20 — 당일 작업 모음 (던전 커스텀 반복 모드 구현 등)

- **던전 커스텀 반복 모드 구현 완료 (미배포 — 실기 검증 후 릴리스)**: 확정된 리스트 방식과
  스펙 감사 17건 전체 구현. GUI/워커 병렬 구현 → 진리표 테스트 → 검증 파이프라인.
  - GUI(mabinogi_gui.ps1): 반복 그룹 4번째 라디오 '커스텀 반복'(던전 전용·영속화, 던전 외
    카테고리 시 표시상 무한 폴백+선택 보존) / 던전 상세 리스트 빌더(ListView 표: 체크/#/난이도/
    스테이지/은동전/더블 루팅, 머리글 체크 칸=전체 선택, 체크 일괄 삭제·↑↓ 이동, 편집 즉시 저장) /
    소진 대응 커스텀 전용 키 2개 / 리스트 반복 무한·N바퀴 + 진행 초기화 / 순수 판정 함수 8종
    (전진·완주·오류 재시도·지문 등). 진행 기록은 GUI가 코드 0 수신 시 전진+즉시 저장, 완주 시
    자동 초기화, 오류는 같은 항목 2회 자동 재시도(3회째 정지). config.json에 customRepeat 섹션
    신설 + Update-ConfigToLatest 화이트리스트 등재(업데이트 시 리스트 보존·진행 기록만 초기화,
    coordsVersion 6 유지)
  - 워커(mabinogi_run_once.ps1): HONEYNOGI_CUSTOM_* 6종 소비(ITEM/PREV/RESTART/POSITION/
    LIST/MARKER). 항목 기반 난이도·스테이지·토글, PREV 동일=다시 하기·그 외=선택 화면 경유
    (Invoke-DgBackToSelection 추출 — 기존 경로와 동작 동일), 새 시작+던전 안·결과 화면=정리만
    하고 exit 10(수동 판 오계상 방지, 오류 재시작은 정상 계상), 클리어 확정 시 완료 마커 기록
    (마무리 오류의 은동전 이중 소모 방지), 커스텀 한정 격상: 난이도 확정 실패·소모량 불일치
    (초과/미달)=입장 불허 exit 4, 스테이지 라벨 미발견='미해금 추정' exit 4. 소진 대응 진리표
    (Get-CustomCoinDecision, 전용 키), 매칭은 우연한 만남 강제. **커스텀 env 없으면 기존 경로와
    완전 동일(비커스텀 회귀 없음, 전 분기 if/elseif 분리 확인)**
  - 검증: PS 5.1 파서 0건(양 파일, 2회) / 진리표 테스트 3종 신설(test_custom_progress·
    exitcodes·worker: 전진 off-by-one·완주·종료 코드×마커 매트릭스·재시도 카운터·소진 조합·
    PREV/복구 분기) 포함 전체 12종 통과 / 빌드 OK / BOM 전 파일 확인 / 스펙 대조 누락 0건.
    남은 것: 실기 검증('확인 필요' 섹션) → v1.1.1 GitHub 릴리스
- **커스텀 반복 실기 검증 2차: 전환 설계 v4 확정** (21:50~ 실측 3건 + 클릭 실험·연속 캡처).
  - 실측: ① 결과 화면 '나가기' = **필드행**(선택 화면 아님 — 1차 수정의 나가기 설계 폐기)
    ② 1-3 '다음 층으로' = 2층 구역 선택 화면(2-1/2-2/2-3+난이도) ③ 다시 하기 화면 = 같은 층
    구역 선택 가능(역방향 포함, '다음 구역으로'도 동일 화면) ④ 석상 상호작용 화면 = 전 구역·
    난이도 무제약이나 스폰 위치가 범위 밖 + 필드 클릭=이동 + 프롬프트 순간 표시 → 자동화 보류
  - v4 구현: 마무리 갈림길 = 같은 층 '다시 하기' / 1-3→윗층 '다음 층으로'(조각 탐색+상태 기반
    재클릭) — 커스텀 마무리에서 나가기 클릭 완전 제거(검증 확인). 시작 분기에 stay-select 신설
    (같은 층 다른 구역이면 기존 스테이지 카드 선택 로직 재사용). GUI에 Get-CustomTransitionIssues
    신설 — 2층→1층 금지 / 1층→2층은 1-3 출발만 / 바퀴 순환도 검사(혼합 리스트=1바퀴 전용,
    무한·N≥2면 시작 거부+안내), [추가] 시 경고. 순수 함수 6종 + 진리표(test_custom_transitions
    신설 포함 13종 전체 통과), 빌드 OK
  - 주의(잠복, 현재 도달 불가): 3층 스테이지가 게임에 추가되면 GUI 규칙의 '1-3' 하드코딩과
    워커의 일반식(X-3 → 정확히 한 층 위)이 불일치 — 스테이지 목록 확장 시 GUI를 워커식으로
    일반화할 것
  - 실기 확인 항목: 다시 하기 화면에서 구역 카드 좌표($ndStagePoints 재사용) 적중 여부 /
    '다음 층으로' 클릭 후 화면 전환 감지 / '층으' 조각 OCR 판독
- **커스텀 리스트 [추가] 시점 차단 팝업 (사용자 확정)**: 마지막 항목 → 새 항목 전환이
  불가능한 조합(2층→1층 / 1-3 아닌 1층→2층)이면 추가하지 않고 MessageBox 안내
  (마지막↔새 항목 한 쌍만 count/1바퀴로 검사 — 기존 위반과 무관하게 이번 추가만 판정).
  팝업 금지 규칙의 예외로 CLAUDE.md 에 명시(버튼 클릭 즉답 팝업 — 실행 중엔 버튼 비활성이라
  무인 운용 무관). ↑↓ 이동/삭제는 재배치 중간 상태 보호를 위해 차단하지 않음(경고+시작 게이트).
  문법 0건·테스트 13종 통과·빌드 OK
- **커스텀 실기 검증 3차 사고 수정: 옵션 화면 구역 카드 오클릭** (23:39 실측 — 사용자 제보
  "1-1 클릭하는데 소탕 부분 선택"):
  - 원인: 다시 하기 옵션 화면의 구역 지도는 **오른쪽 중앙 가로 배치**(1-1≈830,308 /
    1-2≈918,310 / 1-3≈1022,266)인데 stay-select 가 선택 화면용 세로 지도 좌표
    ($ndStagePoints)를 재사용 → 왼쪽 소탕 카드 자리를 3회 클릭 후 안전 정지(코드 4 —
    격상 규칙이 오구역 입장은 막아줌)
  - 수정: 옵션 화면 전용 Get-DgOptStageCardPoint 신설 — 구역 지도 영역(750,170,360,210)에서
    카드 숫자 라벨 글자 탐색 우선 + 실측 예비 좌표 폴백. 검증: 사고 캡처로 OCR 재현 —
    '1-3' 라벨은 글자 탐색 적중(1006,297), 1-1/1-2 는 라벨 미판독 → 예비 좌표가 카드
    정중앙 적중 확인. 문법 0건·테스트 13종·빌드 OK
  - 2026-07-21 후속 확인: 2층 옵션 화면은 1층과 배치가 다름. 위 20:55 오류 캡처로 층별
    예비 좌표를 분리해 해결했으며, 2층 2-1/2-2/2-3 좌표 진리표를 추가함
- **커스텀 시작 게이트에도 팝업 추가 (사용자 확정)**: ↑↓ 이동 등으로 규칙 위반이 남은 채
  시작을 누르면 로그와 함께 MessageBox '시작 불가' 팝업으로 위반 내역(앞 5건+초과분은 로그
  안내)과 해결 방법(순서 조정 / 혼합 리스트는 '횟수 1바퀴') 표시. 시작 버튼 클릭 즉답이라
  팝업 예외에 해당(실행 중엔 시작 버튼 숨김). 문법 0건·테스트 13종·빌드 OK
- **커스텀 반복 실기 검증 1차 사고 수정: '다시 하기' 옵션 화면에 '<' 없음** (20:25 실측,
  사용자 제보 적중 — 화면에 '<' 버튼이 없고 난이도 알약은 있음).
  - 사고: 일반 1-1 완료 → 다시 하기 → 어려움 2-3 전환 시 '<'(43,67) 4회 허공 클릭 →
    선택 화면 복귀 실패 코드 1 → 재시도 2회 동일 실패 후 정지 (error_20260720_h20m26s04)
  - 원인: '옵션 화면엔 <가 있다' 전제가 사용자가 직접 연 화면에만 성립 —
    다시 하기 경유 화면은 '<' 없음 (CLAUDE.md 도메인 노트에 실측 기록)
  - 수정(계약 v3): ① GUI가 다음 항목(HONEYNOGI_CUSTOM_NEXT, 리스트 순환)을 워커에 전달
    ② 회차 마무리 14-2 갈림길 신설 — 다음 항목이 같은 구역이면 '다시 하기', 다른 구역이면
    **'나가기' → 선택 화면으로 나가며 종료(코드 0, 마커 선기록)** ③ 난이도만 다른 전환은
    다음 회차가 옵션 화면 난이도 알약을 글자 탐색 3회로 클릭(실패 시 안전 정지, 색 사후 검증은
    실측 후 보강) ④ 옵션 화면 시작 복구는 제목 구역 판독 후 같으면 그 자리 진행(stay-adjust),
    다르면 '<' 시도 후 실패 시 오류 대신 안전 정지+안내(go-back). 순수 함수 3종
    (Test-CustomSameStage/Test-CustomTitleStageMatch/Get-CustomOptionStartAction) 신설
  - 검증: 문법 0건 / 진리표 3종(⑦구역 비교 8, ⑧마무리 갈림길 6, ⑨제목 판독·시작 분기 14케이스)
    추가 — 전체 12종 통과 / 빌드 OK / 비커스텀 경로 회귀 없음(안전 중지·잔량 부족 나가기 규약 불변)
  - 실기 확인 항목: 어려움 2-3(2버튼 결과 화면)에서 '나가기' 고정 좌표 적중 여부(기존 안전 중지
    경로와 공유되는 기존 특성) / 알약 클릭 실측
- **커스텀 반복 은동전 합계 표시 (사용자 요청)**: 하단 줄 진행 초기화 옆에 리스트 소모량 합산
  라벨 추가 — 무한이면 `바퀴당 은동전 50개`, 횟수 N바퀴면 `바퀴당 50 · 총 150개` 동시 표시.
  순수 함수 Get-CustomCoinTotalPerLap + 즉시 저장 경로(Save-CustomRepeatToConfig)·복원 시 갱신.
  진리표 4케이스 추가(빈 리스트/혼합/단일 항목 배열 풀림/중복 합산) — 전체 12종 통과, 빌드 OK
- **커스텀 반복 소진 대응을 항목별 속성으로 개편 (사용자 확정 시안)**: 전역 체크박스 2개
  (소진 시 미사용으로 계속/더블 루팅 불가 시 소탕만 계속 + customRepeat 전역 키) 폐지 →
  각 항목이 `소진 시(멈춤/미사용으로 진행)`·`더블 불가 시(멈춤/소탕만 진행)` 값을 가짐.
  - GUI: 입력 줄 아래 라디오 2줄(은동전/더블 체크 시 표시, 기본 멈춤) → [추가] 시 항목에 저장,
    리스트 7열(…/소진 시/더블 불가 시, 해당 없으면 —). [추가] 버튼은 리스트 옆 버튼 열
    최상단([추가][삭제][↑][↓])으로 이동. 라디오 줄 수에 따라 그룹 높이 동적 재배치.
    더블+멈춤 항목은 소진 시 도달 불가라 exhaustContinue 도 false 정규화(표기 '—' 역해석과
    일치시켜 재저장 시 지문 불일치로 진행 기록이 초기화되는 사고 차단)
  - 토큰/지문 6조각으로 확장(난이도|구역|coin|double|exhaustContinue|noDoubleSweep) —
    GUI 생성↔워커 파싱↔지문 단일 구현 공용 확인. 워커 Get-CustomCoinDecision 을 단계식
    진리표로 재작성(더블 20개: ≥20 정상 / 10~19 더블불가 설정 / <10 소탕만 선택 시에만
    소진 설정 판정). 압축 표기도 `어1-3(20,소·진)` 형식으로 갱신
  - 검증: 문법 0건 / 커스텀 테스트 3종을 v2 진리표로 재작성(더블 32케이스 전수 포함) —
    전체 12종 통과 / 빌드 OK / 전역 키 잔재 0건(남은 continueWithoutCoin 등은 단일 모드
    전용 키로 유지 대상) / BOM·보간 함정 점검 통과
- **커스텀 반복 UI 표기 개선 (사용자 피드백)**: ① GUI의 '스테이지' 표기를 게임 용어 '구역'으로
  통일(단일 모드 라벨·리스트 열, config 키 stage 는 유지) ② 리스트의 은동전/더블 루팅 2열을
  판당 소모량 1열로 통합 — 더블 루팅 '20개' / 소탕만 '10개' / 미사용 '0개' (Add-CustomListRow
  표기 ↔ Get-CustomItemsFromList 역해석 쌍으로 수정). 문법 0건·테스트 12종 통과·재빌드 확인
- **파티원 대기 로그의 변수명 한글 흡수 수정** (run_once 3804행): `"최대 $memberWaitSeconds초"`가
  `$memberWaitSeconds초`라는 변수로 해석돼 대기 시간이 빈 값으로 출력되던 기존 버그(절대 규칙 3
  함정, 커스텀 검증 중 발견) → `${memberWaitSeconds}초`로 수정. 문법 검사·재빌드 확인

- **제어 버튼 상태 전환 UI (사용자 제안)**: 대기 중에는 시작(F9)만, 실행 중에는 시작이
  사라지고 그 자리에 안전 중지(F9)/즉시 중지(F10)가 나타나는 방식으로 변경
  (Set-UiRunning에서 Visible 전환 - 모든 시작/종료 경로가 이 함수를 거침을 확인).
  대기 중 중지 오클릭·실행 중 시작 오클릭 원천 차단. 설명서 버튼은 항상 표시 유지.
- **월요일 주간 협동 미션 리셋 팝업 대응** (06:03 실측, 사용자가 원인 추정 적중: 월요일
  오전 6시 주간 리셋에만 뜨는 '새로운 한 주가 시작되었어요!' 전체 화면 팝업이 어비스
  복귀를 막음 → 출석/스텔라 픽 체인은 정상 처리됐지만 이 팝업은 아무 핸들러도 몰랐고,
  알 수 없는 화면 X 후보(1229,67 등)도 버튼 위치와 달라 못 닫아 복귀 시간 초과).
  - 수정: 공용 이벤트 핸들러(Invoke-EventSkipOrConfirm)에 분기 추가 — 하단 버튼 줄
    (rgClearExit)에서 '협동'+'참여' 조합 감지 시 '닫기' 글자 탐색 클릭(실패 시 예비
    좌표 495,654). 시작 체인·어비스 복귀 루프 모두 이 핸들러를 쓰므로 양쪽 커버.
  - 검증: 오류 캡처(1272x717)와 현재 화면(1908x1076) 모두 '닫기협동미션참여하기'
    완벽 판독 + '닫기'@(495,654) 동일 좌표 확인. 진리표 7케이스 테스트 추가
    (클리어 점수표의 '협동 보너스'와 구분 포함) - 전체 9종 테스트 통과.

### 2026-07-19 — 당일 작업 모음 (던전 시작 검증 실전 통과·클리어 점수표 보조 신호 등)

- **던전 시작 '좌상단 < 복귀' 실전 검증 완료** (20:20 실측): 설정 1-3 + 룬다 2-3 입장
  화면 열어둔 채 시작 → 다른 스테이지 감지(제목 '〈른다2층3구역') → **< 클릭 1회 만에
  선택 화면 복귀** → 어려움/1-3 재선택 → 정상 진입. ESC 방식 실패 후 교체한 경로가
  한 번에 통과 - 던전 시작 검증 기능 전체 검증 완료.
- **업데이트 안내 팝업 실전 확인 완료** (사용자 확인): v1.0.7 릴리스 후 구버전 GUI에서
  시작 시 안내 팝업 + 우하단 다운로드 링크가 정상 표시됨 - 새 버전 알림의 첫 실전 동작 검증.
- **클리어 화면 보조 신호 추가: 좌측 점수표** (사용자 제안). 하단 '화면을 터치' 문구는
  캐릭터가 겹쳐 계속 깨지는 위치라, 문구 판정이 전부 실패했을 때 좌측 점수표 영역
  (185,300,230,165)을 추가로 읽어 **'처치' + ('완벽' 또는 '보너스')** 조합이면 클리어로
  판정. 실측: 클리어 캡처 2장 모두 라벨 정상 판독('처치완벽한전주권장전투력재도전보너스…'),
  결과/옵션/전투 화면 판독에는 미검출(오탐 없음). 제목 '던전 클리어!'는 스타일 폰트라
  '코전쎠/'로 깨져 신호로 부적합(실측 2장 공통) + 그 영역은 전투 중 유저 닉네임이 읽히는
  위치라('〈고이〉고의:爭' 실측) 오탐 위험 → 제외. 점수표 진리표 7케이스 테스트 추가.
  - **콘텐츠별 수동 검증 완료 (같은 날, 사용자가 클리어 화면을 띄워 두고 4장 실측)**:
    던전 소탕(문구 '화면을지해주세요'로 깨졌지만 점수표 백업 ✓) / 던전 일반(둘 다 ✓) /
    사냥터 소탕(파티원 말이 점수표를 가려 '보너스'→'보네스'로 깨졌지만 OR 조합의
    '완벽'으로 매칭 ✓) / **어비스 = 점수표 자체가 없는 구성**(빈 판독 → 보조 신호
    미발동·오탐 없음, 문구 6조합이 유일 신호 - '면을터치해주세요' 깨짐도 매칭 ✓).
    새 깨짐 2종('화면을지~', '면을터치~')을 진리표에 추가 (문구 14케이스 + 점수표 7케이스).
- **클리어 문구 깨짐 변종 2종 추가 대응** (18:43 다른 PC(1278x720 창) 실측:
  피오드 어려움 2-3 클리어 화면에서 600초 내내 감지 실패 → 시간 초과. 캐릭터가 문구
  뒤에 겹쳐 서서 판독이 심하게 깨졌고, **같은 화면인데 판독마다 다르게 깨짐** —
  오류 시점 진단은 '면百 치해', 캡처 재현은 '화면치6H天서j“'. 기존 4조합 전부 빗나감).
  - 수정: '면'+'치해' 및 '화면'+'치' 조합 추가 (비대상 실측 판독 '경험치 고二',
    '보상을확인해주세요', 'Space' 등에는 안 걸림).
  - 검증: 오류 캡처 재현으로 구 판정 False → 신 판정 True 확인, 진리표에 두 변종 추가
    (총 12케이스) - 전체 테스트 8종 통과.
- **어비스 클리어 감지 폴링 간격 2초 → 1초 통일** (18:11 실사용 피드백: 보스 클리어 후
  '화면을 터치' 감지가 늦게 느껴짐). 클리어 확인은 원래 매 바퀴 첫 순서였으나 어비스만
  바퀴 끝 대기가 2초(던전은 1초)라 감지 지연이 최대 ~3.5초였음 → 던전과 동일한 1초로
  통일 (감지 지연 최대 ~2.5초, 평균 ~1초대). 부수 효과로 컷신 넘기기(3바퀴마다)/구매
  팝업 닫기(2바퀴마다) 반응도 2배 빨라짐. 컷신 클릭 직후 2초 대기는 이중 클릭 방지용으로 유지. (08:26 다른 PC
  실측: 클리어 → 터치 → 전리품 공개 클릭 1회 후 82초 방치 → 결과 화면 시간 초과.
  캡처 판독 '발견한전' — 진행 클릭이 라벨 지점을 눌러 게임 커서가 '발견한 전리품'
  라벨 위에 주차됐고, 다음 폴링부터 탐색어 '전리품'이 빗나가 클릭이 멈추는
  **자기 커서로 자기 감지를 가리는** 구조).
  - 수정 ①: 진행 클릭을 라벨 지점 대신 빈 배경(400,300 - 캡처 픽셀 R12G12B13 확인)으로
    이동 → 커서가 라벨/카드를 가리지 않음 ②: 탐색어 '전리품' → '발견' 조각으로 완화
    (부분 가림에도 감지 유지). 결과 화면에도 같은 라벨이 있지만 다시 하기 버튼 탐색이
    루프에서 먼저 돌아 오탐 없음 (기존 순서 유지).
  - 검증: 오류 캡처 판독 재현(3/4/5배율 모두 '발견한전' → 옛 탐색어 실패, 새 조각 매칭)
    + 진리표 6케이스 테스트 추가(test_loot_label_fragments) - 전체 8종 테스트 통과.
- **개선점 (5) 회귀 테스트를 저장소에 보존**: `tests\` 폴더 신설 — 판정 로직 진리표
  7종(클리어 문구 깨짐 / 던전 시작 스테이지 검증 / 은동전 역방향 해제 루프 / 버전 비교 /
  배열 반환 함정 / 좌표 게이트 카드 보강 / 공지 팝업 감지) + 일괄 실행기
  `run_all_tests.ps1` (PS 5.1, UTF-8 BOM). 전부 통과 확인. 본체 판정식 변경 시
  해당 테스트의 사본 로직도 함께 갱신할 것(각 파일 머리에 명시).
- **개선점 (2) 좌표 버전 게이트에 어비스 카드 좌표 포함**: 게이트가 ocrRegions/clickPoints만
  무시하고 `dungeons.profiles.<던전>.card`는 빠져나가던 구멍 보강 — 게이트 발동 시
  내장 최신 카드 좌표($builtinDungeonCards, config profiles와 동일 값)로 대체하고
  사용자 선택값(selected/stage/match)은 유지. 내장 목록에 없는 미래 던전은 대체값이
  없어 프로파일 값 유지(한계 주석 명시). 진리표 5케이스 테스트 통과.
- **개선점 (8) DISM 진행률 스트림 예외 경로 해제**: FileStream/StreamReader를
  try/finally로 감싸 ReadToEnd 예외 시에도 핸들 해제 (StreamReader.Dispose가 내부
  스트림까지 닫는 규칙 반영 - sr 우선, 없으면 fs).

- **아침 6시 공지 게시판 팝업이 어비스 복귀를 막던 사고 수정** (06:42 다른 PC(타 PC,
  1417x799 창) 실측: 출석부→스텔라 픽 처리 후 공지 게시판 팝업(공지사항/이벤트/쿠폰
  입력/FAQ)이 화면을 덮음 → 가장자리로 필드 HUD가 계속 보여 '필드 상태'로 판정 →
  ESC 클릭이 팝업에 막힌 채 18회 반복 → 선택 화면 복귀 시간 초과. HUD가 '아는 화면'이라
  알 수 없는 화면 X 복구도 발동 안 함).
  - 수정: 복귀 루프(Return-ToAbyssSelection)의 HUD 분기에서 ESC 클릭 전에 공지 게시판
    팝업을 먼저 감지(Test-NoticeBoardPopup - 하단 탭 줄 영역 170,495,930,62에서
    '쿠폰' 또는 '공지'+'이벤트' 조합)하고, 보이면 팝업 우상단 X(1092,135)를 눌러 닫음.
  - 검증: 오류 캡처 판독으로 탭 줄 5단어 전부 정위치 판독('공지사항'@217,526 /
    '쿠폰'@662,526 등 - 감지 조건 성립), X 좌표는 캡처 픽셀 실측 + 기존 공용 X 후보
    (1090,137)와 동일 계열로 교차 확인. 팝업이 없으면 감지 미발동으로 기존 동작 그대로.
- **은동전 미사용인데 소탕 카드가 켜진 채 입장 시도하던 사고 수정** (00:21 다른 PC(sangj) 실측:
  설정 미사용 + 게임에 소탕 카드 '선택됨' 방치 + 잔량 6개 → 카드 글자가 '선태되'로 깨져
  판별 불가 → 해제 클릭 못 함 → 입장 거부 45초 헛대기. `[설정]` 스냅샷 덕에 설정 확인 즉시 가능했음).
  - 수정 ①: 카드 판별 조각에 '선태' 추가 (주/보조 영역 - '선택됨' 깨짐 실측 대응)
  - 수정 ②(핵심, 사용자 제안): **입장 버튼 소모량 역방향 검증** — 미사용(설정이든
    '소진 시 미사용으로 계속' 강등이든)인데 입장 버튼에 10/20이 보이면 카드가 켜진 것
    → 카드 고정 좌표를 눌러 해제 → 재확인. 던전/사냥터 2곳. 버튼 숫자가 카드 글자보다
    크고 또렷해 카드 OCR이 깨져도 동작 (오류 캡처에서 '10' 판독 검증).
  - 해제 루프는 상태 기반: 10/20이 그대로 보일 때만 클릭(최대 2회), null은 캡처 정상일
    때만 해제 인정(캡처 실패 순단 가드), 잡음 숫자는 순방향과 같은 기준으로 경고 후 진행.
    9개 에이전트 교차 검증에서 확정된 2건(캡처 실패 null 오인 / 잡음 숫자 블라인드
    재클릭으로 카드 재점등 후 throw) 반영. 시나리오 7케이스 단위 테스트 전부 통과.

- **적용 설정 스냅샷을 로그 파일에 기록 (GUI 화면에는 미표시)**: 워커 시작 시
  `[설정]` 접두어로 앱 버전(HONEYNOGI_APP_VERSION)·반복 모드(GUI가 환경변수
  HONEYNOGI_REPEAT_INFO로 전달 — 무한/횟수 n·m회차/시간 지정)·콘텐츠·coordsVersion과 설정 섹션들(dungeons/
  normalDungeon/huntingGround/timeoutsSeconds/afterEntry/revive/rdp/window/
  diagnostics)을 한 줄 JSON으로 기록 (설명용 `_`키·부피 큰 profiles 좌표 제외).
  GUI 로그 tail은 `[설정]` 줄을 건너뛰어 화면은 그대로. 오류 세트(error_*.log)에
  사본이 포함되므로 다른 사용자의 오류를 받을 때 그쪽 설정을 즉시 볼 수 있음.
  실 config로 출력 형태 검증 완료.
- **로그 보관 정책 변경**: 오류 세트(error png/log)와 회차 로그(run_*.log) 보관을
  각 20개 → 10개로 축소, 파일명 시각을 읽기 쉬운 h/m/s 표기로 변경
  (예: `run_20260718_h21m49s09.log`, `error_20260718_h21m49s09.png`).
  정리는 파일명이 아닌 수정 시각 기준이라 옛 형식 파일이 섞여 있어도 함께 정리됨.
  내장 config 기본값과 이 PC의 실 config(keepScreenshots)도 10으로 변경 —
  단, 다른 PC의 기존 config에는 20이 남아 있어 그쪽은 20개 유지(다음 config 재생성 때 10 적용).
- **던전/사냥터: 사용자가 클리어 화면을 직접 터치해 넘긴 경우 대응** (21:52 실측:
  어려움 1-3이 18초 만에 끝나 사용자가 클리어 화면을 먼저 터치 → 게임은 결과 화면인데
  워커는 클리어 문구만 계속 대기). '이미 지나감' 감지가 어비스 전용(`-not $DungeonMode`)
  이어서 던전/사냥터에는 없던 구멍. `Wait-ForDungeonClearScreen`에 콘텐츠별 결과 버튼
  탐색 주입(`-FindResultButton`: 던전='다시 하기'/사냥터='새 임무 선택') 추가 — 결과
  버튼이 보이고 HUD가 없으면(전투 중 오탐 방지) 'reward'로 반환해 터치를 건너뛰고 결과
  처리로 진행 (호출부 else 분기는 기존에 이미 있었음). 실측 캡처 검증: 결과 화면 판독
  '다시하기다음층으로'(탐색 히트) + HUD 영역 '013,354,499'(부재 확인). 어비스 경로 무변경.
- **클리어 문구 새 깨짐 유형 대응** (21:27 실측: 어려움 1-3 클리어 화면에서 2분 이상
  터치를 못 하고 대기). OCR이 '화면을터치해 주세요'의 '터'를 통째로 삼켜
  '화면을치해주세요'로 읽혔는데 기존 세 조합('화면을'+'터' / '터치해' / '터치하')에
  모두 빗나감. '화면을' + '주세요' 조합을 추가 (원문 문장에만 있는 조합이라 오탐 없음
  - 판정 8케이스 단위 테스트 통과: 깨짐 4종 감지, '보상을확인해주세요' 등 비대상 3종 미감지).
- **새 버전 안내 팝업 추가 (v1.0.3)**: 구버전 실행 시 우하단 링크에 더해 안내
  팝업(확인 1번, 안내만) 표시. 자동화가 이미 실행 중이면 팝업 생략하고 링크만
  (무인 운용 보호). 팝업 확인과 무관하게 자동 동작 없음.
- **최신 버전 확인 기능 추가 (GUI)**: 시작 시 1회 GitHub 릴리스 API로 최신 태그를
  백그라운드 러닝스페이스에서 확인 → 새 버전이면 우하단 버전 표시가 '새 버전 vX.X.X
  다운로드' 링크로 바뀜(클릭 시 릴리스 페이지). 서버/DB 불필요 — 릴리스 태그가 곧 기준.
  실패(오프라인/한도/저장소 문제)는 조용히 무시하고 정상 시작, 팝업 없음(무인 운용 보호).
  검증: 버전 판정 8케이스(1.0.10>1.0.9 포함) 통과 + 실 API 호출로 저장소 공개·태그
  v1.0.1 형식 확인. 참고: 이 알림은 v1.0.2부터 내장이라 v1.0.1 사용자는 한 번은
  수동으로 받아야 함.
- **던전 시작 시 '다른 스테이지의 진입 옵션 화면' 방치 사고 수정** (실측: 설정 일반
  1-3인데 게임이 2-3의 진입 옵션(입장) 화면에 열려 있던 상태로 시작 → 검증 없이 2-3 입장).
  - 원인: 시작 화면 판별이 제목에 '구역'만 보이면 '다시 하기' 복귀 회차로 간주하고
    난이도/스테이지 선택을 통째로 건너뛰었음 (옵션 화면이 설정과 같은 스테이지인지 미검증).
  - 수정(0-1단계 신설): 옵션 화면이면 제목의 'N층 M구역'을 설정과 대조. 불일치가
    의심되면 700ms 후 재판독까지 해서 **다른 스테이지가 명확히 읽힌 경우에만**
    선택 화면으로 되돌아가(상태 기반: 옵션 화면이 그대로 보일 때만 ESC → 예비로
    우상단 X, 최대 입력 4회, 전환 중 판독 불명확이면 입력 없이 재확인, 필드 이탈
    감지 시 즉시 중단) 난이도/스테이지 선택부터 다시 진행. 복귀 실패 시 명확한
    안내와 함께 오류 종료. 숫자를 명확히 읽지 못하면 기존처럼 그대로 진행(복귀
    회차 오판 방지), 재판독이 빈 문자열(일시 캡처 실패)이면 첫 판독 유지.
  - 검증: 판정식 진리표 10케이스 단독 테스트 전부 통과 + 17개 에이전트 교차 검증
    (지적 13건 중 11건 반박 기각, 확정 2건은 반영: ① 뒤로 가기를 '조작 후 검증'에서
    '검증 후 조작(상태 기반)'으로 교체 ② '층' 소실 오독('23구역')도 잡도록 재판독
    판정식 구분자를 {0,2}로 완화 - 오판해도 재선택이라 결과는 항상 설정 스테이지).
  - 1차 실전 테스트(18:44)에서 뒤로 가기 수단이 틀렸음을 실측: **ESC는 한 단계
    뒤로가 아니라 던전 UI 전체를 닫고 필드로 나가버림** (우상단 X도 같은 계열).
    감지·필드 이탈 중단·오류 안내는 설계대로 동작했고, 뒤로 가기를 **좌상단 '<'
    클릭(43,67 실측)**으로 교체. 옵션 화면 캡처 실측으로 '<'@(42,64) 판독 확인.
  - 난이도 구멍도 함께 해결: 옵션 화면 상단에 난이도 알약(일반/어려움)이 있음을
    실측(영역 600,95,190,50 / '일반'@652,120·'어려움'@723,120 판독 확인) —
    옵션 화면에서 시작하는 회차는 설정 난이도를 한 번 눌러 확정 (이미 선택돼
    있어도 부작용 없음). 이 화면 선택 강조는 보라색 계열이라 금색 기준
    사후 확인은 생략(클릭만).
- **스크롤 보정 첫 빌드의 배열 반환 사고 수정** (18:07 실측 오류: 스테이지 클릭에서
  `"System.Object[]" → "System.Int32" 변환 불가`).
  - 원인: `Get-GameRegionOcrWords`가 `return ,$words`로 감싸 반환한 것을 호출부가
    `@()`로 다시 감싸면서 '단어 배열을 담은 1칸짜리 배열'이 됨 → foreach가 단어가
    아닌 배열 자체를 돌아 `.Text`가 텍스트 배열이 되고, 스테이지 라벨('1-3')이
    포함돼 있으면 매칭이 참이 되어 `[int]$mapWord.X`(X값 배열)에서 변환 오류.
    ICO 생성 때와 같은 PS 배열 반환 함정 계열.
  - 수정: 함수는 `return $words`(파이프라인이 단어 단위로 풂), 호출부 `@()`가 모음 —
    단어 0/1/N개 모두 정상임을 단독 재현 테스트로 확인(구방식은 로그와 동일 오류 재현).
- **던전 스테이지 지도 스크롤 보정** (17:50 실측 오류: 1-2 클릭이 1-3 선택).
  - 원인: 스테이지 지도가 세로 스크롤 패널이라 클릭할 때마다 살짝 오르내려
    (사용자 관찰) 노드 위치가 흐름 — 고정 좌표가 스크롤 상태에 따라 옆 노드에
    떨어짐. 실측: 같은 화면에서 노드가 기준보다 100px 이상 밀려 있었음.
  - 수정: 클릭 전에 지도 라벨을 OCR로 읽어(Get-GameRegionOcrWords 신설)
    ① 원하는 스테이지 라벨이 읽히면 그 지점을 바로 클릭,
    ② 아니면 읽히는 라벨/층 제목(1층/2층/1-3/2-2/2-3 등 - 큰 글자는 잘 읽힘)으로
    세로 오프셋을 계산해 기준 좌표 보정, ③ 전부 실패하면 기준 좌표(기존 동작).
    매 재시도마다 다시 계산 + 기존 진입 버튼 문구 검증 유지.
    기준 좌표도 현 UI 실측 캡처로 리베이스 (2층 배치가 세로형으로 바뀌어 있었음).
    검증: 오류 캡처 판독 단어 4개 전부 앵커와 일치(Δ=0) → 1-2를 정확히 클릭.
- **던전 결과 화면 버튼 배치 대응 (coordsVersion 5→6)** — 스테이지/난이도에 따라
  버튼 구성이 달라짐 (2026-07-18 사용자 게임 2장 실측):
  1-1~2-2 = +다음 구역으로 / 1-3 = +다음 층으로 / 일반 2-3 = +다음 난이도로 (3버튼,
  다시 하기가 가운데 637,655로 이동) / 어려움 2-3 = 나가기·다시 하기 (2버튼).
  옛 고정 좌표(757,654)는 3버튼 배치에서 '다음 ~로' 자리라 다른 스테이지/난이도로
  넘어가는 오클릭 발생 → 클릭을 '다시 하기' 글자 탐색 지점으로 교체하고 탐색 영역을
  모든 배치를 덮게 확장(540,625,340,60). 두 캡처 판독 검증: '다시'@(614,654) 매칭,
  '다음 구역으로/난이도로'는 탐색어에 안 걸림. 17:04 오류의 근본 원인이 이것 + 계속 팝업.
- **던전 '다시 하기' 중 '던전 탐험을 계속하시겠습니까?' 팝업 처리** (다른 PC 17:04 실측 오류).
  - 원인: 던전 밖에서 진행할 퀘스트가 있으면(예: 주간 목표 달성) 다시 하기 뒤에
    이 팝업(계속하기=ESC / 나가기=Space)이 끼어드는데, 무조건 재클릭이 팝업의
    '나가기' 자리를 눌러 던전 밖 필드로 나가버림 → 옵션 화면 대기 시간 초과.
  - 수정: 다시 하기 대기를 상태 기반으로 교체 — '계속하'가 보이면 계속하기 클릭
    (지점 탐색 실패 시 ESC 예비), 재클릭은 결과 화면(다시 하기 버튼)이 그대로
    보일 때만. 사냥터 '새 임무 선택' 대기에도 같은 팝업 처리 추가
    (그쪽은 Space가 '나가기'라 절대 Space로 넘기면 안 되는 화면).
- **지난 회차 로그 보관 (최근 20개, 오류 세트와 동일 개수)** — 회차 시작 시 이전
  로그를 삭제하는 대신 `Log\run_시각.log`로 이동 보관, 초과분은 오래된 것부터
  자동 삭제. GUI 화면 중복 출력 방지 동작은 동일(파일이 치워지는 건 같음),
  시뮬레이션 12회차 테스트 통과(개수 유지·오래된 것 정리·순서 보존).
- **복귀 중 스텔라 픽(스텔라그램) 즉시 처리** — 복귀 도중 스텔라 픽 팝업이 뜨면
  기존에는 '알 수 없는 화면 20초 → X 닫기'로 느리게 넘겼고 오늘 픽을 못 고를 수
  있었음 (2026-07-18 06:00 실측). 복귀 루프에 1단계(카드 선택)/2단계(확정 버튼)
  처리를 추가 — 출석부처럼 바로 인식하고 처리 (상한 5회, 넘으면 X 폴백 유지).
- **GUI 허니 테마 + 꿀단지 아이콘 적용** (테스트 폴더에서 시안 확인 후 사용자 승인).
  - 크림 배경 + 꿀 갈색 글자/섹션 제목, 시작 버튼 = 꿀색, 즉시 중지 = 연빨강, 로그만 진갈색 콘솔풍.
  - 테마는 모든 컨트롤 생성 후 색만 입히는 독립 블록(`Apply-HoneyTheme`) — 기능 코드 무변경.
  - 꿀단지 아이콘(`app.ico`, GDI+로 자체 제작 16/32/48/256): exe 파일 아이콘(`/win32icon`) + 창/작업표시줄(`$form.Icon`, launcher가 추출) 모두 적용.
  - 알려진 한계: '시간 지정' DateTimePicker는 흰 배경 고정 (WinForms 제약).
  - 다크 테마 시안은 사용자 기각 → 밝은 허니 테마로 확정.
- **프로젝트 리네임: 꿀비노기 (HoneyNogi)** — 전면 적용.
  - exe: `MabinogiAuto.exe` → `HoneyNogi.exe` (제품명/파일 설명도 변경)
  - 창 제목: GUI '꿀비노기 컨트롤 패널', 워커 콘솔 '꿀비노기'
  - 데이터 폴더: `%LOCALAPPDATA%\MabinogiAuto` → `HoneyNogi` — 최초 실행 시 구버전 폴더의 `config.json`을 자동으로 이어받음 (설정 유지)
  - RDP 예약 작업: `MabinogiRDPToConsole` → `HoneyNogiRDPToConsole` — 옛 이름 작업은 발견 시 자동 제거
  - 뮤텍스/내부 클래스명도 HoneyNogi 계열로 통일
  - 다른 PC 배포: 새 `HoneyNogi.exe` 복사 후 실행하면 됨 (설정 자동 이전). 옛 `MabinogiAuto.exe`와 동시 실행만 피할 것
- **다른 PC(작은 창 1368x771) 어비스 완주 검증 완료** (사용자 확인 — 문제없음). 연쇄 사고 6건 수정 이력은 07-17 '다른 PC(작은 창) 운용 안정화' 참고.
- **기술 부채: 어비스/던전/사냥터에 복사돼 있던 공통 블록 3종을 헬퍼로 추출.**
  - `Invoke-AfterEntryKeys`: 입장 직후 키 입력 (어비스/파티원/던전/사냥터 4곳)
  - `Wait-ForResultScreen`: 결과 화면 대기 — 컷신 넘기기/클리어 재터치/전리품 공개 진행/캡처 실패 시간 동결 포함 (던전/사냥터 2곳, 반복 버튼 탐색만 스크립트블록으로 주입 — PS 동적 스코프 동작 단위 검증 완료)
  - `Invoke-SafeStopExitIfRequested`: 결과 화면 안전 중지 나가기 (던전/사냥터 2곳)
  - 로그 문구/동작 완전 동일(기계적 추출), 재입장 목적지 등 다른 부분은 각 흐름에 유지. 사용처 4+2+2곳 교체 확인, 문법/빌드/리소스 검증 통과.
- **어비스 파티(파티장) 모드 실전 완주 검증 완료** (사용자 확인).
  짠 파티 그대로 입장(토글 켜져 있으면 끔) → 인원 부족 시 도전 확인 팝업 Space → '입장 취소' 표시로 준비 대기 확인(재클릭은 '입장하기'가 남았을 때만) → 전원 준비 시 자동 입장 → 클리어 → 각자 나가기 → 재진입 반복.
- **어비스 파티(파티원) 모드 실전 완주 검증 완료** (사용자 확인).
  메뉴 이동 없이 필드 대기 → '준비 완료' 클릭('취소' 보이면 성공, 파티장이 취소하면 재클릭) → 따라 입장 → 클리어 터치 → 나가기 → 필드 대기 반복. 대기 중 안전 중지는 즉시 소비(회차 미완료, 코드 4).
- **사냥터 전체 자동화 실전 완주 검증 완료** (사용자 확인, '(개발 중)' 표기 제거).
  난이도 → 은동전/더블 루팅 카드(임무 카드 줄 수에 따라 흐르는 버튼 위치 대응) → 소모량 교차 검증 → 입장('입장하기'/'임무 시작') → 사냥 → 클리어 → 컷신/전리품 공개 → 새 임무 선택(가방 정리 화면 Space 처리 포함) → 첫 화면 복귀 → 다음 회차 반복.
  은동전 소진 시(10개 미만, 또는 더블 루팅 20개 부족 + '소탕만 계속' 꺼짐) 첫 화면 X → 결과 화면 나가기까지 눌러 사냥터 밖으로 나가고 종료(코드 4).
  개발 중 잡은 사고 6건(새 임무 선택 버튼 구성/임무 시작 표기/카드 위치 유동/가방 정리 화면/재클릭 파티찾기 오클릭/소진 나가기 층 구조)은 07-17 항목들 참고.
- **OCR 메모리 직행 전환(임시 PNG 제거) 실전 검증 완료**: 위 완주 테스트가 모두 새 파이프라인으로 진행되어 감지 흐름 이상 없음 확인.

### 2026-07-17 — 당일 작업 모음 (다른 PC 운용 안정화 연쇄 6건·OCR 메모리화 등)

- **다른 PC(작은 창 1368x771) 운용 안정화 — 연쇄 사고 6건 수정** (각 건은 04:25~06:01 오류 세트 실측 기반, 아침 6시 리셋 체인 자동화 검증됨):
  1. 어비스 메뉴 감지 실패(작은 글자) → 확대 배율 3→4 + '비스' 조각 매칭
  2. 'Home' 배지를 HUD로 오인해 우편함이 열리는 사고 → 메뉴 감지 2차 신호('게임 종료' 문구) + 알 수 없는 화면 지속 시 X 후보 순환 복구 추가
  3. 옛 config 좌표가 새 좌표를 덮어 어비스 클릭이 헛클릭(16회) → 좌표 버전 게이트(coordsVersion) 도입으로 구조적 차단
  4. 함께하기 탭 '이동하기' 미처리 타임아웃 → 혼자하기와 동일한 이동 처리 추가
  5. 클리어 터치가 등급 연출에 무시됨 → 나가기 보일 때까지 3초 간격 재터치
  6. 출석 완료 보상 화면이 Space 조작이라 못 넘김 → '지원' 문구 감지 시 Space
- **Invoke-ClickUntil 캡처 실패 빈틈 수정**: 클릭 후 5초 내부 대기 중 캡처 실패(RDP 끊김→본체 전환)가 일어나면 제한 시간이 연장되지 않고 그대로 소모되던 문제 (23:47 실측: '새 임무 선택' 클릭 직후 RDP가 끊겨 복구 2초 뒤 억울하게 시간 초과). 내부 대기 중 실패가 감지되면 바깥 루프의 실패 처리(시간 연장)로 즉시 이동.
- **사냥터 결과 화면을 Test-KnownScreen에 추가**: 시작 시 이 화면을 출석/이벤트 화면으로 오인해 중앙 클릭 5회 + X 후보를 누르던 문제 수정 (중앙 클릭은 전리품 아이템 상세를 열 수 있어 위험).
- **클리어 대기 시간 초과가 '전투가 긴 판'을 오류로 끝내던 문제 수정** (다른 PC(sjm49) 19:19 실측: 어려움 파티전이 240초 한도를 넘겨 보스 15.2% 시점에 오류 종료). 한도가 다 돼도 퀘스트 추적기에 클리어 목표가 남아 있으면(= 던전 안, 전투 중) 60초씩 연장 대기. 절대 한도는 설정값의 3배(최소 30분). 오류 시점 캡처로 연장 조건 발동 검증 완료.
- **파티(파티장) '입장 취소' 오클릭 사고 수정** (17:00 로그 실측): 준비 대기 화면의 '입장 취소' 버튼이 상세 화면 입장 버튼과 같은 자리 + '입장' 글자 포함이라 "반응 없음"으로 오판해 재클릭 → 준비 요청 취소. 수정: 버튼 글자에 '취소'가 보이면 준비 대기 시작 성공으로 판정하고, 재클릭은 '입장하기'가 그대로 남아 있을 때만 수행.
- **OCR 임시 PNG 제거**: OCR 1회마다 '캡처→PNG 저장→디스크 재읽기→OCR'이던 것을 비트맵 픽셀을 메모리에서 곧바로 SoftwareBitmap으로 만들어 넘기는 방식으로 교체 (LockBits→AsBuffer→CreateCopyFromBuffer, `Invoke-OcrOnBitmap` 헬퍼). 모든 OCR 경로 2곳 적용. 검증: 실제 화면 6케이스 인식 텍스트 완전 일치 + OCR 1회 약 220ms→113ms (약 2배). 디스크 쓰기 제거로 SSD 부담·임시 파일 잔재도 사라짐.
- **'권장 창 크기 (OCR 인식 최적)' 기능 추가** (사용자 제안): 게임 창을 OCR 실측 기준의 깔끔한 배율(화면에 들어가면 1908x1076=1.5배, 아니면 1272x717=1.0배)로 즉시 변경. 어중간한 배율(예: 1368=1.0755배)에서 글자가 뭉개져 오독되는 문제('보스'→'니人')의 근본 대책. GUI는 DPI 가상 좌표라 즉시 적용은 DPI 인식 헬퍼로 처리. 이후 체크박스에서 '권장 창 모드' 버튼으로 변경.
- **난이도/탭 사후 검증 실측 보정** (상세 화면 전체 캡처로 정밀 측정):
  - 재클릭 오클릭 버그 수정: '어려움' 재탐색이 '매우 어려움'의 '어려움' 조각을 잡던 사고 → 재클릭은 첫 클릭 좌표만 재사용.
  - 판별 오판 수정: 실측(금색 239,174,66 테두리 dy±16, dx±12) + 임계값 방식(난이도 3/18, 탭 2/8)으로 교체. 검증: 어려움 선택 6/18·나머지 0/18, 함께하기 선택 5/8·혼자하기 0/8.
  - 던전/사냥터 난이도 알약은 미실측(유사 UI 추정) — 안 맞으면 경고만 뜨고 진행.
- **난이도/탭 클릭 사후 검증 추가** (사용자 제공 실측 스크린샷 6장 기반): 선택 강조색 공통 시그니처로 판별, 클릭 후 확인 → 실패 시 1회 재클릭 → 그래도 실패면 경고만 남기고 진행. 난이도 3곳 + 탭 4곳 적용.
- **아침 6시 리셋 체인 완전 자동화 실전 검증 완료** (06:22 로그, 각 단계 1회 시도 통과): NPC 대화 → 출석부 건너뛰기 → 출석 완료 Space → 스텔라 픽 1단계(카드) → 2단계(확정) → 복귀.
- **config 자동 이전(업데이트 마이그레이션) 도입**: exe가 내장 최신 config를 `config.default.json`으로 항상 추출하고, GUI 시작 시 사용자 config의 coordsVersion이 낮으면 최신 config 기반으로 사용자 설정만 옮겨 담아 재생성 (단위 테스트 8건 통과). 워커의 좌표 버전 게이트는 2차 방어로 유지.
- **스텔라 픽 2단계 화면 실측 및 처리 추가**: 1단계(카드 3장) 선택 → 2단계(캐러셀 + 하단 초록 '스텔라 픽' 확정 버튼) 감지·클릭 → 결과 팝업 '확인' 처리로 연결.
- **기술 부채 (1)(2) 정리**: `Wait-ForExitButton`을 `Wait-ForScreen` 래퍼로 통합, 출석 '건너뛰기/확인' 탐색을 `Invoke-EventSkipOrConfirm` 헬퍼로 추출.
- **게임 UI 개편 범위 대응 완전 완료**: ESC 메뉴(아이콘 그리드) / 어비스 선택 화면(배너 3장) / 클리어 문구 폰트 / 던전 잔량 표시 등. 최종 검증: 어비스 2회차 연속 완주(복귀 18단계 포함), 경고·오류 0건.
- **클리어 문구 감지 보강**: OCR이 '면'을 'n'으로 깨뜨리는 사례('화n을터치해주l요') 대응 — '터치해/터치하' 조각 조건 추가 (깨짐 3종 + 오탐 2종 단위 검증).
- **클리어 화면 재시작 분기 추가**: 완료 화면에 멈춘 채 재시작해도 '화면 터치부터 진행'으로 이어감.
- **던전 은동전 잔량 오판 수정**: 잔량 영역 폭 175→225 확장 + 쉼표 제거 파싱 + 6자리 초과 읽기 실패 처리 (결과 화면 26/선택 화면 46 정확히 읽음 실측).

### 2026-07-16 — 당일 작업 모음 (팝업 처리·오류 세트 저장·F9/F10 단축키 등)

- 스텔라 픽 데일리 팝업 처리 추가 (카드 선택 → X 닫기 폴백) + 이벤트 넘기기를 던전/사냥터 시작에도 적용
- 알 수 없는 화면 폴백 개선: 중앙 클릭 5회 후 우상단 X 닫기 + 시도 로그 추가
- ESC 메뉴 UI 개편 대응 (어비스 971,387 실측 반영)
- 오류 시 로그 사본 세트 저장 (`error_시각.png` + `error_시각.log`, 각 20개 보관)
- 전역 단축키 F9(시작/안전 중지)/F10(즉시 중지) + 버튼 표기
- 던전 파티찾기 '우연한 만남' 토글 미해제 버그 수정
- GUI 중복 실행 방지(뮤텍스), 준비 실행 무한 반복 상한(3회), 강제 종료 후 눌린 키 해제 확장
- 소스 전체 점검 후 18건 수정 (캡처 실패 상태 머신 공용화, 안전 중지 신호 소비, 준비 실행 회차 미계상, 컷신 하단 재확인 버그 등)
