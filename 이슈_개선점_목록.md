# 이슈 / 개선점 추적 목록

> **이 파일은 아직 안 끝난 것만 담습니다.**
> 앞으로 만들 기능은 `개발사항.md`, 끝난 것은 `이슈_해결_이력.md` 로 갑니다.
> 해결되면 **이 파일에서 지우고 이력으로 옮깁니다** — '수정 완료' 표기로 남겨두지 않습니다.
> 그래서 체크박스도 쓰지 않습니다. 완료는 체크가 아니라 '이동'으로 표현됩니다.
>
> 오류 분석 자료는 `Log\error_시각.png`(화면) + `error_시각.log`(로그 전문) 세트입니다.

---

## 한눈에 보기

| 구분 | 건수 | 지금 손댈 수 있나 |
|---|---|---|
| **1. 지금 고칠 수 있는 것** | 12 | ○ 원인이 확정됐거나 코드로 결함이 확인됨 — 순서대로 |
| **2. 관측이 먼저 필요한 것** | 5 | △ 실제로 일어나는 걸 봐야 함 |
| **3. 재현 조건이 갖춰져야 하는 것** | 4 | ✕ 파티·계정·타 PC 필요 |
| **4. 개선점** | 11 | △ 전부 우선순위 낮음 |

> **2026-09-17 전수 확인**: 36건을 코드·이력·로그 표본·보관 캡처로 다시 봤습니다(이력 09-17 항목). 원인이 확정된 것은
> 1절로 올리고, 결정만 남았던 5건은 이력으로 옮겼으며, 사실과 다르던 서술을 고쳤습니다.
> 아래의 "로그 N건"은 **개발 PC 로그 폴더의 08-14~09-17 역사 표본**(09-17 10:08 휴지통 복구로 옛 로그가 되돌아온 상태 —
> 이력 09-17 관측 A, 원본 사본은 `실측기록\20260917_Log폴더_덮어쓰기_보존_원본사본\`) 기준이고, 그 표본은 개발 PC 단일 항목
> 커스텀 회차에 쏠려 있습니다 — 파티 모드·자동 부활·토글 클릭·1-3→2층 전환은 **실행 자체가 0건**이라 "0건"은 안전의
> 증거가 아니라 그 경로를 안 밟은 증거입니다.
>
> **2026-09-18 재검증**: 1절 착수 순서를 세우면서 09-17 표 기준 29건(8+5+4+12)의 근거 문장을 소스·config·`tests\`·로그
> 폴더·보관 캡처로 다시 대조했습니다(정적 검증 + 오프라인 OCR 재현, 실기 아님 — 기록은 이력 09-18 항목). 코드와 다르던
> 서술 7곳을 고쳤고('재시도 5회 소진' → 60초 마감 등), 4절에 있던 '복귀 대기 루프 마감 경계'를 1절로 올렸으며, 로그 폴더에서
> 목록에 없던 실사고 3건(점검 티커·서버 끊김·자동 재도전 토글)을 찾아 등재했습니다. 29 → 32건 산식: 타 PC 제보 1불릿을
> 3불릿으로 나누고 옛 '파티 찾기 1바퀴' 불릿을 그중 ② 에 흡수(+1), 콘솔 프레임 이관·점검 이벤트 신규(+2), 4절→1절 이동(±0).
> **현재 `Log` 폴더는 10개 회전이라(09-16 이후만 남음) 역사 표본이 아닙니다** — "로그 N건"은 전부 위 보존 사본(워커 로그
> 233개 = run 170 + error 62 + 현재 회차 1) 기준입니다. 각 항목의 피해는 **관측된 피해 / 코드로 확정된 잠재 피해**를 나눠
> 적습니다 — 안 밟힌 분기를 관측처럼 적지 않습니다.

---

## 1. 지금 고칠 수 있는 것

> 코드 확인·오프라인 재현으로 원인이 확정된 것들. **순서 = 착수 순서.** 등급은 규칙 13 기준
> (A = 그대로 진행, B = 설계 합의 필요). 실기 조건이 있는 것은 설계·진리표까지 먼저 하고 실기는 조건이 될 때.
> A급 4건 중 소스에 닿는 3건(계측·영역 확장·동승)은 한 소스 커밋(빌드 1회)으로 묶되 규칙 11대로 실기 확인된 단위까지만
> 커밋합니다 — 빌드 전제: `$appVersion` 2.1.10 은 released 잠금이라 2.1.11 올림(사용자 확인).

- **계측 2줄** (A): ①**워커 시작 로그에 창 정보 1줄** — 무조건 남는 시작 줄(`[준비] 게임 확인`·`[설정] …`)에는 실측
  창 크기가 없습니다. 실측 WxH 는 창 정렬이 실제로 일어난 회차의 `[준비] 게임 창 정렬(…)`(14380, `MoveWindow` 를 부른
  회차만) 과 진단 캡처의 `[진단]` 줄에만 남습니다(보존 표본 233개에 정렬 줄 0건). 08-13(DWM 스트레치/네이티브 1908)·
  09-11(RDP 150%) 두 실사고가 캡처 픽셀로만 판별됐습니다. → 창 정렬 직후 `[준비]` 줄에
  `창 WxH, 제목줄 Npx, 창 DPI/모니터 DPI` 1줄. 판정에 쓰지 않습니다. 컨트롤 패널 로그는 파일에 남지 않으므로 **워커
  로그**여야 제보 분석에 닿습니다. (09-17 개정에서 옛 4절 'DPI 확대 창 감지·안내' 항목을 이 계측 1줄로 좁혀 흡수)
  ②**생활 시작 팝업 가드 소진 로그 1줄** — 2절 '팝업 가드' 항목 참조. `$initialPopupRounds` 가 10 에 닿는 순간 기존
  메타(라운드 수·클릭 전송/생략 사유) + 마지막으로 처리한 분기명(세 분기가 `$popupHandled` 만 세우므로 지역 변수 1개 추가)
  1줄. 소진을 말하는 문구가 코드에 없어 지금은 grep 으로 발생률을 알 수 없습니다.
  **'동작 불변'은 판정에 안 쓴다는 뜻이지 비용 0 은 아닙니다**: 창 WxH 는 기존 `GetWindowRect` 값으로 즉시 되지만 **제목줄
  px·창 DPI 는 워커 DllImport 목록(2188~2257)에 없는 P/Invoke**(`GetDpiForWindow` 는 Win10 1607+ 전용, `GetClientRect` —
  DPI 인식 *설정* 3종은 이미 있음)가 필요하고, 창 정렬 블록은 최상위 try 안이라 가드 없는 예외는 그 PC 의 모든 회차를 시작
  직후 코드 1 로 끝냅니다 — 2266~2277 의 DPI 컨텍스트 try/catch 선례처럼 감싸 실패 시 '측정 불가' 로 적거나, GUI
  `WindowProbe` 가 쓰는 `GetDeviceCaps`(gdi32) 방식으로 대체. 개발 PC 실기 1회로 출력 확인 뒤 커밋.

- **타 PC 제보(2026-09-15, 1272 창) ① '매우 어려움' 미탐 — 난이도 알약 영역 상단 확장** (A): 입장 감지 실패(추적기 영역
  상단 잘림)는 v15 영역 확장으로 고쳤고(이력 09-15), 이 건은 보관 캡처를 워커와 같은 영역·배율로 오프라인 재현해 원인을
  확정했습니다(이력 09-17 관측 C, 09-18 재현 일치). 난이도 알약 영역 `$rgDifficultyTabs` [30,150,500,60] 의 **상단 잘림** —
  제보 캡처(`error_20260915_h02m48s43.png`)의 알약은 y=136~162 라 영역 상단 150 이 글자 한가운데를 지나고, 개발 PC 는
  y=163~195 라 온전히 들어갑니다. 현행 영역 판독은 빈 문자열, 위로 **확장**한 [30,120,500,90] 은 두 PC 캡처 4장(제보 1장 +
  `실측기록\20260916_어비스상세_제목판독_탭별\` 3장) 전부 '입문 어려움 매우 어려움'(지옥1 은 3_혼자하기 제외). 30px
  **이동**만 하면 개발 PC 캡처가 제목 조각으로 깨집니다(09-17 '01 惢『므 이-', 09-18 재현 '61상의징박지' — 문자열은 재현마다
  다르나 결과는 같음). 4절 '매우 어려움 판독 폴백 확장'(`Select-DgDifficultyWord`, 던전·심층 경로)과는 **함수도 원인도 다른
  자리** — 어비스 상세는 `$selectDungeonDifficulty` 블록(14647~)의 `Find-GameTextPoint` + `$rgDifficultyTabs`(소비처 3곳
  전부 이 블록 안, GUI·tests 참조 0건). 관련 값·판정식은 v2.1.8 → v2.1.10 사이 변경이 없어 그 PC 에서는 지금도 같은 증상이
  날 것으로 예상하지만, **제보자가 v2.1.10 으로 다시 돌린 로그는 아직 없습니다**.
  **피해**: 관측된 것은 일반 반복의 `[경고] … 현재 선택된 난이도로 진행`(제보 로그 9회차 중 3회차 — 02:46·02:47·03:19 —
  에서 관측, 각 회차 1/50 설정; 캡처엔 '매우 어려움'이 이미 선택돼 실피해 0). 코드로 확정된 잠재 피해는 일반 반복의
  **오난이도 진행**(다른 난이도를 골라 둔 채 50회)과 커스텀의 **코드 4 정지**(14702~14705) — 둘 다 관측 아님.
  → **A**(좌표 상수): 영역 확장 + coordsVersion 15→16(config·워커 상수 동반, `tests\test_audit_fixes.ps1` 524~528 의
  리터럴 15 단언도 함께 — 안 올리면 회귀 전체가 exit 1). **coordsVersion 증분은 전 사용자 config 이전 = 커스텀 진행 기록
  초기화 1회**(v2.1.10 이 15 를 일부러 유지한 비용) — 아래 '타 PC 제보 ②' 가 config 좌표 값을 바꾸지 않는 탐색형이면 증분은
  이 한 번뿐. 진리표는 **캡처 4장으로는 제목 오탐을 검증하지 못합니다**(4장 모두 제목이 단어로 안 잡혀 '제목 조각을 오탐하지
  않는지' 조건이 공허하게 통과) → 단어 목록 수준의 모의 진리표(제목 단어 — 특히 '매우'/'입문' 을 포함하는 던전명 — 와 깨짐
  문자열 포함)로 하고, 기대값은 EXACT 가 아니라 **PARTIAL '매우'**('매우어려움' 키는 OCR 이 두 단어로 쪼개 ExactText 가 항상
  실패하고 3732~3736 의 위치 게이트 없는 Contains 폴백(읽기 순서 첫 단어)만 탑니다 — 확장으로 위쪽 띠가 먼저 읽히므로 그
  띠의 글자가 클릭 표적이 될 수 있는 경로가 규칙 8 의 '새 경로'). 규칙 13 문면상 A 지만 같은 부류의 v15 선례(설계 합의 A안 +
  리뷰 + 보관 캡처 전수 재생)를 따라 **구현 리뷰 1회**는 붙입니다. 실기: 개발 PC 어비스 상세 1회 + 제보자 v2.1.10 재실행 로그.
  ※ 알고 두기(관측 0건, 규칙 8 보류): 키 '어려움'은 읽기 순서 첫 EXACT '어려움' 단어를 채택하므로 '어려움' 알약이 깨지고
  '매우 어려움' 알약의 뒷단어만 정상이면 '매우 어려움' 알약을 누릅니다 — `Confirm-DifficultySelected` 주석(6661~6664, 07-17
  실측)이 재탐색 금지 사유로 든 기전이 첫 탐색에는 그대로 남아 있음.

- **다음 소스 커밋 동승 — 주석·문구 정정 3곳 + 거짓 로그 3곳 조건부 + '다음 층으로' 현행 단언 1건** (A, 전부 동작 무관):
  ①`build/build_exe.ps1` 48(주석)·50·56(throw) '저장소에서 복원하세요' → '개발 PC 보관본' (build/ 는 미추적이라 저장소 사본이
  없음 — 사용자 결정 2026-08-01; CLAUDE.md 로컬 전용 절의 '추적 중' 꼬리도 제거). **①과 CLAUDE.md 는 미추적이라 커밋에
  실리지 않는 로컬 편집**이고, throw 문구는 빌드 게이트라 오타는 빌드가 잡습니다. ②`mabinogi_run_once.ps1` 1378·7773 주석
  `$ndStagePoints`(07-24 폐기, 정의 0건) → `$dgSelStagePoints`(907) ③`mabinogi_run_once.ps1` 9045~9049 주석이 옛 항목명
  ('Get-GamePixel 캡처 실패 플래그 대칭')을 인용 — 결정("대칭 미도입: 성공 등록은 빈 프레임 오해제, 실패 등록은 표본×재시도
  곱셈 — 앞선 OCR 판독의 플래그로 동결을 판단하는 현행 유지, 09-16 수정이 이 비대칭을 알고 수용")을 현행 문구로. **이 결정
  문장은 이력에 없으므로**(v1.2.1 보류 목록에 이름만) 이력에 먼저 기록하고 주석에 그 날짜를 적습니다. 9049 의 AI 도구 이름
  표기는 규칙 14 로 같이 제거. ④**거짓 로그 3곳** — 4절 '팝업 공용 4곳' 의 A급 부분: 보상 화면(6134)·협동 완료(6077)는
  전송 여부와 무관하게 '확인 클릭' 을 기록하고, 협동 완료의 **좌표 폴백(6087)은 클릭(6089) 전에 '실측 좌표로 클릭' 을
  기록**합니다(`Click-GamePoint` 는 `Click-ScreenPoint` 위임이라 메타는 세팅됨). 셋을 `$script:lastClickPerformed` 조건부로 —
  반환(`Start-Sleep` 후 `$true`)과 호출부의 메타 판독은 그대로라 로그만 바뀝니다. 문구 리터럴은 유지(`tests\test_deep_dungeon.ps1`
  344 가 존재 단언), `tests\test_after_entry_popup.ps1` 52 는 문구~`return $true` 120자 근접 단언이라 if 로 감싸는 모양에 따라
  갱신. 6122 주석의 도구 이름도 규칙 14. ⑤4절 '다음 층으로 완료 판정' 의 현행 조건
  (9370 `Contains('구역') -or Test-DgSelectionTitle`)을 고정하는 회귀 단언 1건(tests 에 없음 — 표류 감지용, 본체 착수 때 교체
  대상). 변이 검증 1회. 러너 `$expectedTestCount` 83→84.

- **콘솔 카드 진단 프레임 이관** (A, 파일 이동만): 2절 '콘솔 카드 글자 렌더' 의 자료 정리 부분.
  `실측기록\20260917_Log폴더_덮어쓰기_보존_원본사본\` 의 carddiag 111장 중 정식 폴더가 없는 것 — **09-12 10:03~10:13 set-fail
  10장**(어느 문서에도 없던 세션), **09-07 17:11~17:19 전체 프레임 10장**(set-fail 6·fallback-before 4 — 전체 프레임은 전부
  상이하나 카드 ROI 는 3세트가 각 3장씩 동일, 고유 ROI 4; roi 60장 동거), **09-07 13:21~13:22 1908 창 세션 3세트**(set-fail
  2·stop 1, roi 포함 21장 — 09-17 이관 목록에 빠져 있었음, `던전이미지\던전\20260907_*` 와의 대응 미확인), **09-17 00:04:58
  set-fail·00:04:59 stop(1272)·05:46:40 stop(1024x577) 3장**. 09-14 는 `20260916_콘솔카드_선택됨깨짐_프레임12장\`(실제 프레임
  10장 + 스윕 txt 2)에 이미 있고 그중 3장(17:46:56·17:47:04·22:17:14)은 보존 사본에도 없는 유일본이며
  `tests\test_dg_card_console_offline.ps1` 19~25 가 이 경로를 직접 참조합니다 — 옮기지 말 것. 이관 뒤 2절 항목의 경로를 갱신.

- **게임 점검 이벤트가 회차를 코드 1 체인으로 끝냄 — 관측 3건, 목록 신규(2026-09-18)** (B): 개발 PC 09-16~09-18 오류 세트
  10건 중 점검 기인 7건(① 4 + ② 1 + ② 후속 재시작 2) + 정황 1건(16:11), 별건 2건(09-16 19:43 ×2 심층 알약 이형). 이틀에 점검
  3회 — 09-17 06:00·16:10, 09-18 05:55. 작성 시점 Log 의 08:55~ 회차 10건은 전부 정상(회전으로 소실 — 보존 안 함). 현재
  피해는 코드 1 → GUI 재시작 → 같은 오류로 **오류 세트가 회차마다 2~3개씩 쌓이는 것**(점검 시작 뒤엔 어차피 진행 불가).
  ①**점검 안내 티커가 옵션 화면 난이도 알약을 덮음** — `error_20260917_h16m08s46/54`(v2.1.10, 1908x1076)·
  `error_20260918_h05m41s31/38`(1272x717): 캡처에 "[점검 안내] 9/17(목) 16:10부터 …" / "9/18(금) 05:55부터 …" 티커가
  일반/어려움 알약 위를 지나가고, `옵션 화면에서 난이도 '어려움' 글자를 3회 모두 찾지 못했습니다 → 선택을 확정하지 못했습니다`
  → 코드 1 → GUI 재시작 → 같은 오류(체인 2건, 첫 건은 직전 양보 20초+46초). `Test-GameRestartPopup`(6096~)이 잡는 '너무
  오랫동안 실행' 팝업과 **같은 자리·같은 증상**이지만 매칭이 '오랫동안'+'실행되' 라 티커는 범위 밖. (09-16 19:43 ×2 는 심층
  '어려움' 알약 이형 — 4cce8ef 처리, 캡처에 티커 없음 — 별건.) 2절 콘솔 05:46:40 프레임에도 06:00 점검 티커가 주 판독 영역
  우상단과 겹쳐 있었습니다.
  ②**서버 끊김 모달(ERROR 83)이 클리어 대기 위에 떠 600초를 소진** — `error_20260918_h06m04s25`(1272x717, 던전 1-2 우연한
  만남·solo): 05:54:25 클리어 대기 시작 → **600초 동안 로그 0줄** → '던전 클리어 화면 감지 대기 시간이 초과됐습니다' 코드 1.
  캡처는 던전 안(추적기 '던전 클리어 1층 2구역') 위에 '게임 서버와 연결이 끊어졌습니다 … (ERROR 83)' + [Space] 확인 모달 —
  05:55 점검 시작과 일치. ERROR 83 감지는 **생활 경로에만** 있고(12130·12156·13104·13357·13437·13534 — 생활 선례)
  `Wait-ForDungeonClearScreen` 은 모르므로 전투 연장 판정도 실패해 한도를 다 씁니다. 이어진 GUI 재시작 2회(06:05:49·
  06:07:13)는 `이벤트 화면 자동 넘기기가 끝나지 않았습니다`(Clear-EventOverlay 20라운드 소진) +
  `던전 화면이 아닙니다 (제목 영역 OCR: '00병약이소녀')` 로 코드 1 → 정지. 진단 OCR 3영역 전부 빈 문자열.
  ③**결과 화면 '자동 재도전' 토글+말풍선 때문에 '다시 하기' 가 안 눌린 정황** — 3절 '던전 파티 찾기 다시 하기' 항목에
  기록(9590 throw, 16:10 점검 시작과 겹침).
  ④**점검 뒤 게임을 다시 켠 타이틀 화면에서 시작 → 팝업 넘기기 20라운드 → 코드 1 체인** (2026-09-18 18:40~18:44 추가 관측) —
  `error_20260918_h18m40s59/h18m42s30/h18m44s03`(v2.1.10, 커스텀 던전, 창 1276x719): 타이틀 화면을 `Clear-EventOverlay` 가
  NPC 대화·알 수 없는 화면으로 보고 중앙 클릭·X 후보를 20라운드 돈 뒤 `던전 화면이 아닙니다 (제목 영역 OCR: 'NEXDN')` 코드 1
  → GUI 재시작 → 같은 오류 ×3. 진단의 하단 문구 OCR `게임 시작 @2025 NEXON Korea Corp. & devCAT CO., LTD. …` 가 타이틀
  화면의 확실한 신호 — `Test-KnownScreen` 에 등록해 시작 게이트에서 조건부 정지(코드 4)로 돌릴 후보(규칙 '새 시작 화면은
  `Test-KnownScreen` 에 등록'). 로그 폴더는 10개 회전이라 이 세트 3건이 들어오며 09-16 19:43 등 앞 세트가 밀려났음.
  → 관측 2건+캡처 2장(①)·1건+캡처(②)·3건+캡처(④)라 규칙 8 충족. 설계 합의: 티커/서버 끊김을 **조건부 정상 정지(코드 4, `[완료]`
  사유)** 로 돌릴지, 대기 후 재시도할지. 티커는 옵션 화면 난이도 3회 재시도 루프(`Set-DgOptionDifficulty` 6730~6738) 안에서
  감지(문구 '점검' 조각 — 오탐 검증은 캡처 2장 + 티커 없는 옵션 캡처), ERROR 83 은 생활 경로 감지의 전투 경로 이식(같은
  문구·같은 모달 — 클리어 대기 루프에 넣으면 규칙 15 동결·`Test-SafeStopDuringCaptureFail` 계약을 같이). 실기는 다음 점검
  공지 때(공지는 사전에 뜸).

- **타 PC 제보 ③ `Test-AbyssMenu` 가 상세 화면을 ESC 메뉴로 오판 + ④ 네이티브 1908 메뉴 '어비스' → '어H|스'** (B, 한 묶음;
  원인 재현: 이력 09-17 관측 C, 관련 판정식은 v2.1.10 까지 변경 없음 → 그 PC 에서 재발 예상):
  ③**"ESC 메뉴로 오판해 엉뚱한 자리를 눌러 상세가 열림"은 서술이 반대**: 직전 회차(`run_20260915_h02m47s58`)가 입장 클릭을
  보내지 못한 채 끝나 **상세 화면이 남아 있었고**, `Test-AbyssMenu`(5004~)가 `$rgAbyssMenu` (850,180,350,520) 판독문의
  `Contains('어비스') -or Contains('비스')`(+2차 신호 `Test-MenuExitLabel`)로 전리품 패널 헤더를 ESC 메뉴로 오판했습니다.
  클릭 (901,462) 는 **'주요 전리품 / 남은 클리어 전리품 0/3 회' 헤더 줄**(기준 y 461 — '어비스 전리품' 헤더는 y≈506; 클릭 전
  캡처가 없어 추정)이고 5초 뒤 화면은 상세 그대로(무해), 뒤이은 ESC 가 '<' 역할을 해 02:48:48 선택 화면 복귀 → **코드 10
  준비 실행 13초, 회차 손실 0**. 재현 확률은 낮습니다(제보 캡처 원점 ±6px 스윕 169조합 중 19건 = 11%, 개발 PC 캡처는 헤더
  글자가 깨져 안 걸림). 유발 상태(중단 뒤 상세 화면 잔존)는 제보 폴더 9회차 중 4회차가 사용자 중지로 상세/매칭 중 끝났지만
  실제로 상세를 남긴 것은 h02m47s58 1회차(매칭 대기 중 중지 3회차는 입장 클릭 뒤라 필드에 남음) — **관측 1건**, 중단 위치가
  상세이면 PC·창 크기와 무관하게 재발. **코드로 확정된 최악 갈래(미관측)**: 감지는 '비스' 조각으로도 참인데 복귀 클릭 탐색
  (`Return-ToAbyssSelection` 10437, `Find-GameTextPoint -SearchText '어비스' -ExactText '어비스'`)은 '어비스' 포함 단어를
  요구해 **비대칭** — '비스'만 읽히는 프레임에선 감지 참·탐색 실패가 반복돼 60초 마감 throw(10659) 코드 1.
  ④**1908 창 메뉴 '어비스' → '어H|스'** = 캡처(`error_20260915_h03m18s00.png`, 1908x1076, ESC 메뉴 화면)의 제목줄이 31px 라
  **네이티브 1908 창**(DWM 스트레치 47px 아님 — 옛 'DPI 확대 창 계열' 꼬리표는 틀림). 같은 영역·배율로 재현하면 배율 3
  '이d|스', 4~8 '어H|스', 흰 글자 이진화도 실패 — 배율 사다리·이진화로는 못 고칩니다('비' 의 ㅂ+ㅣ 가 갈라지는 자소 단위
  깨짐). 실패 지점은 `Test-AbyssMenu`(감지 성공 로그 있음)가 아니라 `Return-ToAbyssSelection` 의 클릭 대상 탐색. **재시도
  횟수 상한은 없습니다** — 2초 간격 재시도가 60초 마감(HUD 30+메뉴 15+선택 15, 10381; 카운터 10440~10444 는 진단 캡처
  1회용)까지 이어진 뒤 '어비스 선택 화면 복귀 대기 시간이 초과됐습니다.' throw(10659) → **코드 1 + 오류 세트**(옛 '재시도
  5회 소진' 은 제보자가 03:18:10 마우스 이동으로 끊은 지점이지 코드 한계가 아님). 범위는 시작보다 넓음 —
  `Return-ToAbyssSelection` 은 클리어 후 정상 복귀(15207)에도 쓰여 **그 PC 가 1908 이면 첫 판 뒤 매 회차 복귀에서 코드 1 =
  어비스 자체가 불가**. 개발 PC 네이티브 1908 은 08-13·09-15 실기에서 같은 경로를 통과했으므로 **제보 PC 렌더에 한정된
  관측**(제보자는 1분 안에 1272 로 돌아가 03:19 회차는 (1029,432) 즉시 탐지). 같은 PC 가 1272 에서는 정상.
  → **B**: 시작 디스패치(14486~14495 — `Test-AbyssMenu` 가 상세 제목 판정 14603~ 보다 앞)에 상세 화면 선판정, 또는
  `Test-AbyssMenu` 의 '비스' 단독 감지 폐기 — `Return-ToAbyssSelection` 의 같은 판정(10428)과 2차 신호 `Test-MenuExitLabel`
  경로를 범위에 넣고 함께. ④는 메뉴 영역 한정 '어비스' 이형 조각을 **클릭 탐색 쪽**에 넣되, 오탐 검증(상세 화면을 메뉴로
  인정하지 않는지)은 **네이티브 1908 상세 화면 캡처**로만 됩니다 — 지금 없음(제보 1908 캡처는 ESC 메뉴, 개발 PC 상세 캡처는
  1272). 개발 PC 실기 = **네이티브 1908 로 상세 화면을 열어 둔 채 시작**(③ 선판정 통과 확인 + ④ 오탐 검증 재료 동시 — 개발
  PC 는 08-13·08-15 네이티브 1908 실기 이력이 있음). 오판 자체의 재현은 제보 캡처 스윕(11%)이 담당. 판독문을 진리표로 저장.
  캡처·로그: `던전이미지\실측기록\20260915_타PC_어비스_입장미감지_트래커상단잘림\`(캡처 3장·로그 10개 = run 9 + error 1;
  현재 회차 사본 `mabinogi_run_once.log` 는 error 회차와 동일).

- **타 PC 제보 ② '우연한 만남' 토글 탐침이 배경을 'off' 로 확신 + 어비스 '파티 찾기' 1바퀴 실기** (B; 원인 재현: 이력 09-17
  관측 C): 탐침 `$ptAbyssChanceToggle` (1208,339) 이 **배경**을 찍음(제보 캡처 RGB (47,39,69) — 합 155 > 45 라 검정도 아니라
  6569 `return 'off'`; 같은 캡처 (1208,406~420) 이 초록 = 토글 실제 ON). 전리품 패널이 1단(광기의 동굴)이면 토글 y≈406~423,
  2단(허상의 정박지)이면 y≈330~352 — **던전별 레이아웃 차이**라 고정 좌표 하나로는 못 맞춥니다.
  **피해는 매칭 방식별로 다릅니다**(옛 서술 '켜진 채 오입장 — 파티 찾기' 는 틀림):
  · 우연한 만남(제보자 모드, 관측): 매 회차 'off' 오판 → (1208,339) **빈 배경 블라인드 클릭**(4회차 중 조작 생략 1회 제외
  3회차) → 0.9초 뒤 재판독 'off' → `[경고] … 켜진 것을 확인하지 못했습니다 - 현재 상태로 진행` → 실제 ON 이라 입장 정상
  (02:47:57·02:49:44). 실제 OFF 였다면 09-16 '동료가 필요합니다' 팝업 복구가 받음. 무해하지만 규칙 4(상태 기반 클릭)의 상시
  위반이고, 다른 1단 던전(흩어진 물길)에서 그 좌표에 무엇이 있는지는 미확인.
  · 파티(파티장), 실행 0건(보존 표본 어비스 11회 중): 'off' 오판 → 14829 `토글 꺼짐 확인 - 짠 파티 그대로 입장` → 토글 ON 인
  채 입장(**빈자리에 모르는 사람 합류** — 오입장급).
  · 파티 찾기, 실행 0건(보존 표본 어비스 11회 중): 15033 `[어비스] '우연한 만남' 토글 꺼짐 확인 (파티찾기 준비)` 뒤
  `Invoke-ClickUntil -Point $ptPartyFind`(836,655) — 제보 PC 1단 기하에서는 넓은 '입장하기' 단일 버튼이 x=850 부터라 (836,655)
  는 **버튼 밖 14px** → 클릭 무효 30초 → 대기 초과 throw 코드 1(오입장 아님). 개발 PC 2단 캡처는 x≤760 부터 버튼이라 안쪽 —
  코드 주석 14969 의 '잘못 누르면 우연한 만남으로 입장' 은 2단 기하에서만 참인데 2단에선 탐침이 정상.
  → **B — 던전 경로의 자기앵커 이식**: 던전은 08-13 같은 실사고(네이티브 1908 고정점이 토글 밖)로 고정점을 폐기하고
  `Find-DgChanceTogglePoint`(6527~, '우연한 만남' 라벨 앵커 + 미검출 fail-closed, 고정점 폴백 금지)로 이미 합의·실측했고
  어비스는 '실측 없어 무변경'(1358~1361) 이었을 뿐입니다. 이제 실측(1단·2단 y)이 있으니 라벨 영역·오프셋만 어비스 상세로
  잡아 이식 — 판독·클릭 좌표를 함께, 양보 뒤 재탐색, 못 찾으면 클릭 안 함(그 뒤 경고 진행/정지는 매칭 방식별로 합의).
  (대안이던 우측 열 초록/회색 알약 스캔은 던전 선례 이식으로 대체 — 설계 합의 때 재론.) **탐색형이면 config 좌표 값을 안
  바꾸므로 coordsVersion 증분 불필요**(키를 없애면 configSchemaVersion +1 — 규칙 5 후단). `tests\test_audit_fixes.ps1` 702~703
  '어비스 고정점은 무변경(실측 없음)' 단언은 전제가 깨졌으니 던전 선례(696~699: 고정점 사용 0곳 + 앵커 헬퍼 ≥2곳) 형태로
  갱신, `tests\test_abyss_chance_toggle_yield.ps1` 58 의 (1208,339) 모의 사본도 함께. 파티장 분기(14815)도 같은 탐침이라 같이
  바뀝니다 — 그래서 아래 '파티장' 2인 실기는 이 건 뒤.
  **실기·배포 게이트 = 개발 PC 우연한 만남 경로 1단·2단 던전 각 1회**(광기의 동굴 상세를 열어 토글 y 와 **하단 버튼 좌우
  경계**(토글 켬/끔 두 상태의 파티 찾기·입장하기)를 함께 측정 — 위 '타 PC 제보 ③④' 의 상세 화면 세션에서).
  **파티 찾기 1바퀴**는 같은 날 하면 좋지만 게이트가 아닙니다(사람 많은 시간대에 배포를 묶지 않음) — 파티 찾기 경로는
  **실기 완주 검증이 한 번도 없습니다**: 07-22 는 우연한 만남·혼자하기·파티(파티장) 3종만, 07-31 은 전 단계까지(매칭 인원
  부재). 그 뒤 분기 수정 2회(07-31 토글 fail-closed, 09-09 양보 배치). 파티 찾기 항목만 든 리스트(입문 1~2개), 사람 많은
  시간대, 토글 판정 직후 상세 화면 캡처와 로그 대조. 통과면 07-22 이력에 줄 추가 후 이 문단 삭제. (09-17 3절에서 이동 —
  개발 PC 단독으로 가능)
  → 제보자에게 **v2.1.10 으로 한 판 돌린 Log 폴더 + 상세 화면(함께하기 탭) 캡처 + 게임 UI 크기·모니터 해상도·Windows
  배율**을 요청합니다 — 이 로그로 확인되는 것은 **①② 재발 여부와 입장 감지 v15 통과**(④는 1908 로 돌린 회차가 있을 때만;
  2절 '제목이 안 읽히는 옵션 화면' 은 던전 옵션 화면 문구라 어비스 로그로는 확인 불가 — 08-12 제보자와 동일인이면 던전 회차
  로그를 함께).

- **행동불능 거점 부활 — 조작 생략이 20초 상한을 소모** (B): `Wait-ForDungeonClearScreen` 의 거점(캠프파이어/여신상)
  분기(4550~4595)는 클릭 뒤 `$script:lastClickPerformed` 만 보고 `$script:lastClickSkipReason` 을 읽지 않아, **사용자 조작으로
  생략된 클릭도 20초 전송 상한 시계를 돌립니다**(4589 `AddSeconds(20)`) → throw '거점 부활 입력을 20초 동안 한 번도 전송하지
  못했습니다'(4591, 코드 1 — 일반 반복이면 자동화 전체가 즉시 정지, 커스텀은 재시작 경로). 규칙 4("user-active 생략은 예산을
  소모하지 않는다") 위반이 코드로 확정된 자리입니다. **발동 조건은 옛 서술('커서가 게임 창 위에서 조작')보다 넓습니다**:
  `Test-UserRecentlyActive` 는 GetLastInputInfo(2432~2434) 기반 **시스템 전역 입력**이고(`Wait-UserYieldEnd` 2684 주석) 이
  경로(사망 판독→앵커 탐색→클릭)에는 클릭 전 양보(`Move-CursorOutsideGame`)가 없어, 사망 화면(남은 부활 0회/가루 부족)이 떠
  있는 동안 **다른 창에서 타이핑·마우스 이동만 20초 이어져도** 매 회전 생략이 반복돼 throw 됩니다 — 같은 PC 로 다른 작업을
  하는 실사용 조건. R키 경로는 `Press-KeyVerified` 가 조작을 안 보므로 생략 자체가 없고(R 경로의 20초 throw 4611 은 전면 확인
  실패용), 전멸 경로는 상한이 없습니다(아래). 캡처 실패 중엔 사망 판독이 빈 문자열이라 4490 이 시계를 **초기화**합니다(동결이
  아니라 초기화 — 오발동 없음, 현행 유지). 자동 부활 0건은 '사망 0회 표본'(현 폴더 던전 21회차 부활 켜짐, 사망 없음)이라
  발생률 근거가 아니고, 제보 PC 는 revive.enabled=false 라 이 경로 밖.
  → 설계 합의(사유 3갈래: user-active 는 상한 미설정·미전진 후 재감지 / cursor-not-ready 만 20초 소모 / 캡처 실패는 현행
  4490 초기화 유지 / 로그에 사유 표기). 같은 루프의 구매 팝업(4444~4463)·컷신(4701~4709) 3갈래는 **로그 문구만** 가르는
  선례이고(09-18 코드 확인: 두 분기 모두 사유별로 시계·예산을 나누지 않음), 사유로 시계·예산을 가르는 가장 가까운 선례는
  어비스 토글 루프 15002~15004. → `test_wipe_revive` 갱신: 150~153 의 개수 단언(throw 문구 2·`AddSeconds(20)` 2)은 리터럴이
  그대로면 안 움직이므로 **수정 자체를 지키는 새 단언**(user-active 분기가 AddSeconds(20) 앞에 있는지) + 변이 검증. 106행
  `$workerSource` 는 주석 포함 원문이라 throw 문구·AddSeconds(20) 을 주석에 인용하면 개수가 조용히 어긋남 — 116행
  `$anchorCode` 처럼 주석 제거 사본으로. 실기는 **가루 0 의도적 사망(재화 — 사용자 승인 뒤) + 부활 프롬프트 뒤 20초 이상
  의도적 마우스 조작**(user-active 갈래; cursor-not-ready 갈래는 정상 세션에서 못 만들어 진리표·변이만) — 3절 '부활 오계상
  실기 확인' 과 같은 세션.

- **전멸 재도전 — 커서 미확인에 상한이 없어 원인 문구가 뭉개짐** (B): 전멸 분기(4491~4543)에는 20초 상한이 없어 클릭이
  cursor-not-ready 로 계속 생략돼도 재시도만 하다가 판 대기 한도(전투 연장 시 최대 `$extendLimit` = max(한도×3, 1800초),
  4325 — 연장은 `Test-CombatStillRunning` 이 전멸 화면에서 참일 때만이고 캡처로 확인된 바 없음; 거짓이면 기본 한도에서 같은
  문구)에서 **'던전 클리어 화면 감지 대기 시간이 초과됐습니다'**(4359) 로 나와 원인을 오해하게 합니다. 사용자 조작
  (user-active) 생략에 상한이 없는 것은 규칙 4 계약대로라 결함이 아닙니다 — 결함 범위는 cursor-not-ready 뿐. → 위 거점 건과
  **한 변경·한 진리표·한 실기**: cursor-not-ready 만 20초 상한 공유 + 전용 문구, `test_wipe_revive` 개수 단언 2→3.

- **파티원 나가기·복귀 — 남은 한계는 HUD 대기의 재클릭 부재** (B): 옛 "이미 해결됐을 수 있다" 유보는 **틀렸습니다**.
  `Invoke-AbyssPartyMemberCycle` 4단계의 나가기 클릭은 v2.1.11(2026-09-18 보상 화면 3버튼 핫픽스)부터 `Invoke-AbyssExitClick`
  헬퍼가 담당해 **전송 확인·커서 미확인 3회 재시도·조작 양보 뒤 재판독**은 해결됐습니다(이력 09-18). 남은 것은 헬퍼가
  'skipped'(커서 미확인 3회)·'gone'(선택 화면·ESC 메뉴로 판정)을 돌려줘도 이어지는 필드 복귀 `Wait-ForScreen`(homeEndEscHud
  30초 — 양보 차분·캡처 실패 동결은 반영)이 **재클릭 없이** 초과 throw → 코드 1 로 끝나는 구조(헬퍼 호출 10486 →
  `Wait-ForScreen` 10487, v2.1.11 기준 — 이름으로 찾을 것). 파티원은 커스텀 미지원(GUI 매칭 라디오 3종·워커 파서 183)이라 일반 반복 즉시 정지 +
  파티장 준비 대기 연쇄 차단. 08-01 수정은 `Return-ToAbyssSelection` 한정이라 무관합니다(파티원 사이클은 그 함수를 호출하지
  않고 복귀·계상이 4단계 안에서 끝남). 부수: '준비 완료' 클릭도 전송 미확인으로 `$readyClicked=$true` — 다음 회전의 상태 기반
  재클릭은 되므로 실효는 로그 정직성과 안전 중지 소비 게이트가 꺼지는 것. 사용 빈도는 표본으로 알 수 없습니다(보존 표본 어비스
  11회 중 파티원·파티장 회차 0 — 현재 Log 폴더 10개 회전에는 어비스 회차 자체가 0). → HUD 대기를 `Invoke-ClickUntil`(조건
  `Test-HomeEndEscHud`, 원천 조건 나가기 버튼 — 헬퍼의 글자 위치 재탐색과 결합) 위임(09-10 뒤로가기 패턴) + '준비 완료' 전송
  확인을 설계 합의 → AST 진리표 + 변이 → **2인 실기는 아래 파티장 건과 같은 세션**(성립 시 — 실기 대기가 다른 작업을 막지
  않음).

- **파티장 입장 대기 — 30초 마감이 생략·양보·동결을 모름** (B): 첫 클릭(14840~14842)도 전송 여부를 읽지 않습니다. 30초
  루프(14844~14867, 하드코딩) 안에는 버튼이 '입장하기' 로 남아 있으면 3초마다 상태 기반 재클릭(14862~14866, 이것도 전송
  미판독)이 있어 **짧은 조작은 흡수됩니다** — 결함은 30초 마감이 클릭 생략 사유·양보 차분·캡처 실패 동결
  (`Test-SafeStopDuringCaptureFail` 포함)을 반영하지 않고, 만료 뒤 **재클릭 없이 경고만 남기고**(14868~14870) 공용 매칭 대기
  300초(partyMatching, `Wait-ForScreen` 이라 이쪽은 양보·동결 반영)로 넘어가 코드 1 로 끝나는 구조입니다. **추가 결함 후보
  (2026-09-18 신규)**: 같은 30초 루프의 14851 `Press-KeyOnce -VirtualKey 32`(인원 부족 '도전하기' Space)는 전면 확인 없는 키
  입력이라 08-11 ④ 계약(`Press-KeyVerified`) 밖이고, 판정 문구 '도전하' 는 하단 `$rgClearExit` 광역 판독. 위 '타 PC 제보 ②'
  가 파티장 분기의 탐침(14815)도 바꾸므로 2인 실기는 그 건 뒤. → 설계 합의(사유 3갈래, 30초 마감을 `Get-YieldAdjustedDeadline`
  로 연장, 만료 시 '경고 진행' 유지 여부, Space 키 계약) → 파티장 절 AST 점소싱 진리표 + 변이 → 2인 실기 전 배포는 09-10
  선례대로 진리표 기준.

- **복귀 대기 루프들의 캡처 실패 마감 경계 — 형제 3곳이 throw** (B, 4절에서 이동 2026-09-18): 마감 직전 회전의 판독이 캡처
  실패 플래그를 세우면 다음 while 조건(`(Get-Date) -lt Get-YieldAdjustedDeadline …`)이 먼저 끝나 복구 탐침·안전 확인 0회로
  종료합니다(09-14 설계 검토의 모의 재현). 마지막 판 확인 루프(9176~9273)는 exit 0 + 경고로 끝나 부정 판정 확정은 아니지만,
  **형제 루프 3곳은 같은 경계에서 throw(코드 1)** — 9419 '다음 층으로' 대기 초과 / 9590 '다시 하기 → 진입 옵션 화면' 대기
  초과 / 10251 사냥터 첫 화면 미확인. 규칙 15 ③(부정 판정·throw 확정 금지)·④(`Test-SafeStopDuringCaptureFail`)·⑤(회전마다
  캡처 시도) 동시 위반이 코드로 확정된 자리입니다. 세 루프의 플래그 검사는 본문 서두(9357·9473·10182)와 스윕 가드뿐이라
  판독 도중 세워진 플래그는 다음 while 판정 전에 재검사되지 않습니다 — 대조: `Wait-ForScreen`(4274~4283)은 `& $Condition`
  **뒤에** 플래그를 보고 마감을 되돌린 다음 while 을 판정해 같은 경계가 닫혀 있음. 관측: 캡처 실패와 마감이 겹친 회차 0건
  (캡처 실패 13건은 전부 이동·클리어 대기·클리어 후 터치 구간; 09-17 16:11 의 9590 throw 는 캡처 실패 줄 0건·재클릭 11회라
  다른 원인 — 3절 '다시 하기' 항목). **관측 0건은 규칙 8(새 방어를 추가할지)의 기준이지 절대 규칙 15 의 면제 사유가
  아닙니다** — 이력 09-14 냥코인 항목이 "실사고 관측 없음 — 관측 조건 없는 절대 계약" 으로 같은 규칙을 적용한 선례이고, 위
  '거점 부활 20초'(규칙 4 위반 코드 확정 + 표본 0건)와 같은 증거 등급입니다. 창이 좁고(판독 도중 플래그가 서고 그 사이 40초
  마감이 지나야 함) 마지막 판은 exit 0 이라 1절 안에서는 후순위. → `while ($script:screenCaptureFailing -or …)` 형태로 실패
  중에는 동결 본문에 들어가게 하거나 `Wait-ForScreen` 처럼 판독 뒤 플래그를 보고 마감을 되돌리는 형태 — `test_stall_guards`
  는 조건식이 정확히 `$script:screenCaptureFailing` 인 while 만 세므로 전자는 안쪽 `if (…) { continue }` 를 유지하면 개수(36)
  불변, 제거하면 깨짐. 실기는 창 최소화로 캡처 실패를 유발해 재현. 손댈 주석 블록의 AI 도구 이름은 규칙 14 로 같이 정리.

---

## 2. 관측이 먼저 필요한 것

> 규칙 8("그 상황을 실제로 관측했는가?")에 걸리는 자리들입니다.
> 근거가 모자라 **일부러** 남겨 둔 것이지, 잊은 것이 아닙니다.

- **콘솔 카드 글자 렌더가 세션마다 다른 이유 — '실피해 없음' 마감 철회, '도전' 이형 채택은 사유 문구 확보 뒤**: 09-07/09-14
  콘솔 프레임 12장은 '선택됨'이 결정적으로 깨졌고(`人에E}1되` 계열), 09-16 콘솔 회차 9개(같은 1024x577 창)는 8회가 '됨'을
  그대로 읽어 조각 '태되'가 양쪽을 덮었습니다(이력 09-16). 그런데 **09-17 05:26~05:44 v2.1.10 콘솔 회차 10개는 전부 정상
  판독이었지만, 11번째 회차(05:46:20 시작)가 `carddiag_20260917_h05m46s40_stop.png`(1024x577, 옵션 화면)를 남기고
  끝났습니다.** 09-16 의 "콘솔 9/9 '도전' 정상" 기록 자체는 유지하되 세션 전체 무피해로 넓힌 결론만 철회합니다. `stop` 태그는
  커스텀 코드 4 정지 경로 6곳(8440·8461·8467·8503·8651·8660 — 전부 `$script:customMode` 의 `exit 4` 직전)에서만 저장되므로
  정지 경로 진입은 확정입니다. **09-18 오프라인 재현으로 경로를 좁혔습니다**(이력 09-18): ①05:46 에 짝이 되는 set-fail png 가
  Log·보존 사본 어디에도 없음(00:04:58 set-fail → 00:04:59 stop 짝과 대비; 보관 정리는 최신 10장 유지라 지워질 수 없음) →
  클릭 전 미판독(6회전 소진 → set-fail 저장) 경로는 배제. ②직전 10회차 로그는 매 회차 "은동전(소탕) 버튼 클릭 → 미사용으로
  변경" 뒤 1~2초에 "= 미사용(도전) 확인" — 카드가 매번 '선택됨' 으로 돌아와 클릭이 필요했으므로 옛 서술 "은동전 카드 이미
  '도전'" 은 **'워커가 끈 뒤'** 로 읽어야 합니다. ③따라서 경로는 **클릭 후 '도전' 재판독 3회 실패(7121~7128) → "= 미사용으로
  설정 (재확인 생략)" → 소모량 판독(8491) null → 8498 게이트 → 8504
  `[완료] 카드 설정을 확인하지 못했고 소모량 표시로도 확인할 수 없습니다 - 반대 설정 입장을 막기 위해 정지합니다`**(png 의
  입장하기 버튼에 소모량 숫자 없음). 타임라인(클릭 ≈05:46:33 + 재판독 3회 ≈7~8초 + 소모량 판독 → 05:46:40)도 정합. 로그가
  유실돼(아래) 사유 문구는 실측 미확정이지만 코드·png 로 이 문구를 추정합니다. ④05:46:40 프레임을 워커 파이프라인(배율
  5·3·4 × 주/보조 영역)으로 돌리면 6판독 전부 unknown — 주 영역은 전 배율 빈 판독, **보조 영역만 s3·s4 '노선'**(버튼 중심
  (463,313) 과 좌표 일치 = '도전' 자획 소실 오독, 2글자라 7075 `$unknownLetters -ge 3` 폴백 후보도 못 됨). 대조군 09-14·09-12
  프레임은 소스 7045~7048 주석의 깨짐 문자열과 글자 단위 동일해 파이프라인 유효. ⑤그 프레임에는 "[점검 안내] 9/17(목)
  06:00부터 점검…" 티커가 옵션 화면 중앙을 가로질러 **주 판독 영역 우상단과 겹칩니다**(보조 영역과는 안 겹침 — 보조도
  '노선' 이라 티커가 원인이라는 증거는 없음). 그 세션 마지막 회차가 점검 14분 전(1절 '점검 이벤트').
  세션별 0%/100% 양상(깨진 세션 09-07 17시·09-12·09-14·09-16 21:05, 정상 세션 09-16 21:03·23:42~·09-17)이고 무엇이
  가르는지는 여전히 모릅니다. 05:46 회차 로그는 09-17 10:08 휴지통 복구로 같은 이름의 옛 파일에 덮여 유실됐습니다(이력 09-17
  관측 A — `rdp_redirect` 09-13~09-16 23:42 기록은 실측기록 사본에 있어 복구 불필요; 보존 사본의
  `run_20260917_h05m46s20.log` 는 05:44:14 시작 **직전 회차**로, 파일명이 보관 시각일 뿐). 보존 표본 235개 로그에 'carddiag'
  문자열 0건 — 카드 진단이 저장된 회차의 로그는 하나도 없어 로그 표본으로는 교차 확인 불가.
  → 지금: 프레임 이관(1절). 채택은 **설계 합의** — '도전' 판정은 7057 `-eq '도전'` 정확 일치라 '노선' 같은 이형은 코드 변경
  없이는 절대 challenge 가 되지 않고, 잘못된 '도전' 확정은 8487 소모량 검증 생략으로 이어져 인식률보다 검증 범위가 넓습니다.
  다음 콘솔 정지 때 사유 문구('재확인 생략' 줄 + 8504 문구) 확보. 정상 판독 회전에도 진단 캡처 1장을 남기는 안은 판독 루프
  안 IO·보관 상한(09-07 '진단 스샷 축소' 이력) 때문에 설계 합의.

- **제목이 안 읽히는 옵션 화면에서 일반 회차도 진행시킬지**: 지금은 복구 회차만 받아들이고 일반 회차는 정확히 멈춥니다
  (`[완료] 진입 옵션 화면으로 확인되지만 제목 판독이 계속 실패합니다`, 코드 4 — 복구 회차도 마무리가 next-floor 면 exit 0 이
  아니라 throw 7707). 출처 사고(08-12 23:52 타 PC 1908 창, v2.0.3, coordsVersion 7)는 08-13 세 겹(제목 영역 상단 45→34 =
  coordsVersion 9, 넓은 판독 s5 사다리, '입장하기' 게이트 이진화)으로 대응됐고, 보존 표본(`[설정] window` 1908 인 로그 231건
  포함)에 `옵션 화면 인식 (제목 판독 실패 …)` 와 위 정지 문구가 0건입니다 — 단 **제보자 PC 의 수정 이후 로그가 없어** 풀렸다는
  증거는 아닙니다. 게임 재시작으로 풀리는지는 이력 08-13 항목에 "재시작 후에도 재발" 제보(구빌드)가 이미 있습니다 — 현행
  빌드로는 미확인. 진행까지 열려면 ①지금 구역이 어디인지 알려 줄 독립적인 증거(지도에서 선택된 칸 판별 — 실측 없음) ②입장
  확인 등 제목에 기대는 4곳의 대체 신호가 함께 필요합니다.
  → 이 문구는 **던전(일반/심층) 진입 옵션 화면** 경로(7516~7526·7720~7723)에서만 나오므로 어비스만 돌린 09-15 제보자 로그로는
  확인되지 않습니다 — 08-12 제보자(1908 던전, v2.0.3)에게 **던전 회차가 든 현행 로그**를 별도로 요청(09-15 제보자와 동일인이면
  1절 '타 PC 제보 ②' 요청에 합침). 있으면 코드 4 분기에 진단 캡처 1장(`Write-DgStageDiagnostics` 호출부 5곳에 이 분기 없음)을
  남기는 것부터(설계 합의) → B급 진행 설계. **충분한 실행 로그에서 재발이 없을 때만** 3절 이월(로그를 못 받은 것과 재발 없음은
  다름).

- **팝업 가드를 다 쓴 뒤 남은 공지를 읽고 들어가는 문제**: 초기 확인 루프(`while ($initialProbes -lt 20)`, 13125)의 팝업
  처리 10라운드를 소진하면 그 루프 안에서는 팝업 감지가 꺼집니다(대기 루프에는 별도 방어가 있음 — 13144 주석). 공지가 남아
  퀘스트를 가린 채 화면 가장자리 표시만 보고 '퀘스트 없음' 으로 확정하면, 메뉴를 열어 **진행 중이던 남의 채집을 끊을 수
  있습니다.** 모의 시험에서는 공지에 가린 판독 2회·메뉴 진입 3회를 확인했지만 **실기 재현은 아직 없습니다.** 카운터
  (`$initialPopupRounds`)는 **팝업을 처리한 회전마다(성공한 닫기 포함) 늘고 사용자 조작 취소만 제외**합니다(13170·13174) —
  옛 "커서 확인 실패가 10번 연속이어야" 는 틀린 서술이고 "팝업 처리 10회 누적"이 맞습니다(소진이 서술보다 쉬움). 소진을
  알리는 문구가 코드에 없어 로그 0건은 발생률 근거가 못 됩니다. 09-11 실기(이력 09-10 항목)에서 가드가 게시판을 보고 닫는
  정상 경로는 확인됐습니다. 보존 표본의 생활 회차는 08-15 v2.0.3 11건뿐(가드는 v2.1.8 도입)이고 9월 생활 로그는 실측기록
  5건(0910·0911×2·0913×2, 정상 경로만).
  → 1절 계측 1줄이 들어간 뒤 제보 로그 정례 grep: `[생활] 공지 게시판 X 닫기 건너뜀 (커서 미확인)` 반복 뒤
  `[생활] 내 정보 화면 확인`. 소진 이후 동작(정지 vs 계속)은 관측한 뒤 설계 합의.

- **클리어 화면 기다리기에 양보 시간을 반영할지**: 다른 화면 대기(`Wait-ForScreen`)에는 2026-09-09 에 반영했지만
  `Wait-ForDungeonClearScreen` 만 제외했습니다(함수 안 주석 4456 이 '연장 정책은 별도 이슈'로 명시). 이 감시는 연장 상한과 부활
  입력 한도를 따로 관리해 **어떤 예산을 제외할지가 다릅니다.** 보존 표본: 클리어 대기 시작 155회 중 대기 중 양보 12구간
  16건(1~49초, 전부 정상 클리어), '대기 시간이 초과됐습니다' 6건은 **전부 양보와 무관**(설정 60초 1·자동사냥 미점화로 연장
  소진 3·판독 공백 2, 모두 v2.1.7 이전) + **09-18 06:04 v2.1.10 1건**(ERROR 83 서버 끊김 모달 — 1절 '점검 이벤트' ②, 양보 줄
  0건) = 7건, 양보로 잘못 만료된 사고는 0건. 실험을 하려면 dungeonClear 를 낮춰도 연장 상한이 max(한도×3, 1800초)이고
  전투 신호로 60초씩 연장되므로 90초 조작만으로는 09-09 모의의 재확인에 그칩니다 — 양보 종료 시각·그 화면·연장 로그·종료
  사유를 같이 보고, 정상이면 "해당 조건 미재현 — 현행 유지". 거점 부활 20초 상한 문제는 1절로 분리했습니다.

- **채집 화면이 아직 유효한지 보는 검사 2곳**: '행이 하나라도 있으면'(12751) / '내 정보 화면만 아니면'(12543) 으로 옛 좌표를
  그대로 승인합니다. 다만 뒤따르는 검증이 오클릭을 잡아 회전 중단으로 끝나므로 피해가 회전 1회에 그칩니다 — 단 **상세 팝업
  제목 검증('다른 대상의 상세 팝업입니다', 12790~12793)이 잡는 것은 제목이 s3·s4 두 배율 다 정상 판독되는 대상에 한합니다**:
  두 배율 모두 unreadable 이면 이름 근거 행(`text`)은 12838~12840 으로 통과하고, 순서 근거(`order`)는 링크 게이트(12975~)가
  같은(오클릭한) 팝업의 `$firstFrameTitle` 과 비교해 못 잡습니다. 스킬 셀 게이트(12543)의 오클릭은 상세가 아니라 12555~12570
  시그니처 검증이 담당. 새 화면 판별 조건을 추가하지 않는 쪽이 낫다는 판단이 서 있습니다. 실패 경로 관측 0건(도입 09-09 뒤
  실기 2건은 성공 경로).
  → 다음 생활 실기 때 '대상 선택' 게이트가 양보에 걸린 순간 목록을 한 칸 드래그해 '다른 대상의 상세 팝업입니다' 로 회전
  1회에 그치는지(그리고 다른 대상 퀘스트가 생성되지 않는지) 확인 — **제목이 안 읽히는 대상(물 등)에서 한 칸 밀린 경우**도
  추가로. 착수하려면 다시 읽기·좌표 갱신 범위를 실측으로 먼저 정할 것.

---

## 3. 재현 조건이 갖춰져야 하는 것 — 장기

> 사람·계정·우연이 필요해 **지금 당장은 확인할 수 없는** 것들입니다.
> 평소처럼 돌리다가 걸리면 로그로 확인합니다. (파티·부활 4건은 코드로 결함이 확정돼 1절로 이동 — 2026-09-17)

- **부활 오계상 수정의 실기 확인** (2026-08-11 이월): 클릭 생략 기전 자체는 같은 점검의 사냥터 커서 흔들기 실측이
  증명했습니다. 08-11 배선(계상·성공 로그·대기 리셋을 전송 뒤에만)은 현행이고 `test_wipe_revive` 가 단언합니다. 이미 수정된
  배선의 실기 확인이지 재구현 대상이 아닙니다. 자동 부활 실행 표본 0건(사망 0회). → 1절 부활 2건의 실기(가루 0 의도적 사망
  + 20초 이상 조작으로 생략 클릭을 만든 상태)와 **같은 세션**에서 '생략 안내 다음 줄에 클릭 로그 없음·전송 시점에만 부활
  완료'를 확인.

- **던전 파티 찾기에서 '다시 하기' 시 파티가 유지되는지** (2026-07-15 이월): 직접 재입장 갈래는 08-17 파티 재입장 대응이
  매칭 방식과 무관하게 처리합니다 — **남은 질문은 파티가 남은 상태로 옵션 화면에 복귀했을 때 하단 버튼 배치**뿐(틀리면
  오입장이 아니라 입장 대기 초과 코드 1). 보존 표본에 파티 찾기 던전 회차 0건. **새 관측(2026-09-17 16:11, v2.1.10, 1908x1076,
  커스텀 129바퀴)**: '다시 하기' 클릭 16:10:32 뒤 결과 화면이 그대로 남아 상태 기반 재클릭 11회(4초 간격) 전부 무효 → 9590
  throw 코드 1(`error_20260917_h16m11s14`). 캡처의 결과 화면 3버튼(나가기/다시 하기/다음 구역으로)은 그대로이고 그 위에
  **'자동 재도전' 토글 + "다음 구역에 도전해 봐요." 말풍선**이 있음(이력 09-17 의 '같은 패치의 던전 자동 재도전' 미확인
  항목의 첫 실물), 커서는 '다시 하기' 위. 16:10 점검 시작과 시각이 겹쳐 서버 무응답이 원인일 가능성이 큼(정황 — 클릭 좌표
  정상, 1절 '점검 이벤트' ③). 배치 불변, 토글 1개 추가 — 파티 유지 질문과는 별개 정보. → 사람 많은 시간대 비커스텀 파티
  찾기 2회차 이상 실기 기회에 재입장 로그·옵션 복귀 하단 캡처 확보.

- **해금 안 된 구역 감지**: 해금 안 된 구역이 있는 계정이라야 재현됩니다. 07-22 계기 사고는 실제 미해금이 아니었고 이후
  미해금 화면 관측 0건. 현행은 코드 정적 확인상 fail-closed(구역 선택 실패 시 커스텀 코드 4 / 비커스텀 throw + 진단 png)라
  오입장 경로는 없습니다 — 미해금 계정 실측은 없음. → 미해금 화면 캡처가 오면 착수. ('미해금이거나 화면 인식 실패' 문구
  분리는 07-24 완료)

- **게임 무한 로딩 감지** (관측 1건 — 실측 부족으로 설계 보류): 2026-08-11 20:51 타 PC 제보. 클리어 화면 위에 로딩 표시가
  무한히 남아 정위치 터치 18회(90초)가 전부 무반응 → '결과 화면을 찾지 못했습니다' 오류로 오진했습니다. 08-02(v1.2.1)부터
  무응답 전용 문구('클리어 화면이 터치에 반응하지 않습니다')가 있지만 마감 시점의 클리어 화면 재판정이 통과해야 나오고, 이
  건은 그 재판정이 실패해 일반 문구로 떨어진 것으로 보입니다(정황 — 재판정 원문은 로그에 남지 않음; 근거는 실측기록 +
  소스 대조). 클릭 전송·좌표는 정상이었고(커서 안내 0건 + 캡처의 커서 위치 일치), 입장 로딩도 32초(평소 3~7초)로 그 세션
  전반이 느렸습니다. **무한 로딩은 어느 화면에서든 걸릴 수 있어**(사용자 확인) 클리어 한정 문구 수정은 기각했습니다. 정답은
  범용 로딩 표시 감지인데, 그 표시는 회전 애니메이션이라 **픽셀 모양이 프레임마다 다르고 실측이 1프레임뿐**이라 설계를
  보류했습니다(규칙 8). 오류 캡처에는 그 표시가 찍히므로 지금도 제보 분석으로는 판별할 수 있습니다.
  → **여러 화면·여러 프레임의 캡처가 모이면 착수.** 실측 보관: `던전이미지\실측기록\20260811_무한로딩_*`

---

## 4. 개선점 — 급하지 않음

> 실사고 수정이 아니라 "더 좋게" 만드는 것들만 둡니다. **전부 우선순위 낮음** — 급한 것은
> 애초에 여기 오지 않습니다. 구현하면 검증(문법·테스트·빌드·가능하면 실측)을 거쳐 이력으로 옮깁니다.
> 순서: 계약 위반이 코드로 보이는 것(픽셀 '모름'·다음 층·Space·팝업 공용·스위치 형제) → 화면 읽기 구조 → 컨트롤 패널.

### 판정·복구 보강 — 전수 점검 보류분 (2026-08-01 설계 합의)

- **픽셀 판독 실패를 '모름' 으로 구분하기**: 상태를 뒤집는 픽셀 판독 3종(우연한 만남 토글·자동사냥·어시스트)은 이미
  'unknown' 3상태를 돌려줍니다. bool 로 남은 것은 난이도 알약 `Test-DifficultySelectedAt`(창 좌표 실패도 `$false` = '비선택'
  으로 뭉개짐; 호출부는 같은 알약을 최대 2회 재클릭 — `Confirm-DifficultySelected`·`Set-DgOptionDifficulty`; 커스텀 선택
  화면은 3회전×3 까지)과 구역 카드 `Test-DgCardPixelAt`(호출부 일부는 픽셀 미확인이어도 그대로 시도, 심층 3곳은 픽셀 실패를
  무시 — 진입 버튼/제목 2차 검증에 맡김)입니다. 알약 재클릭은 멱등, 카드는 2차 검증이 잡습니다. 픽셀 판독 실패 로그 0건.
  알약 3상태화는 직접 호출 3함수 8회(6692 / 6753~6825 6회 / 9736)의 계약이 걸려 보류.

- **'다음 층으로' 완료 판정에 층 검증이 없음**: 좌상단 제목에 '구역' 글자만 있으면 성공으로 처리해서(9370), **층이 안 바뀌고
  원래 층 화면이 떠 있어도 "회차 완료"로 적고 정상 종료**합니다. 그러면 다음 회차는 윗층을 하러 시작하는데 화면은 아랫층에
  남아 있고, 그 화면에는 뒤로 가기 버튼이 없어 스스로 돌아갈 수 없습니다. 정상 목적지(2층 선택 화면)의 제목에는 '구역'이
  없어(보관 캡처; `Test-DgSelectionTitle` 은 '구역'/'층' 포함 제목을 선제외) 그 분기가 정상 경로에서 참일 이유가 없습니다.
  🛡️ 안전망은 다음 회차의 시작 구역 검증이라, 오입장 대신 정지로 끌고 갑니다. 다만 사용자가 직접 윗층 선택 화면을 열어
  다시 시작해야 합니다. 보존 표본에 1-3→2층 전환 0건.
  ※ 같은 상황의 복구 경로는 "1-3 다음은 2층 선택 화면만 완료로 인정" 으로 이미 엄격합니다 — **두 경로의 엄격함이 서로
  다른 것**이 이 항목의 핵심입니다. (3차 점검 보류) 현행 조건을 고정하는 회귀 단언 1건은 1절 '다음 소스 커밋 동승' ⑤.

- **협동 미션 완료 '확인' 을 못 찾았을 때 Space 폴백**: 08-01 같은 커밋에 실측 좌표 클릭 폴백(6081~6093)이 이미 있어 '없는
  폴백'이 아니라 **좌표 클릭 대신 키 입력을 쓸 것인가**의 문제입니다. `Press-KeyOnce` 는 조작 감지·전면 확인 게이트가 둘 다
  없고 `Press-KeyVerified` 는 전면 확인만 있어 규칙 4 계약 밖 — 채택하려면 키 입력 계약부터 설계 합의(1절 파티장 항목의 Space
  키와 같은 계약). 보존 표본에 이 화면 감지 0건. (사용자 문의 — 결정 대기: 기각이면 이력으로)

- **팝업 공용 함수가 위임하는 4곳의 취소 처리** (보상 화면·협동 미션·협동 보드·네트워크 불안정 닫기): 사용자 조작으로 클릭이
  취소돼도 양보 없이 1~3초 고정 대기 뒤 '처리했다'로 돌아오고, **보상·협동 완료 2곳(+협동 폴백)**은 전송 여부와 무관하게
  '확인 클릭'·'실측 좌표로 클릭' 을 기록합니다(협동 보드·네트워크는 전송 시만 기록). 구매 팝업 직접 닫기(09-13 수정)와 달리
  남은 자리 — 호출부 25곳 공용이라 손대면 전 흐름의 대기가 바뀝니다. 그 화면들이 조작과 겹친 관측은 아직 없습니다(09-14
  마지막 판 설계 검토에서 지적). 부수(관측 0건, 규칙 8 보류): `Close-CoopMissionScreen` 만 형제 3함수에 있는 캡처 실패 선두
  게이트가 없고(OCR 빈 문자열로 `$false` 라 동작 문제는 아님), `Close-QuestRewardScreen` 의 '확인' 탐색은 배율 3 단일이라
  같은 자리를 읽는 협동 완료의 3→4→5 사다리(08-01 '>poce'/'할인' 오독 대응)와 다릅니다 — 보상 화면에서 같은 오독이 관측되면
  2절 후보.
  → 관측되면 직접 닫기와 같은 계약(취소 → 기다린 뒤 즉시 반환)으로 맞춤. 거짓 로그 3곳을 `$script:lastClickPerformed`
  조건부로 바꾸는 것(동작 불변, A급)은 1절 '다음 소스 커밋 동승' ④.

- **스위치 루프 형제 3곳의 '커서 미확인 뒤 재판독' 불일치** (어비스 '파티 찾기' 끄기, 던전 경로 켜기, 던전 경로 끄기):
  어비스 끄기 루프(14986~15009)만 클릭이 커서 미확인으로 전송되지 않으면 재판독 없이 옛 상태로 코드 4 정지합니다. 던전
  켜기(8698~8739)·끄기(8951~8994)는 탐침 재판독이 있으나 정지 사유 문구가 커서 미확인을 드러내지 않고, **던전 끄기는
  throw(코드 1, 8988)** 로 격상됩니다(커서 미확인 뒤 정상 판독이면 'on' 그대로라 throw). 09-16 '우연한 만남' 켜기 수정은
  합의 ②대로 다시 읽어 on 이면 진행하게 했지만 형제들은 그 범위 밖이라 그대로입니다. 보존 표본: 토글 로그 158줄 중 157줄은
  '켜짐 확인', 1줄은 09-16 켜기 분기 진입 후 조작 취소 — 클릭이 실제 전송된 회차 0건. → 커서 미확인 정지가 실기에서
  관측되면 세 곳을 같은 모양(재판독 뒤 off 만 정지)으로 맞춤(던전 끄기 throw→코드 4 는 종료 코드 변경이라 GUI 분기 확인
  포함). 손댈 때 `test_yield_batch`·`test_audit_fixes` 의 해당 단언 갱신.

### 화면 읽기 구조 — 실측이 더 모이면

- **큰 창에서 글자가 덜 확대되는 문제**: 글자를 읽으려고 화면 조각을 잘라 늘릴 때, 늘린 크기가 **실제 창 크기와 무관하게
  고정**돼 있습니다(`Get-GameRegionCapture`). 그래서 창을 크게 띄울수록(1908 창이면 1.5배 큰 원본을 같은 크기로 줄여
  맞추므로) **실제 확대되는 정도는 오히려 줄어듭니다**(배율 4 → 실효 2.67배). 카드·난이도 알약·협동 창에서 났던 다중 스케일
  사고들의 공통 배경입니다. 1908 창은 두 종류입니다 — DWM 스트레치(1272 렌더를 1.5배, 제목줄 47px)와 네이티브 1908(제목줄
  31px, 다른 제보 PC 는 38px — 이력 08-13) — 정규화로 정보가 느는 쪽은 네이티브뿐이고, 09-15 의 '어H|스' 오독은 네이티브
  에서도 실효 5.33배·이진화까지 회복되지 않아 정규화가 구제했을 사례가 아닙니다. 지금은 그때그때 배율 사다리로 대응 중이고,
  근본 정규화는 **전 화면 판독에 파급이 커서** 실측 세트(두 종류 구분)를 모아 따로 검토합니다. (08-03 실사고에서 구조 확인)
  ※ 잘라낸 자리를 클릭 좌표로 되돌리는 계산은 창 크기와 무관하게 정확합니다 — 이 고정이 **클릭 위치를 틀어지게 하지는 않습니다.**

- **'매우 어려움' 판독 폴백 확장** (던전·심층 경로 `Select-DgDifficultyWord`): 마지막 폴백이 위치 조건이 붙은 '어려움'
  키에만 걸려 있습니다. 1908 창 '매우 어려움' 미탐 로그 0건(09-13 1건은 계정에 없는 난이도의 정상 정지). 1908 창의
  '매우 어려움' 실측(깨짐/정상 양쪽)이 모이면 '매우' 조각 조건을 검증한 뒤 넓힙니다. ※ 09-15 타 PC 제보의 '매우 어려움'
  미탐은 **어비스 상세 경로**(1절 '타 PC 제보 ①')라 이 항목과 별건.

- **재화 줄을 아이콘으로 찾기**: 공물·은동전 잔량 위치를 고정 x좌표 대신 아이콘 그림 대조로 찾으면 UI 개편이나 재화 순서
  변경에 강해집니다. 원본 그림은 확보돼 있습니다(2026-08-04 사용자 제공). 잔량 판독 실패 로그 0건(실패는 대개 무로그로
  진행). 창 크기별 배율 대조가 필요해 실사고가 재발하면 착수합니다.

- **한 번 찍은 화면을 여러 감지가 나눠 쓰기**: 지금은 한 번의 확인에 감지들이 각자 화면을 찍습니다(`Test-KnownScreen`
  미일치 시 11개 판정에 `Get-GameRegionCapture` 27회, 클리어 대기 1회 폴링 4~6회). 이걸 1회로 줄이는 건데, 감지 사이에
  화면이 바뀌는 타이밍 문제로 **회귀 위험이 이득보다 큽니다.** 계측 2건 — 09-13 냥코인은 판독(OCR 1회 63ms)이 병목이
  아니라고 명시, 08-22 생활은 재판독 생략의 시간 이득이 관측되지 않아 남은 오버헤드를 확인 간격(안전 계약)으로 결론 —
  둘 다 전체 감지 경로의 비용을 말하지는 않아 항목은 유지합니다. → **확인 속도가 실제로 부족하다고 계측될 때만** 진행.
  ※ (판독 엔진 대체로서의) 엔진 교체·메모리 읽기·그림 대조 방식은 검토 후 기각했습니다
  (설치 없는 단일 exe · 여러 PC · 원격 접속 제약에서는 지금 구조가 최선) — 위 '재화 줄 아이콘' 항목과는 별개 결정.

### 컨트롤 패널 · 빌드 도구

- **목록 편집 저장 실패 시 완전 되돌리기**: 추가/삭제/이동은 저장에 실패해도 경고만 뜹니다(07-31 문구 강화 완료). 저장
  실패는 다음 시작 시 `Save-SettingsFromUi` 재병합으로 자연 해소되지만 GUI 를 그냥 닫으면 유실되고, GUI 로그가 파일에
  남지 않아 관측 채널이 없습니다(저장 실패 관측 0건). 화면을 다시 맞추는 방식(셀 편집 원복을 일반화한 '편집 전 스냅샷/실패
  시 복원' 공통 헬퍼) 검토 — 리스트 4개×3경로 구현이라 규칙 8 경계.

- **창 정렬 스냅 임계값**: 1340x754 창이 1272x717 로 스냅되지 않고 1px만 조정됩니다. 회차 시작 정렬은 nearest 가 기본이고
  GUI 가 저장 시 nearest 로 강제하며, 권장 크기는 '권장 창 모드' 버튼 1회 적용 방식(08-13)입니다 — 자동 스냅 도입 여부는
  결정된 적 없음. 좌표가 전부 비율 환산이라 **좌표는 무해함이 확인됐습니다**(07-30 타 PC 완주 실측) — 단 판독 품질까지
  무해한 것은 아닙니다(07-28·08-13 비권장 크기 OCR 열화 관측). 보존 표본에 정렬 실행 줄 0건. 권장 크기로 당길지 결정만
  남았고, 실사용 관찰로 충분하면 그대로 둡니다.

---

## 5. 지난 기록

**최근 검증 기록**과 **해결됨(이력)** 은 [이슈_해결_이력.md](이슈_해결_이력.md) 로 옮겼습니다
(2026-08-11 분리 — 이 파일이 4570줄까지 커져 진행 중인 항목이 묻혔습니다).

- 수정을 마치면 그 이력은 **이력 파일의 `5. 해결됨` 맨 위**에 씁니다.
- 실기로 확인한 내용은 이력 파일의 **`4. 최근 검증 기록`** 에 씁니다.
- 이 파일에는 **아직 안 끝난 것만** 둡니다. 새 기능은 `개발사항.md`.

---

## 부록 — 항목별 코드 위치

> 본문은 일부러 평이한 말로만 썼습니다. 실제로 작업할 때 찾아갈 지도를 여기 모아 둡니다.
> 줄 번호는 **2026-09-18 기준**이라 수정이 쌓이면 어긋납니다 — **이름으로 찾으세요.**
> 같은 날 v2.1.11 핫픽스(어비스 나가기 헬퍼 3함수를 `Test-ExitButton` 뒤 4973~5097 에 삽입)로 **4972행 이후 번호가 +125**
> (복구 루프 `Return-ToAbyssSelection` 의 나가기 분기 축소 뒤 +119, 시작 디스패치 축소 뒤 +118) 밀렸습니다 — 아래 표는 핫픽스
> 전 번호입니다(헬퍼 호출부 현행: 파티원 10486 · 복구 루프 10676 · 시작 보상 14621 · 시작 클리어 14640 · 본류 15324).
> 코드 위치가 있는 항목만 싣습니다(팝업 가드는 계측 행에; 부활 오계상·해금·재화 아이콘·목록 편집은 코드 위치 없음).
> 마지막 3행은 항목이 아니라 공용 헬퍼·계약입니다.

| 본문 항목 | 파일 / 이름 |
|---|---|
| 계측 — 창 정보 1줄 / 생활 가드 소진 1줄 | 창 정렬 14311~14383 (`[준비] 게임 창 정렬(…)` 14380, 정렬 발생 회차만; `GetWindowRect` 만 사용) / DllImport 목록 2188~2257 (DPI 조회·ClientRect 계열 없음 — DPI 인식 설정 3종은 2215~2222 에 있음), try/catch 선례 2266~2277, GUI `GetDeviceCaps` `mabinogi_gui.ps1` 80·106 / 생활 가드 `$initialPopupRounds` 13124·13146·13174 (user-active 미소모 게이트 13170), 초기 확인 루프 13125 |
| '매우 어려움' 영역 확장 | `$rgDifficultyTabs` 669 = config `ocrRegions.difficultyTabs` 359~364 / `$selectDungeonDifficulty` 14647~ (탐색 14652·14669·14694, 커스텀 exit 4 14702~14705, 경고 14706~14708; 호출 14806·15079·15161) / `Find-GameTextPoint` Contains 폴백 3732~3736 / coordsVersion: config 30, 워커 `$coordsVersionCurrent` 456, 워커 게이트 458~464, 빌드 차단 `build/build_exe.ps1` 107·126~127, GUI `Update-ConfigToLatest` `mabinogi_gui.ps1` 412~ (게이트 425~429, 진행 초기화 471~527) / `tests/test_audit_fixes.ps1` 524~528 |
| 다음 소스 커밋 동승 | `build/build_exe.ps1` 48·50·56 / `mabinogi_run_once.ps1` 1378·7773 (`$dgSelStagePoints` 907) / 9045~9049 / 거짓 로그: `Close-CoopMissionScreen` 6045~ (로그 6077, 폴백 로그 6087 → 클릭 6089), `Close-QuestRewardScreen` 6114~ (로그 6134); 대조 협동 보드 5975·네트워크 6040 (전송 시만) / `tests/test_deep_dungeon.ps1` 344, `tests/test_after_entry_popup.ps1` 52 / '다음 층으로' 9370~9373 / `tests/run_all_tests.ps1` 20 `$expectedTestCount` 83 |
| 콘솔 프레임 이관 | `Save-DgCardDiagnostics` 7271~ (보관 정리 7327~7335, `keepScreenshots` config 666 = 10) / 'stop' 호출 6곳 8440·8461·8467·8503·8651·8660, 'set-fail' 7205, 'fallback-before' 8571 / `tests/test_dg_card_console.ps1` 175 (stop 6 단언), `tests/test_dg_card_console_offline.ps1` 19~25 (09-14 폴더 참조) |
| 점검 이벤트 | 옵션 난이도 3회 탐색 `Set-DgOptionDifficulty` 6718~ (루프 6730~6735, 호출 6731·양보 후 6845, 문구 6734/6738; Strict 호출부 7802·8271, throw 7805·8275) / `Find-DgDifficultyPoint` 1259~ (선택 화면 호출 7962·7983·7994·8011·8032 는 별도 영역 `$rgDgDifficulty`) / `Test-GameRestartPopup` 6096~ (매칭 6111) / ERROR 83 생활 감지 12130·12156·13104·13357·13437·13534 / `Wait-ForDungeonClearScreen` 4297~ (전투 연장 4325·4336, 마감 throw 4359), `Test-CombatStillRunning` 5150~5156 / `Clear-EventOverlay` 20라운드 |
| 타 PC ③④ | `Test-AbyssMenu` 5004~5020 (Contains 5014, 2차 `Test-MenuExitLabel` 5019) + `$rgAbyssMenu` 625 / 시작 디스패치 14486~14495 (14487 '시작: ESC 메뉴 감지'; 상세 제목 판정 14603~14613 보다 앞) / `Return-ToAbyssSelection` 10369~ (마감 10381 = HUD+메뉴+선택, 카운터 선언 10388, 메뉴 판정 10428, 클릭 탐색 10437~10441, 재시도 로그 10441 "메뉴에서 '어비스' 글자를 찾지 못했습니다" — 카운터는 진단 캡처 1회용, throw 10659; 정상 복귀 호출 15207) / `Find-GameTextPoint` 3693~ (정확 3721~, Contains 폴백 3732~3736) |
| 타 PC ② / 파티 찾기 | `Get-ChanceToggleState` 6545~6570 ('off' 6569) + `$ptAbyssChanceToggle` 641 = config `clickPoints.abyssChanceToggle` 266~269 / 던전 선례 `Find-DgChanceTogglePoint` 6527~6544·`Select-DgChanceToggleAnchor` 6502~, 고정점 폐기 주석 1358~1361 / 파티장 14811~14832 (탐침 14815, '꺼짐 확인' 14829), 우연한 만남 14871~14957 (블라인드 클릭 → 경고 14952, 켜짐 확인 14956), 파티 찾기 14967~15040 (주석 14968~14969, '꺼짐 확인' 15033, 클릭 15036~15040 `$ptPartyFind`), 공용 매칭 대기 15045~15060 / `tests/test_audit_fixes.ps1` 696~703, `tests/test_abyss_chance_toggle_yield.ps1` 58 |
| 거점 부활 / 전멸 / R키 | `Wait-ForDungeonClearScreen` (4297~) 안 — 전멸 4491~4543 (클릭 4511·4527, 전송 판정 4516·4529) / 거점 4550~4595 (클릭 4564~4565·4572~4573, 20초 상한 4589~4591, 초기화 4490) / R키 4596~4616 (throw 4611) / 마감 throw 4359, 전투 연장 4325·4336 / 3갈래 로그 선례 4444~4463(구매 팝업)·4701~4709(컷신), 시계 게이트 선례 15002~15004 / `Test-UserRecentlyActive` 2438~ (GetLastInputInfo 2432~2434; `Wait-UserYieldEnd` 2684 주석) / 진리표 `tests/test_wipe_revive.ps1` 106(원문)·116(`$anchorCode`)·150~157 |
| 파티원 사이클 | `Invoke-AbyssPartyMemberCycle` (10266~10367) — '준비 완료' 10328~10331 (안전 중지 게이트 10315, 상태 재클릭 10324), 4단계 나가기→복귀 10359~10364 (`Wait-ForScreen` throw 4294) / GUI 매칭 라디오 `mabinogi_gui.ps1` 3014·3021·3027, 워커 파서 183, 일반 반복 코드 1 처리 `mabinogi_gui.ps1` 8521·8561 |
| 파티장 입장 대기 | 입장 클릭 14840~14842, 30초 루프 14844~14867 (Space 14851, 재클릭 14862~14866), 경고 14868~14870 → 공용 매칭 대기 15045~15060 (`timeoutsSeconds.partyMatching` config 176 = 300) |
| 복귀 대기 루프 마감 경계 | 마지막 판 9176~9273 (동결 9179~9185, exit 0 9266~9273) / 형제 9355→9419 · 9471→9590 · 10180→10251 (throw; 서두 플래그 9357·9473·10182) / 대조 `Wait-ForScreen` 4274~4283 / `tests/test_stall_guards.ps1` 42·49 (조건식 필터)·88 (개수 36) |
| 콘솔 카드 글자 렌더 | `Set-DgToggleCard` (6919~) — 판정식 7052~7061 ('태되' 7054, '도전' 정확 일치 7057), 깨짐 문자열 주석 7045~7048, 회색 픽셀 7095~7115, 클릭 후 재판독 7121~7128 ('재확인 생략' 7126), unknown 폴백 7075, `$setTryMax` 6976, `$cardScales` 7002, set-fail 저장 7203~7205, 경고 7211 / 호출부 8487·8491·8498~8505 / `[진단] … '태되' 조각으로 판독` 7090 / 진리표 `tests/test_dg_card_console_offline.ps1` |
| 옵션 제목 미판독 | `$optionsByProbe` 7516~7526, 복구 회차 한정 수용 7702~7712 (next-floor throw 7707), 코드 4 분기 7720~7723 (문구 7522·7721) / `Write-DgStageDiagnostics` 호출부 7785·7844·8204·8253·9270 (이 분기 없음) / 복구 경로 `Get-CustomRecoveryReadyAction` (363~) |
| 채집 화면 유효 검사 | 정의 `Test-LifeYieldBeforeInput` 12393~ (`ScreenStillValid` 매개변수) / 호출부 12486·12509·12543·12751·12975 (약한 조건 12543·12751), 시그니처 검증 12555~12570, 상세 제목 12790~12793('다른 대상')·12820~12822·12838~12840 |
| 클리어 화면 기다리기 | `Wait-ForDungeonClearScreen` (4297~), 양보 미반영 주석 4456·선언부 3041~3042 / 비교: `Wait-ForScreen` (4248~) 4268·4292, `Get-YieldAdjustedDeadline` (2719~) |
| 던전 파티 재입장 / 다시 하기 | 9550~9587 (감지 9550~9568, 커스텀/비커스텀 분기 9572~9587) / '다시 하기' 재클릭 마감 → 9590 throw |
| 무한 로딩 전용 문구 | 6453~6454 (재판정 6453, 문구 6454 '클리어 화면이 터치에 반응하지 않습니다') |
| 화면 조각 확대 | `Get-GameRegionCapture` (3530~), 고정 확대 3594 |
| '매우 어려움' 판독 폴백(던전 경로) | `Select-DgDifficultyWord` 1131 (유일 호출 1284, `Find-DgDifficultyPoint` 1259~ 안; 옵션 화면 호출 6731·6845 / 선택 화면 호출 7962·7983·7994·8011·8032) |
| 픽셀 '모름' | 3상태: `Get-ChanceToggleState` 6545~·`Get-AutoHuntState` 4731~4763·`Get-AssistState` 4789~4802 / bool: `Test-DifficultySelectedAt` 6572~6646, 직접 호출 6692(`Confirm-DifficultySelected`)·6753~6825(`Set-DgOptionDifficulty` 6회)·9736 / `Test-DgCardPixelAt` 1087 정의, 호출부 1406~1414·1459·1489·1500·3855·3880·3888~3889·3906·3923 |
| '다음 층으로' 완료 판정 | 9370~9373 ('구역' 조각 분기 9370) — 현행 단언은 1절 동승 ⑤ |
| Space 폴백 | 좌표 클릭 폴백 6081~6093 / `Press-KeyOnce` 10673~10681, `Press-KeyVerified` 10683~10716 |
| 팝업 공용 4곳 | 보상 화면 6114 (로그 6134) · 협동 미션 완료 6045 (로그 6077, 폴백 로그 6087·클릭 6089) · 협동 보드 5954 (로그 5975) · 네트워크 불안정 5999 (로그 6040) — 거짓 로그 3곳은 1절 동승 ④ |
| 스위치 루프 형제 3곳 | 어비스 끄기 14986~15009 (break 15004, exit 4 15006~15009) / 던전 켜기 8698~8739 (탐침 8728~8733, exit 4 8739) / 던전 끄기 8951~8994 (탐침 8981~8986, throw 8988, unknown exit 4 8994) |
| 캡처 공유 | `Test-KnownScreen` (5230~, 5239~5241 `Test-AbyssMenu` 선호출) |
| 창 정렬 | 회차 정렬 14311~14383 / GUI 저장 시 nearest 강제 `mabinogi_gui.ps1` 7772~7779, 권장 창 모드 `Apply-RecommendedWindowSize` 118 |
| 글자 찾아 좌표 잡기 (공용) | `Find-GameTextPoint` (3693~) |
| 퀘스트 추적기 영역 (v15, 공용) | `$rgQuestTracker` (712) / 생활 전용 `$rgLifeQuestTracker` (719) |
| 클릭 전송 계약 (공용) | `$script:lastClickPerformed` (설정 2824·2883) / `$script:lastClickSkipReason` (`user-active` 2834~2841 · `cursor-not-ready`) / `Click-GamePoint` 2931 → `Click-ScreenPoint` 위임 / `$script:userYieldTotalMs` — CLAUDE.md 규칙 4 |
